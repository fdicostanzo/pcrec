# Lane rel13 report — docs/guide/, the user guide ([REL-1.3]/[GUIDE-1])

Worktree `worktrees/rel13`, branch `lane/rel13`, off `main` at 6a54b0ac
(post-[REL-1.11], D118/[REL-1.10] merged).

## Delivered

`docs/guide/` (new): `README.md` (index), `CLAUDE.md` (charter/file list),
and eight chapters — `getting-started.md`, `building-with-make.md`,
`using-the-matcher-from-c.md`, `using-the-library.md`,
`encodings-and-subjects.md`, `features-and-modules.md`,
`tuning-and-limits.md`, `troubleshooting.md` — plus `docs/guide/lang/README.md`
(one-line placeholder for [LANG-1]). `README.md`'s "Using it" section gained
one pointer line to `docs/guide/`. `docs/CLAUDE.md` gained the `guide/` row.
No `docs/dev/plan.md`/`dev_journal.md` edits (out of scope per brief).

Documents the interface AS BUILT post-D118/[REL-1.10] (positional operands
are `.rxt` file inputs, `--pattern` is the literal-pattern flag, `-I`/
`--lib-path`), D115's `--version`/`PCREC_VERSION`, and [REL-1.11]'s library
`features` field with its NULL-means-empty-not-std1 trap named plainly in
`using-the-library.md`. No drifting numbers anywhere except the version
string; every other fact points at `docs/spec/` or a live `--list-*`/
`--explain` command.

## Validation — every snippet built and run in this worktree

All commands run from `worktrees/rel13` against a fresh `make -j4 CC=gcc-16`
build (build/pcrec, abi 28) unless noted. Scratch files under this
session's scratchpad `rel13/` subdir.

1. **getting-started.md** — `build/pcrec --version` → `pcrec 0.1.0-beta`.
   `build/pcrec -p rx --emit-main -o matcher.c --pattern 'a(b|c)+d'` +
   `gcc -O2` + `./matcher 'xxabcbdyy'` → `match 2 7`, exit 0. `-o -` form
   verified to print a header-free self-contained `.c` to stdout (spot
   checked, no `#include` line). Generated header's provenance line
   confirmed: `/* Generated by pcrec 0.1.0-beta (abi 28)... */`.

2. **building-with-make.md** — `examples/makefile`: `make PCREC=../../build/pcrec`
   built `gen/{greet,digits,money}.{c,h}` from the two pooled `.rxt`
   sources in one `pcrec -o gen/ src/*.rxt` call, archived
   `libmatchers.a`, linked `example`. `./example` output:
   ```
   greet:  hello, World!        -> match [0,13), name=[7,12)
   greet:  goodbye, World!      -> no match
   digits: 12345                -> match
   digits: abcde                -> no match
   ```
   (one pre-existing `-Wcomment` warning in `main.c`'s own header comment,
   unrelated to this lane, not touched).

3. **using-the-matcher-from-c.md / using-the-library.md** — the 30-line
   library example (adapted from `docs/spec/match_api.md` §8.0, extended
   with the `features="all"` field to exercise the NULL-trap paragraph
   honestly against a lookaround pattern `(?=a)b|[0-9]+`): compiled with
   `gcc -O2 -Ilib -o libdemo libdemo.c build/libpcrec.a`, ran, printed
   `compiled ok`; the emitted header's provenance line and `RX_NCAPS 1`
   confirmed present.

4. **features-and-modules.md** — `--list-syntax` header line confirmed;
   `--explain '\p{L}'` output confirmed (`requires module 'unicode-props'`,
   full row printed). `--pattern '(?<=a)b'` under `--features std1`
   confirmed refusing `requires module 'lookaround'` (offset 0).

5. **tuning-and-limits.md** — the D73 worked example re-verified LIVE in
   this worktree (the CLI-form command in `docs/spec/limits.md` §4 itself
   still uses the pre-D118 positional-pattern syntax and no longer runs
   as printed there — flagged below, not fixed by this lane): with
   `--pattern` substituted for the bare positional pattern,
   `build/pcrec -p rx --features all --emit-main -o rec.c --pattern '^(a(?1)?b)$'`
   + `gcc -O2` + a 684-byte subject → `match 0 684` (exit 0); a 686-byte
   subject → `frames` (exit 3, `PCREC_ERR_FRAMES`). Matches the spec's
   own figures exactly.

6. **troubleshooting.md** — exit-code table verified live:
   `--emit-main` binary on `a`/`b`/no-arg → exit 0/1/2 respectively
   (`match 0 1`, `nomatch`, `usage: ... <subject>`). `pcrec`'s own exit 1
   on an unclosed group (`missing closing ) for group (pattern offset 1)`)
   and on a `requires module` refusal, both confirmed.

7. **`--list-limits`** header line confirmed live (used only to point the
   reader at it, no numbers copied into the guide).

## Control validation

- `make -j4 CC=gcc-16` — clean (no rebuild needed after doc-only changes
  since the earlier build in this session; confirmed nothing under
  `src/`/`cli/`/`lib/` touched).
- `make strict CC=gcc-16` — **clean**: `strict: whole tree compiles clean
  with -Werror -Wshadow`.
- No `make test`/heavy suite run (per brief: a full gate was running
  elsewhere on this box; this lane touched only `docs/` and `README.md`,
  nothing `make test` exercises).
- Searched for a doc-link checker (`grep`/`find` over `scripts/`, `tests/`
  for `*link*check*`/`*docs*`): **none exists in this tree.** Manually
  verified every markdown link in the eight new chapters plus the index
  resolves to a real file (script check, all clean, no MISSING lines).

## One finding for main, not fixed here (out of this lane's scope)

`docs/spec/limits.md` §4's own "Command" block (the D73 worked example)
still shows the pre-[REL-1.10]/D118 CLI form — a bare positional pattern
with no `--pattern` flag — which no longer parses (`pcrec: ...: not an
existing file; a literal pattern is given with --pattern`). The example's
NUMBERS are still correct (re-verified live above with `--pattern` added),
only the command's own syntax is stale. `docs/dev/lanes/iface_digest.md`
§4's own note about `limits.md:779` being stale from an earlier CLI shift
is the same class of miss recurring — this one is `limits.md`'s own §4,
not a different section. Flagging rather than fixing since `docs/spec/`
edits are outside this lane's charter (guide only).

## Report

Branch `lane/rel13`, all work committed. No follow-up round anticipated
unless the interface shifts again before beta ships; if it does, a fresh
lane should start from the "Every code snippet BUILT AND RUN" transcripts
above rather than re-deriving them.
