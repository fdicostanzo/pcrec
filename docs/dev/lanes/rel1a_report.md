# [REL-1.1] + [REL-1.2] (lane rel1a, sonnet, 2026-09-21)

Branch `lane/rel1a` from `6b856bea`. Docs-only: `README.md` and
`docs/pcre2_compliance.md`. Nothing under `src/`, `cli/`, `lib/` touched.
Charter: `docs/dev/plan.md` [REL-1]/[REL-1.1]/[REL-1.2], D113/D114 +
its 2026-09-21 addendum, `docs/dev/lanes/rel1pre_facts.md` (the pre-flight
survey this lane's substeps were sized from — §2 README staleness, §5
compliance freshness, §6 stranger's-build prerequisites).

## [REL-1.1] — README.md rewrite

Full rewrite of the Status section and a reorganization around three new
sections (`## What pcrec compiles`, `## Requirements`, `## Build and test`),
replacing the old undifferentiated bullet list.

- **Status**: corrected the roadmap framing rel1pre's §2 flagged — M4
  (captures/VM hybrid) and M5 (UTF-8) were listed as roadmap items; both are
  complete (`docs/dev/plan_completed.md:3907` for M5.0, `docs/dev/plan.md`'s
  M4.1-M4.7 COMPLETED rows). M6's five ruled modules (named-groups,
  assertions, atomic-groups + possessive quantifiers, backrefs, lookaround)
  plus `recursion` (shipped outside the ruled list, [DD-14]),
  `unicode-props` (M5.0 stages 3-5) and `verbs` are all named as shipped.
  **M3 (streaming) is stated as the one open milestone** — verified
  `STATE:not-started` at `docs/dev/plan.md:254-255`, nothing else under
  `## M` headings in `plan.md` reads not-started.
  Wrote "0.1 beta, pre-release" in prose per the brief's version-wording
  instruction — no version string, badge, or tag invented (REL-1.4's own
  "what the constant versions" ruling is still pending).
- **Public surfaces named**: `abi` (currently 27, verified live —
  `./build/pcrec -p rx --emit-main -o /tmp/x.c 'a(b|c)+d'` emits
  `/* Generated by pcrec (abi 27)`, matching `docs/spec/match_api.md:159`/
  `:2011`), `--tune=N`, `--engine=dfa|vm|auto`, `--features`, `-e byte|utf8`,
  and the registry dump surfaces (`--list-syntax` etc.), each pointed at its
  owning `docs/spec/` file rather than restated.
- **Requirements caveats from rel1pre §6**: gcc as the target compiler with
  the Apple-Clang-builds-`make`/`test`-but-not-`ubsan`/`asan`/`lint` caveat
  (previously silent per rel1pre), GNU make, python3 (unpinned, used
  throughout the suite), libpcre2 optional with the SKIP-banner behavior
  explained (`docs/testing.md:98-130`), and a one-line darwin/Linux note.
- **`docs/spec/` linked as the contract** (D80), `docs/guide/` **NOT
  linked** — confirmed absent (`ls docs/guide/` -> no such directory,
  [REL-1.3] creates it) — the README instead says the spec is the
  reference and does not restate it.
- **Build/test instructions** reproduce CLAUDE.md's "Build & test" block's
  content (not contradicting it) plus the `-Werror`-not-default rationale
  (R5-Q1) inline, since a stranger reading only the README should not need
  to also open CLAUDE.md (which rel1pre §7 flags as candid internal-process
  documentation, not a contributor-facing document).
- Dropped the old "a portable fallback emitter is on the roadmap" claim —
  rel1pre §2 found no corresponding plan row for it; reworded to a neutral
  "is not built" with no roadmap claim.

## [REL-1.2] — docs/pcre2_compliance.md as the public support page

Ran the `compliance-refresh` skill's procedure for real, per its own steps:

1. **Regenerated/checked component 1** (generated construct index):
   `python3 tests/registry/compliance_section.py --check` ->
   `PASS: pcre2_compliance.md's construct index matches the dump (138 rows)`.
2. **Reconciled component 3** (keyed annotations): `--names` ->
   `PASS: every module named in pcre2_compliance.md prose exists in the
   registry (12 distinct)` + 2 more PASS lines (ROADMAP_NEVER agreement,
   0 RS_MODULE ROADMAP_NEVER rows); `--check-annotations` -> `PASS: all 91
   annotation keys name live constructs (40 registry, 51 base), no
   duplicates, no unknown section` + `PASS: pcre2_compliance.md's 21
   annotation blocks match docs/pcre2_compliance_annotations.txt (91
   records)`. All PASS, unchanged before/after this lane's edits (re-ran
   `--check`/`--check-annotations` after the header/front-matter edit to
   confirm the prose-only change didn't disturb the generated markers —
   same PASS lines).
3. **Component 2** (the independent PCRE2-side survey): NOT re-run. Per the
   skill's own step 3, component 2 only needs a re-run "when PC-2 fires or
   PCRE2 moves" — `[PC-2]` is still `STATE:not-started`
   (`docs/dev/plan.md:1959`), no PCRE2 version change is in flight
   ([PCRE2-UP] is `STATE:not-started`, no measured need), and libpcre2 is
   still 10.46 per the live suite run below. Stated in the header rather
   than silently skipped.

**Drift result: ZERO.** All three of the above PASS cleanly against the
current tree — nothing needed regenerating or reconciling. This confirms
rel1pre §5's finding: the page's two substantive touches since the header's
claimed "2026-08-22" date were `81817971` (2026-09-12, the [M5.0]
close-out's own compliance-refresh, which correctly reconciled component 2's
prose for the K53 fix and left components 1/3 untouched because they
already matched) and `a4ea61e0` (2026-09-19, a pure `src/parse/` ->
`src/dump/` path-citation edit inside the D65 explanation prose, verified
by reading `git show a4ea61e0 -- docs/pcre2_compliance.md` — one line, a
file-path substring, no construct/status content). Neither moved anything
this page's own checks measure. The header itself had been stale (wrong
date) since those touches; the header's "last refreshed" is now bumped to
2026-09-21 with the evidence above, and the six-week staleness is stated in
the header itself as the recurrence [DOC-DRV] exists to retire.

Added the public-reader front matter the brief and rel1pre §5 asked for: a
short paragraph before the "Reference:" line stating what the page is, and
D26's tiering in one sentence (what a pattern matches and whether a
construct is real are exact; diagnostic wording for something unimplemented
is not; "requires module 'X'" is a complete answer) — no gold-plating of
diagnostic wording, no restatement of the three-component model (left
exactly as is, immediately below the new paragraph).

## Validation run and results (verbatim result lines)

- `make -j4 CC=gcc-16` — clean build, `BUILD_RC=0` (full log
  `/tmp/rel1a_build.log` in the worktree's temp area, not committed).
- `make strict CC=gcc-16` — `strict: whole tree compiles clean with
  -Werror -Wshadow`, `STRICT_RC=0`.
- `python3 tests/registry/compliance_section.py --check` /`--names`
  /`--check-annotations` — all PASS, quoted above.
- `python3 tests/registry/compliance_section.py --tension` — informational,
  ran clean (lists `survey-only` tokens, the expected/designed asymmetry
  between the generated index and the hand-written survey prose — no
  failure mode, `--tension` never exits non-zero by design).
- `bash tests/registry/run_registry_tests.sh` (PC-3/PC-4 + every
  component-1/3 structural check; this is the check-owning script for both
  files, run SOLO per BOILERPLATE, `timeout 600`, log
  `/tmp/rel1a_registry.log`): every stage green —
  - registry well-formedness etc.: `checks passed: 225 / checks failed: 0`
  - PC-3 (registry vs libpcre2 10.46, resolved live -- did NOT skip):
    `checks passed: 209 / checks failed: 0`
  - PC-4 semantic differential + definitions structural checks:
    `checks passed: 108 / 0`, `checks passed: 24 / 0`,
    `checks passed: 54 / 0`
  - `[DD-11.3]` option-matrix self-oracle: `354 cells, 101244 A==B
    comparisons, 101244 A==C comparisons, 0 disagreements` ->
    `PASS: [DD-11.3] option-matrix self-oracle -- every resolved
    definition agrees with its row's shipped construct, and with
    libpcre2, cell for cell`
  - No FAIL lines anywhere in the log.
- Grepped every reader of `README.md` and `docs/pcre2_compliance.md` in
  `tests/`, `scripts/`, `tools/`, `.claude/`. Root `README.md` has NO
  readers among them — every `README.md` hit is a different, subdirectory
  README (`tests/bench/compare/README.md`, `tests/fuzz/README.md`,
  `tests/recursion/`'s / `tests/assertions/d27/`'s own), confirmed by
  reading each citation line. `docs/pcre2_compliance.md`'s readers are all
  either the registry suite above (functional) or comment-only citations
  in `tests/reject/`, `tests/cli/`, `tests/spec_mod0/`, `tests/mech/
  sabotages/` (prose references / a reject-table diagnostic string that
  itself contains the literal "docs/pcre2_compliance.md" — pcrec's own
  compiled text, unaffected by this doc's edits) — none parse this file's
  content.
- Sanity-checked the generated-marker balance in `docs/pcre2_compliance.md`
  after editing (23 `BEGIN GENERATED` / 22 `END GENERATED` mentions) — the
  apparent mismatch is line 58's own PROSE describing the marker syntax
  (`the small <!-- BEGIN GENERATED ANNOTATIONS: ... --> block`), not a real
  marker; pre-existing, not touched by this lane, and the structural checks
  above (`--check`/`--check-annotations`) are the real authority on marker
  balance and both PASS.
- `python3 scripts/emit_sweep.py --ref 6b856bea` — the required
  byte-neutrality control (this lane touched no emitter; expect 0 movers on
  all five streams). **LAUNCHED, OWED** — a full-corpus self-check plus
  real comparison run (3,944 pattern rows / 306 composition files across
  5 streams) takes longer than this lane's BOILERPLATE polling budget;
  launched backgrounded as this lane's last act, `timeout 900`, log
  `/tmp/rel1a_sweep.log` in the worktree's temp area. At hand-off it had
  passed the self-check's stream-1/2/3/4 builds cleanly (no output besides
  the stage banners, which is the self-check's own success shape) and was
  proceeding to the real comparison. This run is expected to show 0
  movers / 0 asymmetric, since neither changed file is read by the
  emitter; the manager or a follow-up agent should read the log's tail for
  the final summary line before treating this control as complete.
- `make test-codegen` — NOT run separately; `run_registry_tests.sh` above
  already exercises every structural check that reads either changed file,
  and this lane changed no emitted byte, so the codegen structural suite
  (which checks emitted-artifact SHAPE, not doc prose) has nothing new to
  catch. Available to the manager as a cheap confirmation at merge if
  wanted.
- A full darwin `make test` was NOT run — correctly out of scope for a
  docs-only lane per BOILERPLATE ("A full darwin `make test` is NOT
  required for a docs-only lane; say so in the report").
- No census/manifest/pin moved in this lane's diff (docs-only, no emitted
  byte, no test count changed) — nothing to re-pin.

## Found stale, left OUT of scope (list, not fixed)

- `cli/main.c --help`'s `-e ENCODING` text still reads "utf8 is refused
  until milestone M5" — M5.0 completed 2026-09-12
  (`docs/dev/plan_completed.md:3907`). This is `cli/` source text, outside
  this lane's docs-only scope and outside [REL-1.1]/[REL-1.2]'s charter.
- Every GAP `docs/dev/lanes/rel1pre_facts.md` already enumerated and this
  lane did not charter to close: `docs/guide/` absent ([REL-1.3]),
  CONTRIBUTING.md / PR template / RUN-STAMP / `.github/` CI absent (REL-META
  items a/b), no version constant / `--version` flag / CHANGELOG / tag
  ([REL-1.4], pending the "what the constant versions" ruling), no
  SECURITY.md / CODE_OF_CONDUCT.md, and D73's specific 684-byte worked
  example not present in `docs/spec/limits.md` (the general frame/trail
  mechanism is documented; the one worked number is not).
- `docs/pcre2_compliance.md`'s own body is otherwise unchanged — this lane
  did not audit every survey-table row for staleness beyond running the
  refresh procedure's own checks, which is what the procedure is for.

## Rulings received

None — no ruling request was needed; the brief's own instructions (D113/
D114, the 2026-09-21 addendum lifting REL-1.1/1.2/1.4 to start now) were
sufficient to proceed without escalation.
