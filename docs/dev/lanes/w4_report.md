# [REVW.4] WAVE 4 (CLI + CONFIG) — DELIVERY REPORT

Lane `w4` (opus), branch `lane/w4` from `main` at **`39860b66`** (the
merged fact sheet). Six item commits plus one repair commit, in the
brief's dependency order. Every stated cost is measured; every anchor and
reader was found by grep and re-driven after committing.

---

## 0. READ THIS FIRST — five things that are not in any item's commit

**(1) The ruled ship shape for `lib/pcrec.h` was PRICED AND DECLINED, and
the alternative is smaller in every column.** D111's lean was a GENERATED
BLOCK between markers, regenerated by `make gen-tables`, with a structural
check. Measured: the flags enum spans **578 lines for 26 enumerators** —
roughly 552 of them the per-bit PROSE that IS the public header's
documentation — so generating the enumeration means relocating all of it
out of the enum or moving it into an internal `src/core` file, plus a
generator (~60 lines) and a drift check (~40). **650-700 lines against the
ruling's own ~150 stop threshold**, so I stopped on the sub-item as
instructed. What ships instead: a row names its bit as a **TOKEN**, not a
number, and the X-macro stringifies it where a name is wanted. The bit is
therefore spelled exactly ONCE in this tree — in the public header, where
it is a contract — and `axes.def` holds no copy to drift from.
**`lib/pcrec.h` is byte-identical across the whole wave.** D111's premise
was that the table would carry the bit VALUES; it does not have to, and
once it does not, there is nothing to generate, no marker, no generator and
no drift check. The seam D111 worried about is additionally **already
checked by a pre-existing arm** — `axes_registry_check.sh` sweeps every
`1u << N` bit in the header and fails if `--list-axes` names it on no row —
driven in the failing direction here (delete the `PCREC_NO_CLS_FOLD` row →
`run_axes.sh` FATALs, the registry check goes 2 red, and the CLI stops
accepting the flag: three independent detectors, none of them new).

**(2) L11-F6's "X9 IS THE WHOLE REMEDY" for `emit_predicate_axes` is
FALSE, and the census says so.** That row (and synthesis §2.2, and
ADDENDUM-1's "pure case") holds that the axis-table centralization is the
entire fix for a 178-code-line function with no `if` and no loop. After
X9 the function is **still 294 span / 178 code lines** — unchanged, measured
by `tools/review/function_census.py` before and after. What X9 removes from
that file is real (a 17-entry `DENY_NAMES` table, the `cli_flag_of`
(axis, candidate) ladder, the `V()` stringifier at 17 sites, 15 hand-typed
`cli_flag` strings) but it is the flag COLUMNS, not the rows. The function
is long because it carries ~40 per-CANDIDATE rows each with its own
`applies` prose, which is not axis-table data and which no table can hold.
**A centralization removes the columns a table owns, never the rows a
narrative owns** — worth carrying into wave 5 and [EMIT-VERB].

**(3) The sheet's own §0.3 correction is itself wrong, in a way worth
naming.** It reports `emit_predicate_axes` as "294 code lines, not 178"
and concludes the review's figure is "under half the current count". The
regenerated census reads **span 294, CODE lines 178** — so the review's
178 was the census's code-line figure and is still exactly right; only the
path and the line range moved. `cli_parse` the same way: 429 span, 247
code. *A census with two length columns can be quoted with the wrong one,
and the two differ by 40% here.*

**(4) The `cli/main.c:<line>` citation rot is not 5 of 28. It is
effectively all 27.** Resolving every citation against the branch-point
file before touching anything: cli.md's `--step-budget` cite landed on
`parse_raise_only`, its `--engine=` cite inside the `--list-schema` help
STRING, its `--count-groups` cite on the `-fno-atomic-discharge` arm,
limits.md's `--fno-step-budget` cite on a `parse_raise_only` comment. All
27 are now re-aimed BY SPELLING; `grep -rno "cli/main\.c:[0-9-]*"
docs/spec/` returns nothing.

**(5) One re-pin was OWED BY ITEM 2 AND MISSED, and the reason the ritual
missed it generalizes.** `run_registry_tests.sh` guards `limits_check.sh`'s
PASS-LINE COUNT and item 2 took it 22 → 24. The D94 ritual finds readers by
grepping for the number that moved; item 1 moved the ROW COUNT (57 → 58)
and that grep found every reader. Item 2 moved a different number and this
guard spells it `22` — a value with no textual relationship to anything
item 2 touched. **There was nothing to grep FOR.** Same class as
`battriage_report.md`'s `EMITTED_BYTES` manifests after the abi-25 label
fix: *a reader whose text never cites the number still moves with it.*
Landed as its own commit (`95537791`) with the guard's comment now naming
itself as the second reader.

---

## 1. Commits, in order

| # | commit | item |
|---|---|---|
| 0 | `1491ef3c` | regenerate `tools/review/out/*.tsv` at the branch point |
| 1 | `0b16b98c` | F2 — `SIZE_TERM_BAR_DEFAULT` → the `limits.def` row `PCREC_SIZE_TERM_BAR`; `percent` joins the unit vocabulary |
| 2 | `69b58474` | D107/Q6 — `limits_check.sh`'s name-keyed filter INVERTED; sabotage S261 |
| — | `95537791` | item-2 RE-PIN: `run_registry_tests.sh`'s coverage guard 22 → 24 |
| 3 | `e447d76f` | L11-F4 — the mode mutual-exclusion relation written once |
| 4 | `5c2443ff` | X9/D111 — `src/core/axes.def`; both awk scrapers deleted |
| 5 | `d0c8b4c2` | M3 — the `--` guard once, 5 value arms through one parser, 6 menus table-driven |

---

## 2. Per item

### Item 0 — the census artifacts

Re-ran all five generators at `39860b66`. Functions 860 → 896,
clone-candidate member rows 258 → 274, literals 12,617 → 12,691 (5,149 →
5,172 distinct), `#include` statements 200 → 202 / local edges 94 → 96 /
back-edges 0, churn rows 54 → 55. Every file's stated `rows N` header
re-verified against a body re-count. Finding: §0.3 above.

### Item 1 — F2

`PCREC_SIZE_TERM_BAR`, row 58 of 58, `unit percent`, `kind selection
knee`, `override none`, `anchor ""`.

Two column choices made against the sheet's open question 1, both for
consistency with the row's own sibling rather than by preference:

- **`override` is `none`.** The constant has no `#ifndef` guard today and
  inventing one would ADD a `-D` lever that does not exist (D77).
  `--tune=N` moves it (95 / 85) exactly as it moves the threshold, and
  `limits.md` §8b's own reading of that column is that `--tune` is not what
  it names — it says the threshold's `-D` "means no CLI override exists for
  it". For the bar, `none` means `--tune` is the only lever of any kind.
- **`anchor` is EMPTY.** Both §8b knees carry an empty anchor today and
  that section's prose says a selection knee earns no anchor on its own;
  making this row the one anchored member of that table would be a novel
  asymmetry inside one section.

`percent` was taken over `unit ""` as the ruling directs, with the argument
written at the point of extension: every other unit names a thing being
counted, a percentage counts nothing, and the `unit` column is what tells a
caller how to read the `value`.

**D80:** `limits.md` §8b "Two `limits.def` knees" → "Three", the table
gains the bar's row, and the section states what is and is not observable.
The two `src/core/limits.def:161`/`:352` line citations in that section are
DROPPED rather than re-aimed — exactly the stale-by-line-number class, and
the sentence reads correctly without them.

**Readers moved by grep** (`grep -rn SIZE_TERM_BAR_DEFAULT`, whole tree):
2 live C sites, `limits_check.sh`'s manifest (+1 name, 57 → 58 in both the
test and its message), `src/core/CLAUDE.md`, `tests/size/CLAUDE.md`, S192's
three comment mentions. Four documents of historical record deliberately
not edited.

**S192 RE-VERIFIED, NOT RE-ANCHORED**, and its header now says why: its
`SAB_BEFORE` is the consumer line inside `size_term_choose`, which reads
the `bar` PARAMETER and never the constant.

Validation: `limits_check.sh` 22/0 with `[count]` reading 58;
`run_size_term.sh` 31/0 including §5's lowered-cap reference-compiler cell
(the only instrument in the tree that watches the bar DECLINE);
`emit_sweep --ref 39860b66` 0 movers on all four streams at full reach.

### Item 2 — D107

The scan is no longer keyed on the name at all: every object-like
`#define NAME <numeric literal>` and every explicitly numeric-valued enum
member under `src/`, `cli/`, `lib/`. **Measured population 40** — 17
defines, 23 enum members — dispositioned as 0 rows, 18 LIMIT-allowlist
entries, 22 NON-LIMIT entries.

**D107 priced this at "~14 allowlist lines" and the real cost is 40.** Its
estimate came from lens 3's F1 population, which counted the `#define`s
the old filter could not see and never counted the enum side at all.

**D107 names three non-limit kinds and the population needed a FOURTH.**
Sentinels, API enum values and schema versions cover 18 of 22; the rest are
CARDINALITIES AND ORDINALS (`VM_NRUNG`, the four `SDR_*`, `TRIE_ENABLED`) —
numbers nothing can be measured against by construction.

Two structural exclusions, both stated in the check with what they cannot
see (because `coding_guide.md` §5 item 4's rule is this arm's own
motivating instance):

- comments and string-literal INTERIORS are masked first, so the ten
  `#define PCREC_VM_RUNG_*` lines `emit_dfa.c` PRINTS INTO ITS ARTIFACTS
  are not read as pcrec's constants. Without the mask the arm would report
  ten failures about a file it cannot fix.
- an ORDINAL ENUM (explicit initializers exactly 0,1,2,… in member order)
  is excluded: delete every initializer and not one value changes, so none
  of those numbers can be a policy. **Structural, not lexical** — it asks
  what the number DOES, never what it is called. ONE non-ordinal member
  re-arms the WHOLE enum, which is why all four `SDR_*` rows are listed
  over `SDR_MAX = 2` alone. Blind spot named in the check: a real policy
  number sitting at its own ordinal position inside an otherwise-ordinal
  enum.
- `COMPILE_MAX_ATTEMPTS` LEAVES the allowlist rather than being carried
  forward: its value is an EXPRESSION over other named values, so the scan
  never reaches it, and arm 3c refuses an entry the scan never reaches.

Three arms, because a classifier over an empty population passes perfectly:
`3a` classify (40/40), `3b` SCAN_FLOOR ≥ 30 (40 found, K35), `3c` every
allowlist name actually REACHED (40/40, [MECH-REACH]).

**Failing direction driven for all three, each reverted:** `#define WEIGHT
37` in `prefix_k.c` → FAIL naming WEIGHT; the historical-miss shape
`#define VM_ISL_MIN_BRANCHES_NEW 2` in `emit_vm.c` → FAIL naming it (both
invisible to the pre-D107 vocabulary); floor raised above the live
population → FAIL; a `GONE_CONSTANT` allowlist entry → FAIL naming it.

**Sabotage S261** (highest id on main was S260): the materiality bar comes
back as a function-local `#define SIZE_TERM_BAR_LOCAL 75`. The VALUE is
unchanged on purpose, so nothing observable moves and only D90's one-home
rule breaks — and the name carries none of the pre-D107 vocabulary, which
is why the row could not be written until the inversion landed: **the
detector had no failing direction for its own central defect.** Driven
SOLO: `limits:1fail/23pass — DETECTED`, unexpected 0, anomalies 0.

`coding_guide.md` §1.8 and §5 item 4 restated per the guide's own
convention.

### Item 3 — L11-F4

One `CLI_MODE_TABLE` (14 modes, each with its field, its field SHAPE and
its CLI spelling), one `cli_modes_active`, four named masks each DEFINED
FROM the one below it. Both rulings taken as given: narrowest-per-site
membership, and the summed-count relation kept as a SECOND relation.

The site-6 name ladder folds too — `%s takes no pattern and no -o` picked
its subject from an eight-arm `?:` chain and now calls `cli_mode_name` over
the same mask. Safe because the count relation above it has already
returned when more than one is set, and the table's order IS the ladder's
order, which is stated at the call site rather than left to be re-derived.

**Acceptance proven, not asserted** (`w4_modesweep.py`, committed): all
469 one-, two- and three-subsets of the 14 mode flags × 3 operand shapes =
**1,407 invocations**, against a build of the branch point, compared on
returncode AND stdout AND stderr.

| measure | before | after |
|---|---:|---:|
| identical (rc + stdout + stderr) | — | 1,406 / 1,407 |
| accepted (rc = 0) | 14 | 14 |
| refused (rc ≠ 0) | 1,393 | 1,393 |

The single difference is `--list-limits` alone, stdout only, both rc 0 —
item 1's new row, not this item.

**THE UNION MEMBERSHIP, PROPOSED NOT BUILT** (ruling (a) invites it):
today's four memberships mean `--probe-ask --flavour=pcre2` is ACCEPTED and
`--source --flavour=pcre2` is REFUSED, for no reason a reader of either
site can give. The union (13 members everywhere) is one line in this
item's own table and would be a cleaner contract. It is a D80 change that
newly refuses combinations accepted today, and its population is not
measured here. **Recommend it for wave 5's D80 batch**, with the
acceptance sweep above as the instrument that prices it: re-run it with
the union mask and the diff IS the blast radius.

### Item 4 — X9 / D111

See §0 (1), (2). Ruling-by-ruling:

**(a) The hand-sort, TABLED BEFORE THE ROW COUNT IS CLAIMED.**
`grep -c "PCREC_NO_\|PCREC_FORCE_" lib/pcrec.h` = 32. The rule I sorted
by: *is it a declaration at all, and does it name an answer-identical SHAPE
choice addressed by a `-f` spelling?*

| class | n | disposition |
|---|---:|---|
| PROSE — comment text, not a declaration | 9 | not a member; **this is most of the gap between 32 and the axis count, and no lens said so** |
| `PCREC_NO_CAPTURES` — a SEMANTIC flag (`--no-captures`, changes what the artifact REPORTS), no `-f` spelling, absent from `emit_predicate_axes` | 1 | not a member |
| the `-f` family: 20 deny bits + 2 force bits | 22 | **20 ROWS** |

Cross-check, independent of the sort: `--list-axes` carries exactly 22
bit-bearing rows, and `run_axes.sh`'s own bits-4..31 derivation reports 22.

**(b)** §0 (1).

**(c)** The row carries default polarity and both spellings.
[EMIT-VERB]'s coming `-fcomments` fits as
`PCREC_AXIS(PCREC_NO_COMMENTS, "-fno-comments", PCREC_EMIT_COMMENTS,
"-fcomments", PCREC_AXIS_DEFAULT_OFF)` with no change to the shape. NOT
BUILT. The polarity column has no reader today and ships anyway, which the
file says out loud: *a column added with its first consumer is a column
every existing row has to be revisited for.*

**(d)** D80: the mechanism paragraph in `tuning.md`; `registry.md`'s two
descriptions of the check's `cli/main.c` leg rewritten (it RUNS the parser
now); all 27 citations re-aimed by spelling (§0 (4)).

**(e)** `emit_sweep.py` gains a `dumps` stream — the seven `--list-*`
surfaces, whole-file byte comparison, always identity-required, population
floor 7. 33 lines of function + 12 of wiring (the ruling's guidance was
≲40). **Its positive control is already on record:** run against the
branch-point binary it reports 1 mover — item 1's new `--list-limits` row,
the exact change all four artifact streams reported as byte-neutral. That
is w4_facts.md open question 6's gap, closed and demonstrated rather than
argued.

**The two scrapers are deleted, and ONE property they held is kept.** After
the extraction the parser and the dump read the same row, so the
reconciliation has nothing to reconcile. What the shared row does NOT make
true is that the grammar arm is still REACHED — a mis-ordered chain or an
empty spelling cell would leave the dump advertising a flag nobody can
pass. `check_cli_flag_accepted` RUNS `pcrec <flag> --count-groups -- 'a(b)'`
and requires exit 0. Behaviour, measured through a different mechanism from
the text it checks. 102 passing checks, the same count the branch-point
script reports: 22 reconciliation checks became 22 live probes, one for one.

### Item 5 — M3

See the commit message for the full breakdown. Three acceptance sweeps,
all against a build of item 4, all 0 differing: 63 diagnostic invocations
(`w4_diagsweep.sh`), 248 `--`-position invocations over 62 flag spellings
harvested by grep (`w4_dashsweep.py`), 1,407 mode invocations
(`w4_modesweep.py`).

`cli_parse`: **429 span / 247 code lines → 304 / 202** after items 4 and 5
together — the ~1/3 the review predicted, measured rather than counted.

---

## 3. The brief's open questions, answered

**8a — "13 codegen scripts referencing `cli/main.c`": VERIFIED, exactly
13.** And the number is less interesting than the shape: every one of the
13 references is the bare path `cli/main.c`, used to COMPILE a reference
binary (`gcc ... "$ROOT_DIR/cli/main.c" $srcs`). **Not one carries a line
number**, so none of them could stale under this wave and the blast-radius
figure is 13 files that need nothing.

**8b — "35 pinned diagnostics in tests/cli": NOT REPRODUCIBLE BY GREP, and
the review's own spelling is not in the file.** `grep -c '"pcrec: --"'`
returns 0 (the sheet found this too). What IS measurable:
`tests/cli/run_cli_tests.sh` pins diagnostic TEXT through **54
`assert_contains` assertions**, every one string-matched against captured
stderr and none line-anchored — which is the property that matters, and it
is why a byte-preserving rewrite needs no re-pin. 284/284 cases green after
every item, nothing re-pinned. **Recommend the 35 be treated as
unreproducible rather than carried forward a third time.**

**7 — the stale census artifacts**: regenerated at the branch point (item
0) and again at the lane tip (this commit), so no figure in this report
rests on a pre-wave-3 row.

---

## 4. Rulings received

Restated as given, all taken:

1. **F2's unit** — `limits.def` gains a `percent` unit, not `unit ""`.
   Taken; the argument is written at the point of extension.
2. **F2 is D80-observable** — `docs/spec/limits.md` hunk in the same
   commit, 57 → 58 rows. Taken (the 58 lands in `limits.def`'s header and
   the check's manifest; limits.md states no row count, so its hunk is §8b).
3. **S192 anchors on the consumer line** — re-verify, do not re-anchor
   unless it moved. Verified unmoved; the row's header now records why.
4. **Item 3 (a)** — the authoritative membership is NARROWEST-PER-SITE;
   today's acceptance preserved exactly; propose the union, do not build
   it. Taken; proposed in §2 item 3.
5. **Item 3 (b)** — the 6th site's summed-count exclusivity stays a SECOND
   relation. Taken.
6. **Item 4 (a)** — hand-sort `lib/pcrec.h`'s 32 hits, table the
   classification and the rule BEFORE claiming a row count. Taken; §2
   item 4.
7. **Item 4 (b)** — the generated-block ship shape, with a ≲150-line stop
   condition. **STOPPED on it**; the alternative and its measured cost are
   §0 (1).
8. **Item 4 (c)** — the row shape carries default polarity and both
   spellings; `-fcomments` must fit without changing it; do not build that
   row. Taken; not built.
9. **Item 4 (d)** — D80 in `tuning.md`; `cli.md`'s citations re-aimed by
   grep (all 27). Taken; zero remain.
10. **Item 4 (e)** — add a `dumps` stream to `emit_sweep.py` if it fits in
    ≲40 lines, else an ad-hoc diff. Taken as a stream; both were run.
11. **Item 5 / D26** — diagnostic WORDING does not change; a moved pin is
    a finding, not a re-pin. Held; no pin moved.

---

## 5. For [EMIT-VERB] and wave 5

1. **`-fcomments` is a row and the shape admits it** — spelled out in §2
   item 4 (c). Nothing in `axes.def` changes to take it; add the row and
   the parser, the dump and the sweep pick it up.
2. **[EMIT-VERB] WILL MOVE EMITTED BYTES, so it is the first customer of
   the `dumps` stream's sibling problem in reverse**: its `--list-axes` row
   is a dump mover AND its artifacts are `.c` movers. Run
   `emit_sweep.py` expecting movers on streams 1/2/4 and on `dumps`, and
   pin the delta per stream rather than asserting identity.
3. **`emit_predicate_axes` is still 178 code lines and X9 did not change
   that** (§0 (2)). If a wave wants that function shorter, the item is a
   CANDIDATE-ROW table (name, stamp value, `applies` prose), which is a
   different table from `axes.def` and a different argument. Do not charter
   it as "the rest of X9".
4. **The union mode membership** (§2 item 3) is a one-line D80 change with
   a ready-made instrument. Good candidate for wave 5's spec batch.
5. **`--warn-emit-bytes=` is the one value arm `cli_int_value` does not
   take** — unsigned, unbounded above, accepts 0. If a later wave wants it
   folded, the helper needs an unsigned sibling, not a widened signature.
6. **The second-reader class** (§0 (5)) is worth a line in the D94 ritual:
   after a grep for the old value, also run the suites that COUNT things
   the change moves. Both instances on record (this one and
   `battriage_report.md`'s) were found by running a suite, never by a grep.
7. **`docs/spec/` now has zero `cli/main.c:<line>` citations.** Keep it
   that way: cite a function or a flag spelling, never a line.

---

## 6. Validation summary (all measured on this branch)

| check | result |
|---|---|
| `make` + `make strict` (gcc-16) | clean after every item |
| `scripts/emit_sweep.py` self-check | PASSED all-identical at full reach |
| `emit_sweep` item 1 (ref `39860b66`) | 0 movers / 0 asymmetric, 4 streams |
| `emit_sweep` item 3 (ref `39860b66`) | 0 movers / 0 asymmetric, 4 streams |
| `emit_sweep` item 4 (ref `e447d76f`) | 0 movers / 0 asymmetric, **5** streams |
| `emit_sweep` item 5 (ref `5c2443ff`) | 0 movers / 0 asymmetric, **5** streams |
| seven `--list-*` dumps, direct diff across item 4 | all IDENTICAL |
| `tests/cli/run_cli_tests.sh` | 284 / 0 (unchanged from the branch point) |
| `tests/registry/run_registry_tests.sh` | 0 FAIL lines; PC-3, PC-4 (62,872 cells), definitions oracle (354 cells / 101,244 comparisons each way / 0 disagreements) |
| `tests/registry/axes_registry_check.sh` | 102 / 0 (same count as the branch point) |
| `tests/registry/limits_check.sh` | 24 / 0 (was 22 / 0) |
| `tests/axes/run_axes.sh` registry-derivation path | 22 axes, bits 4-25, 22 spellings derived, both FATAL guards silent |
| `tests/rxtsource/run_rxtsource_tests.sh` | 212 / 0 / 1 recorded (standing darwin C3 pin) |
| `tests/codegen/run_size_term.sh` | 31 / 0 |
| `make test-codegen` | 8 of 9 scripts; the ninth is the STANDING darwin `nm could not read arm_a.o` red |
| mode acceptance sweep | 1,407 invocations, 0 differing |
| `--` acceptance sweep | 248 invocations over 62 spellings, 0 differing |
| diagnostic sweep | 63 invocations, 0 differing |
| sabotage S261 (new) | DETECTED solo, `limits:1fail/23pass` |
| sabotage S192 (re-verified, not re-aimed) | DETECTED solo, `sizeterm:3fail/28pass, corpus:0fail/21pass`, reach ok(1/1), both populations 1 — its own recorded re-anchor figure, unmoved |

**A NOTE ON DRIVING ROWS SOLO:** `run_sabotage_matrix.sh` takes ONE
selector (`ONLY="${1:-}"`), so `... S192 S208 S209 S261` silently runs
S192 alone and reports "1 rows". A lane driving several rows must invoke
it once per row; the matrix's own trailer names the count, which is what
catches this.

**OWED at hand-off:** the solo mech re-drive of **S208 and S209** — the
two pre-existing `limits` rows whose arm item 2 rewrote. Neither is
re-aimed and neither plants against the inverted filter (S208 edits a
`limits.def` row's VALUE, S209 puts a bare literal back in `emit_vm.c`),
so both are expected DETECTED and S209 may additionally now trip part 3
(a bare literal is exactly what the inverted scan sees). Launched as this
lane's last act, one invocation per row; log path in the hand-off message.
S192 and S261 are COMPLETE and recorded above. Nothing else is owed. The
full battery is the manager's at merge.
