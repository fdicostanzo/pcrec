# [REL-1.4] version plumbing — lane rel1b report

Branch `lane/rel1b` from main `919388bd` (abi 27). D115 (docs/dev/decisions.md)
is the ruling this lane builds exactly: `PCREC_VERSION` = `"0.1.0-beta"`,
independent of `abi`, reported by `--version`, exported from `lib/pcrec.h`,
stamped in the emitted header, seeding a root `CHANGELOG.md`.

## What shipped, per file

- **`lib/pcrec.h`** — `#define PCREC_VERSION "0.1.0-beta"`, placed right
  after the include guard/includes (the first thing a reader meets), with
  a header comment stating the abi-independence rule. A plain `#define`,
  matching the header's existing style for simple constants
  (`PCREC_ENGINE_DFA`/`PCREC_ENGINE_VM`) rather than a new accessor
  function — no second mechanism invented.
- **`cli/main.c`** — `--version`: a `want_version` field added to
  `CliState`'s tail (beside `want_help`, covered for free by
  `cli_extras_clean`'s existing byte-span refusal — no clause of its
  own needed), parsed in the same `else if` chain as `-h`/`--help`,
  checked in `main()` the same way (`printf("pcrec %s\n", PCREC_VERSION);
  return 0;`). Added to the `usage()` text. Also fixed, per the manager's
  rider: the `-e ENCODING` help text still said "utf8 is refused until
  milestone M5" (M5.0 completed 2026-09-12) — rewritten to state both
  encodings compile.
- **`src/gen/emit_dfa.c`** — `PCREC_ARTIFACT_ABI` 27 -> 28. The essential
  provenance line gained the version: `/* Generated by pcrec %s (abi %d).
  Pattern: ... */` with `PCREC_VERSION, PCREC_ARTIFACT_ABI`. Comments on
  both emission sites (`emit_pattern_comment`, `emit_info_def`'s `.abi`
  initializer) updated to record the new bump, in the same style as the
  prior [EMIT-VERB] rider comment; also fixed a stale pointer in the
  `.abi` initializer's comment ("`src/core/limits.def`" -> "this file",
  where the constant actually lives — noticed while rewriting that
  sentence for the bump, not a separate errand).
- **`docs/spec/cli.md`** — new `### --version` subsection (§1, beside
  `-o FILE`), a Revision-history entry.
- **`docs/spec/match_api.md`** §6 — new top change-log entry (`rx_info.abi`
  is `28`... bumped from 27, D115), the prior `27` entry re-chained as
  "the bump before it was [EMIT-VERB]"; "gap-free from `2` to `27`" ->
  "to `28`"; the provenance-line description ("WHAT A DEFAULT ARTIFACT
  CARRIES BY WAY OF COMMENTS") updated to name `PCREC_VERSION` beside the
  abi digit.
- **`docs/spec/limits.md`** — per the manager's rider: the stale sentence
  at the `--max-emit-bytes` override note ("What is NOT yet built is the
  path that COMPILES from a pattern-source file at all — `--source` and
  `--target`") rewritten — that path shipped at [DD-13b.W1.2]
  (2026-08-31). Quoted below.
- **`CHANGELOG.md`** (new, root) — Keep-a-Changelog shape, one
  `[0.1.0-beta] — unreleased` section, facts only from `docs/dev/plan.md`'s
  milestone states (M4/M5/M6 done, M3 open) plus this lane's own additions.
- **CLAUDE.md updates**: `cli/CLAUDE.md`, `lib/CLAUDE.md`,
  `docs/spec/CLAUDE.md` each gained an entry. `src/gen/CLAUDE.md` and
  `tests/codegen/CLAUDE.md` needed NO edit — both explicitly state the abi
  change log lives ONLY at `docs/spec/match_api.md` §6 (D76 addendum,
  [REVW.A1]) and their own `##`-titled sections are milestone design
  records, not a log a bump must append to. No root file inventory exists
  outside CLAUDE.md itself (checked `docs/CLAUDE.md` — docs/-scoped only);
  root CLAUDE.md is out of scope per the brief, so `CHANGELOG.md` is
  mentioned nowhere else.
- **Re-pinned readers** (below).

## Rider 1 — cli/main.c `-e ENCODING` help text (before/after)

Before:
```
  -e ENCODING, --encoding=ENCODING
                 subject encoding for THIS pattern: byte (default)
                 or utf8. Per-compile, never global: two patterns
                 in one binary may use different encodings. utf8
                 is refused until milestone M5
```
After:
```
  -e ENCODING, --encoding=ENCODING
                 subject encoding for THIS pattern: byte (default)
                 or utf8, both compile. Per-compile, never global:
                 two patterns in one binary may use different
                 encodings
```
Checked for a pin: `grep -rn "refused until milestone M5\|subject encoding for THIS pattern" tests/ docs/` hits only an archived probe transcript
(`docs/design/utf8_measurements/out/premises.txt`, never re-run) and
`dev_journal.md` (append-only, quoting the old text as history) — neither
is a live test pin. `tests/cli/run_cli_tests.sh`'s `--help` assertions
(case9/case10) are substring checks unrelated to this text. No re-pin
needed.

## Rider 2 — docs/spec/limits.md (before/after)

Before:
```
   **The state of that instruction, stated exactly.** It shipped as a
   forward reference — the sentence above named a mechanism that did not
   exist. The `config` block now EXISTS in the `.rxt` format
   (`docs/spec/rxt_format.md`, "The head") and pcrec parses it, including
   the `pcrec <raw flags>` line these two overrides would ride. What is
   NOT yet built is the path that COMPILES from a pattern-source file at
   all — `--source` and `--target` — so today the instruction describes
   where the override BELONGS rather than something a caller can run.
   Until that lands, the CLI flags are the only way to raise either cap,
   and this note is here so a caller reaching this page is told which of
   the two it is instead of discovering it at the command line.
```
After:
```
   **The state of that instruction, stated exactly.** It shipped as a
   forward reference — the sentence above named a mechanism that did not
   exist yet. The `config` block EXISTS in the `.rxt` format
   (`docs/spec/rxt_format.md`, "The head") and pcrec parses it, including
   the `pcrec <raw flags>` line these two overrides would ride. The path
   that COMPILES from a pattern-source file — `--source` and `--target`
   — shipped at [DD-13b.W1.2] (2026-08-31, `docs/spec/cli.md` §1's
   `--source FILE` section), so the instruction above is something a
   caller can run today, not only where the override belongs.
```

## `build/pcrec --version`

```
$ build/pcrec --version
pcrec 0.1.0-beta
```
And the emitted stamp (`build/pcrec -p rx -o - 'a(b|c)+d'` head):
```
/* Generated by pcrec 0.1.0-beta (abi 28). Pattern: a(b|c)+d */
```
Verified surviving `-fcomments` (D112 essential class, unaffected by the
comment switch — it rides the one line the switch never removes):
```
/* Generated by pcrec 0.1.0-beta (abi 28). Pattern: a(b|c)+d */
/* Feature set: std1 (modules: classes,modifiers) */
```

## The full "27" reader list, found by grep, with disposition

`grep -rn "abi 27\|\.abi = 27\|abi=27\|abi(27)\| 27)\|ABI 27" --include=*.c
--include=*.h --include=*.sh --include=*.py --include=*.md --include=*.rxt .`
(excluding `worktrees/`, `docs/dev/lanes/`, `dev_journal.md` per the
CLAUDE.md ritual note), plus a second pass for `PCREC_ARTIFACT_ABI`/
`rx_info.abi`/`->abi`/`ABI_EXPECT`/`FILEPIN`:

| site | what it says | disposition |
|---|---|---|
| `src/gen/emit_dfa.c:51` `#define PCREC_ARTIFACT_ABI 27` | the constant | **RE-PINNED** to 28 |
| `src/gen/emit_dfa.c` (`.abi = %d` initializer comment) | change-log paragraph | **RE-PINNED**, new bump described |
| `src/gen/emit_dfa.c` (`emit_pattern_comment` comment) | describes the D112 rider | historical-untouched; new rider comment added alongside |
| `tests/codegen/run_codegen_tests.sh:2860` `ABI_EXPECT=27` | live gate expectation | **RE-PINNED** to 28 |
| `tests/codegen/run_codegen_tests.sh` (giant bump-narrative string) | change-log-in-a-string | **RE-PINNED**, `[REL-1.4]` entry appended |
| `tests/codegen/run_recursion_identity.sh:849` `FILEPIN="...:-74c2192c}"` | the (B) whole-file pin | **LEFT AT PRE-BUMP VALUE, OWED TO THE MANAGER** — same precedent as [EMIT-VERB] (opt5i/ccdiff1): D76 needs a commit reachable after merge, which a lane branch commit is not. Gate is RED on this branch by construction |
| `tests/codegen/run_recursion_identity.sh:820-850` (EMIT-VERB abi 26->27 block) | design record | historical-untouched; new `[REL-1.4]` 27->28 block added after it, stating (A) is untouched (the line sits above `goto <p>_L0;`) |
| `tests/codegen/run_vm_identity.sh:336` "neither the comment axis nor the abi 27 bump is implicated" | historical note, unrelated corpus event (K40) | historical-untouched, not a value comparison |
| `tests/resource/run_resource_tests.sh:554-570` (k59premul byte pin) | `762114` byte pin | **RE-PINNED** to `762125` (+11 = `" 0.1.0-beta"`), arithmetic comment extended, `-fcomments` re-measurement 769844->769855 |
| `tests/registry/run_registry_tests.sh:531` "deny bit 26 ... force bit 27" | AXIS BIT numbers (`PCREC_NO_COMMENTS`/`PCREC_FORCE_COMMENTS`), not `abi` | not-a-reader (different namespace) |
| `tests/registry/limits_check.sh:321-323,468` | names `PCREC_ARTIFACT_ABI` as a SCHEMA VERSION allowlist entry, no numeric value | not-a-reader |
| `docs/spec/tuning.md:2105` "bit 26 ... bit 27" | axis bit numbers, same as above | not-a-reader |
| `docs/spec/match_api.md:2026` §6 | the abi change log | **RE-PINNED** (new top entry; "gap-free from 2 to 27" -> "to 28"; provenance-line description gains `PCREC_VERSION`) |
| `docs/dev/plan.md:573` "Runs after abi 27 lands" | [PLAN-AUDIT] historical gating note | historical-untouched (manager-owned file, not edited by this lane) |
| `docs/dev/plan.md:578` | this lane's own [REL-1.4] row | manager-owned, not edited |
| `docs/dev/decisions.md:7686,7727,7815` | D113/D104-addendum/D115 text, all append-only ADR entries | historical-untouched (decisions.md is append-only; D115's own text at :7815 already correctly anticipated this exact 27->28 bump) |
| `docs/dev/plan_completed.md:3970` | [TOUR-EXEC] archival record, "abi 27 unchanged" | historical-untouched (archived, never edited post-close) |
| `studies/scan_edge_ladder/README.md:67` | historical D112 note | historical-untouched (`studies/` never built/tested by `make`) |
| `docs/dev/cls_tree_study.md:540`, `docs/design/dd13_format/format_design.md:3687`, `docs/dev/decisions.md:509`, `src/parse/mod_backrefs.c:613`, `studies/simd1/*.c` (`z >> 27`) | unrelated numeric coincidences (section numbers, byte offsets, a hash shift) | not-a-reader |

## Checks run, verbatim result lines

- **`make -j4 CC=gcc-16 && make strict CC=gcc-16`** — clean build, then
  `strict: whole tree compiles clean with -Werror -Wshadow`.
- **`make test-codegen CC=gcc-16`** (async, log `test-codegen2.log`) —
  first run caught a real regression from this change (see below); after
  the fix: `run_group: 9/10 scripts passed` — the sole red is the
  standing darwin `nm` probe in `run_inline_capability.sh` ("nm could not
  read arm_a.o"), pre-existing and unrelated to this lane.
  `run_codegen_tests.sh`'s own `ABI_EXPECT=28` check and the giant
  bump-narrative assertion both passed as part of that 9/10.
- **`make test-resource CC=gcc-16`** (async, log `test-resource.log`) —
  exit 0. The k59premul cell: `'a{5,25000}' -fno-scan-edge
  -fno-start-pinned is rescued by [K59-PREMUL]'s drop ladder at 762125
  bytes (was 1104674 before the rung existed; 769835 before emitted
  comments went off by default; 762105 before the abi joined the
  generated-by line; 762114 before the version joined it)`.
- **`make test-registry CC=gcc-16`** (async, log `test-registry.log`) —
  every section `checks failed: 0`, ends `PASS: [DD-11.3] option-matrix
  self-oracle -- every resolved definition agrees with its row's shipped
  construct, and with libpcre2, cell for cell`.
- **`make test-rxtsource CC=gcc-16`** (async, log `test-rxtsource.log`) —
  `checks failed: 0`, `PASS: rxtsource: INV-COMPAT holds over 213 files /
  3944 blocks / 28971 expectation lines`.
- **`python3 scripts/m6read_check_sab_anchors.py`** —
  `sabotages checked: 270 (286 anchor sites)` / `all anchors resolve`
  (286/286, unchanged).
- **`python3 scripts/emit_sweep.py --ref 919388bd --bin build/pcrec`**
  (async, log `emit_sweep.log`, elapsed 266.2s) — self-check PASSED;
  stream 1 (`.c` default engine): movers 3522/3522 reachable; stream 2
  (`.c --engine=vm`): 3523/3523; stream 3 (`--emit-ir`): movers 0
  (expected — the IR listing carries no comment/abi text); stream 4
  (composition): 98/98 artifacts; stream 5 (registry dumps): movers 0
  (expected — no artifact stamp there); asymmetric 0 everywhere. Every
  `.c`/`.h` artifact moves, which is the intended asymmetry (the
  essential provenance line is unconditional). One-specimen diff
  (`'a(b|c)+d'`, ref `919388bd` vs. this branch), full and exact:
  ```diff
  -/* Generated by pcrec (abi 27). Pattern: a(b|c)+d */
  +/* Generated by pcrec 0.1.0-beta (abi 28). Pattern: a(b|c)+d */
   #define PCREC_FEATURE_SET "all"
   ...
  -#include "ref.h"
  +#include "work.h"
   ...
  -    .abi = 27,
  +    .abi = 28,
  ```
  (the `#include` line differs only because the two compiles used
  different `-o` basenames for the diff, not a real difference — the
  same-basename comparison `emit_sweep.py` itself runs excludes it).
  Three real differing lines total: the provenance header, and `.abi`'s
  value — exactly the header-only, one-line-of-text delta this bump
  promises.

**Darwin `make test` is NOT run from this lane** — the manager gates it
on merged main, per the brief.

## Not done / left for the manager or a future lane

- `run_inline_capability.sh`'s darwin `nm` red is pre-existing and
  unrelated; not investigated further (out of this lane's scope).
- No new CLI test case added for `--version` itself — the brief's task
  list didn't ask for one; `tests/cli/run_cli_tests.sh` has no assertion
  on it today. Worth a follow-up row if the manager wants one.
- `src/gen/CLAUDE.md`'s stale `PCREC_ARTIFACT_ABI (src/core/limits.def)`
  pointer (the constant actually lives in `src/gen/emit_dfa.c`) is left
  as found — same error I fixed inline in `emit_dfa.c`'s own comment
  while rewriting that sentence for the bump, but the `CLAUDE.md` copy
  wasn't part of my edit path and isn't a numeric-value reader of `27`,
  so left alone per D26 (no gold-plating outside scope).

## Validation status

COMPLETE. Every check the brief named ran and passed (or is the expected,
pre-existing darwin red). Nothing owed.
