# Code-organization candidates from Frank's tour — a separate list

Opened 2026-09-20 (seventy-second session) beside the tour list
(`2026-09-20-frank-tour.md`). The tour list holds READABILITY tidy-ups whose
proof is a byte sweep. THIS list holds CODE-ORGANIZATION changes — a structure
that would replace a convention, a parallel mechanism, or a comment-held
invariant. Each item is a question, and **"not worth it" is an acceptable
answer**: an item is dispositioned BUILT / NOT WORTH IT / DEFERRED with the
measurement or argument that decided it (D77: build under a named need).

## [ORG-1] src/gen/emit_vm.c — the repeat rung ladder is decided three times

**Observation (manager, walking vm_count_slots with Frank):** `vm_cost_rep`
(2029), `vm_count_slots_rep` (2691) and the emitter's `vm_rep` each re-decide
CURSOR → REVDET → COUNTER → GENERAL by calling the same predicates
(`vm_cursor_fits`, `vm_revdet_fits`, `vm_counter_fits`) in the same order, with
`under_atomic` threaded into each. The invariant that they agree is held by
the banner "slot counting (must mirror the emitter's own rung decisions)" and
by mech rows, not by the code. A divergence sizes the slot array for a rung
other than the one emitted.

**Candidate:** one `vm_rung_of(Vm *v, const Ast *a, bool under_atomic)`
returning an enum (`VM_RUNG_CURSOR/REVDET/COUNTER/GENERAL`), consumed by all
three walks; each walk's ladder becomes a `switch` on the enum. Sub-question:
memoize the decision on the node (or a side table keyed by node) — today
`vm_cursor_fits` fills two scratch buffers (the stride sequence table and the
capture offsets) to answer yes/no in the pre-passes, then recomputes them at
emission.

**Why it might NOT be worth it:** the three ladders are short and the
predicates are already shared — the duplication is the ORDER and the
threading, ~15 lines total; mech rows S-series already witness the mirror;
a memo on the node is a new field for a compile-time-only fact. The measured
need would be a divergence that actually shipped (none recorded) or a fourth
walk arriving (a listing-only pass, say). Tier if built: opus, with
[TOUR-2]'s lane.

**Disposition:** OPEN.

## [ORG-2] tree-wide — should switch arms become per-case functions, as a rule?

**Observation (Frank):** the same comment as [ORG-1] applies to switches in
general — turning individual `case` pieces into functions ([TOUR-2] step 2 is
the vm_cost instance; wave 2 already did it to vm_emit's ten arms).

**Measured at 16343fce (a code-line census of every `switch` under src/ and
cli/, comment lines excluded, case labels at the switch's own indent or one
level in):** 87 switches. Longest arm ≥20 code lines: **3**. ≥40: **0**.
The three: `src/parse/parse.c:693` (the `'x'` escape arm, 39 code lines),
`src/parse/rxt_source.c:1269` (`RXT_C_EXACTLY_ONE_OF`, 25 lines of a 44-line
constraint switch), `src/gen/emit_vm.c:2301` (vm_cost's `A_CAT`, 21 lines —
[TOUR-2] takes it to ~4 with `cost_add`). Everything else is ≤16 code lines
per arm; the long SPANS (possessify.c:891 at 183 lines, revdet.c:101 at 128,
dfa.c:690 at 125, emit_vm.c:2861 at 124) are long because of their COMMENTS,
not their code — the census says the tree's switch arms are already lean.

**Leaning: NOT WORTH IT as a tree-wide pass.** A rule "every arm is a
function" would manufacture ~80 one-to-ten-line functions for no reader
gain. Worth it where an arm carries BOTH code and a correctness argument
(the [TOUR-2] reasoning — the argument becomes the function's header) or
exceeds ~20 code lines: today that is parse.c:693 and rxt_source.c:1269,
two candidates for the tour list if Frank reaches those files, and vm_cost,
already listed. The census script is a scratchpad one-off; if the rule is
wanted as a standing floor, `tools/review/function_census.py` is where a
`longest_switch_arm` column would live (D77: not before a second reader).

**Disposition:** OPEN — leaning NOT WORTH IT tree-wide; three named arms
individually.

## [ORG-3] src/gen/emit_vm.c — split the file at its own banner sections?

**Measured (2026-09-20-readability-experiments.md §3):** over 173 historical
commits, a split at the file's 17 banner sections cuts the mean read closure
to 4,284 lines (35% of the monolith) versus 5,538 by pass and 6,821 by kind;
`pcrec_emit_vm` alone (2,809 lines) sits in 122 of 182 commits.
**Price:** code move byte-neutral and mechanical; 285 sabotage anchors + doc
citations by `emit_vm.c:NNNN` re-aimed. **Sequence:** after [TOUR-1] (which
removes the largest term) if at all. **Disposition:** OPEN.

## [ORG-4] src/gen/emit_dfa.c — three candidates; Frank's overall read: the file is fine

Surveyed 2026-09-20 evening (7,813 lines; 3,255 code / 4,251 comment; 204
functions; organized by named form AXES A-J from line 3,584 with `DfaForm`
derived once at `dfa_form_derive`). Frank: "generally this file seems fine.
its not what i would write but i am learning it works for you and i can
understand it." Candidates recorded, none chartered:

- **(a) emit_attempt (6519-7036, 242 code lines, the file's one altitude
  break):** seven phases by its own block comments (empty-engine check,
  class-axis context, jump rows, seed tables, candidate-start prefilter, the
  three "try the next start" sites, the three-way start dispatch) — the DFA
  side's [TOUR-1] in miniature, one function per phase. Byte-neutral by
  construction; opus.
- **(b) the comment ratio, 1.3:1, carried by two functions:** emit_info_def
  (106 code / 326 comment) and unanch_start (30 / 95) — correction-of-record
  narratives with bracketed tags; the remaining half of what [REVW.A1] did to
  this file's abi log. Prune to invariant + pointer (coding_guide §4.2).
- **(c) 44 clone-census pairs, the most of any file:** mostly HONEST pairs by
  construction — the decl/def emitters per entry, residual decls/defs, the
  indexed/premultiplied table comments — each emitting one shape for two
  spellings. A single emitter parameterized by spelling is the reader's ask;
  it CAN move emitted bytes, so it is code-org, not tour.

**Leaning: NOT WORTH IT for (a) and (c) while the file reads at one altitude
past the derivation; (b) is cheap and rides whichever lane next prunes
essays in src/gen.** Disposition: OPEN.

## [ORG-5] src/parse/parse.c — pcrec_is_bare_anchor is a KIND property in switch clothing

**Observation (Frank, reading parse.c):** "pcrec_is_bare_anchor() screams OO
design" — a switch over the AST kind whose arms all `return` a constant; the
function's own comment says it is "a property of a KIND, not a per-NODE
flag". OO puts it on the node class as a virtual; C puts it in a per-kind
property table (one row per kind, the measured PCRE2 cells beside each row);
the exhaustive no-default switch is the C idiom for the pure-virtual guarantee.

**Measured at 3611f1dc:** 46 switches over an AST kind tag in the tree; **1**
is a pure kind→constant table (this one); 45 are structural walks (cost,
nullability, minw, lowering, emission — pass-shaped, per §2 of the
experiments file). A kind-property table would have one column today.

**Leaning: NOT WORTH IT until a second pure kind property exists** — that is
the trigger (D77: name the measurement). Disposition: OPEN.

## [ORG-6] src/parse — a tokenizer layer (the parser is scannerless)

**Observation (Frank, reading cls_skip / `\Q…\E`):** the quote flag on `Ctx`
is set in the item readers (parse.c 829, 1043 + the endpoint mirror) and
cleared in the skippers (cls_skip, xskip) — opener and closer in different
functions — because there is no tokenizer to own that state; "so this is
because we don't tokenize before parsing?" Yes: a lexer would deliver each
quoted byte as a LITERAL token with a quoted attribute, the range/quantifier
rules would apply to tokens as they do to bytes now (measured: `[\Qa\E-z]`
is the range a-z; `^\Qab\E+$` quantifies the last byte only), and quoting
would live in one place.

**Why the tree is scannerless (as read from the decisions):** regex lexing
is mode-dependent (class / extended-whitespace / quote modes, with different
whitespace rules inside and outside classes), so a lexer needs a
mode-feedback interface from the parser — the same coupling relocated; the
module DOORWAYS (ext.c, the registry's arbitration) decide ownership over raw
spellings and would have to move into or see through the lexer; PCRE2 itself
is scannerless, and mirroring its structure keeps the D26 divergence surface
small.

**Leaning: NOT WORTH IT NOW** — a layer added to oracle-tested code, with
the doorway rework as the hidden cost; the readability it buys (quoting and
the two whitespace skippers in one place) is a header problem today
([HDR-2]). A legitimate first-choice design for a fresh project.
Disposition: OPEN.

## [ORG-7] exported symbols with no reader outside their own file

**Observation (Frank, reading parse.c):** "why is pcrec_ast_class_from_bits
not static?" — it IS cross-TU (ext.c and mod_classes.c call it; the `pcrec_`
prefix is the tree's rule for anything a second translation unit reads, and
libpcrec.a exports every non-static symbol since there is no visibility
control). But the same question, asked of the whole export list at 1afb7b0d:
23 `pcrec_` exports are referenced by no .c other than their definer; 14 of
those are linked by a unit check or a registry check under tests/ (the reason
they are exported); **9 have no reader anywhere** except the census tables:
pcrec_tune_row, pcrec_bufsurface_inert, pcrec_dfa_axis_cands,
pcrec_emit_abi_types, pcrec_nfa_has_asserts, pcrec_registry_verb_name_limit,
pcrec_rxt_constraint_name, pcrec_rxt_schema_opens_group,
pcrec_rxt_source_ncols. Making them static drops nine names from the public
export surface and moves nothing else.

**Leaning: WORTH IT, small** — a sonnet admin item (static + delete the
internal.h declaration + a build), gated by `make strict` and the export
census (tests/spec_mod0/check01_isolation.sh). Disposition: OPEN.

## [ORG-8] src/parse — two ways to handle an escape (esc_char_value vs the registry doorway)

**Observation (Frank):** esc_char_value is "sort of a halfway function" —
most of it could go through the ext doorway; it stays because it is base
tier, and that means two mechanisms for escapes.

**As it stands:** esc_char_value decodes the VALUE escapes (six fixed, bare
and braced `\x`, escaped punctuation, raw high bytes) before any doorway;
everything else returns -2/-3 and the caller rewinds into pcrec_ext_escape
(arbitration, module gate, diagnostic tiering). The six base rows exist in
the registry (ESC_BASE_D, with definitions) and tests/registry/
registry_check.c's own comment says they are "never CONSULTED": the check
proves the row's CLAIM matches the parser, not that the parser reads the row.
Braced `\x{}` was ruled a parse.c special case with no row (2026-08-29).

**One way:** run the doorway first for every escape; a base row's definition
(DEFK_STR / DEFK_TEXTFN) drives the decode; esc_char_value shrinks to the
lexical residue (high-byte read, escaped punctuation). Mostly a relocation,
proved by the existing registry check + corpus. Costs the rulings already
name: a row kind for braced `\x{}`, the class path's scalar-claim shape, an
arbitration per base escape (unmeasurable).

**Under Unicode / other encodings (Frank's follow-up):** the split's shape
does not change — the doorway side is encoding-blind (sets intersect the
universe in the one class constructor), and the encoding reaches the base
decoder at exactly two points (the `\x{}` universe cap; lit_next_cp for a
raw high byte). But the seam already shows once: `\N{U+hhhh}` (a module row,
through the doorway) and `\x{hhhh}` (esc_char_value) are two decoders for
"a code point by number" whose universe cap and surrogate refusal must
agree by hand. [ENC-MODEL] is the natural moment to move the cap out of the
base decoder into the constructor's own universe intersection.

**Leaning: NOT WORTH IT WHILE THE CHECK HOLDS.** Triggers: a base escape's
behaviour changing in one home and not the other; a seventh value escape;
the `\N{U+}` / `\x{}` pair diverging. Disposition: OPEN.
