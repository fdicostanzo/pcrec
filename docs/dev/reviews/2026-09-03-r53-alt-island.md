# r53 — the VM alternation island ([ENG-ISL] STEP 1, lane isl1, tip 799bd29+) — adversarial panel at close

Date: 2026-09-03 21:3x-… EDT, fifty-first session. Subject: lane/isl1
(22+ commits on main 9d8401a; report docs/dev/lanes/isl1_report.md).
Three read-only critics, D6: r53sem (opus; engine semantics vs the
oracle), r53chk (opus; the checks and gates), r53doc (sonnet; the spec/
doc contract and the abi ritual). Critics ran single compiles and
oracles only; no make, no suites. Dispositions are the manager's.

## r53doc — spec/doc contract + abi ritual (delivered 21:4x)

| # | sev | finding | disposition |
|---|---|---|---|
| D1 | HIGH | `VM_ISL_MIN_BRANCHES` (2) / `VM_ISL_MIN_BRANCHES_PREFIXED` (4) are bare `#define`s in emit_vm.c — selection knees like `PCREC_SIZE_TERM_THRESHOLD`, so D90 wants limits.def rows; and the registry's sabotage name-shape filter (`MAX|CAP|LIMIT|BUDGET|_LEN\b|DEPTH|NEST`) cannot see a `MIN_` knee, so the gap is structurally undetectable today | FIX on the branch (landing item 1): limits.def rows + limits.md; the filter gains `MIN`/`THRESHOLD` and the commit says what it missed. Check-design lesson: a name-shape filter is an allowlist of spellings — a new knee with a new spelling is invisible |
| D2 | HIGH | docs/design/artifact_size_term.md:766 states the old §9 witness values (0.7475/0.7548, "within 0.7%") that the island moved to 0.7497/0.7499 and the pool replaced | FIX (landing item 2): corrected with the pool's numbers and a pointer |
| D3 | MED | no landing pointer: engine_m4.md §6.4 ("Trie-factored alternation … DEFERRED — not built"), docs/design/CLAUDE.md's alt_dispatch_study entry, studies/alt_dispatch/CLAUDE.md, studies/CLAUDE.md | FIX (landing item 3): BUILT as [ENG-ISL] STEP 1 with the two deviations named |
| D4 | MED | docs/testing.md has no test-island row (and test-altcls was already missing — pre-existing drift); tests/CLAUDE.md has no tests/island/ bullet | FIX (landing item 4), both rows |
| D5 | LOW | "island" names two mechanisms (engine_m4's DFA exact-match islands vs the VM alternation island) with nothing disambiguating | FIX (landing item 5): one sentence in each place |

Verified complete by r53doc: tuning.md §2.20 (shape of §2.18/2.19, bit
23, table row); match_api.md §6.3's `RX_VM_ALT_ISLANDS` IFF; the abi
17→18 paragraph and opt5d's fifth reader; `--list-axes` row, pcrec.h,
cli.md; the three budgets in D90's allowlist with a cited argument;
Makefile TEST_SECTIONS/.PHONY; the abi-17 grep sweep clean (history
untouched by design); the narrative string (site 3); FILEPIN 9bc7723
with the re-pin-to-merge header; --emit-ir ISLANDS correctly outside
table_contract.md ([DD-8]); run_ir_listing's three-way comparison; no
new refusal or wording (D26 moot); src/gen/CLAUDE.md as built.

## r53sem — engine semantics vs the oracle (delivered 22:15; F5+ owed after truncation)

| # | sev | finding (measured) | disposition |
|---|---|---|---|
| S1 | HIGH | CALLER-OBSERVABLE REGRESSION: `((?:aa\|bb)×11\|zzz)` is REFUSED under the island (1,504,398 emitted bytes vs the 500,000 cap) and compiles at 55,694 B under -fno-alt-island. vm_isl_words' A_CAT cross product: the budgets bound the WORD LIST (8192 / 262,144 B) while emitted size scales with TRIE NODES (~200k nodes ≈ 3 MB at the byte budget). Knee on the VM: k=8 238,754 vs 29,389; k=9 458,275 vs 30,593; k=10 897,983 vs 31,796 REFUSED. test-axes cannot see it (no corpus shape; refused=0) | FIX before merge, general form: the island is built only where its emitted size ≤ the chain's for the same subtree (both computed from the analysis); the budgets become secondary; a DECLINE witness in tests/island; a REFUSAL-IDENTITY check over a synthetic cross-product ladder (never refused-under-island-and-accepted-under-deny) |
| S2 | HIGH | `((?:aa\|bb)×9\|z×18)` (106 chars): island 481,490 B, gcc 42.8 s / 574 MB; chain 53,804 B / 0.26 s — frameless + always_inline replicates the trie six times; a 55-char fuzzer pattern shows the same class (×6.8 bytes, ×17 gcc) | S1's rule declines these; verified after the fix; sharpens [CC-DIFF] STEP 2's case |
| S3 | MED | the limits allowlist argument refuted: MAX_WORDS 512 would accept what 8192 refuses — the constant bounds acceptance | = C6 (limits.def rows, corrected argument; S1 is the witness) |
| S4 | MED | the census's "429 is a lower bound" is wrong in one direction: altcls-merge folds single-byte branches into a class and vm_isl_single declines the subtree (`ab\|cd\|a\|b`: 0 islands default, 1 under -fno-altcls-merge) | FIX the text; expanding a single-byte class into member words is STEP 2 (filed with the tail form) |
| S5 | LOW | not answer-identical under `--step-budget`: the trie walk charges no steps, so a budget-bound cell answers under the island where the chain gives up (details owed — truncated) | pending the rest: expected disposition = the documented "identity modulo which budget binds" class (tuning.md §2.5's rule), stated in §2.20 |

Lesson (learnings §3 candidate): an axis's acceptance is REFUSAL identity as well as answer identity — a mechanism that can only make artifacts smaller in the corpus can make them larger off it; the sweep's `refused=0` counted the corpus, not the shape.

## r53chk — checks and gates (delivered 21:34; F3's tail and F4+ owed after a truncated message)

| # | sev | finding (measured on the lane's binary) | disposition |
|---|---|---|---|
| C1 | CRITICAL | §9's witness pool never reads the compiler's decision: it forces `--unroll=$k` (UNROLL_K_WHY = "option"), computes argmin/K8 itself and compares to a LITERAL 0.75 in the script — change compile.c's constant to 60 or 85 and §9 stays green; its own failure text ("0.70 or 0.80 would fail this file") is false. r42 S6 reopened; what still bounds the constant is §5's cap-rescue cell (below ~92 only) | FIX before merge: the forced-K ladder is the INSTRUMENT (the table); the ASSERTION is the compiler's own default-build decision (the stamp) = the prediction from the ratio vs the bar, on both sides; proven by the 70/80 sabotage in a scratch build, recorded in the commit |
| C2 | CRITICAL | NOT ONE pool member stamps an island (0 on all eight; bytes identical under -fno-alt-island; `((a)\|ab)` never eligible — a capture in a branch); §14.2's "island TAKEN" labels and the "families move in opposite directions" property are false; §11.4 misattributes `((a)\|ab){4000}c`'s move (not the island — the stamp line or the vm_cost arm) — the manager's pool ruling rested on a half-wrong cause | FIX before merge: ≥ 2 members that actually stamp an island (verified by the stamp); §14.2/§11.4 corrected; the false property dropped. Manager's note: the ruling's INSTRUMENT diagnosis stands (a ratio pinned by a fixed pair is fragile), its CAUSE was half wrong |
| C3 | HIGH | the identity gate's island excuse is a BLANKET per-artifact exemption: `prog_region()` is the whole program and `isl_a > 0` (the subject's self-report) excuses any byte difference anywhere in a stamped artifact — an unrelated emitter regression there reads green | FIX before merge, general form: a moved region is excused IFF the subject compiler's `-fno-alt-island` build of the same pattern is byte-identical to the pin (denying the new axis must restore identity); the converse and the non-vacuity floor stay, the floor's number and origin stated |
| C4 | HIGH | the island bucket's non-vacuity floor is 1 (argued in the header, not measured): narrowing the predicate to a handful of islands reads green; §11.2's 166/236/166/123 were PRE-Q4 and the post-Q4 population was never recorded (critic's sample: ~8.5% of 857 alternation-bearing corpus patterns; 3 of 27 in tests/base/alternation_trie.rxt) | FIX before merge: an enumerated `ISLAND_PATTERNS` manifest (~12 named corpus patterns that must stamp) asserted both ways with the file's existing staleness failure; post-Q4 counts recorded |
| C5 | MED-HIGH | the converse ("stamped but unmoved FAILS") has no positive control and compares to the August reference, so it goes vacuous as the emitter moves; and this gate is a byte-INEQUALITY gate, not a correctness gate (a wrong island reads moved+stamped) | FIX before merge: the biconditional made self-contained against the deny axis (stamped ⇒ island build ≠ own -fno-alt-island build; unstamped ⇒ identical), one sabotage witness for the converse; the header says correctness lives in the corpus + test-axes |
| C6 | MED | the limits allowlist argument for the three island budgets ("over budget the chain emits unchanged, so none bounds acceptance") is contradicted by §4 (w-384 compiles as an island, is refused as a chain) and by moved sizing fields (.frame_capacity 3 vs 4) | FIX: the three budgets become limits.def rows with the corrected argument (a budget can only trade an accept for a reject at the cap) |
| C7 | MED | tests/island reads only stamps, all from vm_isl_build — an emitter that stamps and then emits the chain passes 24/24 | FIX: taking ⇒ region differs from the -fno-alt-island build; declined ⇒ byte-identical |
| C7b | MED | the §7 frame witness `(?:abcd\|abc\|ab\|a)z` reads frameless=0 on BOTH builds — the swap did not fix the flaw its comment names | FIX: assert frame_capacity strictly lower on the island build than the chain build for a pushing witness |
| C8 | MED | the "one island, not one per factored run" assertion prints RX_ALTCLS_FACTORED and never asserts it — if altcls stops factoring, the check tests nothing ([MECH-REACH]) | FIX: assert f > 0 as the precondition |
| C9 | MED | §9's byte extraction defaults to whole-file bytes when the warning's wording is missing — every ratio would drift toward 1 silently (the [ART-SIZE] quantity confusion, third time named) | FIX: hard-fail on a missing extraction |
| C10 | MED | the pool floor counts measured members, not band-eligible ones (2 of 8 out of band); the whole ABOVE side is one shape; the band value 0.05 asserted, not derived | FIX: floor over band-eligible members, ≥ 2 distinct shapes per side (chosen with C2's island-taking members), the band stated as asserted |
| C11 | LOW-MED | run_ir_listing.sh demands the island stamp unconditionally while compiling without --engine=vm; the stamp is VM-only, so a selection move makes a spurious red | FIX: compile the list with --engine=vm or condition on the engine stamp |
| C12 | LOW | run_ir_listing.sh:286's `${art_isl:+does not }` always expands — the failure text is wrong in both directions | FIX |
| C13 | LOW | the branch's .gitignore enumerates two rulings files where main has the glob (4101b8b) | FIX: the glob + `.stage`; a merge conflict otherwise |
| C14 | NIT | match_api.md:2168's abi pointer moved though the always_inline spelling is abi 17's | FIX: revert to "the abi-17 entry above" |

Verified sound by r53chk (independently re-measured): the census
re-pin (193/3347/27018 re-derived by its own awk; the +124 passing-cell
delta = island 114 + counterk 10); run.sh's denominators with the same
known-fail offsets; the new corpus cells oracle-verified (10/10 vs
python re); the abi ritual complete at all eight readers with no live
17 left (FILEPIN → merge commit at landing); §9 prints its table on
PASS too and its failure text forbids adjusting the constant; the
narrow decline asserted in both directions; every §14.2 ratio
reproduces to a ten-thousandth — "the numbers are honest; the causal
labels beside them are wrong".

Lesson (learnings §3 candidate): a check that FORCES the knob it exists to test has removed its own subject — the assertion must read the decision the compiler makes on its own; and "excused by the subject's own stamp" is a control sharing a source with what it controls — excuse by DENYING the axis instead.

## Cross-lane finding recorded here (edge1, 21:3x)

[ART-SIZE]'s §9 materiality bar broke TWICE in one evening under two
unrelated changes: the island (both witnesses converged onto the
constant) and K43's +578 B constant on every VM artifact (a K-invariant
constant pulls the argmin-rung ratio toward 1 and flipped the TAKEN
witness). The instrument was the defect — a ratio pinned by two fixed
witnesses — and isl1's witness pool (eight members, table printed,
straddle-within-band, red-with-table) is the general repair; edge1 was
ruled to touch nothing and inherit the pool at merge. Lesson for
learnings §3: a bar whose quantity is a RATIO over emitted bytes moves
under every byte-invariant change; pin it with a population, not a pair.
