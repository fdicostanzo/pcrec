# r53 — the VM alternation island ([ENG-ISL] STEP 1, lane isl1, tip 799bd29+) — adversarial panel at close

Date: 2026-09-03 21:3x-… EDT, fifty-first session. Subject: lane/isl1
(22+ commits on main 9d8401a; report docs/dev/lanes/isl1_report.md).
Three read-only critics, D6: r53sem (opus; engine semantics vs the
oracle), r53chk (opus; the checks and gates), r53doc (sonnet; the spec/
doc contract and the abi ritual). Critics ran single compiles and
oracles only; no make, no suites. Dispositions are the manager's.

## r53doc — spec/doc contract + abi ritual (delivered 21:4x)

| # | sev | finding | disposition |
|---|---|---|---|
| D1 | HIGH | `VM_ISL_MIN_BRANCHES` (2) / `VM_ISL_MIN_BRANCHES_PREFIXED` (4) are bare `#define`s in emit_vm.c — selection knees like `PCREC_SIZE_TERM_THRESHOLD`, so D90 wants limits.def rows; and the registry's sabotage name-shape filter (`MAX|CAP|LIMIT|BUDGET|_LEN\b|DEPTH|NEST`) cannot see a `MIN_` knee, so the gap is structurally undetectable today | FIX on the branch (landing item 1): limits.def rows + limits.md; the filter gains `MIN`/`THRESHOLD` and the commit says what it missed. Check-design lesson: a name-shape filter is an allowlist of spellings — a new knee with a new spelling is invisible |
| D2 | HIGH | docs/design/artifact_size_term.md:766 states the old §9 witness values (0.7475/0.7548, "within 0.7%") that the island moved to 0.7497/0.7499 and the pool replaced | FIX (landing item 2): corrected with the pool's numbers and a pointer |
| D3 | MED | no landing pointer: engine_m4.md §6.4 ("Trie-factored alternation … DEFERRED — not built"), docs/design/CLAUDE.md's alt_dispatch_study entry, studies/alt_dispatch/CLAUDE.md, studies/CLAUDE.md | FIX (landing item 3): BUILT as [ENG-ISL] STEP 1 with the two deviations named |
| D4 | MED | docs/testing.md has no test-island row (and test-altcls was already missing — pre-existing drift); tests/CLAUDE.md has no tests/island/ bullet | FIX (landing item 4), both rows |
| D5 | LOW | "island" names two mechanisms (engine_m4's DFA exact-match islands vs the VM alternation island) with nothing disambiguating | FIX (landing item 5): one sentence in each place |

Verified complete by r53doc: tuning.md §2.20 (shape of §2.18/2.19, bit
23, table row); match_api.md §6.3's `RX_VM_ALT_ISLANDS` IFF; the abi
17→18 paragraph and opt5d's fifth reader; `--list-axes` row, pcrec.h,
cli.md; the three budgets in D90's allowlist with a cited argument;
Makefile TEST_SECTIONS/.PHONY; the abi-17 grep sweep clean (history
untouched by design); the narrative string (site 3); FILEPIN 9bc7723
with the re-pin-to-merge header; --emit-ir ISLANDS correctly outside
table_contract.md ([DD-8]); run_ir_listing's three-way comparison; no
new refusal or wording (D26 moot); src/gen/CLAUDE.md as built.

## r53sem — engine semantics vs the oracle

(pending)

## r53chk — checks and gates

(pending)

## Cross-lane finding recorded here (edge1, 21:3x)

[ART-SIZE]'s §9 materiality bar broke TWICE in one evening under two
unrelated changes: the island (both witnesses converged onto the
constant) and K43's +578 B constant on every VM artifact (a K-invariant
constant pulls the argmin-rung ratio toward 1 and flipped the TAKEN
witness). The instrument was the defect — a ratio pinned by two fixed
witnesses — and isl1's witness pool (eight members, table printed,
straddle-within-band, red-with-table) is the general repair; edge1 was
ruled to touch nothing and inherit the pool at merge. Lesson for
learnings §3: a bar whose quantity is a RATIO over emitted bytes moves
under every byte-invariant change; pin it with a population, not a pair.
