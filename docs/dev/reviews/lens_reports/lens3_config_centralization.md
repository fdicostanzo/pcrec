# LENS 3 — magic numbers, inline strings, config centralization

Lane lens3cfg (opus, REVIEW, read-only to `src/`/`cli/`/`lib/`/`tests/`).
Charter: `docs/dev/reviews/code_review_criteria_draft.md` lens 3.

**WIP — in progress.**
