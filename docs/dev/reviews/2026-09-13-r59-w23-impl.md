# r59 — D6 panel on the W23 implementation note (w23_impl.md rev 1)

Date: 2026-09-13. Object: `docs/design/dd13_format/w23_impl.md` REVISION 1
(lane w23impl, branch `lane/w23impl`, docs-only). Panel: three read-only
critics — A (checks/sabotage/validation design, opus), B (design fidelity
against the ruled record, opus), C (citation/mechanical accuracy, sonnet).
All tree citations verified in the MAIN tree at the merge base; no critic
ran `make` or wrote anything. Manager dispositions inline; the fix round is
lane `w23implfix` (rev 1.1 of the note, same branch lineage).

VERDICT: the note's SPINE holds — the five-merge staging, the R5/`NF != 15`
catch (all three critics independently reproduced it), the no-abi §1.9
argument, the S238/S239-S245 numbering, the production-axis F-Q1 mapping
(critic B S-1: all fourteen productions land, nothing orphaned) and the
forward residue sweep (B S-3/S-4: clean, independently re-run). What fell
is on three axes: one reader class the survey mis-classified (A1), one
delivery axis the staging silently dropped (B-2), and the r58 residue
class — DISPOSITION TEXT — recurring in the note's claims about the bench
documents (B-1, B M-5), found by exactly the third-pass method r58's note
prescribed.

## Blockers — all FIX-NOW

**r59-A1 (critic A). R6 is an INEQUALITY reader and the survey marked it
safe; W23-S4 green would PROVE it broken.**
`tests/rxtsource/run_rxtsource_tests.sh:499` counts every non-comment row
whose field 2 is not `pattern` and asserts ZERO ("the corpus has no
head"). A `#section cases` data row has field 2 = a line number → counted.
After W23.4 the check fails with a head-declaration message at roughly the
corpus's case-line count — a misdirection exactly like R5's. The note's
first-column-is-`line` invariant protects EQUALITY readers only (R2/R3
verified safe); an inequality reader is broken by the same fact.
DISPOSITION: R6's row becomes BREAKS LOUDLY; its repair joins R5's in
W23.4's build order, BEFORE any section is emitted; §1.5's invariant
sentence narrows to equality readers.

**r59-A2 (critic A). §1.8's head-level parity fix is unbuildable in leg C
under the seam ruling; its re-aim passes vacuously.**
`dup_head_description.rxtin` is head-bearing; `verify_rxt.py:407-417`
refuses ALL head-bearing files at the seam, so `check_refusal_all3`
(verdict-only for legs B/C today) goes green with leg C refusing for the
WRONG reason — and if W23-S2's class comparison is applied, it must go
red. The note schedules both and they contradict.
DISPOSITION: only the two HEADLESS fixtures (`nul_byte.rxtin`,
`dup_description.rxtin`) move to `check_refusal_all3`;
`dup_head_description.rxtin` stays single-leg with the reason stated; the
`tests/rxtsource/CLAUDE.md` scope note is NARROWED, not deleted (two
thirds of its scope survives).

**r59-B1 (critic B). §5's three-row bench correction list contradicts the
RULED list — the r58 disposition-text class, fifth recurrence.**
format_design §9's correction table (whose own header says the D78 outbox
message carries exactly this list) additionally carries
`licence`→`license` (their C4 types the old spelling and exits 1), the
`freq` provenance fields, D1's nine→eleven provenance keys, and the `mc`
adapter edit. The note re-derived a three-item list and generalized §9's
note about a DELETED row onto a rename the bench does have to make.
DISPOSITION: §5's correction paragraph and §6.5 item 4 restate §9's list
BY REFERENCE and stop re-deriving it. STANDING LESSON (journal): a
correction list that exists in a ruled document travels by reference,
never re-derived — re-derivation is how the disposition-text class
propagates.

**r59-B2 (critic B). H5 has no step: `include`'s HARNESS half is not in
the delivery, and §4.1 falsely asserts its spec row (S3) already landed.**
Leg A gets include resolution in W23.3; run.sh's `find`-based discovery
(:184-216, never named in the note), entry-set subtraction, closure
accounting, §2.11's fourth failure class (RESOLUTION failure) and the S3
spec hunk appear nowhere — no fixture, no check, no acceptance bullet.
Under F-Q1 this is a Tier-1 capability deferred by staging — the smuggled
cut, on the harness axis. The internal contradiction hiding it: §1.1 says
legs B/C gain no head arms while §1.7 row 5 hands them "include
discovery".
DISPOSITION: FIX-NOW — the fix round adds the H5 harness half to the
staging in the note's own shape (extend W23.3 and W23.5's population
check, or an explicit sub-step): discovery/entry-set semantics for all
three legs stated ONE way (resolving the §1.1/§1.7 contradiction
explicitly), fixtures, a check with a counted population, the census-pin
consequences named, the S3 + fourth-failure-class spec hunks scheduled,
and the bench's E7 (content_hash over included fragments) given its
pcrec-side path. No new Frank item — F-Q1 already rules the scope.

## Must-fix — all FIX-NOW

- **r59-A-M1**: W23-S5's §3.3 property 2 is [MECH-REACH] stated backwards
  — counting declared fixture NAMES cannot see a witness that stopped
  reaching its site. Withdraw the property; the check reads the
  INVOCATION (three-leg call site, or a per-fixture receipt emitted by
  the helper).
- **r59-A-M2 + r59-B-M3**: W23-S6: (a) §3.5's no-sabotage reason is
  refuted by §3.4's own text — flipping `ext`'s cardinality or counting
  aux openers into `a_blocks` IS a plantable one-line defect; write the
  row or state a real refutation. (b) Two of the four arms are plausibly
  zero-population — require the fixture to carry a compiling `pattern`
  block and a refusing sibling, or declare the arms zero. (c) The
  byte-identity claim is unsatisfiable as specified — an aux edit shifts
  `line`/`block_line` columns below it; state the fixture constraint
  (aux blocks last, or line-normalised comparison) and name the
  line-number exception to §2.27.3 clause 5 explicitly, with why it is
  not a graduation event.
- **r59-A-M3**: R5's repair lands with both new arms at zero population
  (no sections exist yet at that commit). The repair's own commit carries
  a synthetic stream exercising both arms — which is also the only
  ENFORCEMENT of the "repair before emit" ordering. Per critic A N4: the
  positive control must be a section whose width DIFFERS from the main
  table's 16 (`cases` is also 16 — a coincidence that would mask).
- **r59-A-M4**: repairing R5 changes the detector for sabotage rows
  S200-S203 (S200's `SAB_DESC` is already stale — "16 fields where the
  header declares 15"). W23.4's build order gains: re-run S200-S203
  against the repaired assertion, confirm each fails for its own reason.
- **r59-A-M5**: the NUL rule is specified at two scopes — legs A/B
  file-wide pre-parse, leg C decoder-scoped. Specify ONE scope (whole
  file, pre-parse) in all three legs; add a second fixture with the NUL
  in a comment line (the existing fixture is structurally incapable of
  seeing the divergence).
- **r59-A-M6**: §3.2's "every three-leg assertion compares diagnostic
  CLASS, never exit code" requires rewriting `check_refusal_all3`
  (verdict-only today) and nothing schedules it. Add the rewrite to
  §6.2's build order; state what happens to the seven existing verdict
  call sites.
- **r59-A-M7**: W23-S3 iterates the dump, so a missing dump row is
  invisible; after W23.1 the parser's kind set IS the table (no third
  source). Name the independent denominator: a compile-time total from
  the exhaustive switch asserted against the dump's row count, or a
  pinned count with its re-pin ritual.
- **r59-B-M1 + C#2**: §4.3's absence grep is mis-measured (`provides`,
  `capable` occur as ordinary English in src/ and tests/ today) and its
  landing condition is unsatisfiable on the untouched tree. Scope the
  grep (line-initial format spellings under format-bearing paths;
  word-boundary/hyphenated forms), re-measure, state the true baseline.
- **r59-B-M2 + C#1/#3**: SW12's two comment sites are wrong line ranges —
  the `name`-arm pointer is `rxt_source.c:1176-1180` (NOT `:1126-1130`,
  a duplicate-description comment), the header is `:269-297`. Inherited
  from the design's SW12 — fix BOTH documents (format_design.md carries
  the same wrong range; the fix round corrects it there too as a
  drive-by with its own citation).
- **r59-B-M4**: bench B7 is booked "existing behaviour, must not regress"
  but the ruled disposition is FIRST LIVE VERIFICATION here. Add the
  fixture: a `@file:` subject carrying NUL + invalid UTF-8, through
  driver.c's argument form (H6/S5).
- **r59-B-M5**: bench A1's "include at head AND block scope" does not
  parse against §1.3's grammar (`include` is decl-line only). Verify the
  reading; if A1 needs a probe edit it joins the §9 correction list — a
  second A2-shaped item to catch NOW, not at their restart.
- **r59-B-M6**: §6.5 item 4's "run the 41 checks ourselves" is ~25/41
  executable (bench-side tools for ~16 don't exist in our checkout).
  Name the split in the step brief; say where fixtures come from; scope
  the acceptance bullet to the runnable set.
- **r59-B-M7 + C-note1**: leg B's arm chain is 22 arms (18 pinned + 5
  appended, per format_design §2.25.5's own ruled count), not
  "seventeen". Substance unchanged; the number a code lane sizes from
  must match the ruled record.
- **r59-C (citations)**: fix the six drifted/wrong rows — §6.3's
  refuse-then-map cites (`:848`/`~856-860`/`:863`), `mrl.c` house-rule
  cite (`:14-16` or `:39-45` per B-N2 — fix round reads the file and
  cites the true span), `cli/main.c` branch end `:1216`, fixture count
  49 not 46, plus B-N3 (61 true call sites, not 63) and B-N4's range
  cluster. C#3: "nineteen fixtures" clarified as table rows vs files.

## Notes — fold in where cheap

- A-N4: R5-repair positive control width ≠ 16 (folded into A-M3 above).
- A-N5: DECIDED (5) (sections FOLLOW the main table) is load-bearing for
  R2 and untested — add the one awk-arm ordering assertion to W23-S4.
- B-N5: F19 is a no-op (K57 already filed) — reword as a pointer.
- B-N6: `--list-schema` takes `table_contract.md`'s Scope row at birth
  but never names `tests/lib/table.sh` — wire the new producer through
  the check, not just the spec row.
- B-N7: the W23.3 intermediate pin has a `pattern-esc` row with no `esc`
  column — latent (population 0 on the corpus) but the note claims the
  pin landable; state the caveat or move the column.
- B-N8: §7.3 classification confirmed — all four manager-ruleable.

## Manager rulings on §7.3 (final, r59-R1..R4)

- **R1 (Q-W23.1)**: ACCEPT the implemented default — the diagnostic CLASS
  tag is emitted on stderr in a stable machine-parseable position, one
  channel. ADD (per B-N8): a `docs/spec/cli.md` hunk under D80 beside
  SW2's lexical hunk — a machine-readable diagnostic structure is a
  caller-observable CLI surface.
- **R2 (Q-W23.2)**: ACCEPT — `route` denormalised onto each case row (the
  block's route repeated); §2.24's own gloss is per-case by meaning.
- **R3 (Q-W23.3)**: ACCEPT — `#section surface` in the same stream; a
  flag would hide exactly the absence the mechanism exists to surface.
- **R4 (Q-W23.4)**: ACCEPT the recommendation — K57 is NOT fixed in W23
  (own entry, population 0, two independent reds must not share a step).
  The fix round DEFINES `prose_dedent.rxtin`'s waiting state:
  `tests/rxtsource/` has no known_fail mechanism, so the fixture asserts
  the CURRENT (wrong) behaviour with a comment naming K57 — going red
  the day K57 is fixed, which is the expiry event that re-aims it.

## Method note for the record

The lane's self-run residue sweep reported 0 and the forward pass
genuinely is 0. Both of critic B's blockers and four of its must-fixes
came from r58's THIRD pass — reading every disposition against the
document it makes a claim about. B-1/B-M5 are that class exactly; B-M1 is
the same shape one level down (a grep whose RESULT was asserted rather
than run). The class has now recurred FIVE times across two days; the
by-reference rule in r59-B1's disposition is the structural counter.

Critic A's inversion finding (A1: a green W23-S4 proving R6 broken) is
this round's sharpest single observation — a check certifying the failure
of a reader the survey had marked safe.
