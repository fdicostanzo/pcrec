# r57 — D6 panel on format_design.md REVISIONS 3+3.1 ([DD-13b.W23] STEP 1/1.1)

2026-09-12 evening, sixty-first session. Subject: the full W23 design
revision on branch `lane/w23design` (rev 3 by lane w23design, rev 3.1 by
lane w23recon; review base = diff main...lane/w23design at 04ec3942).
Three READ-ONLY critics, distinct lenses, all opus, none ran `make`;
every measured claim below is a real `build/pcrec` probe transcript in
the critic's report (scratchpad fixtures, nothing written in-repo).
Ruling frame: Frank's two 2026-09-12 rulings (internal consistency /
version-if-needed / schema validation / explicit sub-block syntax /
structurally parseable without context; and ownership — long-term
viability outranks bench convenience). Findings verbatim in the three
critic messages (session transcript); this file is the compiled record
with the manager's triage dispositions. Fix lane: w23fix, same branch.

## Panel verdict, one paragraph

All three lenses agree the two-layer factoring is the RIGHT answer to
the rulings and that revision 3.1's own new measurements are real and
reproduce exactly (the ragged-body narrowing F6 byte-for-byte; F1 exact
in both legs; the census exact at 210/3,936/28,943; the version decline
coherent with its arithmetic checked). What fell: the structure layer's
"one residual keyword fact" claim (the BLOCK SCALAR is a second,
undeclared structural device — two independent lenses converged on it),
§2.25's completeness claim (the five constraint kinds do not cover four
of W23's own refusal rules, one needing precisely the cross-scope kind
the same section defers), the additivity audit (three, then a fourth,
undeclared accept→reject narrowings), and the bench check B6 refusal
(EMPTY POPULATION under S2 — a second opener line is simply the next
block). None is fatal; every fix is a boundary stated honestly or a
kind admitted under the membership rule's own bar.

## Findings and dispositions

Severity is the critic's; disposition is the manager's. ALL are FIX-NOW
in revision 3.2 unless marked otherwise.

### Grammar/structure lens (critic r57grammar)

| id | sev | finding (compressed) | disposition |
|---|---|---|---|
| G-B1 | BLOCKER | The block scalar is a SECOND undeclared structural device: inside `description \|`, indented `#` is prose and ragged indentation is legal prose shape (probed, rc 0 today), unrecoverable from S0/S1/S2 — a generic reader must suspend structure rules and can only know to by reading `\|` + the parent's `value: prose`. §3.2 H12 already assumes this; §1.2.1 never declares it. §1.2.4's "\| … neither decides where a line attaches" is measurably false | Declare it: add S3 OPAQUE REGIONS to §1.2.1 (a `\|`-valued content line opens a region whose EXTENT alone is structural — first line at indent ≤ opener's — and whose interior is bytes). State that the structure layer takes TWO schema parameters (`opens_group`, `value = prose`), both from `--list-schema`; re-run §1.2.4's marker comparison against the two-parameter baseline. Converges with S-M1 |
| G-B2 | MUST-FIX | THREE more accept→reject narrowings beyond the ragged body, all measured live today: N1 indented `#` inside a block scalar (the shipped parser's own comment leaves this question OPEN and 3.1 closes it by side effect); N2 ragged prose in a block scalar (plus an independent shipped defect — the dedent strip silently CORRUPTS a dedented prose line); N3 TAB indentation (all three shipped readers accept tabs as indent; S0 counts only spaces — probe: a tab-indented config body works today) | Each gets the full §1.6.4 declared-narrowing package (population, forced-vs-chosen, spec sentence in SW16). N3 manager leaning: REFUSE tabs in indentation by name (a depth rule over mixed tabs/spaces is a permanent confusion source; long-term-viability criterion) — lane may overturn with an argument. N2's shipped dedent corruption: file a known_issues entry regardless of 3.1's direction |
| G-B3 | MUST-FIX | §2.25.2's `value: prose` and `children: none` columns contend with no stated precedence — as written every block-scalar continuation is a schema error against its own parent; the construct §1.2.5 widens is unrepresentable in the table §2.25 calls normative | `children` gains a `prose` member, or `value: prose` is stated to override `children` for attachment and `--list-schema` prints both; reconcile H12 which already assumes the second reading |
| G-B4 | SHOULD | §1.2.4 count 1 overstates: the marker's disagreement-state error class does not disappear under bare indentation — it RELOCATES to the schema layer ("parent takes no children"); and `\|`'s own no-continuation refusal is exactly the state count 1 calls inexpressible | Reword: the error moves layers. The DECISION (bare indentation) stands; count 2 is measured-true and the strongest |
| G-B5 | SHOULD | `provides`/`requires` rename buys a reading symmetry the grammar lacks: the two ends are different kinds (line kind vs tag key) and the closed set constraining `provides` is declared under the OTHER end's name (`vocabulary requires …`) — no syntactic path from `provides` to its value set | §2.26 item 4 states which half of the pairing it delivers; §2.16 gains a pointer sentence from `provides` to its governing declaration |
| G-B6 | NIT | §1.2.3's 28,943 is the expectation-line count; a generic reader sees 35,961 non-blank non-comment lines | Use the true number (the point gets stronger) |
| G-B7 | NIT | "19 head blocks with bodies" is 20, across 13 of 45 fixtures; uniformity CONFIRMED exact (every indented fixture line is width 2) | Correct the count |

Survived (stated by the critic after attack): F6's method and result;
the census (incl. the 25.8% blank-preceded measurement and its
inference); F1 in both legs; M8's refusal in both arms (leg A by
construction, legs B/C once H12 is built — booked honestly); the
version decline + reservation (probed: `version 2` today hits the
unknown-token arm, exactly what SW13 says must become a by-name
refusal); the provenance unification incl. the `analyzer` keep; §5.2a's
own items 2 and 4 both FAIL as attacks (the schema is not a partial
declaration on the axis item 4 probes — but see S-BL1 for the axis it
is). Five pinned probe cells (prose_hash, prose_ragged, prose_dedent,
config_tab_body, config_mixed_indent) are ready-made fixtures for
A3/A4's three-leg indented-line obligation — the fix lane takes them.

### Schema/checks lens (critic r57schema)

| id | sev | finding (compressed) | disposition |
|---|---|---|---|
| S-BL1 | BLOCKER | The five constraint kinds miss FOUR W23 refusal rules: (a) `provides` ↔ `vocabulary requires` is CROSS-SCOPE — the kind §2.25.4 defers with a trigger a W23 production meets NOW (the deferral and the production contradict in one section); (b) the `authored` rule needs FORBIDDEN-IF (must-be-absent-when), no kind expresses it; (c) subject-`as` is mapped to `unique-by`, whose semantics would REFUSE §2.18's own normal spelling (a functional dependency is not a uniqueness key); (d) `under`'s duplicate key ranges over fields INSIDE a `qualified-line` value — needs a value-shape field extractor | Per-rule, honestly: (a) ADMIT the cross-scope kind now — the membership rule's own bar is met; (b) admit forbidden-if (or spell it absent-when under required-if's family if that is genuinely one kind); (c) a functional-binding kind or corrected mapping; (d) parser-code-not-schema WITH the reason, or a deferred extractor with a real trigger. What must not survive: §2.25.3 claiming five kinds cover the delivery |
| S-BL2 | BLOCKER | The `pattern`/`pattern-esc` "one block, both spellings" refusal has an EMPTY POPULATION under S2 (measured: two adjacent `pattern` lines are two blocks, rc 0) — no parse state holds both; recovering it needs a keyword-dependent structural exception | DROP the refusal; the answer is "the second opener starts a new block." Bench correction list gains a B6 row: the check's premise dissolved under the two-layer split |
| S-M1 | MUST-FIX | The ragged BLOCK SCALAR is a second live narrowing-or-unspecified region (converges with G-B1/G-B2 N2); §1.2.5's widening GROWS the population; no fixture carries a ragged prose body | Folded into G-B1's S3 fix + G-B2's packages; add the fixture |
| S-M2 | MUST-FIX | `validated_by: all-readers` is a per-row claim about two parsers the schema does not drive — no check, empty corpus population (0 files use W23 productions), and the stated defence addresses omission, not wrongness | Every `all-readers` row owes a three-leg fixture; "the fixture population covers every such row" becomes its own check |
| S-M3 | MUST-FIX | S-R2's named detector (bench C5/C6) lives in the OTHER repo — planting it turns nothing red in pcrec's battery | Name the pcrec-side fixture pair as the detector; bench checks are corroboration |
| S-M4 | MUST-FIX | S-R3's plant (hand-written table replacing the walk) passes if the copy is faithful — it catches drift, not the plant; a sabotage row must fail NOW. Seven of eight columns are unchecked against behaviour | Re-spell the plant as a hand-written dump DISAGREEING on one row; widen the cross-check past `closed` |
| S-M5 | MUST-FIX | The three-leg indented-line fixture must sit at the PRE-BODY position or it cannot reach leg C's measured defect ([MECH-REACH]); both legs' diagnostics reproduced | A3/A4 name the position; the fixture asserts diagnostic CLASS, not exit code |
| S-M6 | MUST-FIX | The bench correction list misses D1 (nine→eleven provenance keys + two renames — the D1 ROW knows, the LIST doesn't) and the `provides` row cites a population that is empty (zero of the 41 checks spell `capable`) | Add D1; delete/reword the empty citation (over-warning erodes the list) |
| S-M7 | MUST-FIX | SW17 misses `docs/spec/table_contract.md`, whose scope section enumerates every conforming table producer and says new commands adopt AT BIRTH — a reader found by grep, D94's failure verbatim | Add the row |
| S-S1 | SHOULD | §2.25.4's "generating legs B/C" deferral has a PERMANENT reason and a measurement trigger — a decline dressed as a deferral | State it as a DECLINE (manager concurs: the reason is architectural) |
| S-S2 | SHOULD | The `wave` column's only consumer has an empty population at delivery | Keep ONLY if SW13's partial-build derivation is real during W23's own staged implementation (it is a real consumer during rollout); else drop. Lane decides, stated |
| S-S3 | SHOULD | Two of three `validated_by: none` items are not (scope, line-kind) rows (config resolution is the absence of `--resolved`) — the D4-probe items most likely to fall back to prose | Give the three a stated home in the table or a named sibling surface |
| S-S4 | SHOULD | F2's census floor left stale at r44 values (179/3,265/26,691) — a 30-file regression passes; parser-vs-parser equality shares the same `find` and cannot rescue it | Re-pin the floor at 210/3,936/28,943 in the delivering change; name the mover |
| S-S5 | SHOULD | "Structurally impossible" is true of leg A only; two sections say it unconditionally while two others say the honest version | Qualify the two phrasings |
| S-S6 | SHOULD | S-R4 bundles two plants; only the removal produces the named symptom — the duplicate-row plant is silent under first-match-wins | Split the row; name the second detector |
| S-S7 | SHOULD | At-most-one-per-parent is called a `unique-by` row in §9 C7 and a CARDINALITY value in §2.25.3 — one fact, two mechanisms, inflating `unique-by`'s earned-ness | One mechanism per fact; reconcile both sections |
| S-S8 | SHOULD | Leg B refuses every W23 line by CATCH-ALL fall-through, so a verdict-only C1 differential reads "all three legs refuse" while leg B has no rule — `all-readers`' one live check satisfiable by accident | The differential compares diagnostic CLASS for `all-readers` rows |
| S-N1 | NIT | `--list-schema` is the SEVENTH registry dump, not the sixth (three sites; registry.md's own ordinals) | Correct all three |

Survived: the one-derivation claim for leg A (hunted — the only residue
is `tok_is(l,"pattern")`, which IS the declared `opens_group` column);
both new measurements reproduce exactly; S-R1 is a good row; the
`vocabulary`-as-`source: file` nesting ("strongest thing in §2.25");
required/required-if/closed correctly mapped for §2.14 rules 1/3/4;
the checklist mapping behaviourally right on 39 of 41; §1.2.4's
marker-vs-indentation pricing.

### Consumer/needs lens (critic r57consumer)

| id | sev | finding (compressed) | disposition |
|---|---|---|---|
| C-M1 | MUST-FIX | A SECOND undeclared accept→reject narrowing in §2.22: `x_y` beside `x-y` with the call spelled `(?&x_y)` COMPILES today (three-way probe, byte-identical artifact; the sibling is inert) and becomes a collision refusal under "exact spelling does NOT win". Unmeasured, and §1.6.4 rule 3's forced-not-chosen clause is not met (the rule is explicitly a choice against a tie-break) | Measure the population (corpus + bench); add as 2b to §1.6.1 with its package; if genuinely a choice, §1.6.4 gains a fourth case rather than a stretched third. Re-aim §5.2a attack 1 (it pointed at the head only — the second narrowing was in semantics) |
| C-M2 | MUST-FIX | The zero-reader-change claim's site list misses TWO shipped rxt_source.c comment sites (:281-289, :1128-1131) that state the repealed boundary AS A RULING and invoke D94 by name; and the repair REMOVES the ability to declare a deliberately non-callable definition — a design consequence, not a comment edit | SW12 gains both sites. The non-callable-definition loss goes to FRANK as W23-F4 — manager recommendation: ACCEPT the loss (the collision refusal makes calls explicit and named; `export` governs delivery separately; no current customer for non-callable-by-design — D77) |
| C-M3 | MUST-FIX | §2.22's citations point at the generic plumbing (and misname `def_by_name`); the real precedent (rxt_compose.c:854-864, the qualified-rowname synthesis) is uncited; `pcrec_rxt_prefix_from_name` silently TRUNCATES at dstsz and the one existing consumer refuses over-long names FIRST — §2.22 carries no length discipline, so two long names differing past the buffer would bind silently; PCREC_MAX_GROUP_NAME=128 makes a >128-byte definition buildable-but-uncallable with no schema row | Cite :854-864; state the refuse-before-mapping length rule for the second consumer; add the 128-byte callability bound to §2.25 or state it inherited from PCRE2 under D26 |
| C-S4 | SHOULD | Roadblock #6 closed by construction for `include` (verified via §2.5's fragment restriction) and OPEN for `lib`: nothing says what a build-mode file sees when it `lib`s a describe-mode file | One clause in §2.20 rule 4 naming the `lib` closure ("`lib` contributes definitions only" made explicit) |
| C-S5 | SHOULD | `use` under `describe` is "legal and INERT" — unstated whether its referent still RESOLVES; a typo caught in build mode would go silent in describe mode (AR-3's forbidden shape) | State: `use` resolves in both modes; unresolvable name refused in both |
| C-S6 | SHOULD | `mc` by reference is sufficient for byte/well-formed subjects and insufficient on ILL-FORMED UTF-8: match_api §3.1.1 never defines "character boundary" there, the shipped advance is a skip-continuation-bytes rule a foreign adapter cannot derive from prose, and the bench's corpora are hazard-tagged. Also: the python arm reimplements the advance — the second-implementation shape §2.19 argues against one production over | SW7's normative text states the advance rule on ill-formed input (adopt the shipped rule: from pos+1, skip bytes 0x80-0xBF); E5 becomes utf8-bearing and says so; acknowledge the second-implementation tension with the C1 cross-check as the paid-for answer |
| C-S7 | SHOULD | N-27 (Tier 1 MUST) carried on its blocking half only: byte length, per-subject description, `periodic` have no production, no refusal, no deferral — silently bench-side; the only genuine gap in the 53-row walk | One passage in §2.18: carry, or refuse with the D77 shape (len derivable from the file; description/periodic named bench-side manifest data with the trigger that would move them) |
| C-S8 | SHOULD | §0.7's "24 arms" is 22 (17 pinned + 5 post-pin; two grep hits are pre-loop skips) — the stale-pinned-number failure §1.1 was corrected for, reintroduced in the correcting revision's own paragraph. Conclusion (no whitespace tolerance) CONFIRMED | 22, with the derivation |
| C-N9 | NIT | Leg C's finding over-reads: :424's indentation check IS unconditional for body lines; the defect is pre-body only | One scoping clause |
| C-N10 | NIT | `--list-source`'s `pattern` column now means two things (verbatim vs re-escaped), disclosed in §2.19 but not in §2.24's column list | Mark the `esc` dependency where consumers look |
| C-N11 | NIT | `description`→prose-value is called one of "§2.26's four moves"; it is §1.2.5's consequence | Fix the framing (and the outbox message inherits it) |

Survived: NEEDS COVERAGE exact (36/9/5/3 verified row by row; every
Tier1+Tier2 need has a named carrier in the one delivery; only C-S7's
non-ID slice is uncovered); the four renames orphan nothing (EBNF
carries only the new spellings; the freq unification preserves all five
fields; `analyzer` deliberately kept); the zero-reader-change claim AT
THE GRAMMAR level (three byte-identical name grammars; every derived
identifier is a legal PCRE2 group name); the design's own named counter
hunted harder and confirmed (no fourth name-table site; leg C's
composed-block skip means the bench's regime blocks are INVISIBLE to
that differential — §2.22 acknowledges partially; the bench hears the
full version via the outbox); roadblocks #1-#5 closed as claimed (#4 on
the blocking half); the bench correction list complete-and-honest
apart from S-M6; G3 holds with no stale denominator.

## Cross-lens convergences (the panel working as designed)

1. G-B1 + S-M1 + G-B2(N2): three independent arrivals at the block
   scalar as the un-modeled object. One fix (S3 opaque regions + the
   §1.6.4 packages + the B3 column reconciliation) discharges all.
2. S-BL2 + the consumer lens's B6-satisfied row: the bench's B6 check
   was mapped SATISFIED by a refusal with an empty population — K35
   caught at design time, before the check existed.
3. C-M1 + G-B2: the additivity audit pattern — 3.1's own instrument
   (the §1.6.4 standing rule) was right and was not swept across the
   whole delivery; five narrowings now known (ragged body + N1 + N2 +
   N3 + x_y-collision), each needing the same package.

## Frank queue after the fix round

- W23-F1 `configs describe` (D93) — unchanged, ready-to-ratify.
- W23-F2 `provides` in-format — unchanged in substance.
- W23-F4 (NEW, from C-M2): the derived-identifier repair removes
  "declare a deliberately non-callable definition" (a boundary a
  shipped comment records as a feature). Manager recommends ACCEPT.

## Fix lane charter (w23fix, opus, same branch)

Apply every disposition above; revision 3.2 + report addendum 2; the
five grammar-lens probe cells and the schema lens's fixture obligations
land as named fixtures in the A3/A4 plan; re-sweep the WHOLE delivery
with §1.6.4's rule (convergence 3) and state the narrowing census as a
closed list. Nothing under src/ or tests/ — the fixtures are PLANNED in
the check plan, built by the implementation lanes.

## ROUND 2 — focused re-check on revision 3.2 (critic r57recheck, same evening)

Two tasks: verify the three blocker fixes as landed; run Addendum 2's
own new attack ("walk every §2 refusal rule and ask what it accepted
yesterday"). Coverage disclosed explicitly: 16 rules swept by direct
probe (leg A + leg C), the rest by stated reasoning; leg B not probed
(leg C refuses head-bearing files outright, verified — leg A is the
sole reader of the head productions today).

VERDICT: (b) S-BL1 and (c) S-BL2 HOLD OUTRIGHT under attack — the
eight-kind count honest at every site, each new kind's W23 customer
verified in its home section, the §2.25.4 self-contradiction genuinely
fixed, every echo of the dropped refusal gone; "the schema section is
now the strongest part of the note." (a) G-B1 holds in DIRECTION and
fails end-to-end on its own axis. NOT yet mergeable; one short round.

| id | sev | finding | disposition |
|---|---|---|---|
| R2-F1 | MUST-FIX | S3's normative trigger is UNPARAMETERIZED — read literally it opens a region on ANY bare-`|` content line; witness `pattern \|` (a legal alternation-of-empties, rc 0 today) becomes a refusal. The EBNF has the right rule; the normative sentence contradicts it | FIX: the trigger clause names the prose-valued-kind condition + TRIMMED value (the shipped trailing-space fixture is the precedent) |
| R2-F2 | MUST-FIX | The COMMENT line is the third undeclared structural fact: S0 declares it, S1/S3 never say its effect, and the shipped readers TERMINATE continuations/regions on a column-1 `#` — two probed reject→accept WIDENINGS falsify §1.6.1 claim 2 (incl. one that gives an opener two disjoint prose regions, which S3's extent rule cannot express) | FIX: one sentence each in S1 and S3 — a comment terminates, matching all shipped readers; claim 2 stays true |
| R2-F3 | MUST-FIX | The WHITESPACE-ONLY line is BLANK to S0 and CONTINUATION to the shipped parser (probed 4 ways) — the SIXTH and SEVENTH narrowings, and the second kills MULTI-PARAGRAPH PROSE (the indented whitespace-only separator is today's only paragraph-break spelling inside a block scalar, r46sem-10 having closed the empty-line one). S3 also self-contradicts: finding "the first BLANK line" IS an S0 classification inside a region it declares unclassified | FIX, manager leaning: S0's BLANK narrows to TRULY EMPTY; whitespace-only becomes attachment-relevant with empty value (matching the shipped readers), bytes inside opaque regions (preserving the paragraph break) — dissolving both narrowings; scope S3's "not classified" to dispatch, not the boundary test. Lane works the spelling; if it finds this worse than taking the narrowings, argue it |
| R2-F4 | SHOULD | Which column does the structure layer read — `value = prose`, or the value/children PAIR? Three sites disagree; it decides whether S-R5's plant is detectable | Say it once; S-R5's detectability is the tiebreaker |
| R2-B | MUST-FIX | CENSUS SCOPE HOLE: STEP 0's two accept→reject refusals (M1 NUL, M5 duplicate description — M5 silent in TWO readers) live nowhere in a list calling itself closed | Census carries them marked "landed by STEP 0 (lane rxtnul), not W23", or the census heading states its scope |
| R2-C | SHOULD | LATENT NARROWING GENERATOR: `cardinality` is normative on every schema row and the shipped W1 surface is INCONSISTENT (duplicate `export` refuses; duplicate `name`/`engine`/`encoding`/`features`/`budget` silently last-wins) — whatever H16 assigns to the five silent kinds is an accept→reject chosen implicitly | DECIDE NOW, manager leaning: scalar kinds REFUSE duplicates (the `export` + STEP-0 `description` precedents; internal consistency); lane MEASURES the five populations first and reports before taking any non-zero one; census rows + H16 carries the choice explicitly |
| R2-D | NIT | 28,943 reproduces only counting `perr` as an expectation line (28,488 without) — the floor S-S4 re-pins, underivable by the next person | One parenthetical stating the definition |

Also: all three Addendum-2 pushbacks endorsed by the critic; the lane's
population numbers all re-derived and reproduce; K57 confirmed filed
with repro. Fix round 2 = lane w23fix2, touching §1.2.1 S0/S1/S3,
§1.6.1/§1.6.1a, §2.25.2's cardinality values, and the one parenthetical
— then MERGE pending Frank's W23-F1/F2/F4 ratifications.
