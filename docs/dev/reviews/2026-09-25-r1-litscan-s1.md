# r1 — S1 light D6 panel on the [OPT-LITSCAN] S1 design note (docs/design/litscan_s1.md @ lane/s1design)

2026-09-25. Two read-only sonnet critics with distinct lenses:
s1crit-cost (cost/census/D122) and s1crit-sem (soundness). Both read
`docs/design/litscan_s1.md`, its instruments in `docs/dev/optloop/s1/`, and
the compare-stack map (`compare_stack.md`); neither ran `make`. Dispositions
are the manager's, applied by lane `s1rev` the same day. The "applied in"
column names the commit on `lane/s1rev` that carries the fix.

## s1crit-cost

| id | sev | finding | disposition | applied in |
|---|---|---|---|---|
| C1 | should-fix | router's arm(c) "program-identical to `25b1984f`" (§10) is asserted with no artifact — critic verified by building both pins: only header/abi/`.vars`/stamps differ | ADD a reproducible recipe script + its recorded output to `docs/dev/optloop/s1/`; cite it from §10 | `7cbdd540` — `s1/router_c_identity.sh` + `s1/router_c_identity_output.txt`, cited from §10 |
| C2 | should-fix | `census.py`'s `classify()` B-branch does not encode §1.1 clause 3's second disjunct (scan offset `pin+idx==0` AND memchr byte == `run[idx]`); 3 corpus B rows were excluded only in PROSE (74→71), not re-derived | FOLD the clause-3 predicate into `classify()`; re-run `census.py`; the committed counts must reproduce the note's numbers (34/235, 523/3,576) with no hand subtraction — if they do not, report the new numbers | `c718cdd3` — **did not reproduce 523; reports 513.** The gap is 8 rows, not 3 (5 more: plain `memchr`-form pick not at offset 0, or offset-0 pick under a non-plain-`memchr` prefilter form the note's own hand count missed). `class_c_split.py` added to re-derive C1/C2/C0 from `census.tsv`'s raw columns; §6's table, its prose, §0 item 5, §9 Q2 all corrected in the same commit. Bench's 34/235 IS reproduced exactly (all 3 excludable rows are corpus-only) |
| C3 | note | the run-pinned row lives in a separate 3-row k-set table in `prefix_k.c`, not `dfa_pfs[]`; D122 addendum 2 warns against parallel selectors | ADD one explicit paragraph: why the k-set table is a feeder analysis (it chooses offset SETS for the one existing `ofsskip` search) and not a second form selector; mark it "for Frank's sign-off" | `c718cdd3` — §1.1, flagged for Frank's sign-off |

## s1crit-sem

| id | sev | finding | disposition | applied in |
|---|---|---|---|---|
| S1-1 | should-fix | the C1 loop guard `pos + maxk < n` must widen to `max(selection maxk, o+L−1)`; unwidened = answers identical but ASan heap-buffer-overflow READ (witness `[ab]/user`, `a\K/user`, subject ending `a/us`) | WRITE the maxk rule into §1.1 and §3; ADD a sabotage row to §7 (guard not widened) detected under ASan (or a structural check), witness `[ab]/user` on a subject ending `a/us` | `c718cdd3` — §1.1 "The loop guard must widen with the run", §3's fact list and its "elision" bullet both updated to `max(maxk, o+L−1)`, sabotage row (d) added to §7 |
| S1-2 | should-fix | §8.1's "pre-existing gap" (K64 sibling) REFUTED: on a hybrid `rx_prefilter` rejects before any VM attempt and every reqbyte-necessary byte is prefilter-necessary (`A_LOOK`/`A_BREF`/`A_VAR`/`A_CALL` declined, `A_ATOMIC` transparent both sides, count-collapse keeps `rmin ≥ 1` bytes); witnesses all nomatch at 0.00 s; the implication conjunct is a COST property, not K64's no-match proof | REWRITE §0 item 6 and §8.1 accordingly; CLOSE §9 Q4 with this argument; note (not a change request) that k64fix's `!prefilter_collapsed` conjunct is more conservative than needed | `c718cdd3` — §0 item 6 and §8.1 rewritten, §9 Q4 marked CLOSED, the k64fix conservatism note added as a non-binding observation |
| S1-3 | confirmed | run-pinned soundness: 0 diffs on 12 class-B shapes + utf8 + `(?i)`/1234 at 4,001 subjects × every `search_from`; sabotage (a) fires 2,019/4,001; caseless excluded BY CONSTRUCTION (folded letter = 2-member set, never in a run) but `(?i)` artifacts whose run has no letters DO move, soundly (`(?i)/1234` class B, `(?i)x/1234` C1) | RECORD the evidence in the review; REWRITE §8.3's "no caseless" to say exactly that | `c718cdd3` — §8.3's bullet rewritten; evidence recorded above and in the note itself |
| S1-4 | should-fix | §3 grounds the offset-0 scan's soundness on `k0` (role A) but the refusal is sound by clause 2's `k[0]` singleton (role B) — MISCOMPILE-1's confusion; `{0*}` breaks two stated invariants (`ofsk_emit_verify`'s `pcrec_ctx_fail` "offset 0 always a verify", `prefix_k.c`'s "`o->k[0]` ALWAYS role B … never the scan") | FIX §3's grounding; NAME both invariant edits as required code/comment changes in the implementation plan; state the clause-2 check reads `us.ofsk.k[0]`, never `k0` | `c718cdd3` — §3 rewritten to ground on clause 2 / `us.ofsk.k[0]`; both invariant edits named as implementation obligations in §7 |
| S1-5 | should-fix | G1 conjunct underspecified | SPECIFY: "verifies ⊇ run" = every offset `o+i` selected with `count==1` and `byte==run[i]`; it reads the SELECTED `DfaPf` AFTER the deny mask (`-fno-offset-skip` restores the pre-check); `dfa_cand_scan_byte` returning offset-set scan bytes needs its own "memchr form only" guard for the density clause + a sabotage row (or extend row (c)); impact cost-only per S1-2 | `c718cdd3` — §1.2 gains `verifies(sel, o, L)`'s precise definition, the post-deny-mask reading rule, and the density-clause guard note; sabotage row (e) added to §7 |
| S1-6 | note | "the first skip returns n" false on a seeded machine (`\bin\b`, class A, word seed state 4, `search_from>0` after a word byte → DFA steps over the rest of that word run before the prefilter runs) | ADD as a hazard to §5's H-list: linear, no VM attempt, performance only | `c718cdd3` — H4 added to §5's hazard table |
| S1-7 | note | REQ_RUN byte-identity after P4 extraction confirmed by construction; the run term must never be emitted as a string inside a C comment without `emit_comment_safe_byte` (`\*/x` is a real class-B member, run `2a2f78@0`) | ADD both as implementation obligations; the identity gate on the extraction commit alone is the acceptance | `c718cdd3` — both added to §7's "Implementation obligations" bullet |

## Frank's §9 answers — PENDING

Not part of either critic's findings; recorded here as still open, per
the brief. No rulings have landed as of this review.

- **Q1** (convert the floating-run pre-check to `<p>_ofsskip`, IN S1):
  **PENDING.** The note's own recommendation is YES, as S1's last commit,
  inside the same abi event.
- **Q2** (class C2 / the "scan must move" override): **PENDING.** The
  note's own recommendation is to LEAVE it (S1 is identity-only; D77).
  C2's disposition above corrects the population this question is over
  (12 bench / 40 corpus, plus 8 corpus C0 rows of a narrower, related
  shape) but does not itself answer the question.
- **Q3** (the VM seed, §8.2, as its own plan row): **PENDING.** The note's
  own recommendation is a NEW ROW, `STATE:not-started`, with §8.2 as its
  measured trigger.

## Triage summary

3 findings from s1crit-cost (1 should-fix confirmed-and-corrected larger
than estimated, 1 should-fix landed as asked, 1 note landed as a
sign-off-flagged paragraph); 7 findings from s1crit-sem (2 should-fix
soundness/read-safety corrections, 1 should-fix refutation that also
closes an open question, 1 should-fix specification gap, 1 confirmed
result written into the note, 2 notes recorded as a hazard and as
implementation obligations). All applied in `c718cdd3` except C1, applied
first in `7cbdd540`. No finding refutes S1's central mechanism (the
selection row, the G1 conjunct, the P4 primitive) or its acceptance
targets (§5). The one number that moved materially is the corpus census's
"program changes" count, 523 → 513 — read C2's row above and
`docs/design/litscan_s1.md` §6 before citing either figure. Three
questions remain open to Frank (§9 Q1–Q3, listed PENDING above); no new
rulings are recorded by this review.
