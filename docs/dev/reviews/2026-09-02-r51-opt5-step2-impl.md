# r51 — D6 panel on the [OPT-5] STEP 2 IMPLEMENTATION (merged da4fe60 + de32a4b, abi 16)

2026-09-02, fiftieth session, ~19:3x-19:5x EDT, run read-only BESIDE the
union battery on the merged tree. Two critics: r51sound (sonnet —
soundness/engine semantics, with LIVE witnesses: emitted with the built
`build/pcrec`, compiled singly, driven at every `search_from`), r51check
(sonnet — check design, learnings §3 lens). Inputs: the rev 2 note,
the r49 review, `docs/dev/lanes/opt5i_report.md` (its eleven §7
findings), the merged code.

## §1 PANEL VERDICT

THE ELISION HELD. r51sound found NO refutation on any of the six attack
surfaces (dead seed / P3; the `-1` gate with a scan edge above the
probe at `search_from` ∈ [0, n+1]; the `-fprefilter` hybrid end to end;
F1/F3; views under `(?m)`/`$`/`\z` — 18 combinations all declined; empty
subjects and the `\B` trio), pinned vs `-fno-start-pinned` byte-for-byte
and vs python `re`. It corroborates opt5i's F11 (no live-fs-but-dead-seed
witness constructible) and F1/F3 as accurate and benign.

THE CHECKS ARE WEAKER THAN THE CODE. r51check found six weaknesses in
the shipped checks — none in the emitter — of which three are the
learnings-§3 shape (a control that shares text with what it controls;
a population floor copied from a sibling; a check with no sabotage
row). All go to a FIX LANE after the battery; none blocks the merge or
the pin (the battery is the net for the code; these are nets for the
nets).

## §2 FINDINGS, TRIAGED (disposition after each)

MUST-FIX (fix lane r51fix, after the battery, mech solo rows for any new
sabotage row; ids S223+ arbitrated at merge):
1. [check 1] `run_search_pinned.sh` §7 (:592-598) greps for the P0/P3
   assertion function's NAME and two message strings — never for its
   one CALL SITE (`emit_dfa.c:5123`). Deleting the call and leaving the
   definition as dead code passes vacuously; and the file's own header
   says the liveness clause has no sabotage witness, so §7 is its ONLY
   guard. DISPOSITION: §7 greps the call-site expression (function name
   followed by `(cx,`) in the emitter's routing function, and states
   that it is a wiring check; plus a sabotage row S223 "assert call site
   deleted" (SAB_EXPECT=DETECTED by §7, SAB_REACH on the grep line) so
   the guard has a reverse tripwire.
2. [check 2] `S220_pinned_view_clause_dropped.sh:87`'s `SAB_REACH_POP`
   is copied from S218/S222 (the `(?m)` manifest, floor 12) while S220's
   own header says that population does not discriminate for P2 — its
   real population is `\B`, `\B\B`, `\Bx*`. DISPOSITION: re-derive the
   POP field to that manifest (a three-pattern list, floored at 3, read
   from a named file), or drop the field with the reason in the header;
   never a decorative floor.
3. [check 3] `run_vm_frameless.sh` ships with ZERO committed sabotage
   rows — three failing directions were planted by hand (stamp inverted;
   recomputed from `v.npush`; emitted conditionally) and reverted, and
   it is the ONLY instrument scoping `goto *` correctly on the default
   and force axes (rule 1's whole-file grep is wrong there: 199 false
   positives). DISPOSITION: rows S224 (stamp inverted) and S225
   (recomputed from `v.npush`) with SAB_REACH from birth; the
   conditional-emission plant folds into S224's second hunk if the
   detector is the same line.
SHOULD-FIX (same lane):
4. [check 4] `run_search_pinned.sh:524` floors the force-axis pinned-
   hybrid population at 12 against a measured 23 (48% slack) where the
   sibling floor (:501) sits at 140/175 (20%). On the population that
   was INVISIBLE before the force arm, the loose floor is the wrong
   direction. DISPOSITION: floor 20.
5. [check 5] §8's "ALL-AND-ONLY" claim over VIEW_DECLINE_MANIFEST is
   tested on five hardcoded literals plus an unrelated `(?m)` count; the
   "observable proxy" its comment describes is never computed.
   DISPOSITION: either compute the proxy over the corpus once (a swept
   set, floored) or reword the claim to the five named witnesses; the
   comment must not describe a sweep the script does not run.
OWED MEASUREMENT (recorded, not a lane item):
6. [check 6] the three size-cap witnesses were rewritten NON-NULLABLE so
   the start-pinned predicate declines them — honest for the caps'
   check, but "a LARGE PINNED artifact still hits its resource cap
   correctly" is now an unmeasured interaction. DISPOSITION: one
   witness, measured, on the [ART-SIZE]/[OPT-5] boundary when
   [OPT-VEDGE] or [LIM-2] next touches the caps.

## §3 WHAT HAPPENS NEXT
The battery decides the pin. The fix lane lands items 1-5 as a small
follow-up (its own mech solo rows; no abi movement expected — checks and
sabotage rows only), then a `make test-codegen` + the affected mech rows
solo. Item 6 rides the next caps row.

## §4 r51sound's REPORT (verbatim)

r51sound — SOUNDNESS, [OPT-5] STEP 2 (axis J, start-pinned search elision), da4fe60/main (abi 16). No `make` run; verified against the built `build/pcrec`, single-artifact `gcc` compiles in scratch, driven with a hand harness calling `<prefix>_search` directly at chosen `search_from`. No refutation confirmed.

(1) P3 liveness / dead seed. Read `start_pinned_assert_routing`/`start_pinned_applies` (src/gen/emit_dfa.c:4482-4544): the liveness half is asserted (`s1u[u] < 0` → `ctx_fail`), not merely declined. Tried `\ba|c*`, `\Ba|c*`, `\ba|c*|\B9`, `(?:\bc*|\B)`, `(?:\b|\B)c*` — all five compile and STAMP `pinned`. Fuzzed `(?:\bc*|\B)` and `\ba|c*` against python `re` over ~15 subjects at every `search_from` in [0, n+1]: 0 mismatches. Corroborates opt5i F11 (P3 evaluated 0 times); no live-fs-but-dead-seed witness constructible; the assertion never fired.

(2) `-1` gate vs scan-edge-above-probe. Rebuilt `[a-z]{0,8}|9$` and `a*|\b9`, both `pinned`. Drove `_search` at every `search_from` in [0,n] plus n+1 on "abc"/"aab": matched python `re` exactly, including `search_from == n` and n+1 (found=0). Cross-checked `[a-z]{0,8}|9$` against a `-fno-start-pinned` build over "abc", "", "9", "aaaaaaaaaaa", "z9", "9z" at every `search_from`: byte-for-byte identical.

(3) Hybrid + `-fprefilter`. `-fprefilter '\Ka*'` stamps `RX_ENGINE "vm"`, `RX_DFA_START "pinned"` on the inlined prefilter; full `--emit-main` VM artifact on "bbb", "aaa", "": correct. `a\Kb*` (`-fprefilter`) correctly declines (`reverse-pass`, `memchr` prefilter; the prefix `a` breaks P1); its VM answer matches `(?<=a)b*` on "abbb" → (1,4). `emit_vm.c`'s `window_end`/`attempt_position` emission (:9800-9900) never branches on the pinned axis; `window[0][0]` is a lower bound and `window[0][1]` an unconditional-clamp upper bound, as before STEP 2.

(4) F1/F3. F1: the range guard `if (search_from > subject_length) return 0;` is emitted in `emit_scan_loop`'s seed init, before the `for(;;)`, so it fires identically pinned or not; `[a-z]{0,4096}` (pinned ∩ search-filter) `_match` returns the whole-match length on "hello". F3: `window_end` is textually absent unless `v.nclamp > 0` — a parameter that does not exist on unclamped artifacts, not a half-applied guard.

(5) View invariance. `pcrec_state_view_invariant` (src/opt/scanedge.c:206) refuses on `eolvar>=0 || endvar>=0` before consulting `up[]`; a state whose accept differs at the LAST position carries a nonzero eolvar/endvar by construction. 18 combinations of `(?m)^`, `(?m)$`, `\A`, `\z`, `\Z`, `\G`, `\B`: all declined (17 `reverse-pass`, 1 routed to the VM via a lookbehind).

(6) `\B`, `\B\B`, `\Bx*` decline (`reverse-pass`); `x*` pins. Empty-subject and `search_from==n` covered in (1)/(2).

Not attempted: a corpus-scale sweep (no `make`); P0's routing assertion (`\G`/BOT), which is engine selection's decision, not this design's.

## §5 r51check's REPORT (verbatim)

(1) run_search_pinned.sh §7's P0 assertion check is pure grep-for-identifier, not a wiring check. :592-598 does three `grep -q` substring tests against src/gen/emit_dfa.c (the function name `start_pinned_assert_routing`, `"P0 routing "`, `"liveness conjunct should"`) and never confirms the function is CALLED; the one call site is emit_dfa.c:5123. A sabotage deleting the call while leaving the definition passes vacuously. The file's own header (emit_dfa.c:4475-4481) says the liveness half has "NO SABOTAGE WITNESS and cannot currently have one" — §7 is the ONLY guard, and it misses call-site deletion. No row in S218-S222 targets this.

(2) S220's `SAB_REACH_POP` (:87, `docs/dev/opt5m2_m2_changed_patterns.txt|^\(\?m|12`) is copied from S218/S222; S220's header (:38-45, :62-65) disclaims that population ("not a discriminating population for THIS predicate"); its real population is `\B`, `\B\B`, `\Bx*`, which the SAB_REACH probe (:85) checks via `\bx*`/`x*`. The POP floor is decorative: a vanishing `\B` population goes unmonitored while the `(?m)` floor stays green.

(3) run_vm_frameless.sh ships with ZERO permanent sabotage rows (:292-301: "exercised by hand rather than by a permanent mech row" — stamp inverted, recomputed from `v.npush`, emitted conditionally). The rule-1 whole-file `goto *` grep it cites as coverage is WRONG on the default/-fprefilter axes (its own header :39-58: 199 false positives from a hybrid's inlined ENG_ATTEMPT prefilter step). It is the only instrument on those axes and has no committed row — contra learnings §3 and tests/mech/CLAUDE.md's maintenance rule. A future rewrite of `vm_goto_star`'s scoping (:99-104, :217-223) has nothing holding it.

(4) run_search_pinned.sh:524 `-lt 12` against measured 23 (52% margin) vs :501 `-lt 140` against 175 (80%). The force-axis floor tolerates the pinned-HYBRID population halving, on the population that was invisible before the force arm (K35 shape).

(5) §8's "ALL-AND-ONLY" (:611-613) over VIEW_DECLINE_MANIFEST: the "OBSERVABLE PROXY" (:615-619) is never computed; §8 tests five hardcoded literals (:623-627) plus a count of ALL `(?m)`-prefixed corpus patterns (:652), most not manifest members. True only over five literals.

(6) Minor — §6a's test-resource re-derivation rewrote the three witnesses NON-NULLABLE so they dodge axis J entirely; honest for the caps, but "a LARGE PINNED artifact still hits its resource cap correctly" is an unmeasured interaction before and after.

Sound: §2's cross-directional stamp/body/mirror check, §3's from-text fold, §10's every-startpos differential reading caps[0][0], the S218 two-hunk defence-in-depth discovery, S219's measured UNREACHED; sabotage transcripts for S218/S219/S221/S222 verified against emit_dfa.c:4515-4544, 4482-4510.
