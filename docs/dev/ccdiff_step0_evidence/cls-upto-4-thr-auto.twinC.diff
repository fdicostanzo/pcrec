--- /tmp/claude-1001/-home-duxevents-pcrec/dcce9a31-e3ae-41e3-8913-a4a918af3f32/scratchpad/ccdiff/cells/cls-upto-4-thr-auto/artifact.c	2026-09-02 17:46:02.997180441 -0400
+++ /tmp/claude-1001/-home-duxevents-pcrec/dcce9a31-e3ae-41e3-8913-a4a918af3f32/scratchpad/ccdiff/cells/cls-upto-4-thr-auto/twinC.c	2026-09-02 17:50:06.825653549 -0400
@@ -230,10 +230,12 @@
         // counted, so they are ONE loop carrying only the cursor.
         // The class's cell above reads "dead" because of this.
         if (forward_state == 0 && scan_position < subject_length && (unsigned char)(subject[scan_position] - 97) <= 25) {
-            unsigned long scan_run_length = 1;
+            const size_t rx_edge_start = scan_position;   /* [twinC] single-IV edge */
+            const size_t rx_edge_limit = (subject_length - rx_edge_start > 4UL)
+                                         ? rx_edge_start + 4UL : subject_length;
             scan_position++;
-            while (scan_position < subject_length && scan_run_length < 4UL
-                   && (unsigned char)(subject[scan_position] - 97) <= 25) { scan_position++; scan_run_length++; }
+            while (scan_position < rx_edge_limit && (unsigned char)(subject[scan_position] - 97) <= 25) { scan_position++; }
+            unsigned long scan_run_length = (unsigned long)(scan_position - rx_edge_start);
             if (scan_run_length == 4UL) {
                 forward_state = 2;
                 last_accept_position = scan_position;
@@ -258,10 +260,12 @@
             // counted, so they are ONE loop carrying only the cursor.
             // The class's cell above reads "dead" because of this.
             if (reverse_state == 0 && rewind_position > search_from && (unsigned char)(subject[rewind_position - 1] - 97) <= 25) {
-                unsigned long scan_run_length = 1;
+                const size_t rx_edge_start = rewind_position;   /* [twinC] single-IV edge */
+                const size_t rx_edge_limit = (rx_edge_start - search_from > 4UL)
+                                             ? rx_edge_start - 4UL : search_from;
                 rewind_position--;
-                while (rewind_position > search_from && scan_run_length < 4UL
-                       && (unsigned char)(subject[rewind_position - 1] - 97) <= 25) { rewind_position--; scan_run_length++; }
+                while (rewind_position > rx_edge_limit && (unsigned char)(subject[rewind_position - 1] - 97) <= 25) { rewind_position--; }
+                unsigned long scan_run_length = (unsigned long)(rx_edge_start - rewind_position);
                 if (scan_run_length == 4UL) {
                     reverse_state = 2;
                     match_start_position = rewind_position;
@@ -377,10 +381,12 @@
         // counted, so they are ONE loop carrying only the cursor.
         // The class's cell above reads "dead" because of this.
         if (anchored_state == 0 && scan_position < subject_length && (unsigned char)(subject[scan_position] - 97) <= 25) {
-            unsigned long scan_run_length = 1;
+            const size_t rx_edge_start = scan_position;   /* [twinC] single-IV edge */
+            const size_t rx_edge_limit = (subject_length - rx_edge_start > 4UL)
+                                         ? rx_edge_start + 4UL : subject_length;
             scan_position++;
-            while (scan_position < subject_length && scan_run_length < 4UL
-                   && (unsigned char)(subject[scan_position] - 97) <= 25) { scan_position++; scan_run_length++; }
+            while (scan_position < rx_edge_limit && (unsigned char)(subject[scan_position] - 97) <= 25) { scan_position++; }
+            unsigned long scan_run_length = (unsigned long)(scan_position - rx_edge_start);
             if (scan_run_length == 4UL) {
                 anchored_state = 2;
                 last_accept_position = scan_position;
