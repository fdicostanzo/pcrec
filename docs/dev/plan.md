# pcrec Project Plan

Working plan derived from ../../APPROACH.md. Milestones M0–M7 mirror APPROACH §9.

## Step-state format (grep'able)

Every step line matches exactly:

    - [Mx.y] STATE:<state> — <title>

States: `not-started` | `started` | `completed` | `blocked` | `deferred`

Find work:

    grep -n "STATE:started" docs/dev/plan.md
    grep -n "STATE:not-started" docs/dev/plan.md
    grep -cE "^[[:space:]]*- \[[^]]*\] STATE:not-started" docs/dev/plan.md   # ANCHORED count (81 on 2026-09-22); the unanchored grep also hits "formerly STATE:not-started" prose
    grep -c "STATE:completed" docs/dev/plan_completed.md

Completed rows are archived in docs/dev/plan_completed.md (this file keeps
zero STATE:completed rows; a completed-count grep here counts nothing but
recipe text).

Rules: update the STATE tag in place when a step changes state; expand a milestone
into substeps only when work on it begins (replace its single `[Mx.0]` line);
note blockers inline after the title with `(blocked: reason)`.

## Queue discipline (Frank, 2026-08-12)

**The BOONIES TIER sits well after the general work.** [M4-CALLOUTS],
[M4-SUBST], [V-E], [V-F], [SR-10] and their kin are PARKING SPOTS, not queue
positions: recorded so nothing is re-derived, started only after the spine —
M3 streaming, M4 captures + backtracking VM engine, M5 UTF-8, M6 feature
modules, M7 differential fuzzing — is done, unless Frank explicitly pulls
one forward. A boonies row growing substeps while spine rows sit not-started
is the smell this note exists to stop.
(Spine order re-ruled 2026-08-13: STD1 → M4 → M5 → M6 → M3 → M7 — see
"Development order" below.)

## Lane structure (Frank, 2026-08-29 — D86)

Lanes are TYPED, within the 3-concurrent ceiling: one FEATURE lane
([DD-13b]/[LIB], [DD-12]/[M5] UTF, modules), one OPTIMIZATION lane
([OPT-*], [ENG-PGO]; from the bench's outliers, measured need first),
and one ADMINISTRATIVE/STRUCTURAL lane AS NEEDED ([DD-11], [SPEC-1]
lanes, checks, doc consolidation) when a row is available and nothing
of higher priority wants the slot. A closed lane's successor comes
from the same column. Rows still open on Frank's word.

## Development order (ratified 2026-08-13)

The spine, in this order, one line of rationale each (full session rationale:
dev_journal.md 2026-08-13, sixteenth session):

- **[STD1]** — its suite re-baseline only widens with time; land it before
  anything else queues behind it.
- **M4 — captures + backtracking VM engine** — the biggest user-visible gap
  (captures) and the owner of the most parked decisions ([DD-2], [DD-7],
  [DD-9], [SR-8]).
- **M5 — UTF-8** — before M6, so feature modules are born CharSet/UTF-aware
  ([DD-12], [DD-1]) instead of being rebuilt once UTF lands.
- **M6 — PCRE feature modules** — lookaround, backrefs, atomic groups,
  named-groups, conditionals, recursion. The assertions module is the one
  M6 piece that needs no VM — a flexible slot, schedulable whenever a lane
  is free.
- **M3 — streaming input** — moved to last-but-one because its API must be
  designed once against the FINAL engine+feature set ([OS-3]'s evidence —
  lookbehind/backref window retention); building it earlier would mean
  rebuilding it.
- **M7 — hardening** — differential fuzzing was already pulled forward (to
  M2); the residue is testdata import and the freestanding/embedded
  profile.

Sequencing notes: the backrefs / atomic-groups / substitution-template
design notes (under M4 below) should be WRITTEN BEFORE M4 starts — they are
M4's design customers, and the substitution template compiler is
matcher-independent (Frank, 2026-08-12), so its design note need not wait
on M4 either. After M7, [BENCH-1] builds the feature-spanning benchmark +
prioritizer; then the OPT waves open, [MECH-3] first, with the prioritizer
setting the work order.

## Next: M4 (milestones start with Frank)

[STD1] completed 2026-08-13 (seventeenth session) — archived in
plan_completed.md. Per the ratified order M4 is next, and all three
pre-freeze design inputs are now LANDED AND RULED (D38, eighteenth
session, 2026-08-14): the [PC-5] flag-disposition table landed (merge
258fd79) and its dispositions are ruled wholesale, archived at
plan_completed.md's 2026-08-14 group; the subst-template design note
merged 3d5a9e8 with all fourteen of §9's questions fully ruled
(docs/design/subst_template_design.md); the M4-CALLOUTS callout-ABI
alignment proposal has its rulings applied
(docs/design/design_callout_abi.md) with only the syntax spelling (§6 Q5)
and the embedded-code restrictions (§6 Q6) left open. [M4.0] EXPANDED
into substeps M4.1–M4.7 (2026-08-14, Frank's go). AS OF THE NINETEENTH
SESSION'S CLOSE (2026-08-14): M4.1, M4.2 and M4.3 are ALL COMPLETED —
both design docs written, paneled (R21), fix-rounded, and the freeze is
DECLARED as the working baseline (D44 + addendum; D41–D44 rounded out
the ruling surface, K17 found-and-fixed, K18 opened and scheduled).
NEXT: [M4.4] — implementation is open.

## M4 — Captures + backtracking VM engine

Expanded 2026-08-14 (eighteenth session; Frank's go — design and panel
first, implementation next session at the earliest). The milestone's two
design docs do not exist yet and NOTHING M4-shaped has been panel-critiqued;
the two ruled pre-freeze docs (design_callout_abi.md, subst_template_design.md)
are themselves unpaneled and are swept into M4.3's panel by their own
stated terms.

- [M4.1] COMPLETED 2026-08-14 (merge 65b16c6) — row archived in
  plan_completed.md; the doc (docs/design/match_api_m4.md) is PROPOSED
  until M4.3's panel closes, and its §13 ASKs (naming picks + the
  rx_ctx-fixed-names confirmation) go to Frank with the panel materials
- [M4.2] COMPLETED 2026-08-14 (merge b726386) — row archived in
  plan_completed.md; the doc (docs/design/engine_m4.md) is PROPOSED
  until M4.3's panel closes. Headlines: DD-9 DECIDED (hybrid cannot own
  case (f) — re-home to BENCH-1's worklist, Frank's confirmation
  pending); SR-8's flip is smaller than its row (zero constructs become
  compilable); three ABI tensions reported (§11) and twelve ASKs (§12)
  ride to Frank/panel; four requirements handed back to
  match_api_m4.md (search-entry negative returns, the capture-delivering
  entry gap, ASK-4 closure per §5.7, symbol spellings)
- [M4.3] COMPLETED 2026-08-14 (R21; row archived in plan_completed.md) —
  the D6 panel ran (3 critics, 36 findings, 11 tier-1), every tier-1/2
  finding is dispositioned (D44 + the r21-fixes doc round + the k17-fix
  code lane, ubsan+asan green on the composed tree), and **the MATCH-API
  FREEZE IS DECLARED** as the M4 WORKING BASELINE (D44 addendum's
  weight: revisable at M4.7's post-run review, D40 regime 1).
  match_api_m4.md is the frozen surface; engine_m4.md is the design of
  record. Standing consequences: K18 (opened by the K17 fix's own sweep)
  is design-first-before-[M4.6]; the full battery (lint/bench/mech)
  remains owed at [M4.4]'s close per the standing schedule.
  IMPLEMENTATION IS OPEN from [M4.4]
- [M4.4] COMPLETED 2026-08-14 (merge c18e904, break commit 1dbb6ce, lane
  m44-apibreak) — row archived in plan_completed.md. The announced API
  break landed as ONE break commit: `<prefix>_span` RETIRED for the
  caps-array search signature (all emit sites incl. the three from
  D44/A-7, %zu→%td), the six fixed ABI types under the prefix-independent
  `PCREC_RX_ABI_H` guard, `<prefix>_match`/`_match_caps`/`_info`
  emitted unconditionally, `pcrec_error.input`, the `pcrec_options`
  flags word (PCREC_CASELESS/PCREC_EMIT_MAIN; PCREC_NO_CAPTURES
  reserved), RX_ERR_STEPS/RX_ERR_FRAMES reserved, +3 codegen checks
  (34→37: ncaps-is-the-macro, NCAPS>1⇒VM, cross-prefix one-TU compile);
  populations conserved and accounted; S04 sabotage retargeted to
  guard-neutering. FULL battery green at close (test/strict/ubsan/asan/
  lint/bench/mech 35-rows-0-undetected) — the lint/bench/mech debt owed
  since the eighteenth session is DISCHARGED. One as-built deviation
  awaiting a Frank ruling: `rx_info` is emitted as a struct TAG only
  (bare typedef collides with default-prefix `<prefix>_info` = literal
  `rx_info`) — see match_api_m4.md §5's as-built note
- [M4.5] COMPLETED 2026-08-15 — row archived in plan_completed.md.
  VM emitter core LANDED end to end across five substeps (a: capture
  .rxt format + python span oracle; b: src/gen/emit_vm.c, 65k-pair
  oracle sweep, §5.4 byte-identity gate; c: DD-8 tracer + D45 compile
  budget + K19 cap + K20 fix; d: D27-blinded author, 230/230, two
  contract-text gaps -> §2.2 addendum; e: D46 per-quantifier rung
  bitmask + boundary tests + coverage fill). Close battery all seven
  legs green at 7e3ff93; counts: corpus 1704, cli 247, reject 528,
  codegen 38, registry 168, PC-3 163, PC-4 62,872/0, trie 7,
  vm-identity 8, ir-listing 78, vm 28, thread 8, known_fail 1 (K18
  deliberate), mech 44 rows/0 undetected. Standing consequences:
  [M4.6] gate = K18 design-first — **DISCHARGED 2026-08-15**, design
  note paneled (R23) and BUILT by the k18-rewrite lane ([K18-FIX] in
  plan_completed.md; corpus 1704 → 3198); [ENG-BREP] alongside it;
  struct rx_info spelling RULED 2026-08-18 (D57: struct tag blessed);
  K19 residual ruling still open with Frank.
- [M4.7] COMPLETED 2026-08-18 (thirty-first session; fix merge d523a88) —
  row archived in plan_completed.md. M4's hardening wave: seven substeps
  (a-g). Standing consequences: docs/spec/match_api.md is the CONTRACT
  (R29-paneled, fix pass landed — the give-up-code space is now stated
  in BOTH shipped comments); capturediff + test-resource ride make test;
  corpus 10,369; gate 13 cases; D54-D57 placed; K7/K24 closed,
  K25/K26/K27 open; NEXT MILESTONE RULED: M6 first (Frank, 2026-08-18).

Design notes moved here from [MOD-0.1]'s archived entry (docs/dev/plan_completed.md),
2026-08-13 — M4's design customers, per the Development order above:

**DESIGN NOTE FOR `backrefs` (Frank, 2026-08-12 tenth session): the
engines column's blanket VM_ONLY on the digit rows is provisional and
splits under an AOT compiler.** At match time a backreference is a string
compare against the group's captured text — which is exactly what the
backtracking VM will do, and what the DFA engine cannot (subset
construction erases thread identity; no execution point knows a capture,
and `(a*)b\1`'s state would need unbounded text — the pumping-lemma
classic). But when the referenced group's language is FINITE, the backref
is REGULAR and compiles away statically: `(abc)\1` is `abcabc`, `(a|b)\1`
is `aa|bb` — expand each choice with the reference synchronized, pure
DFA, zero runtime cost, and only an ahead-of-time compiler can afford the
expansion (bounded by the existing NFA/DFA caps and gcc-compile-time
budgets; infinite-language groups keep the VM). So the module's engine
answer is per-PATTERN, not per-row: finite-group backrefs → ENGM_DFA via
expansion, infinite-group → ENGM_VM. The `engines` column stays design
intent until then (nothing consumes it before SR-8/M4); do not read the
rows' VM_ONLY as a measured limit.

**SAME-SESSION COMPANION NOTE for `atomic-groups`/possessives (Frank):
the (?> row's VM_ONLY splits the same way, and the naive intuition is
BACKWARDS twice over.** A DFA never backtracks in the first place —
subset construction keeps every alternative alive, which is exactly the
NON-possessive semantics — so `a*+` is not a free annotation: it CHANGES
the language (`a*+ab` matches nothing, `a*ab` matches "aab"), and a
naively-determinized atomic group silently implements the wrong one.
But the language stays REGULAR (atomic/possessive are CUT operators;
Berglund et al., "Cuts in Regular Expressions" — cuts preserve
regularity with possibly-exponential conversion), so pure-DFA
compilation is achievable, and the construction's one primitive —
determine the sub-expression's OWN priority-first match endpoint online,
ignoring the continuation — is precisely the priority accept-pruning
pcrec's subset construction is already built around. Blowup bounded by
the existing caps; the disjoint-follow special case (PCRE2's own
auto-possessification direction, a*b ≡ a*+b when nothing that follows
can start with an `a`) is free in both directions. Engine answer again
per-PATTERN: cut-constructible → ENGM_DFA, else VM.

## M5 — UTF-8

- [UTF-PAT] STATE:not-started (CHARTERED by Frank 2026-09-05, fifty-fifth session, UNSCHEDULED: "using utf as the Regex encoding. i'm guessing its close but needs validation and testing/support") — UTF-8 AS THE PATTERN-TEXT ENCODING: the contract, validated and tested. Frank's guess measured CORRECT at chartering: stage 2's pattern-char decode already handles raw UTF-8 pattern text under `-e utf8` — a raw `[α-ω]` (bytes 5B CE B1 2D CF 89 5D) emits the IDENTICAL machine to `[\x{3b1}-\x{3c9}]` (manager probe, artifact diff: only the provenance stamp/pattern-comment lines differ). What the row owes is everything AROUND that: (1) the CONTRACT stated in docs/spec/ (cli.md/registry.md): pattern text is interpreted in the PATTERN's encoding = the `-e` encoding (byte: pattern bytes are bytes — a raw `[α-ω]` under byte is legitimately the byte-class reading; utf8: pattern bytes decode to characters), including what an ILL-FORMED pattern byte sequence does (expect: clean compile error naming the offset, never a silent byte reading — MEASURE first, today's behavior unverified); (2) a VALIDATION SWEEP: raw-vs-\x{} artifact equivalence over a generated population (literals, classes, ranges, negation, case boundaries at 0x7F/0x80, astral, combining marks in literals), oracle-verified against 10.46 compiled WITH PCRE2_UTF on the pattern; (3) a D27-style corpus arm in tests/utf8/ (raw-spelling twins of existing blocks; the .rxt format's own file-encoding question — a `pattern` line carrying raw UTF-8 — is the manager's call per dd13b); (4) provenance surfaces: the pattern-comment/rx_info.pattern render for non-ASCII pattern text (today it renders \xNN-escaped — probably right, but say so in the spec); (5) registry/compliance rows if any construct's answer changes by pattern encoding. NOT this row: UCP semantics for \w/\d (stage 3-4 territory), utf8-by-default (a separate ruling if ever). Sonnet-sized measurement+corpus+spec; engine changes only if (1)'s measurement finds a silent-byte hole (then it is a miscompile-class fix, escalate).
- [K50-BNDSTART] archived to plan_completed.md (completed 2026-09-06 — merged 8e0fe77f, abi 24, K50 fixed on all three mechanisms + the site-2 second face; D97 ratified PCREC_ERR_STARTPOS; Linux battery owed at next slot)
- [UTF-RW] STATE:not-started (CHARTERED by Frank 2026-09-05, fifty-fifth session, SEQUENCED after the utf build (M5.0 stages 3-5) lands: "go out and find some good real world utf regex for testing. This will be foreign (for me) language stuff and I don't want the English bias built into the system to cause us to miss solid use case") — THE REAL-WORLD UTF REGEX HARVEST + THE CLASS-SHAPE CENSUS. Two deliverables from one collection: (1) THE HARVEST — sourced real-world UTF-8 patterns from NON-ENGLISH-LOCALE ecosystems, deliberately outside this project's own authoring bias: validation patterns from CJK/Arabic/Devanagari/Thai/Cyrillic/Greek/Hebrew codebases and frameworks (form validators, ID/postal formats, tokenizers, log/line parsers written BY speakers of those languages, not English-locale approximations), i18n test suites (W3C/CLDR-adjacent), combining-mark and RTL material — every pattern with PROVENANCE AND LICENSE recorded (REFERENCES.md-style citation discipline; no unlicensed corpus text lands in the tree), oracle-verified against 10.46 under PCRE2_UTF, landed as a tests/utf8/ corpus arm (not D27-blinded — a harvest is public material, the blinding lever does not apply; the D27 lever for this milestone already ran) AND proposed to the bench as a real-world utf SET (their syntax-census/[B36] machinery is the natural consumer — coordinate via the channel). (2) THE CLASS-SHAPE CENSUS over the harvest, which is the row's measurement payload (Frank, same day: real classes may hold MANY NON-CONTIGUOUS characters — "degenerate cases... which would need to be if-then'd. Perhaps a strategy is bitmaps over clusters. Needs real world examples"): per-pattern distributions of class interval counts, per-LEAD-BYTE cluster counts, lowered branch counts, VM chain lengths and DFA state counts, split by script family — the population that decides whether Frank's CLUSTER-BITMAP candidate (per-lead-byte continuation-set bitmaps — a 2-byte character's continuation space is 64 values = one 8-byte mask per cluster, replacing a chain of range tests in the VM lowering) is a real form-ladder rung or a shape real patterns never produce. D77 discipline: the candidate is NAMED here and BUILT only on the census's numbers. Standing evidence to weigh, not assume: the DFA side already cluster-shares aggressively (\p{L} = 648 intervals minimizes to 283 states — the byte-class equivalence map is doing the collapsing; the hazard is the VM route's linear branch chain, and any fix is a [FORM-CHAR]-family form question); form_char_step0's family-B result (byte-tier bitmap tables LOSE .text vs range compares on sparse ASCII classes) does NOT transfer unexamined — a continuation-cluster mask is a different size/shape trade at a different alphabet width. Sequencing note: the harvest wants stages 3-4 compiled (\p, Unicode fold — most real i18n patterns use them); a class/literal-only SUBSET is compilable at stage 2 and may be collected early if a lane is idle. Sonnet-sized collection + census; any resulting form rung is its own measured engine row.
- [XARCH] STATE:deferred (TABLED by Frank 2026-09-06, fifty-sixth session: "all are good work efforts but lets table xarch for now. note that there will be a round in the future for architecture specific opt" — the clang column, the iso-ts redo and the pinned-10.46 jit column all wait for that round; the step-0 memo + evidence dir stand as its seed, scratch tier.) (CHARTERED by Frank 2026-09-05, fifty-fifth session: "Can we test macOS vs Linux arch compiler bench rates? I get that the tests are slow because they execute a lot of processes" — STEP 0 lane launched same hour, Mac side only) — CROSS-ARCH COMPILER + MATCHER RATES, macOS/M1/gcc-16 vs Linux/Ryzen/gcc-15.2, on IDENTICAL emitted C (byte-identity cross-box already proven: 2,962 size-log rows). Deliberately separated from the suite-slowness question, which is ANSWERED ([TT-14]: spawn tax, not compute). TWO HALVES: (1) COMPILE RATES — a full Mac `test-corpus` with SIZELOG at the committed size log's own commit, then a per-artifact join of the two logs' gcc CPU/wall columns (~2,960 paired cells, no new Linux run needed); (2) MATCHER THROUGHPUT — pcrec built at the bench's pin 334fd10e on the Mac, their own cells re-emitted (bounded ladder, floor x all three --vm-entry-shape rungs, altwide w-8/64/256 vm+auto, iso-ts), timed under an INTERLEAVED-trials protocol (opt4_impl's trap: this box spans 20% run-to-run uninterleaved), compared as RATIOS against the bench's PINNED ledger numbers at the same pin. THE HOOK: the ARM disassembly (o17facts, I-50 §2) shows the forced-VM forward shape TIGHTEST on ARM where gcc-15.2/x86 measured it x2.0 SLOWER — half (2)'s floor cells empirically test whether the bench's tripwire regression is toolchain/arch-specific, complementing the Linux probe owed in I-50/DONE. DISCIPLINE: every number SCRATCH TIER — nothing enters the bench's store or is comparable to their quiet-box floors (memory pcrec-cross-platform-verification); ratios first, absolutes disclosed with load/thermal caveats; NO jit column at step 0 (local libpcre2 is 10.48-Homebrew vs the reference 10.46 — U13's version noise, deferred until a pinned 10.46 build exists locally if ever needed); the bench repo is READ-ONLY (patterns read from the local clone; D78 one-writer rule untouched); memo docs/dev/xarch_step0.md + evidence dir, offered to the bench via the channel for [B36]'s possible arch column. Sonnet lane, Mac-local, runs while the Linux battery drains. STEP 0 DELIVERED AND MERGED same day (docs/dev/xarch_step0.md; lane xarch0): compile rates Mac÷Linux median gcc-CPU 0.518 (~1.93x faster per artifact, IQR 0.503-0.536, flat DFA-vs-VM, 0 byte movers of 2,925 joined rows — byte identity re-proven); throughput: forced-VM cells 1.4-2x faster on ARM, auto/DFA ~1.07-1.10x slower on the wide altwide rungs, bounded ns-scale cells carry P/E-core spread (disclosed, not smoothed); THE HOOK ANSWERED — forward TIES plain on ARM (<1%, 3 independent runs): the x86 ×2.0 floor regression is gcc-15.2/x86-specific, AND a finding nobody predicted: `shared` is ~3x slower than plain/forward on ARM (the noinline+descriptor call is ~100% of floor's per-attempt cost there) — weigh in any future entry-shape governor. iso-ts INCONCLUSIVE (synthesized subject, wrong sign — redo with the bench's own subject tooling if it matters). REMAINING (unscheduled, Frank's call): a clang column, the iso-ts redo, a pinned local 10.46 for a jit column; the hook result relays to the bench with today's DONE.
- [FORM-CHAR2] STATE:not-started (CHARTERED 2026-09-06 at O-19 §1; measurement lane, sonnet-sized, next admin slot) — THE FOLD'S TIMING BILL ARRIVED: the bench's abi-23 AFTER measures [FORM-CHAR]'s cls-fold SLOWER on its one corpus witness (ci-256 forced-VM x1.027 search / x1.045 thr / x1.095 match vs a 1.34% noise floor) against -20% code / -32% .so / x0.40 compile. O-19 asks (i) per-site instruction counts on the fold-vs-bitmap pair (form_char_step0.md's asm method, one witness deeper), (ii) a repeated-fold-class customer shape (does any real pattern hit the same fold constant hot enough to flip the sign), (iii) THE DEFAULT — Frank rules after (i)/(ii); until then fold stays default-ON (the deny flag -fno-cls-fold is the caller's recourse; the size win is unconditional, the speed loss is at/near noise on the only witness). FRAME SET 2026-09-06 (Frank): the ruling is expected to be a DIAL PLACEMENT, not a boolean — under [OPT-DIAL]'s five-setting direction cls-fold at current numbers is the worked example of "one notch toward size" (penalty under x%, savings over y%), so (i)/(ii)'s numbers feed the tier classification; report them against that rule-shape. **RULED (Frank, 2026-09-11, sixtieth session): SUBSUMED INTO THE [CLS-TREE] DESIGN NOTE (option B)** — cls-fold's end-state is one kit member among several (the m=0x20 one-cube instance of the study's general cube form), selected by the sectioning DP and priced by lambda; NO standalone dial placement for a special case (general-mechanisms rule). (i)/(ii) still run, but as CALIBRATION INPUTS to the design note's op-pricing cost model, not as evidence for a standalone ruling. The timing half is ns/char-class and rides the ubuntubudu route (I-65 session) per the same-day darwin-gate ruling; the asm-counting half is box-independent. Shipped cls-fold stays default-ON with -fno-cls-fold as recourse until the kit replaces it (implement-then-replace).
- [SEL-SIZE] STATE:not-started (CHARTERED unscheduled 2026-09-06 at O-19 §2's finding; Frank rules the direction) — SHOULD AUTO DECLINE A WARNED-SIZE DFA WHEN A VM FORM EXISTS? [LIM-2] N1 accidentally demonstrated the prize: cls-upto-8192 \z's 937 KB warned DFA had been auto-selected where the VM is x6.6 FASTER on match; N1's budget now routes it to the VM by luck of the element count. The general question is a size/speed-aware selection term (OPT-DIAL's territory — the dial inventory names size as the unmeasured axis on 15 of 21 switches); the honest shape is a measured knee, never a warned-size special case (general-mechanisms rule). Blocked on Frank's direction ruling + a corpus census of warned-DFA-with-VM-form population.
- [LIM-OVR] archived to plan_completed.md (DELIVERED 2026-09-22, lane admin1 — the FLAG_D token, cli/main.c cross-check, S208 re-aim; the D26-tier "elements" unit-harmonization residual named in the charter's own last sentence was deliberately NOT touched, per that sentence's own "never alone" instruction)
- [READ-BYTES] STATE:not-started (CHARTERED by Frank 2026-09-05, fifty-fifth session, UNSCHEDULED: "changing the code usage of ascii characters from numbers to char literals if that works or embedded comments if it doesn't. only for code reading for those of us that can't convert numbers to ascii in our heads") — READABLE BYTE VALUES IN EMITTED CODE. Today an emitted compare reads `subject[scan_position] == 58`; the row renders printable-ASCII byte values as char literals (`== ':'`) and non-printable/non-ASCII values as the number plus a fixed-form comment (`== 206 /* 0xCE, UTF-8 lead */` — exact comment vocabulary to be designed; UTF-8 lead/continuation bytes should say what they are, control bytes name their escape). SITES: the emitted compare/range tests (both emitters), table initializers where a legend already exists (the [M6-READ] state legends may already discharge tables — survey first), the class-test range arithmetic (`(unsigned)(subject[p] - 177) <= 14u` wants its range endpoints named in a comment since char literals cannot express arithmetic offsets). RITUAL, all pre-priced by precedent: [M6-READ] is the model (approved-sample-first, then conversion); char literals are compile-time identical so run_object_neutrality.sh proves `.text`/`.rodata` byte-identity for free; the SOURCE bytes move on every artifact → a D76/D94 abi event (bump + re-pin (B) + grep for EVERY reader of emitted text — the manifest/size-log/anchor re-records exactly like this morning's battery repair slice; run the [SABANCHOR] tripwire before delivering); comment-form additions move comment-INCLUDED counts (RX_VM_PROGRAM_BYTES) but not comment-excluded code bytes — size-cap semantics unchanged, say so in the spec hunk; char literals shift code bytes by ±1/value — census the delta, expect noise. ONE RENDERER: a single emit-side helper (value → rendered token + optional comment) used by every site, never per-site formatting (memory: general mechanisms; and a K35-style check that the helper is the only spelling). Sonnet-sized with an approved sample first per [M6-READ]; unscheduled, fits any quiet slot after the current utf8 rows settle.

- [MACPORT-XARGS] archived to plan_completed.md (DISCOVERED ALREADY FIXED 2026-09-22, lane admin1 — Frank himself closed the remaining `xargs -a` sites directly on 2026-09-10, commit fb1b9c5e, four days after this row was chartered and never re-flagged; darwin re-verified green 212/0/1 at branch point 69172a00, no code change needed)

## M6 — PCRE feature modules

- [M6.0] archived to plan_completed.md (CLOSED 2026-08-25, fortieth session, on Frank's ruling — D79: the five ruled modules named-groups/assertions/atomic-groups/backrefs/lookaround all shipped 2026-08-18..24 and `recursion` outside the list as [DD-14]; the M6 milestone completed the spine. Residue re-homed: post-module items (1)/(2) live on [DD-11] by D66 — the 83x partial-anchor gap is the kind of thing the bench loop should FIND, and [DD-11]'s core reduction gets its charter through the loop if it shows at rank; (6) is [SR-8]'s armed second-construct trigger)
- [M6.1] archived to plan_completed.md (completed 2026-08-18, thirty-third session — the R30 panel + focused re-checks + N1 verification closed it; design docs/design/assertions_design.md merged at 0beac07; review docs/dev/reviews/2026-08-18-r30-assertions-design.md; Frank rulings Q1/Q3/Q8 carried on [M6.2])
- [M6.2] archived to plan_completed.md (completed 2026-08-21, thirty-fifth session — module `assertions` closed: five waves + repair slice + D27 blinded corpus, all close-validated; the post-module queue re-homed to [M6.0])
- [M6.3] archived to plan_completed.md (completed 2026-08-18, thirty-third session — see that file; D59, merge commits on main)
- [SPEC-M] archived to plan_completed.md (completed 2026-08-21 — spec_mod0 green, (?m) named exceptions with guards; expiry DD-11)
- [M6.4] archived to plan_completed.md (completed 2026-08-22, thirty-sixth session — module `atomic-groups` SHIPPED: design R31 (21e173e), implementation 69f3b93 + fix 8e4af41 (the D27 corpus found a tier-1 miscompile — the MRL follow-bound crossing the cut — fixed at its root), D27 corpus c324091 134/134; union battery all five stages green on the fixed state (test 21,557/0); mech 99 rows 0 undetected, S88-S101 all DETECTED; gate 13/13 at load 0.39, weakest margin 1.43x (34ede2c); SR-8 built (D67); K29 fixed; U9 held LOUD in tests/known_fail pending Frank's ruling)
- [M6.5] archived to plan_completed.md (completed 2026-08-22, thirty-sixth session — module `backrefs` SHIPPED: design R32 (ca9beef), implementation 3aa446f (lane fb97bab: test 23,798/0, its own ubsan+asan green, identity gate 1501/1502/1501 on three axes), D27 corpus 672a2f4 207/207 with ZERO implementation divergences; union battery all five stages green on 3aa446f; mech 118 rows / 0 anomalies / 2 undetected at 5edba64 → S107 corpus gap closed + DETECTED, S108 RETIRED as measured-unobservable → 117 scored rows, 0 outstanding; gate 13/13 at load 0.36, weakest margin 1.43x (41e541a); registry 106/52/48/6 asserted; DUPNAMES (?J) shipped with first-of-run-by-number-that-is-SET resolution and the tiebreak as correctness; the [M5-SEAM]'s second residual entry pair; S102-S120; residuals: wire S103 to `brefidentity` (registered, zero rows); a MULTI-HUNK sabotage mechanism — DONE post-close 2026-08-23 (the matrix's optional second site; S108 RE-INSTATED two-site and DETECTED; tripwire 118 rows / 119 anchor sites); a sabotage row for possessify.c's Glushkov `case A_BREF` arm (load-bearing, no row); FOLLOW-UPS from Frank's read of the `(abc)\1` artifact (2026-08-22 23:0x, measure first per D18): (a) `engine_callable` seam entries should be `static` — rx_bref_match inherited next_pos's external linkage without an external consumer; (b) a DEFINITELY-SET analysis (dominance through A_CAT, not through A_ALT/A_REP bodies) to elide the UNSET test where the referenced group always publishes before the reference; (c) a memcmp fast path on the success path with the failed-prefix walk only on mismatch — the charge's prefix-length contract keeps; (d) STATIC GROUP WIDTH — a fixed-width referenced group (`(abc)`) makes the compare a constant-length compare with one hoisted bounds check, and under the definitely-set analysis pcrec_minw(A_BREF) may soundly become the group's minw (exact for fixed width) instead of the design's deliberate 0, feeding the MRL prune; the full form is the chartered finite-language expansion (`(abc)\1` → `abcabc`), whose only customer is --no-captures; (e) THE WORK UNIT is bytes examined, an effort PROXY calibrated to the VM's per-byte loop — block compares (backref memcmp, memchr prefilter, cursor scans) over-charge relative to effort, the conservative direction for a DD-2 robustness bound; if the unit is ever meant as effort, charge ceil(n/k) for block operations — a ruling for the unit, not a backrefs one-off; (f) THE SINGLETON-LANGUAGE REWRITE, in Frank's core-reduction form (23:3x): NOT a backrefs one-off — under the predicate |L(G)| = 1 under G's own flags, definitely-set, nrefs == 1, `\1` and `\g<1>` mean the same thing (PCRE2 REVERTS a subroutine call's captures, so inlining a non-recursive call IS the non-capturing copy `(?:G')` the rewrite needs), so the rewrite is a one-line node swap A_BREF → subroutine-call in the core-reduction layer (DD-11's principle, D66's precedent) and the `recursion` module's INLINER does the rest — one path for inlining group code, no separate emitter code; lands when that inliner lands; payoff on the DEFAULT build is the hybrid prefilter coming back ON (no A_BREF in the tree; today fit.prefilter = false for every backref pattern); (b) shares its definitely-set analysis with this predicate — one pass; (d) is the only genuinely backref-local item and is dropped unless a measurement asks; none blocks the close)
- [M6.6] ARCHIVED → docs/dev/plan_completed.md ("2026-08-24 (thirty-eighth session — [M6.6]: module `lookaround` ships)"): M6.6, M6.6.1-M6.6.4 all completed; M6.6.2's `completed-pending-close` was discharged by M6.6.4's close. Archived 2026-08-25 by the thirty-ninth session's manager.
- [ABI-NS] archived to plan_completed.md (completed 2026-08-18, thirty-third session — D60+addendum implemented; merge on main; [M6.2]'s wave A is UNBLOCKED)

(2026-08-13: the classes+ and modifiers halves already landed as gated
modules — MOD-0.3 and MOD-0.5, see docs/dev/plan_completed.md. Remaining:
assertions (VM-independent), lookaround, backrefs, atomic groups,
named-groups, conditionals, recursion producers. 2026-08-18 expansion
note: conditionals and recursion are NOT in Frank's ruled M6 list and are
not substeps above — their module refusals stand; they queue behind M6 as
their own future rows if ruled in.)

- [M6-READ] archived to plan_completed.md (completed 2026-08-21, thirty-fifth session — the emitted-code readability pass: approved style shipped from both emitters, legends generated from emitter-owned data, census-proven byte identity; CONVERSION_LOG.md is the findings record; src/gen/CLAUDE.md carries the emitted-vocabulary rules)
## M3 — Streaming input

- [M3.0] STATE:not-started — DESIGN GATE FIRST (R2-A3): D7's "same shape streaming needs" holds only for match-END finding. The reverse pass rescans backward through raw bytes a stream may no longer hold (unbounded for `.*` shapes). Design match-START finding under bounded memory BEFORE writing streaming code; reconcile with APPROACH §6's PARTIAL/WINDOW_EXCEEDED contract
- [M3.1] STATE:not-started — *_stream_* API for the DFA engine, chunk-boundary tests

(2026-08-13: moved after M6 per the Development order above — the streaming
API is designed once against the final engine+feature set; [DD-3] rides
along, [OS-3] feeds the design gate.)

## M7 — Hardening

- [M7.0] STATE:not-started — milestone (expand on arrival): differential fuzzing vs libpcre2, freestanding/embedded build profile, PCRE2 testdata import

## Unscheduled rows (Frank, 2026-09-21, seventy-fifth session)

Chartered by Frank, no milestone and no trigger yet; each starts on his word.

- [EMIT-ENTRIES] STATE:not-started (CHARTERED by Frank 2026-09-21: "emit control for the functions produced (search, match). if X depends on Y and only X is emitted, then Y can be emitted statically") — ENTRY-POINT EMIT CONTROL. Today every artifact exports the five `<prefix>`-scoped entries unconditionally (docs/spec/match_api.md:467; `--no-captures` is the only subtractive lever and it selects an engine shape, not an entry set). The row: a caller names the entry set it wants (a flag, a `.rxt` `target` attribute, or both — the manager designs, Frank rules); an entry not named is emitted `static` when another named entry depends on it (the un-suffixed entries forward to the `_in` twins, match_api.md:188 — so naming `_search` alone makes `_search_in` static and drops `_match*` entirely), and not emitted at all when nothing named needs it. What it buys: smaller artifacts and no exported symbol a consumer did not ask for (the D104 hazard class). Contract: the `rx_info.nentries`/`search_form` stamps already exist to describe the set — the spec hunk says what a reduced set stamps. abi event by construction (D76). Design gate before code (the four lenses, memory `pcrec-design-evaluation-lenses`): is the entry set a per-target attribute (source-file world, D118) or a per-invocation flag, and how the harness's identity gates treat a reduced artifact.
- [LANG-1] STATE:not-started (CHARTERED by Frank 2026-09-21: "a series of investigations about how this tool might be used with specific programming languages. start: C, Rust, C++ ... result is write up doc per language with examples. should actually build and test cases in the language") — LANGUAGE-INTEGRATION STUDIES, WAVE 1: C, Rust, C++. One investigation per language, each a lane, each delivering (a) a write-up under docs/guide/lang/<language>.md (the guide tier, D73: use cases pointing at the spec) with worked examples, and (b) a BUILT AND TESTED example under examples/<language>/ that the suite compiles and runs where the toolchain is present (SKIPs loudly otherwise, the libpcre2 shape). C: the helper functions/macros a consumer wants around the raw entries (the `caps` array shape, `_next_pos` iteration, the give-up codes, `_in` buffers) — which of them belong in the artifact, which in a header pcrec ships, which stay user-side. Rust: how a Rust crate links the generated C (build.rs + cc, or a prebuilt .a per [REL-1.10]'s Makefile shape), the unsafe boundary, what a safe wrapper needs from `rx_info`, whether a `bindgen`-free hand-written extern block is enough. C++: whether an object wrapper earns its place (RAII over `_in` buffers, a match-result type over `caps`), header-only. Each study names the artifact/API changes it would want (fed back as rows, not made in the study). Sequenced after [REL-1.10] (the examples build on the Makefile shape) and alongside [REL-1.3] (the guide gets its lang/ chapters from here).
- [LANG-2] STATE:not-started (CHARTERED by Frank 2026-09-21, "next wave of languages would include higher level like JS, TS") — LANGUAGE-INTEGRATION STUDIES, WAVE 2: JavaScript / TypeScript (Node native addon vs WebAssembly of the generated C — the encoding boundary is the whole question: JS strings are UTF-16, the artifact is byte/utf8), then whatever wave 1 argues for next (Python ctypes/cffi, Go cgo, Zig are the obvious candidates). Same deliverable shape as [LANG-1]. After [LANG-1]'s write-ups exist.

## Beyond M7 — long-term vision (Frank, 2026-08-09)

Direction, not scheduled work. Recorded so the architecture is not painted into
a corner that would make these expensive later. Each becomes a milestone only
after the current ladder is complete and the result is something we are happy
with.

**POSITIONING NOTE (Frank + manager, 2026-08-13 sixteenth session).** The
niche, in one sentence: *PCRE2 semantics, compiled to verified,
dependency-free C — fastest where the pattern is known ahead of time, and
the only one that does compiled substitution.* Its elements, each with an
owner: (1) PERFORMANCE, honestly scoped — an AOT compiler wins where the
compiler saw the exact pattern (specialized single-pattern scanning,
startup, latency), and does not chase Hyperscan-scale SIMD multi-pattern;
[BENCH-1]'s prioritizer maps both the worst cells (fix) and the best cells
(advertise). (2) EMBEDDED / NO-LIBRARY — the defensible core. The incumbent
to name is re2c; differentiation is PCRE2 syntax+semantics, leftmost-first,
the future captures/backrefs tier, and the verification story. What the
code already guarantees is the sales sheet: all-const tables (TS-1,
enforced), no malloc/errno/locale in generated code, thread-safe by
construction, deterministic — certifiable vendored source. M7's
freestanding profile cashes this. (3) THE VERIFICATION STORY IS A PRODUCT
FEATURE — "differentially verified against libpcre2; unsupported syntax
fails loudly, never miscompiles" is a claim no neighbor in this niche
makes. (4) LATENCY — time-to-first-match from process start (~zero for us:
tables page in from .rodata; PCRE2 pays pcre2_compile + JIT warmup every
process) and short-subject per-call overhead, the dimension real workloads
(log lines, field validation) are dominated by and benchmarks skip —
measured under [BENCH-1]. (5) COMPILED SUBSTITUTION as the headline
differentiator — see [M4-SUBST]'s beyond-PCRE2 direction. Developer-
experience directions that serve the niche are the [V-*] rows below,
including V-G/V-H (added this session).

- [TT-4M] STATE:started (CHARTERED by Frank 2026-09-08, fifty-seventh session, re-opening [TT-4]'s closed levers ON DARWIN — the close note's own revisit condition, with the profile now pointing at PROCESS DISPATCH rather than gcc CPU: [TT-14]/[XARCH] half 1 both measure the Mac loss as spawn tax, K44's load-47.6-on-12-cores is the same fact; Frank's directive verbatim: "have pcrec simply build several at once as part of the rxt file process then gcc them all at the same time into a single executable (have main() take options to select) then we could test with fewer processes. validate") — STEP 1, VALIDATE (measurement + prototype in studies/, NOTHING under tests/harness or src/): batch N corpus patterns → distinct-prefix pcrec emissions → ONE gcc link → ONE executable whose main() dispatches by selector, run against the unbatched path for (a) wall + spawn-count deltas at N∈{4,16,64} on this Mac, (b) answer identity over the slice, (c) the one-member-fails degradation cost [TT-4.1]'s memo measured on Linux, (d) the residual pcrec-spawn share, which is the D77 trigger measurement for a multi-pattern pcrec CLI mode (a src/ change built ONLY if that share is the new bottleneck). Separate TUs first (dodges the PCREC_FEATURE_SET collision the stage-B memo names); TU-concat measured second if the link path leaves wins on the table. Harness adoption is STEP 2, its own row, on these numbers. Lane tt4m (sonnet), launched same hour. **STEP 1 DELIVERED + MERGED same day (51106544; docs/dev/tt4m_darwin_validation.md): 2.41x/3.63x/4.28x wall at N=4/16/64, 413/413 answer-identical, heterogeneous --features clean under shape L, the UNPLANNED second lever (126 binaries → 2 collapses macOS per-binary first-launch cost 11.79x standalone), D77 residual NOT met (no multi-pattern CLI mode). STEP 2 OPENED by Frank same evening ("lets proceed through step 2... the same thing should run under linux but i don't see conflict") — HARNESS ADOPTION, substeps: (2a) the owed PARALLEL-dispatch N-sizing measurement (step 1 was serial-only; TT-4.1's Linux data shows too-large N starving cores; measure N × PROCS on this box, and note K44's load-47 pathology as a candidate beneficiary); (2b) design note for the run.sh corpus-path change — batching unit, dispatch protocol (harness-local generated main; [V-E]'s organizer replaces it when that row opens, implement-then-replace), SIZELOG's per-pattern gcc CPU/wall column under a batched compile (options: -c per TU in one gcc process / batch-amortized column / SIZELOG runs stay unbatched), GENCFLAGS-san/LINTGEN axis compatibility, mech's harness-anchored sabotage rows, degradation recovery (drop-named-TU-and-relink), timeout wrapping, LINUX PARITY (shape L is plain gcc; executor validates the Linux arm); light D6 panel on the note; (2c) implementation lane (tier decided off the design); (2d) acceptance = full make test answer-identical counts on darwin + wall before/after, mech clean, then the Linux arm via executor. Lane tt4m2 (sonnet) launched for 2a+2b. 2a+2b DELIVERED + MERGED same evening (N=64/P=8, the design note); r55 LIGHT PANEL run (docs/dev/reviews/2026-09-08-r55-tt4m-batching.md: 3 blocking-tier — routed-block exclusion gap with a live framebuffer.rxt witness, N-scaled D45 budget dilution, the foreordained starvation cell — + phantom census + case-attribution keying; all eight design decisions otherwise independently confirmed against the tree); REVISION lane tt4m2f applied R55-1..9 and MERGED (per--c UNSCALED D45 budgets, routed-cell exclusion from parse_route's own grammar, census archived, keying fixed). (2c) OPENED same night — lane tt4m3 (sonnet): implement HARNESS_BATCH=N in tests/harness/run.sh per the r55-revised design note; CONSTRUCTION + small-slice smokes only while stage-4 lane utf8s4 holds the box (legs ≤4 min); the acceptance battery (2d: full make test answer-identical counts + wall before/after, then the Linux arm via executor) is the MANAGER'S, scheduled after stage 4 frees the box; R55-10's evidence bar rides 2c/2d (acceptance numbers ARCHIVED, the default-on flip decision reads them). **STEP 1 DELIVERED 2026-09-08 (docs/dev/tt4m_darwin_validation.md, studies/tt4m_batchrun/): a measured YES, bigger than [TT-4.1]'s Linux answer — shape L (one gcc call naming N separate .c sources + a dispatch main(), never concatenating source) beats the unbatched shape at every N tried, up to 4.28x wall / 2.79x CPU at N=64 on a 126-pattern/413-case tests/base slice, zero answer-identity mismatches at any N, and no --features homogeneity restriction needed (checked directly, unlike [TT-4.1]'s TU-concatenation). Found an UNPLANNED second lever bigger than the gcc one: fewer distinct executables launched at case-run time (126→2) collapses macOS's per-binary first-launch cost, 11.79x on its own vs. the gcc lever's 2.65x — confirmed by a same-binary-reuse microbenchmark. Degradation reproduces [TT-4.1]'s Linux shape (cheap to detect, linear-in-N to recover). D77's residual trigger for a multi-pattern pcrec CLI mode NOT met (pcrec is ~1.5-2.8% of batched wall at every N). Serial-only — does not replicate [TT-4.1]'s Linux non-monotonic parallel-dispatch finding; owed to STEP 2. STEP 2 (harness adoption) remains its own future row, not started.** **(2c) IMPLEMENTED 2026-09-08 (lane tt4m3, docs/dev/lanes/tt4m3_report.md): `HARNESS_BATCH=N` lands in tests/harness/run.sh per the r55-revised design — unset/0 is today's per-pattern path byte-for-byte (verified against main on several slices), N>=1 stages consecutive eligible blocks per file into batches, excluding perr/H11/routed blocks exactly as designed, with per-member `-c` sub-compiles at the UNSCALED D45 budget (free per-member attribution on a compile failure, no stderr parsing needed), an N-scaled outer wall backstop, and a solo-relink fallback for the rare link-only failure. `tests/harness/dispatch_gen.sh` is the new dispatch.c generator (bash port of the studies/tt4m_batchrun prototype). Two findings surfaced and fixed in the same delivery: bash's `read` collapses/strips consecutive TAB (IFS-whitespace) delimiters even under a single-character `IFS=$'\t'`, corrupting batch member case data — fixed by packing with `\x01` instead; and `tests/lib/size_count.sh`'s SIZELOG stamp extraction hardcoded the literal `RX_*` macro names, silently blank for any non-`rx` prefix — fixed by taking the artifact's `-p` prefix as an explicit parameter (general fix, not batching-specific). Small-slice smokes only (box held by utf8s4): answer-identity verified byte-for-byte on multiple slices covering perr/H11-routed exclusions, `gu`/`engine`/`budget`, `g`/`gp`, flags/features/encoding, RXTDUMP, SIZELOG (post-fix), PROCS>1 threading, and the degradation path (a live planted-bad-TU smoke via a temporary since-removed hook). The full `make test`/`make test-axes`/mech acceptance battery (2d) is OWED to the manager, box-gated behind utf8s4.** **RULED (Frank, 2026-09-16, sixty-sixth session): the HARNESS_BATCH default is NOT flipped. The row's next step RE-AIMS: investigate WHY the whole-suite saving is only ~9.5% against the prototype's own 4.28x/18.65x isolated ratios — tt4m_time.md's 3-of-38-sections attribution is STRUCTURAL, not measured (no per-section timestamps survive in either log); the investigation is a per-section timing attribution that either confirms the unreached-sections explanation with numbers or finds where the batching win is being eaten inside the reached sections. NOT SCHEDULED — do not charter without Frank's word.**
- [TT-4] STATE:completed (CLOSED 2026-08-23 on Frank's ruling after [TT-4.1]'s measurement: gcc is 17% of make test's CPU and make test is ~10 of the chain's ~150 minutes — TU-batching's 3.66x on gcc nets ~12% of suite CPU for the most design surface on the list (feature-set collisions, all-or-nothing batches); Frank: "6 and 7 probably not worth it … spend the effort where it has the greatest impact; if test is 10 minutes, spend the least time there". [TT-4.2]/[TT-4.3] NOT started — the measured NO, TT-3's shape; the prototype and numbers stay in studies/tt4_batching/ for the day the profile points at gcc again. The row's lasting yield is the census method and the [TT-6] finding) — formerly STATE:started (2026-08-23, on Frank's "go tt-4"; expanded into [TT-4.1]-[TT-4.3] below) — BATCHED COMPILATION IN THE TEST HARNESS
  (chartered by Frank, 2026-08-22 22:3x, "charter TT-4 after M6.5 closes,
  measurement stage first"; START AFTER [M6.5] CLOSES — the next
  infrastructure row, pulled forward because every lane today paid the
  cost). THE PROBLEM, measured: `make test` is ~120 core-minutes (~10 min at
  -j12) across ~20,000 tiny compile-and-link gcc invocations; the sequential
  per-section wall times from the [M6.5.2] lane's timestamped run were
  assertions+identity gates 458 s (the four identity gates compile ~1,200
  corpus patterns TWICE, ~2,400 gcc calls), the .rxt corpus 392 s (~1,800
  patterns, pcrec→gcc→link→run each), rungselect 242 s, counterk 166 s, mrl
  115 s, atomic diff 100 s. [TT-3] showed caching cannot rescue this shape
  (compile-and-link single invocations; warm ccache 4x SLOWER). THE IDEA
  (Frank's, and [V-E]'s multi-pattern compilation unit with the harness as
  its first real customer): one translation unit per BATCH of N matchers
  (each already carries its own prefix — the emitter was designed for
  coexistence) plus one dispatching driver — one gcc call and one link per
  batch; the saving is the fixed per-call cost (process start, pcrec.h
  parse, link) times N, largest in the gcc-bound suites; matcher RUN time
  unchanged. STAGES: (1) MEASUREMENT FIRST (Frank's order) — instrument
  tests/harness/run.sh and the main differential drivers to split wall
  time per section into pcrec / gcc / link / run and count invocations over
  one `make test`; identify the two worst sections; prototype batching on
  them in a scratch driver and REPORT THE MEASURED SPEED-UP before any
  harness change (TT-3's lesson: the obvious idea measured as a slowdown).
  (2) DESIGN, gated on (1)'s numbers: batching at the gcc/link step ONLY —
  per-pattern .c artifacts keep existing so every per-artifact instrument
  (codegen rules, identity gates, D46 stamps, the anchor tripwire, the D27
  cells' perr attribution) reads exactly what it reads today; FAILURE
  ISOLATION (a member that fails to compile or crashes under a sanitizer
  must not take its batch down — fall back to per-pattern for the failing
  members and attribute the failure to the right pattern; D45's per-pattern
  gen-timeout semantics preserved; GENCFLAGS rides through); an emitter-side
  question only if two matchers cannot coexist in one TU (then [V-E]'s work
  is the prerequisite, measured not assumed). (3) LANDING: the harness
  batches; `make test` wall time and core-minutes re-measured on the same
  box; the sanitizer batteries and mech ride the same path; docs/testing.md
  records the before/after. NOT in scope: the .rxt-as-specification format
  (a separate discussion Frank has deferred; separable from batching).
  - [TT-4.1] STATE:completed (2026-08-23, merge 19ea394; docs/dev/
    tt4_measurement.md + studies/tt4_batching/) — MEASURED: make test =
    3,777 CPU-s, gcc 647 (17%, 4,845 calls — the row's "~20,000" premise
    was wrong by 4x), pcrec 275 (7%); TU-batching beats link-batching at
    every N, best 3.66x (corpus pool, N=16) / 2.73x (atomic, N=8) against
    the 12-way baseline — the sweet spot is batch COUNT near core width,
    N=64/256 REGRESS under parallel execution (0.41-0.96x); ceiling ~12%
    of suite CPU. Two confirmed obstacles: mixed --features sets collide
    on unprefixed PCREC_FEATURE_SET/PCREC_FEATURE_MODULES #defines; a
    batch fails all-or-nothing (recovery of 15 good members 2.99 s).
    THE BIGGER FINDING: this box's `timeout` is uutils 0.8.0 and costs
    ~108 ms WALL per call at zero CPU (GNU /usr/bin/gnutimeout: 4 ms);
    corpus makes 23,252 such calls ≈ 2,430 s of sleep across 12 workers
    ≈ ~200 s (~43%) of its 469 s section wall, paid again on each
    sanitizer axis → chartered as [TT-6]. Exec-batching (one driver run
    per pattern instead of one per case) is a further 5.57x on corpus's
    per-case loop AFTER the timeout swap, corpus-only (the other sections
    already loop inside their own C drivers). — formerly STATE:started — MEASUREMENT (Frank's order: first, before any
    harness change). Census over one `make test` by section (the `test-*`
    targets): invocation counts (pcrec, gcc one-shot, gcc -c, link) and wall
    time split pcrec / gcc / link / run / harness-overhead; name the two
    worst sections; prototype batching on them in a SCRATCH driver (studies/
    tt4_batching/, never the harness) at several batch sizes and REPORT THE
    MEASURED SPEED-UP, or the measured slowdown (TT-3's lesson). Output: a
    measurement memo with method, blind spots and numbers from runs.
  - [TT-4.2] STATE:closed-not-started (Frank, 2026-08-23: not worth it on the numbers) — DESIGN, gated on [TT-4.1]'s numbers
    (gcc/link-step batching only; per-pattern artifacts preserved; failure
    isolation; D45 per-pattern semantics; GENCFLAGS rides through). Panel
    (D6) if the numbers open the row; an honest NO closes it like TT-3.
  - [TT-4.3] STATE:closed-not-started (with [TT-4.2]; exec-batching, the memo's lever 7, likewise) — LANDING: the harness batches; `make test`
    wall and core-minutes re-measured on the same box; sanitizer batteries
    and mech ride the same path; docs/testing.md records before/after.
    FRANK (2026-08-23, 08:3x): if the row proves worthwhile he wants to SEE
    the before/after timings for testing — present them to him at the
    close, whole chain (make test, battery stages, mech), not make test only.
- [TT-13] STATE:not-started (SCHEDULED FOR LATER by Frank 2026-09-04 13:5x: "schedule mech tiering for later but we need to free up the machine for the nightly bench"; admin column) — THE BATTERY'S GROWTH LAW: TIER BY CADENCE, NOT BY CONSTANT. Facts at filing ([TT-12] STEP 2's first run): mech 118 rows/28 min (08-23) → 222 rows/115 min (09-04) — rows AND per-row cost doubled (each row rebuilds and re-runs a harness section over a corpus that also grew: rows × corpus), so every constant-factor saving ([TT-12] STEP 1's ~75 min) is consumed by the population; the 4-hour wall has held for two weeks through three speedups. STEP 1: the per-merge battery runs the mech rows the merge TOUCHES — `tests/mech/rows_for.sh` ([TT-8], validated in the failing direction) maps changed paths → rows; plus every row whose definition changed; the FULL matrix runs on a schedule (the bench's idle slot, or weekly) or when a check/sabotage site changes; the merge battery's mech line then reads "N of 222 rows (touched)" and the scheduled run keeps the "0 unexpected" claim over the whole population (K35: the untouched rows are a population somebody must still count — the schedule is that count). STEP 2: overlap san and mech (10 workers on 12 cores) after ONE measured concurrent-vs-sequential run rules out shared-resource contention (the [TT-12] STEP 3 shape). STEP 3: the ALLOWLIST rule for the per-merge set — a new row/script/axis joins the scheduled full battery by default and the per-merge set only with a stated reason (the [OPT-DIAL] allowlist shape). Sonnet-sized; scripts/battery.sh + tests/mech + docs/testing.md; not before the machine split (below) settles where the full battery runs.
- [TT-15] STATE:not-started (CHARTERED 2026-09-04 evening, fifty-third session, by Frank, superseding the standing-runner variants discussed under [TT-14]'s re-sequencing: "any solution that i paid for that still took 4 hours makes no sense … 1 hour of a fast aws ec2 machine could be about a $1. if we started it up, then executed the full suite in an hour, it would cost the same $20/month but take less wall time"; admin column) — THE EPHEMERAL CLOUD BATTERY: UNCOUPLE FULL VALIDATION FROM THE DEV BOX. The model: an ON-DEMAND EC2 instance started per validation run, executing the full battery at a big-box knob profile, delivering the trailer + stage logs durably, then STOPPING ITSELF (the self-stop is what makes the billing model honest — cost scales with suites run, ~10-20/month per Frank, not with time owned). WHY THE 4h→~1h CLAIM IS CREDIBLE, from [TT-12]'s own profile rather than hope: the 4h03 was TUNED TO 12 THREADS — mech is 222 independent tree-rebuilds at PROCS=6 (115 min, near-linear scaling), axes run 2-at-a-time (50 min), san's pool caps at -P4 (55 min), test at -j4/PROCS=3 (20 min, an anti-load-stacking shape for 12 cores, not a law); every knob is an EXISTING env var, no harness surgery. COST at 2026 rates: 16 vCPU ~$0.70/hr, 32 vCPU ~$1.40-1.65/hr on-demand → $1.50-2.50/suite, $20-50/month at the stated cadence, $0 in a quiet month; ON-DEMAND not spot (an interruption 90 min into a battery costs more than it saves). NO RECALIBRATION PROJECT: D45 budgets and pins are CEILINGS calibrated on the Ryzen — a faster box passes them; quiet-box timing FLOORS are box-specific and stay with the bench. The resulting split: Mac = dev + targeted sections; EC2 = correctness batteries; bench box = timing. DELIVERABLES (repo-side, buildable before any provisioning): (1) bootstrap script (gcc, libpcre2-8-0 runtime, python3, GNU make, util-linux) or a baked AMI after the first run; (2) a one-command entry — fetch at a SHA via read-only deploy key, run scripts/battery.sh with the big-box knob profile, push trailer + logs to a durable home (results branch or S3), self-stop; (3) the manager's merge discipline gains the step merge → push → kick instance → poll trailer (the existing async-artifact shape); (4) the FIRST RUN IS THE MEASUREMENT — pin ONE instance type in docs from it so runs stay comparable, resize only on its number. FRANK PROVIDES: the AWS account path (credentials, or he start/stops and the scripts stay credential-free on our side), region, first size (manager suggests 32 vCPU c7a.8xlarge-class). The zero-dollar alternative Frank named — another owned Linux box like the bench's — uses the IDENTICAL scripts minus start/stop at the cost of the 4h wall and another machine to own; the deliverables above are deliberately common to both so the choice stays reversible. SEQUENCED BEHIND the [MACPORT] merge (the kit is built against the ported tree). [TT-14] remains the local inner-loop QoL row, untouched by this. **PARKED same evening (Frank, travelling: "i have to park the ec2 idea for a bit … i don't want to start an infrastructure project that will probably take days to iron out the kinks"): the INTERIM is the OLD BOX — full suites run on the bench's machine over ssh in /home/duxevents/pcrec (already set up, batteries detach there), COORDINATED with pcrecdev2 (wait for their current run; the window handshake returns with the direction inverted — pcrec now requests the slot). Frank's scope rule for that box: ssh allowed, stay inside /home/duxevents/{pcrec,pcrec-bench}, exactly like the local mandate. This row re-opens when Frank's travel ends.**
- [SPEC-1] STATE:started (2026-08-25 ~12:5x, fortieth session; STEP 1 DONE — lane srSpec's read-only survey, committed as docs/dev/spec_survey.md: 54 gap rows in 7 sections, a 9-file proposed set, 10 ordered lanes, 5 stale findings — two confirmed by the manager: `cli/main.c:58`'s `--step-budget` help still says "bring-up placeholder" though D51 set 500,000,000 (`src/gen/emit_vm.c:133`), and `docs/pcre2_compliance.md`'s K34 annotation still reads OPEN vs D74. MANAGER RULING on [SPEC-1.9]: `docs/pcre2_compliance.md` is DECLARED SPEC-TIER IN PLACE — its three-component refresh discipline meets docs/spec/CLAUDE.md's bar and its tooling is path-keyed; docs/spec/CLAUDE.md lists it by reference (flagged to Frank; reverse if he wants the move). Sub-rows below; [SPEC-1.1] launched first as lane srLimits) — THE SPEC CONSOLIDATION PASS (Frank,
  2026-08-25, fortieth session, D80: "I think that time is now").
  `docs/spec/` is the dense CONTRACT — what we produce, its quirks and
  shortcomings — for the reader who needs to know EXACTLY what to expect
  (AI and contributor grade). Today it holds match_api.md (2,096 lines,
  the as-built match-API contract incl. §10 the frame buffer) and
  table_contract.md. STEP 1 (a read-only sonnet survey lane, no make):
  inventory every SURFACE pcrec ships against what the spec covers —
  the CLI (every flag, `--features`, `-f` axes, `--emit-*`, exit codes,
  diagnostics' D26 tier), the library entry, the emitted artifact's
  CONTRACT beyond the entries (the D46 stamp family, `rx_info`, the
  identity/abi rules of D76, what a caller may and may not depend on
  across abi bumps), the give-up/limit contract (D22/D49; the step,
  frames, trail, work limits with their numbers — D73's obligation
  lands HERE with the 684-byte example and K33), the module roster with
  each module's quirks and divergences (K34/D74's documented
  divergence; every known_issues K-row that is a shipped behaviour;
  the compliance page's caveats by construct), encodings/options
  (docs/pcre2_options.md is a disposition table — is it spec?), and
  the .rxt format (docs/testing.md owns it today — contributor-facing,
  so spec-tier). Deliverable: a gap table (surface → where it is
  stated today → spec home → size) and the proposed file set, for the
  manager to turn into [SPEC-1.n] rows; NOT the writing. STEP 2..n:
  one lane per document, each claim verified against the shipped
  surface (match_api.md's discipline: cite the header, the artifact,
  the test — never copy from a design doc), D6 critic on each. STANDING
  RULE from birth: any change to a contract updates docs/spec in the
  same change (CLAUDE.md situation index; reviewers check it on every
  merge). Runs concurrently with [CHK-1] — disjoint files.
  - sub-rows 1.1-1.8, 1.10 archived 2026-09-22 -> plan_completed.md (REG-SV and LIM-1, standalone rows in this span, archived too)
- [SPEC-1.9] STATE:completed-in-place (CONFIRMED by Frank 2026-08-28 ("6 ok"); manager ruling above: pcre2_compliance.md declared spec-tier in place; docs/spec/CLAUDE.md lists it by reference — lands with 1.1).
- [SPEC-1.11] STATE:not-started (BOONIES TIER — LATE GAME; Frank, 2026-08-29 forty-fifth session, filed UNPLANNED "(ironic)": a doc plan item for a spec on UPGRADING pcrec) — THE EXTENDER'S SPEC, `docs/spec/extending.md` (name provisional): the recipe-per-extension-kind contract for anyone who grows the compiler AFTER 1.0 — how to add a [DD-11] replacement/definition row (predicate + core-syntax definition; the per-row checks and sabotage rows that must land with it; `--list-definitions` shows it), a registry `RegRow` (D24; the four/five registry surfaces it must appear on and the checks that prove it — PC-3, `--list-syntax`, the reject table), a feature MODULE (parser hook + lowering + tests/<module>/ + "requires module" refusal + compliance-refresh), an emitter FORM / representation object ([ENG-FORM]/D82: one object + one accessor block, its stamp, its deny/force axis row, the form-census floor, the `make test-axes` identity sweep), a STAMP (D81 naming, one derivation two readers, the ABSENCE-as-discriminator hazard), a LIMIT (limits.md's cited-and-re-measured rule, raise-only overrides per D84), an .rxt block kind ([DD-13b]'s grammar), and the ABI ritual (D76's four sites) — each recipe naming the checks that must move in the same change and the spec file that must gain its hunk (D80). It is a SPEC (contract) tier document per D80, not a guide: it says what an upgrade MUST do to be accepted, and points at the design notes for why. WHY LATE: every recipe is a restatement of a mechanism that is still moving ([DD-11]'s table, [DD-13b]'s grammar, the abi resetting to 1 at 1.0); written earlier it drifts with each lane (learnings §3's "one derivation" applies to prose too — the recipes should be written against the STABLE surfaces and, where a surface is listable, say "run `--list-X`" instead of restating the table). Trigger: after [DD-11] and [DD-13b]'s parsers land and the registry surfaces stop growing, or at the 1.0 release-doc pass, whichever is first. Starts only on Frank's word.
- [GUIDE-1] STATE:not-started — THE USER GUIDE, `docs/guide/` (Frank,
  2026-08-25, D80; LOWER PRIORITY, "basically maintained", NO edge-case
  details). A simplified, human-facing guide organized by USE CASE —
  compile a pattern into your program (search / match / captures /
  named results); the anchored and whole-subject idioms (`(?:P)\z`);
  choosing engine and options; when the matcher gives up and what to do
  (the `_in` entries for deep recursion — D73's obligation at guide
  depth: one paragraph pointing at the spec); embedding the generated
  file (no runtime dependency, the abi stamp); using the CLI vs the
  library. References the spec for details, never restates it. Starts
  after [SPEC-1] has the limits section it would point at; [REL-META]'s
  README pass then references the guide.
- [REL-1] archived to plan_completed.md (completed 2026-09-22 — the 0.1.0-beta release tagged on Frank's word; sub-rows REL-1.1-REL-1.11 archived with it, all delivered per D113/D116)
- [OPTLOOP] STATE:started (ITERATION POLICY ruled by Frank 2026-09-22, D119 addendum: REPEAT on capability until under threshold; dive into another subbench when a row's investigation is better served there; open a NEW subbench when something deserves dissection — adaptive, searching; threshold operationalization RATIFIED by Frank 2026-09-22 evening, "Proceed on all" — D119 second addendum) (OPENED 2026-09-22 ~morning EDT, seventy-sixth session, Frank: "start opt loop" — cycle 1 in flight: lanes optrev (analysis, opus) + backtri (triage, sonnet) on the capability pinconfirm-25b1984f + fullroster reports) (CHARTERED by Frank 2026-09-21, D119 — THE OPTIMIZATION LOOP: analysis (targets by priority, grouped by CAUSE; D119's taxonomy + priority rule) → per-mechanism plans → an implementation batch of ≤3 mechanisms, each an axis → surgical correctness (identity gates, test-axes, sabotage) → bench individually in the carve-out against the measured-gap bar → make test on the set → cycle → full suite. ALGORITHMIC ONLY, no SIMD until the end; constrained to the engine architecture (wholly-different = deferral). Starts after [REL-1.8]; cycle 1's analysis is [BENCH-REVIEW] + [BACKLOG-TRIAGE] below on the bench's `capability` subbench) — children expand per cycle as [OPTLOOP.N.analysis] / [OPTLOOP.N.impl].
  - [OPTLOOP.1.analysis] STATE:completed (RATIFIED by Frank 2026-09-22 with the batch-1 swap FIRSTSET→ENDWIN on the I-85 profile; companions: cycle1_caps_view.md (apples-to-apples: auto-caps wins/ties 84/123 vs capture engines; the five mechanisms robust <2%), captures_via_dfa_survey.md (pcrec already ships the RE2/rust two-pass shape as the hybrid prefilter; one-pass DFA ranked first as a future selection rung; TDFA deferred), cycle1_profile.md) (DELIVERED + MERGED 2026-09-22 ~midday, lane optrev, 405668e9: docs/dev/optloop/cycle1_analysis.md — pcrec best-variant fastest/tied on 91/125 ranked cells; 34 losing cells → FIVE mechanisms [OPT-REQBYTE] 3.714 / [OPT-ANCHOR-VM] 2.112 / [OPT-FIRSTSET] 1.002 / [OPT-ENDWIN] 0.871 / [OPT-ATTEMPT-SPLIT] 0.306 (class-weighted log2 score of 10.284 total) + M6 measurement-only; nine cells SIMD-phase deferrals (rust ahead only); engine-selection bucket EMPTY; parked rows ENG-ISL/CLS-TREE/DD-13 score 0 on this subbench ("not in this matrix", not refuted); recommended batch 1 = REQBYTE+ANCHOR-VM+FIRSTSET. NEXT: Frank ratifies §5's six rows; executor profile pass I-85 (§3 blocks M1-M6) is the D77 precondition on impl) (lane optrev — the [BENCH-REVIEW] deliverable on capability@0.1 at pin 25b1984f: every ranked row scored by the D119 priority rule over the FULL roster (fullroster-25b1984f.tsv), bucketed by cause via the D81 stamps read off the beta compiler's emitted .c (no darwin timing), n≈5 target MECHANISMS each with a why-slower diagnosis, a plan or a fundamental/deferral disposition, and the Linux profile measurements owed (executor request to follow); the parked rows [ENG-ISL]/[CLS-TREE]/[DD-13] compete in the same matrix) — Frank ratifies the mechanism list with [BACKLOG-TRIAGE]'s.
  - [OPTLOOP.2.analysis] STATE:started (OPENED by Frank 2026-09-22 evening, seventy-seventh session, "Proceed on all") — cycle 2 = REPEAT on capability at pin 8d716693 (D119 addendum): the ranking waits on the bench's I-87 ledger (O-45, in flight); meanwhile the two design notes c2prep ranked first run as lane c2design (docs/design/reqbyte_freq_pick.md — the rarest-byte pick, the first customer of D83's static named analyses; docs/design/reqpos_2b.md — [OPT-REQPOS] tier 2b), and the owed Linux measurements go to the executor as I-89 (lane linuxask drafts: the all-axes `make test-axes` sweep, [OPT-FIRSTSET] F1+F2, one-pass M-B). DELIVERED 2026-09-22 ~22:1x: I-89 SENT (bench inbox 7e9680e); c2design MERGED — both notes BUILD (the pick over the SHIPPED prior `pcrec_byte_freq_ppm`, byte-encoding only in event 1 after Frank's encoding consideration exposed the prior's flat 2-ppm 0x80-0xFF half; 2b as the necessary literal RUN, own axis + REQ_RUN stamp; one abi event 29→30 proposed; 15 open questions in the notes' closing sections, PENDING FRANK); linuxask's F2 finding (real two-pass binaries read 0/0/0 where the forward-only simulator predicted 0/1/0) → lane fsreconcile (firstset_design.md §4.5), in flight. The cycle-1 EXECUTIVE SUMMARY follows O-45 (memory rule). Feature column stays HELD. O-46 (2026-09-23): one-pass M-B raw timing for all 17 capture-forced hybrid patterns × 4 subjects × 2 arms is in the bench's docs/dev/lanes/b76optloop_report.md §4 (zero answer mismatches); the (arm1−arm2)/arm1 capture-cost shares REDUCED by lane mbread (merged 2026-09-23 ~08:2x, onepass_census.md "M-B — measured"): VERDICT SPLIT — throughput regime median 0.00% (the capture pass is NOT the cost there); match regime median 32.75% IQR [22-45%] (3 of 16 clean HAS-A-TARGET: datetime-moment-iso8601 62%, validator-ipv4-owasp 66%, semdiv-empty-alt-repeat 50%; 5 clean NOT-THE-COST; 8 in the gap) — CONFOUNDED: every match-regime own subject is a 5-93 byte hand literal, so per-call overhead plausibly dominates; the next measurement is the match arm at larger subject sizes (a bench data ask) before candidate (c) is ranked; date-nested-plus's own-subject MISSING (the derived search_short/match regime lookup returned no row — a data question put back to the bench). Block (A), the all-axes sweep, is running detached on Linux (6h cap; O-47 owed).
  - [OPTLOOP.1.impl] STATE:started (LEDGER READ 2026-09-23 ~08:5x, lane b1ledger MERGED ead8bf62, docs/dev/optloop/cycle1_ledger_reading.md + the cycle-1 EXECUTIVE SUMMARY docs/dev/summaries/2026-09-23-optloop-cycle1-exec-summary.md: [OPT-ANCHOR-VM] MEETS 13/13; [OPT-ENDWIN] MEETS 4/4; [OPT-REQBYTE] targets meet (98.2-99.9%), carve-out clause FAILS (12 cells 18.7-38.5%) — attributed: 29 cells = the pre-check emitted ABOVE a one-attempt route's free exit (ADMISSION; the declining rule already exists at emit_dfa.c:2999 for the prefilter, uninherited), 4 = a duplicated/dominated memchr (DOMINANCE), ~4 = a lost gcc partial-inlining split on 24/62 forced-VM artifacts (PLACEMENT; arm64 read, x86_64 replication owed), 50 inside a NULL BAND the ledger contained for free (56/187 artifacts program-identical across the pin, worst +8.46% at 35× IQR with __text byte-identical — both named misses sit inside it). RECOMMENDED DISPOSITIONS, PENDING FRANK: ANCHOR-VM and ENDWIN default-on as shipped; REQBYTE default-on WITH the admission fix (a --tune position would forfeit the loop's largest win); proposed cycle-2 row [OPT-PRECHECK-ADMIT] (G1 dominance + G2 admission first, G3 placement with batch 3). I-93 sent: five executor blocks (axis isolation, the null band, placement disassembly, the placement hand-twin = the fix's acceptance test, perf if available)) (LEDGER O-45 RECEIVED 2026-09-23 ~04:0x, bench 56f4a7e, docs/dev/ledgers/2026-09-23-optloop1-batch1-after-8d716693.md: 39/41 named-target cells MEET the D119 bar; 2 misses = router-prefix-order DFA large-subject throughput +1.2% (~10× its IQR); carve-outs OUTSIDE the bar: nested-comment-rec throughput +18.8-25.0% all four configs, uuid/ipv4-near-miss +18.7-38.5% DFA route; a ~23.1 µs large-subject floor entered from both directions (winpath-near-miss, email-nested-plus rose from ~20-48 ns into it — the whole-window memchr now runs before the engine's O(1) exit). Reading + per-mechanism verdicts + regression diagnosis: lane b1ledger (opus), cycle1_ledger_reading.md + the cycle-1 executive summary; Frank rules default-on vs --tune per mechanism) (lane optimpl1, 2026-09-22 — BATCH 1 IS [OPT-ANCHOR-VM] + [OPT-ENDWIN] + [OPT-REQBYTE], the batch swap Frank confirmed 2026-09-22 on the I-85 profile: FIRSTSET out, ENDWIN in) (batch 1 originally ratified 2026-09-22: REQBYTE + ANCHOR-VM + FIRSTSET; opens on the I-85 profile's confirmation (bench [B73], in flight)) — ≤3 mechanisms from the ratified list, each an axis (-fno- flag, test-axes, sabotage row with reach witness, independent revert); landing bar = measured gap in the bench carve-out (median gain > IQR on the target cells, no cell regresses > IQR; size → a --tune position); make test on the set; full suite at cycle end. **BATCH 1 DELIVERED 2026-09-22 on `lane/optimpl1`, four commit groups with independent reverts (be7e8ef3 / fa1abf3b / 3aa13b6b) plus ONE `abi` 28 -> 29 bump carrying the whole D94 ritual (b7f46d6b): readers by grep, the SECOND READER CLASS by re-measurement (the m5_stage1_stamps.tsv manifest's twelve EMITTED_BYTES rows, run_resource_tests.sh's 762125 -> 762312 rescue pin, both reconciled by DIFFING artifacts written to the same `-o` basename), and a THIRD class that COUNTS rather than cites (the registry's axes-coverage pin 108 -> 120). Report: docs/dev/lanes/optimpl1_report.md. make strict clean; run_prechecks 113/0; test-codegen 9/10 (standing darwin `nm arm_a.o`); cpset-structure 28/0; limits_check 27/0; axes_registry 120/0; rxtsource 212/0; resource 27/0; assertions 54/0; sabotage anchors 289/289. `make test`, `make test-axes`, S265 and the identity-gate (B) re-pin are OWED — the main tree's own `make test` held the box for this lane's whole working period.**
  - [OPT-REQBYTE] STATE:started (RATIFIED by Frank 2026-09-22; batch 1) (SIZE S-M) (PROPOSED 2026-09-22 by [OPTLOOP.1.analysis], docs/dev/optloop/cycle1_analysis.md M1 — the largest single weighted gap in capability@0.1, 3.714 of the losing rows' 10.284) — THE REQUIRED-BYTE WHOLE-WINDOW PRE-CHECK. Compute at compile time, from the AST, a byte every match must contain past its start (PCRE2's PCRE2_INFO_LASTCODETYPE/LASTCODEUNIT, `man pcre2api`); emit it and test it ONCE per <prefix>_search call, before the attempt loop, with the memchr the prefilter path already emits. Extends the prefilter primitive (RX_DFA_PREFILTER "memchr" is the same instrument keyed on a candidate START); serves BOTH engines, which is the point, since the VM's hybrid prefilter is declined outright for backrefs and linked calls (src/opt/select_engine.c:604,651) and three of the five target cells are exactly those declines. Axis -fno-req-byte / PCREC_NO_REQ_BYTE, stamp <PREFIX>_REQ_BYTE, one src/core/axes.def row; sabotage inverts the memchr sense (answer-detectable). PROFILE FIRST (D77): cycle1_analysis.md M1.a/M1.b/M1.c, exact commands for the Linux executor. LANDING BAR — improve: (tag-depth3-bound, thr), (dup-param-detect, thr), (tag-pair-match, thr), (wild-secrets-username-password-pair, thr), (wild-logparse-winpath-grok, thr). Do not regress: (floor-byte, thr), (floor-byte, srch), (nested-comment-rec, thr), (wild-secrets-github-pat, thr), (wild-validator-uuid-grok, thr), (float-literal-bound, thr) — every one a cell whose required byte IS PRESENT in the bench text ('/', '_', '-', '.'), so the memchr succeeds and is pure added cost; plus (router-prefix-order, thr), which records no required unit at all and whose emitted text must therefore be byte-identical. **DELIVERED 2026-09-22 on `lane/optimpl1` (3aa13b6b): `src/opt/reqbyte.c`'s bottom-up SET walk (concat unions, alternation INTERSECTS, min-0 quantifier contributes nothing, a one-byte class is a singleton, backref/call/assertion empty), emitting the RIGHTMOST member like PCRE2's LASTCODEUNIT so a later multi-byte form is a WIDENING; ONE emitter for both engines' search entries. TWO DELIBERATE DEPARTURES FROM PCRE2's FACT: the WHOLE window counts (the consumer runs once per call over `[search_from, subject_length)`, where every byte of every match lies, so `other than at its start` buys nothing), and a lookaround's body is NOT descended into (a lookbehind's bytes sit BEFORE the match start and can be outside that window — a CORRECTNESS decline). Axis bit 30 `-fno-req-byte`, stamp `<PREFIX>_REQ_BYTE`, tuning.md 2.27 + match_api.md 6.3. Gate `run_prechecks.sh` 3, which asserts the `memchr`'s SENSE separately from its ARGUMENT because S265 inverts the sense and leaves the byte alone. THE DERIVATION IS CONFIRMED BY PCRE2's OWN TABLE: the five target patterns stamp `'>'`/`'='`/`'>'`/`'='`/`'\\'`, byte for byte cycle1_analysis.md 2.3(c)'s measured LASTCODEUNIT column, derived by a walk that never saw it. **CARVE-OUT CELL FALSIFIED:** this row names `router-prefix-order` as byte-identical because PCRE2 records no required unit for it — pcrec stamps `'r'` (114), because the alternation INTERSECTION is stronger than PCRE2's derivation there; sound, and that cell is no longer a byte-identity control. S265 OWED (run solo, log named in the lane report). NO TIMING TAKEN.**
  - [OPT-ANCHOR-VM] STATE:started (RATIFIED by Frank 2026-09-22; batch 1) (SIZE S) (PROPOSED 2026-09-22 by [OPTLOOP.1.analysis], cycle1_analysis.md M2 — the single largest RATIO in the matrix, 52,122.95x, and the three worst DEFAULT-CONFIG cells in the set) — THE START-POSITION BOUND IN THE VM'S ATTEMPT LOOP. The DFA emitter already derives a three-valued start_max (0 for ^, search_from for \G, subject_length otherwise; src/gen/emit_dfa.c:6802-6812); the VM emitter writes `for (;;)` with no bound. Derive the same fact one layer up (AST/NFA, since a VM-routed pattern has no DFA to ask) and have both emitters read ONE predicate — implement-then-replace, not a parallel mechanism. Population: 14 of capability@0.1's 64 patterns are PCRE2_ANCHORED and VM-routed at auto-caps; 3 carry RX_VM_PREFILTER "none" and have no rescue at all. Axis -fno-vm-anchor-bound / PCREC_NO_VM_ANCHOR_BOUND, stamp <PREFIX>_VM_START (anchored/gstart/unanchored). NOTE FOR THE SABOTAGE ROW: a bound that only removes provably-failing attempts has NO answer-level detector, so the row is a STAMP row with bracket-array-define as its reach witness. PROFILE FIRST: cycle1_analysis.md M2.a/M2.b/M2.c. LANDING BAR — improve: (bracket-array-define, thr), (bracket-array-define, srch), and at the auto-caps ARM specifically (evil-alt-nested, thr) and (trim-nested-star, thr), which the best-variant ranking hides. Do not regress: (floor-byte, thr), (floor-byte, srch), (nested-comment-rec, thr), (codegrammar-flat, thr) — unanchored VM artifacts whose emitted text must be byte-identical. **DELIVERED 2026-09-22 on `lane/optimpl1` (be7e8ef3): `src/opt/startanch.c`'s three-valued `pcrec_start_anchor` over the LOWERED AST, read by both emitters through `Job.start_anchor`; the VM emits `const size_t attempt_max = search_from;` and its existing continue test reads it, an `unanchored` artifact is byte-identical; `emit_attempt` asserts the SOUND direction of the implication only (AST-anchored => machine-anchored; the converse is expected, since the subset construction prunes more) and was verified silent over 3,252 corpus patterns x 3 engine settings before commit. Axis bit 28 `-fno-vm-anchor-bound`, stamp `<PREFIX>_VM_START`, three `--list-axes` rows, tuning.md 2.25 + match_api.md 6.3. Gate `tests/codegen/run_prechecks.sh` 1; sabotage S263 DETECTED SOLO at `prechecks:4fail/19pass, corpus:0fail/28960pass` — the corpus zero is the point, not a half-detection. Landing-bar stamps confirmed: bracket-array-define / evil-alt-nested / trim-nested-star all `anchored`; floor cells nested-comment-rec / codegrammar-flat `unanchored`. NO TIMING TAKEN — the bench measures the cells after merge.**
  - [OPT-FIRSTSET] STATE:not-started (O-46 2026-09-23 ~07:3x, bench 5eb3be5, [B76] at 8d716693: F1 — the published twin ×1.10 SLOWDOWN DOES NOT REPRODUCE: trials 2-5 twin 1.52-1.66 ns/byte vs base 3.087-3.092 (trial 1 a cold-start outlier on both arms) → the cost model HOLDS (~×1.9 on json-constant) and the decline rule is RETIRED; F2 — ctx.bin 0/0/0 and ctx2.bin 1/0/1 on Linux exactly as darwin → the narrowing WITHOUT the re-seed deletes matches, WITH it is correct. CANDIDATE FOR BATCH 3: the narrowing + the re-seed (pf_emit_ofs_reseed's second call site; an abi event) + the VM consumer; F3 (what the repair costs) is the owed measurement before ratification) (RECONCILED 2026-09-22 evening, lane fsreconcile, firstset_design.md §4.6: the UNSOUND verdict STANDS on the REAL two-pass binaries and the symptom is a DELETED match, not a spurious one — `"atrue true"` shipped (6,10) / twin matches=0 / twin+re-seed (6,10); exhaustive 4.03M subjects: 552 lost, 0 spurious; the reverse walk vetoes the spurious candidate and rx_search then returns 0 for the whole call. The repair ALREADY SHIPS as pf_emit_ofs_reseed (emit_dfa.c:4951, [OPT-K]) — a second call site, not a mechanism; sound for the prefilter-less VM attempt loop (412 artifacts), unsound for hybrid (measured); reach 54 artifacts. I-89's F2 block was NON-DISCRIMINATING — amended (I-89a): `printf 'atrue true'`, EXPECT 1,0,1; F3 no longer conditional on F2) (DESIGN NOTE 2026-09-22, lane c2prep, docs/dev/optloop/firstset_design.md: AS RATIFIED IT IS UNSOUND — narrowing rx_can_begin_match to the AST first-byte set gives `\b(?:true|false|null)\b` a SPURIOUS match at offset 1 on "atrue xnull " (the DFA start state encodes the preceding byte's word class; the subset guard passes the unsound narrowing); repair = one line re-seeding via rx_forward_seed_state (an abi event) or ship only the VM-route consumer (ii); the cost model DECLINES NOTHING (a narrower set never costs at the byte grain; json-constant's ×1.10 is unexplained by its own counts → re-run M3.c on Linux FIRST); the PAIR filter is json-constant's answer; the `freq` findings VALUE and consumer interface specified (the schema block already ships, rxt_schema.def:146 — only config name resolution is open); owed Linux blocks F1-F4 in §7) (RATIFIED by Frank 2026-09-22; batch 2 or later — SWAPPED OUT of batch 1 on the I-85 profile, json-constant twin x1.10 slower: needs a skip-loop cost model + its VM consumer profiled; cycle1_profile.md) (SIZE M) (PROPOSED 2026-09-22 by [OPTLOOP.1.analysis], cycle1_analysis.md M3; NAMES THE CAUSE of [OPT-3]'s own measured symptom, docs/dev/opt3_dfa_scan_measurement.md "the skip loop is entered 190,651 times and skips ZERO bytes") — THE CANDIDATE-START SET, DERIVED FROM THE AST RATHER THAN FROM THE DFA'S START STATE. A leading \b makes the start state track word context, so rx_can_begin_match[256] becomes the 63-byte word class: MEASURED minimal witness, `A[A-Z0-9]{16}` stamps memchr with a 1-byte set and `\bA[A-Z0-9]{16}` stamps byte-class-bounded with a 63-byte one, while PCRE2 records FIRSTCODEUNIT='A' for both. Compute the first-byte set from the bytes that can BEGIN A MATCH, looking through leading zero-width assertions; intersect with today's derivation, never widen (a free compile-time assertion and the natural identity check). TWO CONSUMERS, ONE ANALYSIS: the DFA's existing skip loop (contents change, representation does not) and the VM's eleven RX_VM_PREFILTER "none" artifacts, which gain a candidate-start skip they have none of today (nested-comment-rec: first byte '/', 2.86% of the bench text, 35x fewer attempts). Shares its AST walk with [OPT-REQBYTE]. Axis -fno-first-set / PCREC_NO_FIRST_SET + PCREC_FORCE_FIRST_SET; the sabotage widens the set by one impossible byte and is therefore a STAMP/COUNT row, pinned against a corpus census of |can_begin_match|. PROFILE FIRST: cycle1_analysis.md M3.a-M3.d. LANDING BAR — improve: (wild-secrets-aws-access-key-id, thr), (wild-codegrammar-json-constant, thr), (wild-waf-crs-942140-dbnames, thr), (nested-comment-rec, thr). Do not regress: (floor-byte, thr), (floor-byte, srch), (wild-logparse-quotedstring-grok, thr), (high-byte-run, thr), (uuid-near-miss, srch) — cells whose prefilter is already narrow, where the new derivation must produce the identical set.
  - [OPT-ENDWIN] STATE:started (RATIFIED by Frank 2026-09-22; batch 1 — swapped in on the I-85 profile's clean confirmation) (SIZE M) (PROPOSED 2026-09-22 by [OPTLOOP.1.analysis], cycle1_analysis.md M4 — D77's OWN NAMED CANDIDATE, "a FOLD ON THE IDIOM (the skip loop reasoning about \z), a general optimization for every \z user", now with the measured number D77 said to wait for: 1,401x against rust and 725x against the best scalar engine on `abc$`) — THE END-ANCHOR START-WINDOW BOUND. When every alternative ends in $/\Z/\z outside multiline AND pcrec_maxw is finite, the scan's start window is [n - maxw - eps, n], not [search_from, n]; eps is the $-before-final-newline allowance. Extends the EXISTING position-view axis (--list-axes `view` already recognises the \z/$ view and uses it to pick the -bounded prefilter candidates): this gives that recognised view its second and much larger consumer, the scan's start bound rather than only its accept test. Declines where maxw is unbounded, which costs nothing. Axis -fno-end-window / PCREC_NO_END_WINDOW, stamp <PREFIX>_END_WINDOW; the sabotage clamps one byte too FEW and IS answer-detectable (it drops a legal match). PROFILE FIRST: cycle1_analysis.md M4.a/M4.b/M4.c, including the four correctness carve-out subjects (match at 0 / trailing newline / match at the end of 100 KB / match at the start of 100 KB). LANDING BAR — improve: (wild-semdiv-dollar-trailing-newline-pcre2, thr). Do not regress: (floor-byte, thr), (floor-byte, srch), (wild-validator-email-owasp, thr), (uuid-near-miss, thr), (ipv4-near-miss, thr) — anchored-at-both-ends cells already at O(1), where a second bound must be free. **DELIVERED 2026-09-22 on `lane/optimpl1` (fa1abf3b): `src/opt/endwin.c`'s `pcrec_end_window` (maxw + eps, in BYTES) and ONE emitter, `pcrec_emit_end_window_clamp`, for both engines' search entries. FOUR structural declines, each with its own reason: unbounded width; a multi-byte encoding (the clamp computes a byte offset and a mid-character start is a WRONG ANSWER, K49/K50 — tested as `PcrecEnc.start_cls != NULL`, which discharges a SECOND obligation at the same time, since an encoding with no non-boundary positions is exactly one whose every character is one byte, and that is what lets `pcrec_cwmax`'s CHARACTER count stand as a BYTE count); `\G` anywhere (the one assertion that reads the `search_from` the clamp MOVES — which is also why this mechanism and [OPT-ANCHOR-VM] decline disjointly and cannot interact); a multiline `$` (D62 control 3). Axis bit 29 `-fno-end-window`, stamp `<PREFIX>_END_WINDOW` (a string with a `"none"` member, because 0 IS a legal window), tuning.md 2.26 + match_api.md 6.3. THE ONE MECHANISM OF THE THREE THAT CAN LOSE A MATCH IF WRONG, so it has an answer-level net: `tests/assertions/end_window.rxt`, 66 cases, EVERY claim carried at a subject length that leaves the clamp inert AND at one that makes it fire (harness 66/0; 0 disagreements against python3 `re` under the documented mapping). Sabotage S264 (the window one byte too FEW) DETECTED SOLO at `corpus:178fail/28848pass, prechecks:11fail/50pass`. Landing bar: wild-semdiv-dollar-trailing-newline-pcre2 stamps `4` — and NOTE `cycle1_analysis.md`'s own M4.b hand-twin used 5, over-counting `maxw(abc$)` as 4 where it is 3; the twin is sound, the landed window is one tighter. TWO FLOOR CELLS MOVE: uuid-near-miss and ipv4-near-miss GAIN a window (37, 16) on top of their `start_max = 0` — sound, two instructions on an already-O(1) path, but a move the bench should read as one. NO TIMING TAKEN.**
  - [OPT-ATTEMPT-SPLIT] STATE:not-started (RATIFIED by Frank 2026-09-22; batch 2 or later) (SIZE M-L) (PROPOSED 2026-09-22 by [OPTLOOP.1.analysis], cycle1_analysis.md M5; the emitter's own comment already names the shape, src/gen/emit_dfa.c:13-17 "the slow shape is ^ on only SOME branches") — `^` IN ONE BRANCH STOPS COSTING THE WHOLE DFA TOOLKIT. A pattern containing ^ routes to ENG_ATTEMPT, which has no prefilter, no premultiplied table and no [OPT-5] scan edge; when it is FULLY anchored start_max=0 makes that free, and when it is not it pays for everything (wild-waf-crs-942360-concat-sqli: RX_DFA_PREFILTER/TABLE/SCAN_EDGE all "none", start_max = subject_length, 8.83 ns/byte, the highest per-byte cost of any DFA artifact in the set, against re2-longest's 1.62). Split into ONE attempt of the original machine at search_from, then the ordinary ENG_UNANCH engine built from the pattern with the anchored branches removed; leftmost-first holds by construction. Both halves are engines the tree already emits; the "optional contributor" shape is [K53-SELRETRY]'s. SIZE IS THE ARGUMENT AGAINST IT: up to 2x the table bytes on exactly the patterns whose tables are already largest, so this may belong at a --tune position rather than at the default (D119 item 4's second axis). PRECONDITION: count the shipped corpus's own ENG_ATTEMPT-with-start_max=subject_length population before sizing the row (D81's 2026-08-25 census recorded 180 of 995 DFA artifacts on the attempt scan and did NOT split them by start_max). Axis -fno-attempt-split / PCREC_NO_ATTEMPT_SPLIT, stamp value RX_DFA_SCAN "attempt+unanchored"; both sabotage directions are answer-detectable. PROFILE FIRST: cycle1_analysis.md M5.a/M5.b. LANDING BAR — improve: (wild-waf-crs-942360-concat-sqli, thr), (wild-waf-crs-942360-concat-sqli, srch). Do not regress: (ipv4-near-miss, thr), (uuid-near-miss, thr), (base10num-near-miss, thr), (winpath-near-miss, thr), (wild-validator-email-owasp, thr) — the five FULLY anchored attempt-scan artifacts, whose emitted text must be byte-identical because the predicate is false for them.
  - [OPTLOOP.1.M6] STATE:not-started (RATIFIED 2026-09-22; measurement-only, runs in the I-85 profile pass) (SIZE S, MEASUREMENT ONLY) (PROPOSED 2026-09-22 by [OPTLOOP.1.analysis], cycle1_analysis.md M6) — WHAT THE VM COSTS PER ATTEMPT AND PER STEP. Eleven losing rows (weighted 1.004) are VM rows where pcrec and the winner are both linear and pcrec costs 1.05-5.2x per byte, and NOTHING in the emitted C names a mechanism (rx_reset_for_next_attempt is O(trail depth), i.e. proportional to work already done). Instrument a COPY of nested-comment-rec / quoted-delim-match / balanced-parens-rec with a per-call and a per-dispatch-step counter, run on t-1m, and divide: ns/attempt and ns/step SEPARATELY. That number decides whether the bucket is an attempt-COUNT problem ([OPT-ANCHOR-VM]/[OPT-FIRSTSET] territory) or a per-STEP problem (an emitted-code problem), and NO mechanism should be proposed before it (D77; memory pcrec-build-under-measurement). Nothing under src/. LANDING BAR: n/a, the deliverable is the number.
  - [OPT-PRECHECK-ADMIT] STATE:not-started (PROPOSED 2026-09-23 by the O-45 reading, cycle1_ledger_reading.md §6; PENDING FRANK's ratification) (SIZE S) — ADMIT AND PLACE THE PRE-CHECKS BY COST, three rules with measured populations: G1 DOMINANCE — never emit a whole-window pass the artifact already runs on a byte at least as rare (4 cells; json-array-begin memchr's byte 91 twice); G2 ADMISSION — never precede an exit cheaper than the pre-check: decline where the candidate-start set is a single position (`start_max = 0`, the ^-anchored one-attempt route; 29 cells; the rule exists at emit_dfa.c:2999 for the prefilter and is inherited, not re-invented); G3 PLACEMENT — emit the VM's check in the entry wrappers, not inside rx_search_run's hot function (the partial-inlining split; ~4 cells; x86_64 replication + I-93 block D's hand-twin are its acceptance test). G1+G2 first (a few lines), G3 with batch 3. Removes 33 of the 33 attributable regressing cells; the null band absorbs the rest.
  - [OPT-FREQPICK] STATE:started (RATIFIED by Frank 2026-09-22 ~23:2x, "Agree with 15 design decisions"; lane optimpl2 launched the same hour with [OPT-REQPOS] 2b as ONE abi event 29→30) (SIZE S) — THE FREQUENCY-INFORMED PICK for [OPT-REQBYTE]: the emitted necessary byte becomes the argmin of the SHIPPED prior `pcrec_byte_freq_ppm` (src/opt/prefix_k.c) over the necessary set, ties to the rightmost (today's rule survives as the tiebreak); BYTE ENCODING ONLY in event 1, every other encoding keeps today's rightmost answer byte-for-byte (the prior's 0x80-0xFF half is a flat 2-ppm floor, inverted under utf8); no new axis bit (a value under `-fno-req-byte`); no findings-file plumbing; tuning.md §2.27's "matching PCRE2's own choice" sentence replaced; a sabotage row + a utf8 zero-movers control. Design: docs/design/reqbyte_freq_pick.md (its 9 questions all ruled per recommendation). Bar: one losing cell (nested-comment-rec) + the whole-call wins on rows already won.
  - [OPT-REQPOS] STATE:started (ALL SIX §9 QUESTIONS RATIFIED by Frank 2026-09-22 ~23:2x per recommendation: BUILD 2b / DECLINE tier 2; no decline rule; truncate to 8; the `1ull` widening in this change; ONE abi event with [OPT-FREQPICK]; the caseless population waits for [WORD-FOLD]; lane optimpl2 launched) (RULED 2026-09-22 evening by Frank: a long run is truncated to the LOWEST-frequency 8-byte window containing the memchr byte, reqpos_2b.md §2.4 item 3; design note docs/design/reqpos_2b.md BUILD tier 2b / DECLINE tier 2, merged 701daed4, remaining §9 questions pending) (FILED 2026-09-22 by Frank, seventy-sixth session, on [OPT-REQBYTE]'s landing: "Could it use the position of the REQBYTE to set the search start if it was at a potentially known offset or start back then forward?"; optimization column, cycle 2 analysis candidate; CENSUS DONE 2026-09-22 by lane c2prep, docs/dev/optloop/reqpos_census.md: tier 2 covers 6-8% of patterns and TWO losing cells → does NOT clear D77; RE-SCOPED TO TIER 2b (the pair/word run): 68 bench patterns carry a run ≥2, on 60 the run never occurs in t-1m, median 4.24x candidate cut where it does; all five [OPT-REQBYTE] target rows are tier 3 with the byte ABSENT — nothing positional helps them; PLUS the cheapest finding: the RIGHTMOST pick rule leaves three whole-call answers on the table (a rarer necessary byte ABSENT while the picked byte is PRESENT: nested-comment-rec, email-owasp, crs-942500) — the pick needs a freq value, where a static named analysis (D83 addendum) first pays) — THE REQUIRED BYTE'S POSITION AS A START BOUND, three tiers of ONE mechanism family: (1) FIXED offset k — exists today as the DFA route's prefix-k prefilter (src/opt/prefix_k.c, memchr-at-k), DFA-only; (2) BOUNDED offset range — when the max width LEFT of the required byte (dmax) is finite, no match starts before (first occurrence − dmax), and the attempt loop becomes a SKIP LOOP: on a failed attempt at s with the next occurrence at q, jump to q − dmax; serves BOTH engines and helps exactly where [OPT-REQBYTE] does nothing — the byte PRESENT (the M1.c carve-out: memchr succeeds at offset 1,000,000, then 1,000,000 attempts still run); a WIDENING of prefix-k from an offset to a range, not a parallel mechanism; (2b) THE PAIR (Frank, same conversation: "search byte A then from there check known offset byte B before keeping") — when the walk carries POSITIONS, two necessary bytes at a fixed separation d (inside one literal run, or across a fixed-width gap) make the scan memchr(A) + ONE compare at p+d before any attempt or reverse walk — and B NEED NOT BE A BYTE (Frank, same conversation): A is a byte because memchr locates it; B is a known-offset RUN of 1/2/4/8 bytes compared as ONE unaligned word load against a constant (a 2-byte pair and an 8-byte run cost the same instruction pair), before or after A at any fixed delta the positioned walk knows — and the run MAY OVERLAP A (Frank, same conversation): load the word at p minus A's position within the run, so one compare re-checks A with its neighbours (`https://`: memchr `:`, one 8-byte compare at p−5 checks the whole run); where the run is small classes rather than exact bytes (caseless letters, a digit range) the same compare works as load+AND-mask+compare — [WORD-FOLD]'s cube compare, so the two rows SHARE that primitive; the scalar answer to CANDIDATE DENSITY (the json-constant refutation of [OPT-FIRSTSET]: `t`/`f`/`n` are common, `tr`/`fa`/`nu` are rare), what pcre2-jit's and rust's SIMD pair scans do with vectors — the SIMD form is the deferred constant factor on top, not a different mechanism; with an [ENG-PGO] findings value it picks the RAREST pair, without one the leftmost; (3) UNBOUNDED dmax — an occurrence says a match must SPAN it; rust-regex's reverse-inner shape (literal → reverse automaton to the candidate start → forward); pcrec already owns the reverse DFA in the hybrid prefilter (walked back from a match END; the same machine walked back from a required byte is this tier) — [ENG-ISL] island territory, heavier. D77 TRIGGER = a compile-side CENSUS before any build: how many patterns (bench sets + the corpus) carry a required byte at FINITE dmax (tier 2's population — the five M1 target rows put their byte after an unbounded repeat, so they are likely tier 3), how many carry TWO necessary bytes at a fixed separation and how rare the pair is in the bench texts against the single byte (tier 2b), and which bench cells have the byte PRESENT rather than absent (where tier 2/3 pay and the pre-check does not). Enters [OPTLOOP.2.analysis]'s ranking on that census.
- [EDGE-STAMP] STATE:not-started (CHARTERED by the manager 2026-09-20 ~05:4x from bench outbox O-36 — the SECOND customer of a fact today readable only from an emitted COMMENT) — an INTEGER SCAN-EDGE-COUNT STAMP (`#define <PREFIX>_SCAN_EDGES n` or an rx_info field, the design pass decides) so that the [OPT-5] "SCAN EDGE" comment marker stops being an instrument: customers are the bench's [B32] scan-edge covariate + one selfcheck control (they pass -fcomments as an inert protocol token today) and our own four structural checks that pass -fcomments for the same marker (run_scan_edge_census.sh, run_scan_edge_dispatch.sh, run_search_pinned.sh, run_specimen_identity.sh — evtriage2_report.md; "minting code-level markers would be new scaffolding for an unmeasured need" — the need is now measured twice). Emitted scaffolding ⇒ rides the NEXT abi event (D76/D94), never mints one; D80 spec hunk (match_api.md's stamp catalogue). Admin column, sonnet-sized once the bump is open.
- [V-A] STATE:not-started — PCRE2 compatibility layer: a drop-in surface for callers who already speak PCRE2, so adopting pcrec does not mean rewriting call sites. Interacts with DD-3 (generated-API versioning) — a compat layer is a second consumer of the generated contract. TWO surfaces (Frank, 2026-08-12): the PCRE2-native API, and a POSIX `regex.h` shim (regcomp/regexec/regfree, à la pcre2posix) — a smaller surface with wider adoption reach, since decades of C code speaks regex.h and never touched PCRE2
- [V-B] STATE:not-started — usage libraries for other languages: bindings over the generated C. Note the generated code already has no runtime dependency on pcrec, which is what makes this cheap; keep it that way
- [V-C] STATE:not-started — a grep CLI built on pcrec, the natural end-user demonstration that the speed mandate (D18) actually shows up in a real tool
- [V-D] STATE:not-started (CROSS-NOTE 2026-08-19: [DD-11]'s definitions architecture makes this row cheaper than its design note assumed — a flavour = front end + BINDING LIBRARY over the reduced core; but read DD-11 note (h)'s honest limit: POSIX leftmost-LONGEST is core preference semantics, not a binding) — translators from other regex syntaxes into the base tier: grep/egrep (BRE/ERE), python `re`, and PCRE2-flavour differences. Pairs with V-C (a grep CLI needs BRE/ERE) and with V-A. Design note: these are FRONT-END modules that lower into the existing AST, exactly the shape APPROACH §3's parser extension points already anticipate — no engine work, which is what makes the direction affordable
- [LIB] STATE:not-started — SUBPATTERN LIBRARIES (RULED 2026-08-28, Frank: "depends on rxt format" — the row BLOCKS on [DD-13b], the .rxt format half; the spine question is not asked again until that lands. Frank, 2026-08-24, thirty-ninth session; PLANNED — its place relative to the spine is NOT ruled (Frank ~22:0x: "planned but I don't know that I'd put them on the spine"); to be discussed at [DD-14]'s close): (1) the .rxt format grows so a file can carry MULTIPLE patterns that reference each other through SUBROUTINE CALLS (definitions + patterns in one file, the way [DD-14] is being built); (2) the same mechanism user-facing — a user INCLUDES a library of named subpatterns and calls them from a pattern by name; (3) pcrec SHIPS a LIBRARY STORE of tested subpatterns (email address, IP address, quoted string, HTML pieces, …). Relations: [DD-11] (scope-resolved definitions whose values are inserted — this row is that architecture's user-facing form; a library is a definitions scope), [V-E] (multi-pattern compilation units — the unit boundary), [DD-14.G] (splice + dead-capture elision is what makes a library subpattern cost NOTHING: the artifact byte-identical to the hand-inlined pattern — the email specimen's bar), [DD-14.F] (the DEFINE row is the in-pattern spelling of a definition). Design points to settle at charter: NAME RESOLUTION (pattern's own groups first, then included libraries in order; collisions refused by name; PCRE2 has no include, so this is pcrec's compile-time namespace — the oracle sees the EXPANDED pattern, which makes the harness's expansion the SPLICE-vs-LINKAGE `A == B` control by construction); the .rxt directives (`define name <pattern>` / `lib <file>` per file or block; the harness expands for libpcre2 and passes the library to pcrec); the API surface (`pcrec_options` gains a definitions input; CLI `--lib FILE`; versioning per [DD-3]); the STORE's discipline (each entry oracle-verified, a D27-blinded author writes its corpus, pcrec-bench carries it as a benchmark row; the RFC 5322 email specimen is entry #1). Depends on [DD-14] closing (G in particular). Not started unprompted; the charter conversation opens at [DD-14]'s close with the post-spine direction note (journal 2026-08-24).
- [V-E] STATE:not-started (D88 2026-08-29: ONE artifact per emitted file, always — an emission set is a DIRECTORY of per-artifact files + a manifest header, composition is LINKING; no multi-pattern TU; R-VE-3's cross-artifact table sharing is link-level, deferred D77) — MULTI-PATTERN COMPILATION UNITS and the DESIGN INPUT (Frank, 2026-08-26 ~21:1x, recorded, NOT chartered — no new effort at 95 % of the subscription): make `struct rx_info` the IDENTITY of a pattern in a multi-pattern unit, not the symbol prefix — (1) a nested struct of ENTRY-POINT function pointers (search / match / match_caps / the `_in` variants / info) in rx_info — the external entry surface only; D82's per-machine inline accessors (step/dead/accept) never go behind a pointer; a direct-call caller keeps the named symbol; (2) the user-facing NAME (today's prefix) + the pattern source in rx_info, so the emitter DERIVES unique C symbols per pattern and nobody types prefixes beyond a few patterns; (3) one unit-level `const struct rx_info *const <unit>_patterns[]` + count, so a caller finds an implementation by ANY rx_info field (name, source, engine, ncaps…) with a linear scan and an emitted 10-line helper (generated code stays dependency-free). Consequences: an abi bump (append at the end, the [DD-13c] shape); the first consumer is pcrec-bench's shim (40 `pb_*` accessors over rx_info today) at its first multi-pattern sub-bench — D77's trigger for the runtime surface. Two additions (Frank, ~21:2x): (4) emit the unit-level array SORTED BY NAME so lookup by name is a binary search (the emitter knows every name at generation time; the sort order is a stated contract and a check reads it off the artifact); (5) the whole surface — entry-point pointers, name, the unit array and its helper — is an OPTIONAL emit ([EMIT-SET]'s shape: a caller who only uses direct symbol calls pays nothing for a table it never reads; the deny flag masks it out of the file, answer-identity unaffected). ADDENDUM 2026-09-08 (fifty-seventh session): Frank RE-PROPOSED this independently ("an optional rx organizer — an array of rx_info structs that can be searched"), confirming the direction; two facts moved since recording — `rx_info.name`/`nentries` LANDED ([DD-13b.W1.2]), so item (2)'s identity half exists, and [TT-4M] (darwin batched-build validation) became a SECOND candidate consumer: its step-1 dispatch main() is a hand-rolled degenerate organizer, and step 2 (harness adoption), if the numbers open it, should consume THIS row's unit array instead of keeping a test-only dispatcher (general-mechanisms rule; implement-then-replace fine). D77 trigger unchanged: the first real consumer (bench shim's first multi-pattern sub-bench, or TT-4M step 2) opens the design pass. DESIGN INPUTS (Frank, 2026-09-08 evening, recorded for the design pass, NOT ruled): (a) ERROR ATTRIBUTION is a first-class requirement of any batched build — per-pattern builds attribute trivially; an .rxt-level build must attribute every error to its block (a future multi-pattern pcrec mode's diagnostic contract is compiler-like: `<file>.rxt:<line>: pattern '<name>': ...`, all errors not first-stops); D88's one-artifact-per-TU already makes GCC-side attribution free (the diagnostic names the file = the pattern), and one gcc process over N TUs keeps that while batching the spawn — the failing-member recovery is drop-the-named-TU-and-relink ([TT-4M] deliverable 4 prices it). (b) THE ORGANIZER IS A SINGLE UNIT REGARDLESS OF BUILD SHAPE — it must include every member however many .rxt files/targets contributed. Two candidate mechanisms for the design pass: the emitted SORTED array (item 4 above, binary search, needs one generation point that sees all members = the composer) vs GNU LINKER-SECTION self-registration (each artifact plants its rx_info pointer in a named section, the organizer walks __start_/__stop_ — link-composed, no central generation point, unsorted so linear scan or sort-at-init; gcc-dialect in-bounds). (c) SEVERAL INDEPENDENT SETS are wanted — a set is a link-time grouping (section name or array per set carries the set name); open sub-question: set membership declared in the .rxt (a target property — the lean, targets already live there) vs at emission (a flag).
  CROSS-PATTERN FINDER (Frank, 2026-08-12; boonies tier by his word —
  recorded now, built with a customer). N named regexes into ONE emitted
  file: per-pattern named entry points exactly as today (a statically-known
  caller pays no dispatch — D20's rule holds), SHARED DATA deduplicated by
  CONTENT (the driver is M5: a dozen patterns each carrying a private copy
  of the unicode tables adds up; share by content hash, so sharing is only
  ever dedup of identical bytes and never forces a pattern's specialized
  table into a common shape), and OS-0's finder generalized by ONE AXIS:
  D20's selector already dispatches over option-combinations of one
  pattern; the same interface selects the PATTERN too. **This is OS-0's
  candidate FIRST CUSTOMER** — the finder was deferred for lack of one.
  D20's two structural properties still bind: dispatch resolves once per
  call and never reaches the hot loop; a single-pattern single-option
  request emits byte-for-byte today's output. Usage modes to design BEFORE
  building (Frank: "we should think about how it's used"): CLI
  multi-pattern args with per-pattern names, and a manifest file for build
  integration ([V-F] is the third consumer). NOTE (Frank, 2026-08-13):
  this row also answers two use-case questions from the positioning
  discussion — the system for ORGANIZING/FINDING the regex functions you
  have compiled is this row's finder + named entry points, and the
  MANIFEST is the user-facing FILE FORMAT for specifying regexes (name,
  pattern, flags, encoding per entry → one compiled unit); design the
  manifest as a first-class user surface, not a build artifact.
  AMENDED 2026-08-13 (Frank, composition discussion; syntax and semantic
  choices TBD, his to rule): the manifest gains NAMED DEFINITIONS with
  CROSS-REFERENCES — `regexa = abc|def` usable inside a later
  `regexb = xyz(<regexa>)*` — making the file a module system for
  regexes, built up in steps. TWO COMPOSITION TIERS, kept distinct:
  SOURCE-LEVEL (manifest references, AST-inlined at compile time — zero
  runtime cost, DFA compilability preserved, the default) and LINK-LEVEL
  (M4-CALLOUTS' aligned ABI, for separately-compiled parts and
  non-regex predicates). OPEN CHOICE (Frank's, Q2/K4-tier — measured,
  never read from docs): PCRE2 spelling via (?(DEFINE))/(?&name) desugar
  — composed pattern is valid PCRE2, libpcre2 becomes the oracle for the
  composed form, but subroutine-call semantics are ATOMIC and shift
  capture numbering — vs own reference spelling with plain inlining
  semantics (beyond-PCRE2, clean namespace, same shape as M4-SUBST's
  ruling). Reference CYCLES are rejected with a clean diagnostic (true
  recursion is non-regular; a future VM-side module's business, never
  inlining's). Positioning parity note: re2c and lex/flex both ship
  named-definition composition — established practice in exactly our
  claimed niche; PCRE2 semantics on top is the differentiator.
  AMENDED 2026-08-14 (D39.2, `docs/dev/decisions.md`): rx-reference group
  numbering is APPENDED — the primary keeps its own 1..N stable; each
  inserted regex's groups append at N+1.. in insertion order; names are
  kept, and D39.1's exported name→number index is the lookup path.
  Backrefs inside an inserted regex renumber to their appended positions
  at insert time (compile-time). The name-collision policy RESOLVED by
  the D39 addendum: caller-supplied LABELED REFERENCES per insertion
  ("a:reg1", "b:reg1"), stored as the index's `ref` column; nested
  insertions compose a path ("c:a"). Still open, ruled at V-E design
  time: path spelling/separator, label mandatory-vs-optional for single
  insertions, and lookup-key semantics (name-alone when unambiguous vs
  ref+name). FORMAT OWNERSHIP MOVED 2026-08-17: the manifest FILE
  FORMAT itself is designed at [DD-13] (one unified format serving the
  manifest, the test carrier, and pcrec-bench's sets); this row keeps
  the compilation-unit + finder BUILD and its semantic rulings, which
  [DD-13] inherits as constraints
- [V-F] STATE:not-started — the SOURCE-SCAN TRANSFORMER (Frank, 2026-08-12,
  same discussion, same tier): scan a C program's sources for regex
  markers — `auto regex = rx/abc|def/` shaped — and rewrite them to
  references into a pcrec-compiled companion unit ([V-E]'s output format is
  the natural target). re2c/lex-shaped build tool. The dogfooding
  constraint IS the design constraint (Frank: the scanner uses a regex we
  compiled): the marker grammar must be REGULAR and unambiguous amid C
  strings/comments — chosen to be findable by the tool being sold, which
  makes the scanner both the demo and the spec. Do not start without a
  marker-grammar design note answering: escaping inside `rx/.../`, flags
  syntax, occurrences inside string literals and comments (skip or honor,
  and how a regular scanner distinguishes them)
- [V-G] STATE:not-started — USER-FACING REGEX TESTING (Frank, 2026-08-13,
  positioning discussion; boonies tier): expose the project's own dev
  testing machinery to a developer wishing to test THEIR regexes — the
  .rxt corpus format, the harness runner, and (where installed) the
  python-re / libpcre2 oracle differentials, as a `pcrec test`-shaped
  surface: write cases for your pattern, run them against your compiled
  matcher, optionally cross-check against the oracles. The machinery
  exists and is battle-proven (docs/testing.md); the work is packaging,
  scope (which harness features are user-grade vs dev-only), and docs —
  a docs/spec/ candidate when built. Nobody in the niche ships a regex
  TESTING story; this makes the verification story a user capability,
  not just an internal discipline. AMENDED 2026-08-13 (Frank, composition
  discussion): rides [V-E]'s named definitions — every named part of a
  manifest is independently compilable, so subpart testing is the .rxt
  harness pointed at each definition, per-part expectations in the same
  file; complex regexes become testable in steps, bottom-up
- [V-H] STATE:not-started — DEBUG / TRACE EMISSION MODES (Frank,
  2026-08-13, same discussion; boonies tier): generation-time variants
  that aid understanding and debugging a matcher — a TRACING build
  (emitted matcher logs state transitions, positions, prefilter/skip
  entry/exit; a SEPARATE emitted variant per D18, zero cost in the normal
  emission — never a runtime flag in the hot loop), a VERBOSE compile
  mode narrating compilation decisions (prefilter chosen, skip states,
  trie factoring, caps hit), and the compile-time introspection DD-8
  already owes (--emit-ir / --emit-dot — that row stays the owner of
  those two surfaces; this row is the run-time half). Pairs with [V-G]:
  a failing user test plus a traced matcher is a debugging story no
  regex library offers. TRACE ENRICHMENT NOTE (Frank, 2026-08-21,
  thirty-fifth session — details/design TBD, stays boonies): revisit
  what --trace's emitted instrumentation SAYS — today it narrates
  engine mechanics (push/pop frames, state transitions); Frank wants it
  to also point at the position IN THE PATTERN being worked on (which
  construct — the `\n`, the quantifier — the engine is currently
  matching against, i.e. a pattern-offset/construct reference on trace
  events) and generally describe MORE than frame traffic. The emitter
  knows the pattern offset per emitted site at generation time, so this
  is plumbing generation-time knowledge into the trace strings — same
  family as the [M6-READ] legends (emitter-owned data rendered for a
  reader), which is the natural design starting point. If an enriched
  trace mode ever emits TABULAR output it adopts docs/spec/
  table_contract.md at birth

- [SAFEKILL] archived to plan_completed.md (completed 2026-08-19, thirty-fourth session — scripts/safekill merged db8ddde, all three phases; scripts/tests/safekill.test 13/13 under make testscripts; row flipped 2026-08-22 at the thirty-sixth session's wake-up, where the omission was found)
- [EMIT-SET] STATE:not-started (D88 2026-08-29: ONE artifact per emitted file, always — an emission set is a DIRECTORY of per-artifact files + a manifest header, composition is LINKING; no multi-pattern TU; R-VE-3's cross-artifact table sharing is link-level, deferred D77) — CONTROL OF WHAT IS EMITTED INTO THE C FILE (Frank, 2026-08-24, thirty-ninth session): a caller who only uses `match` does not need `search`; one who never reflects does not need `rx_info`; `main` folds into the same switch — `--emit -search,-rx_info,main` (a comma list of named EMISSION UNITS, `-` removes, bare name adds, defaults = today's artifact). The point beyond trimming: once emission is a SET of named units, VALUE-ADD units become drop-ins on the same switch — the results → static-struct copy helper ([V-I]), the compiled search/replace code ([M4-SUBST]), rx code-management helpers (versioning/identity stamps a consumer can query), the diagnostic variants ([V-H] `--trace`/[ENG-PGO] `--profile` are the same shape: a separate emitted variant selected at generation time), the caller-provided-buffer `_in` entries ([DD-14.FB] — a unit a caller may drop), a [LIB] library's shared regions. Design points at charter: the unit graph (dependencies — `main` needs `search`; `_in` needs the run struct; `rx_info` fields some units reference) resolved at generation with a clean refusal for an inconsistent set (D26 tier 3 wording); the [ABI-NS]/structural codegen checks become per-unit (a unit's presence/absence is a byte-level fact the identity gates assert — the default set byte-identical to today's artifact, the control); `--list-emit` prints the units and their defaults; the CLI/library API surface (`pcrec_options` gains the set; DD-3 versioning). D18's principle applied to the artifact's SURFACE: what is not emitted costs nothing, and a unit nobody asked for is not there to be wrong. Relations: `--emit-main` (today's only switch, becomes the `main` unit), OS-0's named-entry-point discipline, [DD-8] (`--emit-ir`/`--emit-dot` are OUTPUT formats, not units — keep them apart). Not started unprompted; PLANNED, its spine status deliberately open (Frank, 2026-08-24 ~22:0x) — the going-forward conversation at [DD-14]'s close decides.
- [BENCH-UTF8] STATE:started (RULED by Frank 2026-09-22 ~23:4x, seventy-seventh session: "Build the utf bench. But make it somewhat complete, not 'small' — or at least specify that it will grow. It should, at least eventually, exercise any functionality which might be affected by encoding. Classes come to mind."; the FIRST use of D119's addendum's open-a-new-subbench clause) — the `utf8` SUBBENCH, requested as I-90 (bench inbox): the bench BUILDS (D78), pcrec runs; UTF-8 text subjects in several scripts (Latin-1-Supplement-heavy, Cyrillic, CJK, mixed/emoji, one ASCII control); families (a) classes, (b) multi-byte literals/runs, (c) caseless over non-ASCII, (d) multi-byte alternation/quantifiers, (e) assertions (\b/lookbehind/multiline), (f) properties, in the first release; growth plan (g)-(k) named (find-all, invalid UTF-8, start-inside-a-character, surrogates, the `-e byte` mirror arm) — pcrec's tests/utf8 axis01-12 are the correctness axes the subbench measures SPEED on. MEASURED NEED: the shipped byte-frequency prior is inverted under utf8 (reqbyte_freq_pick.md §3), [OPT-OFSK] reads it under utf8 today unmeasured, the freq pick and 2b decline under utf8. First customers: offset-skip rows, high-byte required-byte rows, fold sets. Ledger → cycle 3's utf8 question.
- [PATFACTS] STATE:not-started (CHARTERED by Frank 2026-09-22 evening, seventy-seventh session, D120; TIMING RULED 2026-09-22 ~23:5x: "after cycle 2 or 3 as you see" — the manager's schedule: STEP 1 (the read-only inventory) runs during cycle 3's bench-measurement waits, once batch 2 (the freq pick + 2b, the prior's third reader) has landed and been measured; STEPS 2-3 open after cycle 3's batch lands, so the design sees every analysis the loop has added and no implementation lane is displaced) — ONE ORGANIZED PATTERN-ANALYSIS RECORD: the per-pattern facts every step needs (necessary byte set, necessary run, first-byte set, start anchor, end window, min/max width, fixed-offset prefix, the byte-frequency prior's readers) computed ONCE after `pcrec_lower_enc` and read by every opt pass, engine selection and both emitters, replacing the ad-hoc per-pass walks and cross-file reaches (D120 names the two incidents). STEP 1 = an INVENTORY lane (read-only: every analysis's file, question, tree level, Job field, consumers, encoding dependence; the redundancies); STEP 2 = the design through the four lenses; STEP 3 = implement-then-replace, migrating existing analyses one at a time under the identity gates. Not a cycle-2 dependency.
- [PAT-LINT] STATE:not-started — OPTIONAL PATTERN ANALYSIS: an ahead-of-time
  check that names issues in a pattern the engines will accept but that
  behave badly (Frank, 2026-08-25, fortieth session, from the K34 ruling
  D74; PARKED — no queue position; details and design TBD). The charter
  case: ZERO-PROGRESS LEFT RECURSION — `(a|(?1)a)b` re-enters group 1 at
  the same subject position before consuming, so every non-matching
  subject is a runaway (pcrec gives up `frames`, PCRE2 concludes only by
  an implementation artefact, D74); the same language as `(a(?1)?)b` or
  `a+b`, which recurse after consuming and conclude in both engines.
  Rule 1: a call edge to group G reachable from G's own entry through a
  ZERO-MINIMUM-WIDTH prefix (the compiler already computes minimum widths
  — wave E's root-minw guard — so the analysis is a walk over facts it
  has). WARNING, NEVER A REFUSAL: the pattern is legal and matches on
  positive subjects; §3.3's deliberate same-position recursion with a
  base case is not a defect (the analysis says "may not terminate on
  non-matching input", not "wrong"). Other candidates for the same
  surface, each to be measured for a real occurrence before it is built:
  nested unbounded quantifiers over overlapping languages (the classic
  `(a+)+b` catastrophic-backtracking shape — relevant only where pcrec
  picks the VM engine; the DFA is immune, so the analysis must report
  ENGINE-AWARE, else it warns about a pattern that runs in linear time);
  empty-language callees (already refused at wave E — a diagnostic tier
  question whether it moves here); a dead group that never captures
  (wave G's elision list — informational). SURFACE TBD: a CLI flag
  (`--analyze` / `-W`) whose output is text, or a library entry; the
  D38 naming rules apply to any stamped result. Interacts with [V-G]
  (user-facing regex testing — the natural home if that lands first) and
  [V-H] (trace modes show the runaway; this names it before running).
  NOT QUEUED — parks behind the general work per the boonies discipline.
- [V-I] STATE:not-started — NAMED-RESULTS COPY HELPER (Frank, 2026-08-18,
  thirty-third session; LOW PRIORITY, boonies tier; details and design
  TBD): an emitted per-pattern helper that takes a search's results —
  in particular a captures-delivering search, most particularly one with
  NAMED groups — and copies them into a STATIC STRUCTURE keyed by the
  group names (the natural shape: an emitted `struct <prefix>_groups`
  with one span member per named group, plus a copier from the caps
  array). Generation-time is what makes it cheap: pcrec knows every name
  at emit time, and [M6.3]'s name grammar ([A-Za-z_][A-Za-z0-9_]{0,127})
  makes every group name a lexically valid C identifier ALMOST for free
  — the TBD wrinkles already visible: C KEYWORDS are valid group names
  but invalid member names (`(?<int>...)`, `(?<return>...)` need a
  mangling rule), and D61's caps-layout promise plus rx_group_entry.slot
  are the substrate the copier reads through (slot-aware from birth, so
  a future ref-bearing row costs nothing). Interacts with [V-B]
  (bindings would love the same struct) and the D38 naming rules. NOT
  QUEUED — parks behind the general work per the boonies discipline

- [SIMD-META] STATE:not-started — META-PLAN ROW for the studies/simd1
  research (Frank, 2026-08-16, at adoption): a thinking/triage row whose
  DELIVERABLE IS PLAN ROWS — "wrap most of it into a meta-plan item that
  thinks about how to use this research and which would create those plan
  items." Do not spawn per-finding rows ahead of it; this row decides
  which follow-ups exist and their shapes. The two integration hypotheses
  it must weigh (Frank's framing): (1) PRE-SEARCH — the study's §14
  find-all mode as an exact anchor source feeding the engine (its
  contract was designed for exactly this; touches M4.6's
  prefilter/islands territory and V-C's grep CLI); (2) SNIPPETS INTO
  REGEX PROPER — "some findings suggest snippets could be integrated
  into regex proper if the statistical analysis held up": the §3
  position-encoder menu, §15 run extension for [class]+ atoms, and
  literal-factor scanning emitted INLINE in generated matchers as a SIMD
  emission tier under D18 — gated on the §16 exemplar-statistics /
  background-frequency machinery actually predicting well (the
  "statistical analysis holds up" condition; folds in the [ENG-PGO]
  profile-guided-generation idea from the adoption discussion — the
  exemplar axis is an input to BOTH hypotheses, not its own island).
  Constraints the meta-row must answer, not inherit silently: generated
  code is today SELF-CONTAINED gcc-dialect C with no CPU dispatch — a
  SIMD tier raises ISA flags (-mavx2), runtime dispatch vs
  generation-axis targeting, and fallback tiers (the study's §11
  ~10-macro ISA layer is the candidate shape); every study number is
  Zen 1 and must be RE-MEASURED before load-bearing use (D12/bench
  discipline); reconcile with DD-9/[BENCH-1]'s case-(f) worklist row
  (the study's §12-B shift-and measurement bears directly on it —
  studies/simd1/CLAUDE.md flags this) rather than duplicating it.
  (gate re-pointed 2026-09-22: [BENCH-1] folded into [OPTLOOP]; case
  (f)'s worklist item now lives in an [OPTLOOP] cycle's analysis, e.g.
  docs/dev/optloop/cycle1_analysis.md, rather than a standing BENCH-1
  row)
  Output: a short design note (panel-eligible) + the created rows.
  AMENDED 2026-08-16 (Frank, twenty-seventh session, during the counter-K
  arc): SCALAR-FIRST is the ruled integration posture — figure out how
  SIMD plays in OVERALL before any of it touches current work; the ideal
  shape is "our best non-SIMD approach, then backend VARIANTS on top of
  it". Consequences: (a) in-flight engine lanes take NO SIMD-derived
  design inputs or bench cells (three counter-K bench items sourced from
  studies/simd1 were retracted under this directive the day it was
  given); (b) thinking and planning ARE in-scope and live HERE — two
  parked observations from the counter-K arc are this row's first
  evaluation inputs: (1) counter-K's one-body-copy emission is the
  natural substitution site for a §15-style SIMD run-extension variant
  (one site per quantifier vs N replicated copies — an argument the
  backend-variant layering is cheap over the counter design, worth
  verifying when this row opens); (2) the `#pragma GCC unroll` question
  (simd1 JOURNAL.md:63 — could a pragma replace manual K-copy emission
  on frameless arms?) is parked here, NOT in counter-K's bench, until
  the variants architecture is thought through.

M4-hosted, boonies-queued (Frank's queue discipline places these after the
spine, not before):

- [ENG-THIN] STATE:not-started — MRL CLAMP GATING/THINNING (Frank,
  2026-08-17, twenty-ninth session, ruled at the [M4.6d] accept-as-is
  decision — D51 addendum has the accepted +8% residual this row exists
  to shrink): decide per PATTERN — or per SITE — whether the MRL clamp
  is emitted at all. SOUNDNESS-FREE BY CONSTRUCTION: with or without
  the clamp the VM returns identical answers (the 202,458-cell [M4.6d]
  differential is the demonstration); gating errors cost budget
  exposure or overhead, never wrong answers, so this is pure
  measure-then-implement optimization. TWO HALVES, Frank's two
  questions verbatim: (1) ANALYTIC, compile-time — the k23_design.md
  closed-form step law (validated exact out of sample) gives a
  worst-case unclamped step bound per pattern; clamp only where that
  bound exceeds a threshold against the step budget. Boolean
  is-it-ambiguous gating is NOT sufficient — the +8% shape
  ([a-z]{2,4}){2,8}b IS ambiguous-class; the gate must be
  quantitative. Conservative generalization of the law is this row's
  engineering; when uncertain, KEEP the clamp (wrong-keep costs <=8%
  on a bad shape, wrong-skip costs an honest refusal, neither is
  unsound). Per-site refinement: clamp only quantifiers participating
  in nests whose bound exceeds threshold. (2) EXEMPLAR-STATISTICAL —
  owned by [ENG-PGO], linked: given representative subjects ([DD-13]'s
  exemplar file references are the designed carrier), tune the COST
  side where (1) says the clamp is not strictly needed; exemplar
  stats must never override the safety side (tail inputs are exactly
  what exemplars do not show; DD-2's budget refusal stays the
  backstop). If a caller wants both variants, OS-0's named entry
  points are the D18-compliant shape — no hot-loop dispatch.
  REGRESSION TRIPWIRES already in place: run_mrl_tests.sh §1b (the
  8-step counter-rung acceptance cell) and
  tests/base/d27_k23_ambiguous_decomposition.rxt (89 blinded cells) —
  over-thinning that reintroduces a K23-class blowup fails both,
  loudly. Schedule: boonies-queued with a natural measurement window
  alongside [M4.6e]'s VM perf work.
- [ENG-PGO] STATE:not-started — (CROSS-NOTE 2026-08-29 19:4x, Frank on [OPT-4]'s default: the count-collapse decision "sounds like the kind of thing that analyzing the exemplar file with a profiling version of the pattern would answer" — a second PGO-informed selection point beside [OPT-A]'s rarest byte: the findings file's subject profile (candidate-start density, run lengths at the repeat) decides exact vs collapsed per pattern; the static knee-128 stays the profile-less default.) (CROSS-NOTE 2026-08-29, Frank: the FINDINGS FILE is an .rxt — a `freq <name>` DATA block (a byte-frequency table) that a target's config references; docs/design/dd13_format/frank_inputs.md 2026-08-29, usecases_and_outline.md §6.5; blocks on [DD-13b] wave 2/3.) D83 (2026-08-28) RULES THE SHAPE: exemplar analysis runs OUTSIDE pcrec, once per file, delivering a FINDINGS FILE pcrec accepts; FILE-GENERAL findings (byte frequencies etc., pattern-independent, one exemplar serves many patterns) and PATTERN-SPECIFIC findings (a statistics-recording special compilation, built only for a specific question, later) are two result files and two builds. The bench's [B11.1] presence counts are the first exhibit. FIRST CONCRETE QUESTION for the profile attachment (Frank, 2026-08-25 ~21:2x, at the [OPT-1] two-tier ruling): the tier design is a BET that the fast tier holds the overwhelming majority of real calls; a `--profile` variant run over an EXEMPLAR subject file would put a number on it — the escalation count (FRAMES → deep tier) per call, per pattern, per subject class — and is the trigger D79 names, made concrete: build the profiler when the loop needs this number, and this is the number. CHEAP FIRST STEP (srTier, 2026-08-25): the `-DRX_TEST_TIER_HOOK` build of a tiered artifact already counts escalations per call — a driver over an exemplar subject file yields the escalation rate WITHOUT the profiler; charter that as [OPT-1]'s follow-up measurement before any profiler design. (Cross-note continues.) — CROSS-NOTE (Frank, 2026-08-24, thirty-ninth session, direction not decision): the `--profile` emission mode below gains a SECOND CUSTOMER — the post-spine pcrec-bench OPTIMIZATION LOOP (journal 2026-08-24 direction note): a PROFILE ATTACHMENT on a compiled pattern that says where the matcher spends its time BY SECTION (prefilter scan, skip loop, VM attempt, per rung) and, where possible, in smaller increments, run over a variety of scenarios, to guide optimization PRIORITY — `--trace` is the debugging tool, the profile is the optimization tool. Design starting points (manager, same day): time attribution needs cycle deltas (`rdtsc`/`clock_gettime`) at the section boundaries the emitter already names, not counts alone; the computed-goto labels are natural fine increments (count + cycles per label = a per-construct profile with no `perf`/symbolizer), and can carry the pattern offset the [V-H] trace-enrichment note wants; the profile struct is read through an `rx_info`-shaped accessor; the artifact stamps that it is the profile variant (identity gate: the default artifact is byte-identical across the axis). Same [V-H] namespace as D71.1's counter axis — a second diagnostic axis is the trigger D71 names for designing the GROUP rather than the flag. PROFILE-GUIDED GENERATION (Frank,
  2026-08-16, twenty-seventh session — promoted from the parenthetical
  inside [SIMD-META] to its own row at his ask; the SIMD-META exemplar-axis
  mention now points here). TWO NAMED CUSTOMERS as of 2026-08-17 (D53
  addendum): (1) [ENG-THIN]'s clamp-cost tuning; (2) HYBRID-VS-VM-ONLY
  ENGINE SELECTION per pattern — D53 killed the runtime length branch
  (the deciding variable is match OFFSET, unknowable pre-search), but
  the offset DISTRIBUTION is a static workload fact an exemplar file
  exposes; preferred form is pure PGO (build both variants, run the
  exemplars, keep the measured winner — absorbing per-attempt-cost
  effects no distribution model sees), with OS-0 named entry points
  for mixed/unknown workloads. The idea: compile a pattern exactly as today,
  but with an opt-in `--profile` emission mode whose matcher TRACKS HOW IT
  RAN on the user's exemplar inputs — a PROFILE, not a trace: counters at
  the observation points the emitter already names (D46 stamp points,
  DD-8's tracer sites; same structural constraint — derived from the
  structure the emitter walks, never a parallel description). Stats out
  (candidate-start density, prefilter hit/miss, per-arm hit distribution,
  iteration-count histograms per quantifier, match/fail mix, subject byte
  frequencies), then a RECOMPILE-WITH-STATS channel feeds them back as
  generation inputs. SCALAR CUSTOMERS EXIST — this row is not
  SIMD-contingent: alternation/trie arm ordering by hit frequency;
  prefilter literal choice by measured-rarest byte (scalar memchr benefits
  identically to any SIMD scanner); the §8.1 capture-walk sink knob
  (eager/n-step/accept-time is a match/fail-mix question); [M4.6] engine
  selection; step-budget sizing near K23-style boundaries. RELATION TO
  F-1/D18, stated so the tension is on record as a resolution: per-pattern
  adaptive choices keep getting deferred because an axis must earn itself
  with a measurement — PGO is the EARNING MECHANISM, the standing vehicle
  for every deferred adaptivity question ([ENG-CLAMP], adaptive K, arm
  order), so those rows cite this one instead of re-inventing a
  measurement channel. SEQUENCING: design sketch is panel-eligible any
  time (thinking is cheap); implementation AFTER the M4 spine closes and
  WITH [BENCH-1] (the profile without the bench is a dial with no meter —
  BENCH-1's prioritizer is also this row's proving ground), BEFORE the OPT
  waves it would guide. (gate re-pointed 2026-09-22: [BENCH-1] folded
  into [OPTLOOP]; the proving ground is now an [OPTLOOP] cycle's own
  analysis, e.g. docs/dev/optloop/cycle1_analysis.md, rather than a
  standing BENCH-1 prioritizer) The stats interface is designed scalar-first;
  later SIMD backend variants ([SIMD-META] hypothesis 2's density/
  statistics dependence) consume the SAME channel — which is exactly the
  "best non-SIMD approach, then backend variants on top" architecture
  Frank ruled.
- [ENG-DIRECT] STATE:not-started — **MEASURED TRIGGER 2026-09-02 (bench O-14 §2 / [OPT-VMFL]): the forced-VM route on frames-1 artifacts — a direct-coded automaton by construction — runs ×9.0 faster on a failing literal scan (0.296 vs 2.661 ns/B) once the computed goto is gone; Frank: this IS the direct-DFA idea; [OPT-VMFL] carries its STEP 0 and its selection question; STEP 0 MEASURED 2026-09-02 (optvmfl_step0.md): the direct-branch-dispatcher twin on frames≥2 bodies is NOT a general win (3-4% on a scan-loop-dominated body, flat-to-slower elsewhere) — the ×9 belongs to the SCAN-LOOP SHAPE, which is this row's own claim, not to the dispatch** — DIRECT-CODED DFA EMISSION (Frank, CROSS-NOTE 2026-08-30 (Frank, with [OPT-VMLIT]): direct-coded emission is where the DFA gets STRING EDGES — a linear chain of states whose transitions are singleton bytes emits as one memcmp instead of per-byte table steps; the M2.8 trie's non-branching chains (nfa.c:192) are the finder, [OPT-VMLIT] is the VM twin.
  2026-08-18, thirtieth session: "could we implement without state
  transition tables? ... aabbc sets up the table mechanism and the
  loop but really its just a memcmp ... (?:\d{2,3}\.){3}\d{2,3}
  could be a fixed loop of checking digits followed by period ...
  this is what i think of for dfa and for islands of dfa — no
  overhead"). Emit a DFA-tier pattern's verify automaton as DIRECT
  CODE — the program counter IS the state (re2c's model; lex -f,
  Ragel's goto mode) — instead of the rx_fcls/rx_ftr table loop:
  reducible automaton structure becomes structured code (sequences,
  branches, counted loops), byte tests become gcc-optimizable range
  checks/compares (the OPT-A spelling menu falls out of gcc switch
  lowering for FREE in this model, and the address-taken-label
  pinning largely disappears — plain structured flow), literal
  chains become the OPT-A memcmp lead as a special case. THE DEEP
  PAYOFF beyond constant factors: a bounded repeat in direct code is
  a COUNTED LOOP (register + constant code) where the table DFA pays
  STATES linear in n — the exact K7/a{n} family D56 just bounded;
  direct-coded counting re-widens the a{9795} narrowing by making
  the state-set population it guards simply not exist for these
  shapes (the VM's counter/cursor rungs already prove the loop form
  on the capture side). THE HONEST TRADE, which is why tables stay
  the general engine: dense/irregular automata mispredict as branchy
  code where a table lookup is branchless; large/irreducible
  automata blow icache and D45 compile budgets as code (the cap
  moves from state count to code size); so this is a PER-PATTERN
  (or per-island) selection by measured shape — D46 stamp+force
  obligations apply as a new selection axis when built. EVIDENCE
  MACHINE: [BENCH-CEIL] is this row's instrument — the hand-written
  ceiling arm's code IS what direct emission should approach, and
  the per-case gap table is this row's worklist and its acceptance
  measure. CROSS-LINKS: [ENG-ISL]'s islands, once determinized,
  should be EMITTED via this row's mechanism (the "no overhead"
  half of the island idea — record there); DD-5's switch-based
  emitter is the PORTABILITY cousin but still state-variable-driven
  — true direct coding eliminates the state variable where control
  flow carries it; the K24 counterweight (computed-goto
  unoutlineability) applies to any emission-model change and travels
  with this row. Sequencing: with the OPT waves, after [BENCH-CEIL]
  produces the gap table; unmeasured engine work is not scheduled
  (D12/D18). RULED A THIRD ENGINE TYPE (Frank, 2026-08-18, same
  conversation): the roster becomes table-DFA (current) / DIRECT-DFA
  / VM — with the VM potentially carrying direct-DFA ISLANDS — so
  when built this joins the ENGINE SELECTION axis proper
  (select_engine picks per pattern; ENGM_* gains a member; RX_ENGINE
  stamps a third value; OS-0's named-entry-point discipline applies),
  not just the emitter. On re2c, read by Frank at filing: it
  VALIDATES THE MODEL and is deliberately not the tool — its regex
  language is far from PCRE2 (lexer dialect, longest-match
  semantics). pcrec's shape is what makes the model usable inside a
  PCRE2 compiler: full-dialect parse + per-pattern selection routes
  only the shapes that fit. SURVEY LEAD for the capture question:
  re2c's TDFA (tagged-DFA submatch extraction, Trofimovich) is the
  literature for captures in direct-coded DFAs — the road by which
  direct-DFA could someday serve capture-bearing patterns without
  the VM; survey by measurement per OPT-A's rule when this opens.
- [ENG-ISL] STATE:started **PARKED 2026-09-21 into [BACKLOG-TRIAGE] (Frank, seventy-fourth session: "agree on sequencing" — the bench matrix ranks it against everything else; not before [REL-1]).** (EMISSION NOTE added 2026-08-18: an island, once determinized, is EMITTED via [ENG-DIRECT]'s direct-coded mechanism — that is the "no overhead" half of the island idea; determinization proves the language/preference exactness, direct coding is what makes the island cheaper than the machinery it replaces) — EXACT DFA ISLANDS, deferred OUT of **FRAMING (Frank, 2026-09-02 ~15:2x, fiftieth session) — THE ROW IS BIDIRECTIONAL: "islands of VM in DFA" as well as islands of DFA in VM.** His words: "If the spans in dfa idea works out for some N, then you may want to consider a framing of 'islands of vm in dfa' instead of one off. This framing allows for single dev source and opens other ideas where we show vm to significantly outpace dfa. We use dfa in vm for prefilter. We're blurring the lines." READ THIS WAY, three things already built are instances of ONE mechanism: the hybrid PREFILTER (a DFA island inside the VM's entry, in production), the [OPT-5] SCAN EDGE (the VM's address-only counted loop embedded in the DFA scan loop at a head state, resuming at a fall-through state — STEP 0's own finding: 'emit the VM's shape for any DFA region isomorphic to count-one-class-up-to-a-bound'), and the direct-coded automaton ([ENG-DIRECT], a frameless VM program). The general form: a DFA REGION whose structure is VM-shaped (a counted class loop, a literal chain [OPT-VMLIT], a negated-class run [OPT-NEG], a period-k string loop — [OPT-5] STEP 3) is emitted by the VM's emitter for that shape and spliced at the region's head with the edge's head/fall-through protocol — ONE DEV SOURCE for those loop bodies, both engines reading it, and the DFA route gaining the VM's per-call and per-region wins where the bench shows the VM ahead (its §3.3: VM 633 ns vs DFA 1,906/3,785 on the bounded match axis from 1024 up). TRIGGER (D77): [OPT-EDGE]'s dispatch + floor measurements showing the edge pays for some N; then the reframing is the design, not another one-off edge kind. **FIRST NAMED ISLAND CANDIDATE (Frank, 2026-09-02 ~16:0x, on the bench's altwide set): VM ALTERNATION AS A TRIE DISPATCH.** Facts read off the code: the VM emits an N-way alternation as ONE live frame at a time (emit_vm.c vm_alt: the frame pushed at branch k resumes branch k+1) — so trying w-512 on a subject whose match is the last branch costs 511 push/fail/pop/dispatch round-trips on the SAME subject byte, and each literal branch is its own byte-chain body (the 500,000 B code cap refuses w≥512 on the VM route; srt-512 vs w-512 differ 93,508 B / 13.8% on the VM, 1 B on the DFA — order matters to the VM's emission, not to the automaton). The NFA builder ALREADY composes flat alternations as a PRIORITY-PRESERVING PREFIX TRIE (src/ir/nfa.c:192, M2.8 — rules 1/2 for the `abc|a|abd` and overlapping-class hazards, both oracle-confirmed) and the DFA route is that trie determinized, which is why `auto` is FLAT over w-8..256 (×83 ahead of the JIT) while every other testee rises 74-90×. The island: emit the VM's alternation of literal/class branches as a DISPATCH ON THE DISCRIMINATING PREFIX (a switch on the first byte(s), the trie's fan-out) that jumps to the surviving branch with ZERO frames where the first byte decides, falling back to the frame chain only inside a shared-prefix group with overlapping continuations — nfa.c's trie (with its priority rules) is the FINDER, the emitted switch is the DFA island, the head/fall-through splice is the protocol. MEASUREMENT FIRST (D77): the bench's altwide@0.2 `pcrec-vm-bigcap` arm is the BEFORE; STEP 0 = a hand-twin of w-64's VM artifact with a first-byte switch, timed on this box, plus the code-size delta (the bigcap arm's refusal wall is the other customer). (Bench sizing for the alternation island, same date: the largest raised-cap artifact is s-4096 whole-subject VM at 3,741,164 B, and gcc is SUPERLINEAR in emitted VM code bytes — w-512 5-7 s, w-2048 62-92 s, s-4096 183-334 s per form — so the trie dispatch's code-size win is a compile-time win of the same order, and the size wall is a real customer, not a cap artefact.) **MEASURED NEED for the alternation island (bench O-15, altwide@0.2 at 1989c62): branch ORDER costs the VM ×8.87 at w-256 and ×20.1 at w-512 (srt vs w) while the DFA artifact is byte-identical (1 B apart) — the serial-try mechanism I-33 named, now with a number; the refusal boundary is 256 < w ≤ 384 on BOTH routes (w-384: 43% over the source cap, 1.7% over the code cap); the flat auto line holds to w-2048 under the raise (×627 the JIT).** **MECHANISM SHARPENED (bench, 2026-09-03 ~11:4x, re-pin census at 288d505): the ×8.87/×20.1 order effect is the trie's FACTORING count reaching the VM lowering — `RX_ALTCLS_FACTORED` reads 11 on w-256 vs 57 on srt-256, the two DFA artifacts byte-identical except that stamp line (merges 0 everywhere). So the VM ALREADY consumes the trie's factoring, and sorted input factors ~5× more. A CHEAP STEP-0 LEVER before any island: CANONICAL BRANCH ORDER where it is priority-safe — nfa.c:192's rules 1/2 say exactly when alternation order carries meaning (a branch that ends at a node; overlapping non-identical classes); everywhere else the branches can be sorted by first byte before factoring, which should recover much of the srt-vs-w gap on the VM with no new dispatch. Measured by the srt/w pair itself (bench candidate 2); answer identity is the bar (the reorder must be provably order-blind).** **STUDY PROPOSED (Frank brainstorm, 2026-09-03 ~12:0x: 'a DFA island in the VM for alternates'; 'sort alphabetically with longer matches first; for each match we know if a higher-priority shorter alternative should be tried first'; 'a study on the side without the bench delay'). THE DESIGN THE TWO IDEAS RESOLVE INTO: walk the alternation as a TRIE (sorted, so shared prefixes factor regardless of source order) with every accept TAGGED by its original branch index; the walk collects the accepts it passes and the answer is the LOWEST-INDEX accept seen (leftmost-first, exactly what the DFA route's priority computes) — so sort order affects construction only, never the answer; the collected accepts, in index order, are ALSO the candidate list the VM tries when its continuation fails (PCRE backtracks INTO an alternation: `(ab|abc)d` on 'abcd' must fall from ab to abc), so the island never commits where possessify's analysis does not license commitment — it replaces 'try all N serially' with 'try only the branches that matched, in priority order', which is the general form of the first-byte dispatch (1-byte) and of [OPT-ALTHASH] (k-byte block). WHERE IT DOES NOT APPLY: branches carrying captures/backrefs/lookarounds that need the VM inside the alternation (the island reports spans only), and overlapping leading CLASSES (rule 2), which the walk must treat as an NFA step or decline. STUDY (a `studies/` prototype, own Makefile, no emitter change, no bench window): the bench's altwide patterns (w/srt/pfx3/ci/cnt/s at 64-2048) and subjects in a standalone C harness comparing (a) serial try (today), (b) first-byte grouping with stable order, (c) the sorted trie + tagged accepts + candidate list, (d) the k-byte block hash — measured as tries per subject byte and ns/call, answer-checked against the VM route's own output. One lane, ~a day; Frank's charter owed.** **STEP 1 PROPOSED (2026-09-03, on [ENG-ISL.S0]'s measurement; FRANK'S CHARTER OWED): build algorithm (e) in emit_vm.c's alternation lowering — sorted trie over literal branches, byte compares / first-byte switch per node, end nodes as the only push sites, the corrected commit rule (beats the subtree AND the best deferred), a per-path deferred mask in a slot (zero capacity needed on the bench's pools; size it from the corpus census), mask-ordered fallback on continuation failure; DECLINE on class-leading branches (rule 2) and on branches carrying captures/backrefs/lookarounds; answer identity over the corpus + test-axes; an abi event (new emitted scaffolding; a stamp naming the island's presence); STEP 0 of the build = the hand-twin on a real resumed frame (the continuation-backtracking cost the study could not measure) and a corpus census of alternation shapes that qualify. Queued behind [CC-DIFF] STEP 1 (emit_vm.c).** **STEP 1 CHARTERED 2026-09-03 ~16:2x (Frank: "Eng isl charter approved"): build algorithm (e) in emit_vm.c as the VM's alternation island — lane A of the evening wave (opus): the lowering (sorted trie over literal branches; byte compare / first-byte switch per node; end nodes as the only push sites; the corrected commit rule — beats the subtree AND the best deferred; the per-path deferred mask in a slot, sized from a corpus census; mask-ordered fallback on continuation failure), DECLINE on class-leading branches and on branches carrying captures/backrefs/lookarounds; STEP 0 inside the lane = the corpus census of qualifying alternations + the hand-twin on a real resumed frame (the continuation-backtracking cost the study could not measure); answer identity over the corpus + test-axes; an abi event with a stamp naming the island; suites and timing by DAY (the bench has the night).** **STEP 1 WRITTEN 2026-09-03 21:3x EDT (lane isl1, tip 70c0083, write-only under the hold; report docs/dev/lanes/isl1_report.md): three upward deviations, each forced by a measurement — (1) NO deferred mask/slot: every trie edge is one byte, the walk is a single static path, so the live candidate list at any node is compile-time and the study's commit rule is discharged by construction (RX_NSLOTS unchanged); (2) the island's INPUT is the subtree's LANGUAGE (a finite set of literal words, cross product X-major = PCRE's preference), not its branch list — src/opt/altcls.c factors wide alternations before the emitter sees them, and the branch-list build stamped 11 islands on w-256 (= its RX_ALTCLS_FACTORED count) on nothing but altcls residues, +3.0% artifact; budgets VM_ISL_MAX_WORDS/BYTES/DEPTH as DECLINES; (3) the empty alternative admitted (an accept at the root). Decline = any element not a one-byte class (the `ab[cd]|abx` tail form is a STEP 2 shape, manager-ruled), any quantifier/group/capture/backref/lookaround/assertion/call, over budget, < VM_ISL_MIN_BRANCHES (2, confirmed). Census (scripts/alt_census.py, lower bound): 429 of 1,003 corpus alternations qualify, 368 width-2, mask depth ≤ 4. Width ladder (code bytes, island/chain): w-256 0.856, pfx3-256 0.812, s-256 0.764; w-256 vs srt-256 now 2 B apart (the ORDER effect is structurally gone); w-384 COMPILES on the VM route (427,739 < cap) — the VM refusal wall moves to 384 < w ≤ 512. Answer identity: 62 cells island vs -fno-alt-island vs python re, all agree. INTERACTION FOUND (§4.2, escalated to Frank): every ladder artifact is FRAMELESS, which arms [CC-DIFF] STEP 1(a)'s always_inline gate and inlines the ~70 KB trie into all six entries — w-256 .text 71,293 → 270,544 (×3.8), gcc 1.38 → 5.91 s (×4.3); [CC-DIFF]'s gate measured only small frameless programs, where inlining shrinks; THE GATE WANTS A SIZE TERM (a [CC-DIFF] step of its own; the lane measures the three-point inline ladder tomorrow). Unverified: every suite (make test / test-codegen / test-axes / the island script), all timing. Tomorrow after .lift: suites, the hand-twin (§5), the width-2 cells, the inline ladder, the abi ritual.** **TIMED 2026-09-03 20:5x EDT (isl1, quiet box; island/chain ns ratios): the hand-twin on the sharpest prefix-bearing shape (128 alternatives, every path two accepts, every hit forcing the candidate chain) 0.010 — the resumed frame costs one push + an O(1) resume, Q3's prefix-free-only fallback REFUSED by measurement; width-2 cells: `foo|bar` 0.175, `(?:cat|dog|cow)s` 0.140 (prefix-free wins big at width 2), `fo|foo` 1.131, `(?:ab|abc)d` 1.144 (prefix-bearing tiny loses), `(?:a|ab|abc|abcd)z` 1.001 (wash at 4) — the discriminator is PREFIX FREEDOM, not width (a prefix-free island pushes nothing and [CC-DIFF]'s always_inline then deletes its entry frame). RULED: decline where pushes > 0 && words < 4 (one predicate line, one test cell); VM_ISL_MIN_BRANCHES stays 2.** **STEP 1 MERGED 2026-09-04 00:5x as cee7c741 (abi 18; chain green on main after two test-file fixes — the K35 sort locale in the pool, the possessive corpus blocks' features line). Panel r53 (docs/dev/reviews/2026-09-03-r53-alt-island.md) landed in full before the merge: the size-vs-chain FACTOR rule (2×, (nodes + try sites) × 180 B) with the artifact-wide cap guard; the critic's census 0 refused / max 1.03×; 27,256 answer cells, 0 divergences. OWED: [CC-DIFF] STEP 2 (the inline gate's size term; the wrapper-forward arm; the capability probe); [OPT-DIAL]; STEP 2 shapes (the `ab[cd]|abx` tail form; the class-member expansion).**
  - [ENG-ISL.S0] STATE:completed (CHARTERED 2026-09-03 ~12:1x by Frank: "I approve the study charter"): THE ALTERNATION-DISPATCH STUDY — a `studies/alt_dispatch/` prototype (own Makefile; never built by pcrec's make; lane altstudy, sonnet) comparing four dispatch algorithms for a wide alternation on the bench's altwide patterns and subjects, standalone C harness, answer-checked against the VM route's own output: (a) serial try (today's vm_alt), (b) first-byte grouping with stable order, (c) the SORTED TRIE with priority-tagged accepts + the candidate list (leftmost-first exact; sort order affects construction only), (d) the k-byte block hash ([OPT-ALTHASH]). Measures: tries per subject byte, ns/call by width (64..2048) and by shape (w/srt/pfx3/ci/cnt/s), construction cost and table size; plus the written argument for (c)'s exactness (the lowest-index accept rule; where rule 2's overlapping classes force an NFA step or a decline). Deliverable: studies/alt_dispatch/ + docs/design/alt_dispatch_study.md with a recommendation and the D77 trigger for building the island in emit_vm.c. No emitter change; no bench window. **ALGORITHM (e) ADDED (Frank, 2026-09-03 ~12:2x: 'sort the alternatives, build the trie, note certain nodes on the path (shorter alternatives) are end nodes; if they fail the trie walk continues') — THE VM-NATIVE FORM: the trie walk IS the emitted VM code for the alternation (a byte compare or first-byte switch per node); END NODES are the only places anything is pushed; leftmost-first is kept EXACTLY by a static per-node annotation — each end node knows whether a LOWER original index is reachable deeper on its path (min-index of the subtree): if not, COMMIT (push one frame that resumes the walk deeper, jump to the continuation); if so, DEFER (record the end node, keep walking). When the walk dies or a continuation fails, the recorded end nodes on the path — a small bitmask in a slot, each with its known depth = match length — are tried in ascending ORIGINAL index order (lowest set bit first), which is PCRE's order; one frame per alternation attempt instead of one try per branch. Class-leading branches (rule 2) need an NFA step at that node or decline. The study measures (e)'s frame pushes and tries per attempt beside (a)-(d); it is the algorithm the emitter would actually build.** **MEASURED AND MERGED 2026-09-03 13:0x (lane altstudy, 6fbc3f4; studies/alt_dispatch/ + docs/design/alt_dispatch_study.md): ZERO mismatches — five algorithms vs the serial-try oracle over 25,694,396 subject positions. ns/byte on t-128k-sparse (load 1.0-2.3): w-64 serial 190.0 / trie (c) 28.3 / VM-native (e) 27.1 (7.0×); w-256 767.9 / 35.7 / 34.6 (22×); w-512 1,606.7 / 40.2 / 39.5 (41×); w-1024 3,226.6 / 48.1 / 47.3 (68×); w-2048 6,442.4 / 54.2 / 53.5 (120×); sh1-512 and pfx3-512 clear 100×. (e)'s commit rule needed a CORRECTION beyond R1's wording: an end node may commit only if it beats BOTH every accept deeper in its subtree AND the best already-DEFERRED accept on the path — nfa.c's own rule-1 counter-example `abc|a|abd` on 'abd' catches the naive form; fixed, regression-guarded in tests/unit_trie.c, argued in §3.2. On every bench-derived pattern at widths 8-4096 the deferred mask needed ZERO capacity (substring-free pools never put two accepts on one walked path). CAVEAT (§4.4): the study's (a) is a pure unfactored serial try, so it does not reproduce the bench's ×8.87/×20.1 ORDER penalty (real vm_alt interacts with the trie's factoring) — an unbuilt sixth baseline, not a contradiction. NOT settled (§5, nine items): sh1/pfx3 beyond 512 (bench pools cap there); (d) used open addressing, not a minimal perfect hash (an upper bound); rule 2's NFA-step half (DECLINE implemented, one adversarial case); the VM's continuation-backtracking cost (no outer continuation in the harness — a hand-twin on a real resumed frame is the next D77 measurement). RECOMMENDATION: build (e) as the VM's alternation island; the D77 trigger is met at the narrowest width (7× at w-64) and grows with width.**
  [M4.6] (Frank, 2026-08-17, twenty-eighth session, D50 — the
  [ENG-ABS] pattern: build only behind a MEASURED loss). engine_m4.md
  §6.3's strength-1 mechanism was designed and scheduled before the
  [ENG-BREP] ladder existed; possessify.c/revdet.c now deliver much of
  the same frameless-execution win VM-internally, so the emitter build
  waits for evidence that capture-free VM-FALLBACK FRAGMENTS (not just
  loops — the islands' remaining distinct value) are hot: [BENCH-1]
  floors or [ENG-PGO] profiles showing it. (gate re-pointed 2026-09-22:
  [BENCH-1] folded into [OPTLOOP]; the evidence this row waits for now
  comes from an [OPTLOOP] cycle's analysis, e.g.
  docs/dev/optloop/cycle1_analysis.md, which already scores this
  parked row at 0 on the capability subbench — "not in this matrix,"
  not refuted) The DESIGN STANDS unbuilt
  (engine_m4.md §6.3, annotated in place): fragment→NFA→DFA→table-emit
  seam inside the VM, reusing possessify's unique-iteration analysis
  (the proof islands need is eng_brep_design.md §2.2's corrected rule,
  NOT a new analysis — scoping sweep 2026-08-17). Carries its own D46
  stamp+force obligation when built. Accept-list islands (strength 2)
  and tagged automata remain deferred beyond this row per §6.3's table.
- [ENG-COUNT] STATE:not-started — LARGE BOUNDED COUNTS ON THE DFA SIDE (Frank, 2026-08-29 ~00:5x, forty-fourth session: "I'd like an unscheduled plan item to look at options for these forms but right now I'm not sure it's worth it as they seem pretty edge case and there is more profitable ways to spend time"). UNSCHEDULED, not on the loop's queue; opens only on a measured need. THE FORMS: a huge bounded repeat that the DFA engine can only express as states — `[a-z]{0,30000}` is 30,001 states by nature, `a{0,25000}`, `a{0,20000}`; today the tests/resource K7 pin guarantees they COMPILE within D45's budget (a robustness fixture, not an endorsement), and r41 S1 (2026-08-29) priced them for the first time against a mechanism — [ENG-ABS]'s optional anchored machine added +46 % compiler CPU on exactly these shapes (24.3 → 35.9 s on `[a-z]{0,30000}`) and its artifact 1.32 → 1.98 MB, answered by giving the optional machine its own state ceiling. THE VM SIDE ALREADY HAS THE LOOP FORM: the bounded-repeat COUNTER RUNG ([ENG-BREP]/counter-K — one body copy per K iterations + a trailed counter), with [ART-SIZE] STEP 2 choosing K by size and [ENG-CLAMP] the deferred per-quantifier downshift. OPTIONS TO LOOK AT when this opens (no design here): (a) a SELECTION rule on repeat magnitude — above a measured count, a pattern whose size IS a bounded repeat is routed to the VM counter rung rather than a giant table (a selection outcome, stamped, on the [SEL-1] shape); (b) a hybrid COUNTING form — a DFA for the body plus a counter (a counting automaton), the general form, sized against the same size term [ART-SIZE] built; (c) leave it: the shapes are edge cases, the caps and the K7 pin bound the cost. MEASURE FIRST (D77): how many real patterns (bench, corpus, the K41 fuzz population) have a DFA-side count above the candidate threshold, and what they cost in states, bytes, compile CPU and throughput on both engines today — the number that would trigger (a) or (b). Related: [ART-SIZE] (the size term and the caps), [ENG-BREP]/[ENG-CLAMP] (the VM counter), tests/resource (the K7 pin), r41 S1.
- [ENG-CLAMP] STATE:not-started — the DEFERRED per-quantifier K downshift
  (Frank, 2026-08-16, twenty-seventh session: F-1 ruled strict-§4.5 on the
  R25 panel — decisions.md D47 ADDENDUM has the full ruling and rationale).
  Charter: the binary tractability clamp (K = the constant, or 1 — never an
  intermediate value) computed by a BOTTOM-UP SUBTREE-PRODUCT pass, so that
  small-count nested towers (K22's family: every count below K, blowup from
  depth) COMPILE instead of hitting the interim product guard's fast
  refusal. Seeded work, already committed on the counterk lane and carried
  at docs/design/counterk_impl/: the respecified algorithm, the
  clamp_arith.py arithmetic probe (towers d=18/30/35/40 collapse to product
  2 under it; carries the {1,2} tower as a MUST-STILL-REFUSE row), and two
  findings that are this row's design constraints — the PRODUCT rule is the
  right mechanism, not the body-contains-a-repeat shape rule (which
  over-clamps `(a(b|c)?){0,4000}`), and the {1,2}-tower residual needs the
  mandatory+optional phases merged into one loop with a runtime ctr>=m test.
  Opening this row is a Frank event and REQUIRES amending eng_brep_design.md
  §4.5 (a fresh ruling — that is the point of the deferral); until then K is
  one per-artifact constant and the interim guard's refusal is the ruled
  behavior for the tower family. When it lands, tests/vm/run_vm_tests.sh's
  K22 block inverts (refusal -> compiles-and-runs, refusal re-pinned under
  the deny flag) per R25 C1's rewrite plan.
- [ENG-LOOK] STATE:not-started — LOOKAROUND BY PRODUCT CONSTRUCTION IN
  THE DFA ENGINE (chartered by Frank, 2026-08-23 13:5x, on the manager's
  analysis; Frank's framing: "my concern is unnecessary special handling
  code and duplicate code paths. Ideally we implement this in such a way
  that it produces the 'best' possible code while not actually knowing
  about the special assertions it's implementing … is the one character
  optimization the best form, or merely one that works for the immediate
  need?" — it is the minimal instance, not the form). THE FORM: a
  lookaround over a regular body is a constraint on the SUBJECT, not the
  match — `(?<=L)` at p ⟺ subject[..p] ∈ Σ*·L, a property of text the
  forward scan has already consumed; `(?=L)` at p ⟺ subject[p..] ∈ L·Σ*,
  the same property for the REVERSE scan. MECHANISM: the PRODUCT of the
  main automaton with each assertion body's recognizer (Σ*·L forward;
  reverse(L)·Σ* in the reverse machine), folded in during determinization
  so an assertion is a predicate on the product state; bounded lookahead
  in the forward pass as a k-byte delayed acceptance; unbounded lookahead
  in the forward pass DROPPED (superset — sound for end-finding) and
  resolved in the reverse pass or by the VM; the context seeded from the
  bytes before startpos by running the component over the prefix
  (mechanism 4 generalized). TODAY'S assertions machinery (assertions_
  design.md waves B/C: the previous-byte context bit in the state
  identity, the class-indexed accept table, mechanism 4) IS this
  construction for one-class bodies — it becomes an INSTANCE, then gets
  DELETED once DD-11 rebinds \b/(?m)^/(?m)$/\Z to their lookaround
  definitions and the identity gate proves the product's artifact byte-
  identical (or measured-equivalent) to the hand-built one; where it is
  not, that is the optimizer's next target, never a reason to keep the
  special code. REACH: every bounded-length regular-body lookaround
  becomes DFA-eligible — `(?<=foo)bar`, `(?<!\d)\d{4}(?!\d)` — not the
  four assertion spellings only. MEASUREMENT FIRST: state growth — wave
  B measured the one-bit context's composed worst case at 38,009 states
  against the 32,000 cap; the product with larger components is not
  free. Size the component automata on (a) the expanded assertion corpus
  ([M6.6.1]'s substitution driver) and (b) the real lookaround population
  from the PCRE2 testdata and the D27 corpus; the construction ESTIMATES
  BEFORE COMMITTING and DECLINES to the VM past the caps ([ENG-CUT]'s
  shape). SEQUENCE: after [M6.6] ships the VM semantics (the VM stays as
  the exact verifier and the engine for captures/backrefs inside bodies);
  design gate with a D6 panel; the expanded-corpus three-way self-oracle
  is this row's acceptance test as well as M6.6's. The hybrid's
  prefilter soundness argument (dropping lookarounds = superset) is
  M6.6's to state and this row's to exploit. NO one-character fold ships
  anywhere — Frank's ruling: it is the duplicate path.
- [ENG-CUT] STATE:not-started — THE FULL CUT CONSTRUCTION (chartered by the [M6.4.1] design, atomic_groups_design.md §5.5, 2026-08-22; plan row added at the design's merge per R31 D4): replace `A_ATOMIC(X)` with an equivalent cut-free sub-automaton — run X's own priority-first-accept determinisation to fix, per entry state, the single endpoint the cut commits to, and splice that deterministic prefix into the enclosing machine (the primitive is src/ir/dfa.c's priority prune). Plugs into engine_m4.md §5.2's `discharge` socket and therefore BUILDS that socket's plumbing FROM SCRATCH: [TOUR-5] (2026-09-20) DELETED the scaffolding under D77, because since [M4.6] shipped the socket (2026-08) nothing registered a hook and the rewrite arm could not have served one anyway — it set a local `rewrote` flag without ever publishing a new root, which is the live defect in unused code this row used to cite. So this row now owns the analysis fixpoint, its round bound and the root-publishing path as well as the hook body; engine_m4.md §5.2 keeps the design record. D67's contract still applies: output born ANY_ENGINE, copied nodes keep stamps. SIZE ESTIMATE (analytic, unmeasured on real patterns): worst-case exponential, `|D(X)| × |D(rest)|` states against PCREC_MAX_NFA_STATES and the 32,000/10,000 DFA caps — the rewrite must ESTIMATE BEFORE COMMITTING and DECLINE past the cap; a declining rewrite falls back to the VM, i.e. to module atomic-groups. What it buys: capture-free atomic patterns from VM to DFA — the population is the cut-changes-the-language class MINUS what the free discharge rescues, and it is UNMEASURED on real inputs. EVIDENCE GATE (D50's shape, confirmed by the manager 2026-08-22 under the autonomous-run grant): build when a [BENCH-1]/[ENG-PGO]-class customer exists; the measurement that would open it earlier is that population's size and throughput gap on a real corpus. (gate re-pointed 2026-09-22: [BENCH-1] folded into [OPTLOOP]; a qualifying customer is now evidenced by an [OPTLOOP] cycle's analysis, e.g. docs/dev/optloop/cycle1_analysis.md, rather than a standing BENCH-1 worklist) The day it lands, H4 (the match-here entries' rule) is REOPENED (design §4.4 H5), and the lowering must never ignore atomicity (registry.c:615-622's named trap; sabotage S91).
- [M4-CALLOUTS] STATE:not-started (step 1 flip COMPLETED 2026-08-14, merge 84e5956 — all counts held at baseline, K14 check re-scoped to the RS_MODULE population; step 2 behavior awaits M4, its ABI ruled by D38/D39) — module `callouts` (D36: Frank re-scoped
  `(?C` from NEVER to PLANNED, 2026-08-12 — LOW PRIORITY, deliberately in
  the queue boonies). Two separable steps: (1) THE FLIP, schedulable any
  time a lane is free: registry `(?C` row ROADMAP_NEVER → PLANNED, the
  diagnostic moves from "no module will implement" to "requires module
  'callouts'", reject + case11 pins move with it failing-first (NOTE,
  mod08fix lane 2026-08-12: case11 asserts `(?C1)` `roadmap never`,
  `names —`, and the "no module will implement it" status — load-bearing
  against today's tree; they MUST move inside the flip commit or the flip
  lands red), compliance
  prose updated IN THE SAME CHANGE (the K14 prose⇔column check binds them),
  and note the ROADMAP_NEVER live population drops to zero — the never
  branch stays, column-derived, covered the day a second row exists.
  (2) THE BEHAVIOR, M4-hosted and engine-forcing (VM only — the compiled
  DFA erases the pattern positions a callout fires at): static extern
  binding (`extern int rx_callout_n(const rx_callout_block *)` defined by
  the embedding program — compile-time binding, zero cost when absent;
  V-A's compat layer later implements pcre2_set_callout as a trampoline ON
  TOP of this primitive, not instead of it), callback block and return
  semantics (0/positive/negative) mirroring pcre2_callout_block exactly
  (D26-exact tier), fire-point discipline DOCUMENTED as engine-relative
  with PCRE2's own PCRE2_NO_START_OPTIMIZE latitude as the cited precedent.
  AMENDED 2026-08-13 (Frank, composition discussion; syntax and semantic
  choices TBD, his to rule): PROPOSAL — align the callout ABI with an
  anchored MATCH-HERE entry point every generated matcher also exports
  (`(subject, pos, len) -> matched-length-or-fail` shape; OS-0's named
  entry points are the natural vehicle), so a compiled matcher IS a valid
  callout — link-level regex composition. DEADLINE: this is a match-API
  design constraint and must be decided BEFORE M4's match-API freezes
  (same gate as PC-5). Semantics to document honestly: a callout-as-
  submatcher is OPAQUE to the automaton — atomic (no backtracking into
  it) and un-fusable, partitioning the pattern into [M4.0]'s DFA islands
  around call points. TENSION to resolve at design time: this row's
  pcre2_callout_block-exact mirror vs the aligned ABI — possibly
  reconciled by V-A's trampoline direction (the PCRE2-shaped block as a
  compat layer ON TOP of the aligned primitive)
- [M4-SUBST] STATE:not-started — COMPILED SUBSTITUTION (Frank, 2026-08-12:
  the headline xmas item): the `pcre2_substitute` capability as an AOT
  artifact — pattern AND replacement template compiled together into one
  emitted C function (match + splice), first/global modes, caller-buffer
  zero-allocation mode plus an output-sizing mode. **The template compiler
  is almost completely independent of the matcher (Frank's observation,
  recorded because it sequences the work): it consumes only the
  capture-offset CONTRACT, so its design note can precede M4 even though
  end-to-end substitution is capture-gated.** AOT-only win to preserve in
  the design: `$n`/`${name}` references are resolved and BOUNDS-CHECKED AT
  COMPILE TIME against the pattern's own group count — a template naming a
  group that does not exist is a compile error, where PCRE2 discovers it at
  substitute time. Tier the template language: core `$n`/`${name}`/literal
  escapes first; PCRE2_SUBSTITUTE_EXTENDED forms (\u \l case forcing,
  ${n:-default}, ${n:+yes:no}) earn their rows separately under D18's
  earn-its-axis discipline. **BEYOND-PCRE2 DIRECTION (Frank, 2026-08-13:
  "this might be an area where we provide more capability than pcre2"):**
  richer shell-style transforms and C FUNCTION CALLBACKS as template
  segments — a segment that calls an embedder-defined `extern` to render,
  reusing M4-CALLOUTS' static-extern primitive verbatim (compile-time
  bound, zero cost when absent). Discipline: everything past PCRE2's
  surface lives in pcrec's own clearly-flagged namespace (SR-10's rule) so
  the D26 compatibility story stays clean — PCRE2-compatible core,
  pcrec-extended templates opt-in; non-portability is a non-issue for the
  embedded niche, which is compiling in anyway. **RE-AFFIRMED + SHARPENED
  (Frank, 2026-09-06, fifty-sixth session: "extremely fast search/replace
  engine ... no allocation ... stream or in-place ... the replace front
  end would know the group output so it wouldn't have to interpret the
  replacement string. thats pretty cool stuff"): two MODES join the
  charter with their honest boundaries stated up front.** (1) IN-PLACE:
  free exactly when replacement length <= match length (the write cursor
  trails the read cursor and the invariant holds left-to-right); a
  GROWING replacement cannot be in-place left-to-right without a gap —
  the mode's contract states the boundary rather than letting a caller
  discover it. (2) STREAMING, with the AOT-specific win: a streaming
  replace needs a HOLDBACK buffer for a partial match at the chunk
  boundary, and pcrec knows the pattern's maximum width AT COMPILE TIME
  (cwmax/maxw, already load-bearing for lookbehind) — so a bounded-width
  pattern's artifact STAMPS ITS OWN REQUIRED HOLDBACK CAPACITY (caller
  supplies a fixed buffer of stamped size, zero allocation; the
  [DD-14.FB] BufSurface precedent for caller-provided capacity), and an
  unbounded-width pattern REFUSES streaming mode honestly by name. No
  runtime engine can promise a compile-time holdback bound; it falls out
  of the architecture. Positioning note: this is a completeness-category
  differentiator (memory pcrec-post-spine-direction, 09-06) — subst is
  where full PCRE semantics + AOT beats both the subset engines (no
  captures/templates) and pcre2 (runtime template interpretation)

## The 2026-09-17 code-review refactor (Frank, 2026-09-18: fix-now, then the waves, in order; the guided tour happens AFTER all refactoring)

Authority: `docs/dev/reviews/2026-09-17-code-review.md` (the synthesis — §1
fix-now order, §3 wave plan, §4 rulings all made: D104-D108 + DD-8). Two
method rules bind every wave brief (synthesis §3): anchor populations are
FLOORS re-grepped on the wave's own tree; re-INDENTATION breaks anchors, not
relocation — price "does the moved text keep its column". Checkpoint battery
between waves (D102); box slots handshake with the bench.

## Design-debt ledger (from R1; resolve before the milestone that hits each)

- [DD-2] STATE:not-started — VM engine match/step limits (with M4 design) (R1 A-8). DOWNGRADED by D22: adversarial patterns are out of scope, so this is a ROBUSTNESS feature (a pathological pattern should fail honestly rather than hang), NOT a security boundary, and it must not be designed as one or traded against execution speed. AMENDED 2026-08-14 (D42.6): the row names TWO bounds — the step budget (a step = one backtrack resumption, counted only at the fail label) AND the backtrack-frame/trail capacity that allocation-freedom forces — different failures, different diagnoses (RX_ERR_STEPS vs RX_ERR_FRAMES on the search entry; −1 on match-here per D42.3). Design: engine_m4.md §4
- [DD-9] COMPLETED 2026-08-14 (D42.8) — DECIDED by engine_m4.md §8: the DFA-prefilter/VM hybrid does NOT own case (f) and structurally cannot (the pattern is capture-free, so every piece of M4 machinery is inert on it by the same design that guarantees zero regression). Row archived in plan_completed.md; case (f) is now the known HEAD of [BENCH-1]'s prioritizer worklist, carrying engine_m4.md §8.4's three findings (computed goto is the WRONG lever there, contra this row's old hint; ~2x of the 6.61x gap is the reverse pass; bit-parallel shift-and is the algorithmic candidate, detectable from the built DFA). M4 still owes the family a non-regression floor: the capture-bearing sibling joins the bench at M4.6 (engine_m4.md §8.5)
- [DD-8] STATE:started (OPENED 2026-09-17, Frank, sixty-seventh session, for the TABLE-MECHANISM ADOPTION sub-task — D106) — `--emit-ir` / `--emit-dot` promised in APPROACH §6, never built (R2-A7). OPENING NOTE (D106): `--emit-ir` already SHIPS (the listing, via `vm_render_listing`); what opens is adopting `docs/spec/table_contract.md` for its output, sequenced as a CUSTOMER of the 2026-09-17 code review's wave-1 emission kit (lens 10) — gated on the kit's table-row primitive (`sb_field`/`sb_row`), so the IR sections emit THROUGH the kit rather than a one-off producer. The derive-from-the-emitter's-own-walk constraint (the `VEvent` stream: `vm_lbl`/`vm_push_at`/`vm_set`/`vm_rung_mark`) is absolute — adoption re-shapes the RENDER of that stream, never adds a parallel table producer. SCOPE RESOLVED (D106 addenda, Frank 2026-09-17): MACHINE-FIRST TSV output (a wrapper pretty-prints); the PROGRAM body fits columns (`label|op|args|target|note`) so it is just another `#section` — NO sibling line-oriented contract needed, all one mechanism. `--emit-ir` stays a DEBUG tool (not elevated to a lossless IR); its completeness gap is fine for a debug listing (D106 addendum item 2). Q3 folded in: column WIDTH is not a contract, so `emit_vm.c:8165`'s slot[48] truncation is free to move in wave 2. Prefilter/pure-DFA listing sections are FUTURE (D106 addendum items 3/4; content largely exists via D105's state legend). BUILD CONSTRAINT (D108): this adoption + the wave-1 emission kit are built as a data-in/text-out back-end text layer with a clean walk->event->render seam, forward-compatible with the future IR back-end (D106 item 5 / retargeting to Rust/C++/JS, addendum 2) WITHOUT building it now. `--emit-dot` and any enriched-trace ([V-H]) sub-parts stay not-started within this row. UPGRADED from filler 2026-08-14: Frank REQUESTED the VM tracer as a debug facility — engine_m4.md §10's form (emitted-program listing: labels, choice points with preference order, capture slot assignments, island boundaries, callout call sites; plus an optional resume-frame push/pop trace for one subject). Schedule with [M4.5]'s bring-up; §10's one constraint stands — the dump derives from the same structure the emitter walks, never a parallel description. Companion facility ruled with it: `--engine=dfa|vm|auto` (engine_m4.md §5.6) is do-or-die (clean refusal, no fallback) and, per the R21 E-6 fix, `--engine=vm` disables the prefilter so the two engines are independently comparable end-to-end (Frank's stated use: compare results between the two). TABLE-CONTRACT CONSIDERATION (Frank, 2026-08-21): when this row next opens, decide whether --emit-ir's tabular SECTIONS (slot legend, label table) adopt docs/spec/table_contract.md, and whether the listing as a whole gets a sibling line-oriented contract (header-declared section structure) — its consumers' extractors went stale once during [M6-READ] and declaration-based parsing is the durable fix; the derive-from-the-emitter's-own-walk constraint holds regardless DELIVERED 2026-09-19 (lane dd8): the table-contract adoption LANDED — `--emit-ir` renders as nine named `#section` TSV blocks, the PROGRAM body among them (`label|op|args|target|note`), through the wave-1 emission kit's `sb_row`, derived from the same `VEvent` walk as before; `docs/spec/ir_listing.md` is the new format contract and `table_contract.md` lists `--emit-ir` as a conforming producer (its third, and the first with no anonymous table). Every reader re-pinned in the same change: 11 test scripts converted to declaration-based parsing through `tests/lib/table.sh` (which gained its row-reading half), `run_ir_listing.sh`'s 16 `irsb` baselines deliberately recaptured and the file gained a nine-section header-truthfulness arm (128 -> 144 checks), 6 sabotage rows re-driven solo. NOT an abi event: 0 movers over 2,803/2,804 corpus rows at both `.c` argv shapes and the composition arm, against a pinned branch-point binary, with the `--emit-ir` stream itself as the positive control (2,804 of 2,804 moved). STILL NOT-STARTED WITHIN THIS ROW: `--emit-dot`, the DFA/prefilter listing section (D106 addendum items 3-4) and the enriched `[V-H]` trace. MERGED b99d0eed (--no-ff, 2026-09-19 02:5x, seventieth session): review accepted — the emit_vm.c diff touches vm_render_listing and its tables ONLY (grep of the hunks), .c byte-identical on three streams with a positive control (the --emit-ir stream itself 2,804/2,804 movers), 11 extractors converted to declaration-based reads (0 re-pinned as greps), 6 rows re-driven solo DETECTED (S41/S42 found by the grep beyond the brief's four); post-merge build/strict/alloc green, run_ir_listing 144/0; merge-gate `make test` launched detached (3h bound). Two findings carried out of the lane: the no-nullable-collapsed prefilter diagnostic route is unreachable by any input (cross-noted on [OPT-4.1]'s row — dead diagnostic arm, not a correctness bug, so no known_issues row) and S88's SAB_DOC_FIGURE prediction reads 0 on its atomicdiff arm at first measured drive (owner re-records). The -o basename trap struck a fifth time (dd8 §3.1) — [BSWEEP]'s script carries the fix. The row stays started for its future sub-parts (--emit-dot, DFA/prefilter sections, [V-H]); the adoption sub-task is DONE.
- [DD-13] STATE:started **PARKED 2026-09-21 into [BACKLOG-TRIAGE] (Frank, seventy-fourth session: "agree on sequencing" — the bench matrix ranks it against everything else; not before [REL-1]).** (STAMPS HALF CLOSED 2026-08-26 00:0x — BATTERY GREEN on 32890e2: test 1,555 checks / 1 load-red (endvar identity, solo 2,353/2,353), K32 corpus cell solo 1,634/0, san clean both axes 1h31m, matrix 180/0/6/0 with UNREACHED 0 at 50346fc (docs-only past 32890e2); bench inbox I-5 sent with the pin. The FORMAT half [DD-13b] stays not-started. STAMPS SHIPPED 2026-08-25 78249e6, lane srStamp — D81: `RX_ENGINE` unconditional, `RX_DFA_SCAN`, `RX_DFA_PREFILTER` with five values incl. the -bounded pair; abi 3→4, D76's first non-layout event; run_dfa_stamps.sh 16/0 with four MEASURED validation reds; corpus 995 DFA: none 380 / memchr 327 / byte-class 176 / memchr-bounded 61 / byte-class-bounded 51, unanchored 815 / attempt 180. The bench's I-3 blocker is cleared once the battery is green (inbox I-5). D6 PANEL r37 (docs/dev/reviews/2026-08-25-r37-dd13-stamps.md): both core claims survived independent measurement; two SCOPE gaps chartered as [DD-13c] (lane srStamp2: the `empty` scan value for the four no-loop artifacts; the VM HYBRID's inlined DFA scan stamps its form); the rest landed 55165a9. THE HAZARD, four sites: any check that used a stamp's ABSENCE as the DFA discriminator broke when RX_ENGINE became unconditional — two in codegen and one in parse (fixed by the lane), and tests/atomic_groups §3 (10 reds, found by the d8608ca battery, fixed at the landing); the discriminator is the VALUE. NEXT stamp candidate: the M2.1 self-loop skip count (family (b), naming owed). The FORMAT half of this row ([DD-13b]) is unchanged — not-started.) — formerly STATE:not-started — INPUTS FROM pcrec-bench (pcrecdev2, 2026-08-24 ~23:5x; Frank ruled the bench-set format BLOCKS on [DD-13], scope NARROW — today's .rxt parsed as-is, per-sub-bench tags in a plain sidecar of R-BENCH fields, no grammar): /home/duxevents/pcrec-bench/docs/design/requirements.md v3 (commit 8c34498) §5 (sub-bench DIRECTORY model + sidecar), §4.4 (per-(pattern,testee) OUTCOME enum), §4.5 (a declared VARIANT per engine — identical results on every subject, the sub-bench's OBJECTIVE preserved: a subroutine sub-bench may not be satisfied by inlining), §3 (per-sub-bench REGIME), a sub-bench-level OBJECTIVE field — for [DD-13b] to absorb when it opens. THE UNIFIED PATTERN-SOURCE / TEST FILE
  FORMAT (Frank, 2026-08-17, twenty-ninth session; name TBD): ONE file
  format, grown from .rxt, serving every consumer that today would need
  its own file kind: (1) the [V-E] MANIFEST — a compilation SOURCE for
  pcrec (N named patterns → one emitted unit, perhaps several; [V-E]'s
  named definitions, cross-references via subroutine referencing, and
  the D39.2 appended-numbering rules all bind here); (2) the TEST
  CARRIER — cases exactly as .rxt carries them today, co-located per
  named pattern ([V-G]'s bottom-up subpart testing rides this); (3) the
  BENCH SET format for ~/pcrec-bench (its APPROACH.md §8 Q1 resolves
  HERE) — which forces the format to understand DIFFERENT ENGINES /
  CONFIGURATIONS, not just pcrec. Features Frank named at creation:
  OPTIONS/CONFIG blocks including EXEMPLAR FILE REFERENCES (large
  subjects/corpora live in external files, referenced rather than
  inlined); FILE INCLUDES (so case sets can be extensive and
  MACHINE-GENERATED without bloating the hand-written source, and so
  per-engine/per-configuration fragments compose for the bench use);
  NAMED patterns referring to one another (subroutine referencing).
  PROCESS, staged and gated — the format is hard to change once
  adopted ("we should get it right"), so it is built DESIGN-FIRST like
  the K23 arc:
  - [DD-13a] STATE:completed 2026-08-17 (lane dd13a, merged 52e2702 —
    docs/design/dd13_format/requirements.md: census 54 files / 1,100
    blocks / 9,977 expectation lines; compatibility answered DIALECT
    with R-COMPAT-1; OD-1..5 carried unresolved; five tensions, seven
    anti-requirements, panel attack list) — REQUIREMENTS note:
    enumerate every consumer's needs
    measured against real corpora — the .rxt harness as-is
    (docs/testing.md), the machine-generated D27 sets, [V-E]'s
    manifest + finder, [V-F]'s transformer target, [V-G]'s user
    testing, M4-SUBST templates if they intersect, and pcrec-bench's
    set needs (feature tags, hazard classes, per-case
    expectation-verification method, engine/config sections). The
    compatibility question is answered here: is .rxt a subset, a
    dialect, or migrated?
  - [DD-13b.W1] STATE:completed (CLOSED 2026-09-19, seventy-first session, per docs/dev/plan_audit_2026-09-19.md: delivered, tag stale — Frank\'s ruling on the audit) — formerly STATE:started (W1.1 BATTERY-PROVEN 2026-08-30 15:46:59, battery 4 on cf86987 — test 1,901/0, san 34 scripts 0 reports 102 min, mech 200 rows 0/6-expected/0/0, the eleven S194-S204 rows DETECTED; r46 panel's fixes on lane/w11f pending merge; CHARTERED 2026-08-30 ~02:2x, forty-fifth session part 9, lane w1 / `lane/w1`, opus, on Frank's "keep going on plan items" after his Q2-Q6 ratification 4d12a81) — WAVE 1 of the format (format_design.md §1.4): `name`, `description`, `lib`, `target … [with]`, `encoding`, `features only`, `config` (pcrec/flags/features/encoding/engine/budget + `from`), AST-level composition per D87 with §1.5's three pattern-level extensions and `--emit-composed`, `rx_info.name`, H11's target build path. DESIGN-FIRST: step 1 is an implementation note (`docs/design/dd13_format/w1_impl.md`: what lands where, the check/sabotage plan, the D80 spec deltas, the merge points) reviewed by a D6 panel BEFORE any code; step 2+ the HEAD grammar + parser, then the composer with assigned numbers, then targets. STATUS 2026-08-30 ~02:4x: deliverable 1 (bf843a7/9506e8d) reviewed by panel r45 (docs/dev/reviews/2026-08-30-r45-w1-impl.md — 7 blockers across three lenses, all dispositioned); revisions 2-2.4 closed every finding (ca50878, 2,080 lines; DESIGN PHASE COMPLETE 02:48); [W1.1]+[W1.2] start on the hold's lift per w1_impl.md §7; [W1.3] on Frank's Q-W1/Q-W2; [W1.4] buildable with one new mechanism (the activation bound). The consumer that earns it: [LIB] (all three parts) and [DD-14]'s multi-pattern files (D77 at wave granularity). No W2/W3 production is built ahead of its consumer. — RULINGS 2026-08-30 (forty-sixth session, D89): Q-W1 ADOPTED with the caller-visible half WITHDRAWN (the wrapper's number is INTERNAL; a lib's groups are non-capturing to the caller — `(?:…)`-rewritten when the lib does not reference them, HIDDEN above ngroups when it does; delivery BY NAME only); Q-W2 REFUSED for W1; the Q7 residual ACKNOWLEDGED as structural (K42); PCRE2's grouplist arguments TBD (Frank's candidate syntax on record). W1.3's charter folds the §2.6/§2.7/S9b revision; W1.4's re-check re-anchors §2.8 to PCRE2's grouplist semantics. — [W1.2] STARTED 2026-08-31 (forty-eighth session, lane w12 / `lane/w12`, opus, on the I-25/I-27 unblock + Frank's close ruling; w1_impl.md's "abi 12→13" is stale — abi is already 13 via [OPT-5], the lane writes 13→14 and the final number is assigned at merge serialization). **[W1.2] MERGED 6dbdf41 + BATTERY-PROVEN 2026-09-01 22:42 (battery w12 on 1989c62, abi 15 assigned at the merge behind [CC-CLANG]'s 14; mech 213 rows clean; K44's cell green-by-diagnosis)** — formerly DELIVERED GREEN 2026-09-01 02:24, awaiting merge review: branch `lane/w12` head d03bcaa, 22 commits — full §5 acceptance measured (N targets/N prefixes/one name; H11 three-way agreement; features union with its population repaired; four refusals; F9+nentries; abi 14 at four sites, FILEPIN 0bc6884; gate 16/0 (A)+(B) 0-differing all axes; rxtsource 94/0, C1 unchanged; codegen 5/5 on the fixed tree). THREE defects found by its own validation (519 B/artifact trailing-comment prose; a §1.3 refusal contract never met due to truncation; the fix's path-doubled re-truncation) — journal 09-01 has the record. Manager's ratified lane calls re-openable for Frank at merge: file-wins-over-flag (narrow, RXTFLAGS-consistent), lib existence-STAT. artifact_size_log moves DOWNWARD tree-wide at the next full corpus run (the 519 B fix — expected).
  - [DD-13b.W1.3] STATE:started **PARKED 2026-09-21 into [BACKLOG-TRIAGE] (Frank, seventy-fourth session: "agree on sequencing" — the bench matrix ranks it against everything else; not before [REL-1]).** (**DELIVERED AND VALIDATED 2026-09-04 04:2x — lane w13, branch `lane/w13`, abi 20 (18/19 spent ahead of it), 24 commits, awaiting merge review.** SUITES: `make test` 33/33 sections 0 failures (2,444 PASS, 68 min); `make test-axes` 21 axes ALL answer-identical 0 mismatches + the form census; `make test-definitions` 22/0 (first run ever); `make test-rxtsource` 121/0 with the corpus census UNMOVED at 191/3,325/26,894; the identity gate 16/0 — (B) 2,276 call-free patterns 0-differing at the new pin, (A) 2,226 against the unchanged pre-module pin; `make strict` clean. FRANK'S D89 ADDENDA 1-4 LANDED IN FULL: `export` (default nothing), the three call forms `(?&name)`/`(?&site=name)`/`(?&*=name)` all MEASURED refused by libpcre2 10.46 before adoption, site-qualified rows, the flat import keeping PROVENANCE with the sort keyed on SCOPE (manager's ruling), the per-composition erasure, and RETENTION — a delivering site keeps what its callee matched, verified against the addendum's own inlined-body oracle. The flat injection of every named group is REMOVED. TWO ITEMS ARE W1.3.1 and the report's §18 says why each: the run.sh composed-block path (not contained — four sites in flush_block's tail assume the `rx` prefix, plus a contract question W1.2 deliberately settled the other way) and `(?&site.group)` (measured free; needs a reference class resolving after composition). ONE PREDICTION WRONG, recorded: [M6.5-DUPNAMES]'s order check did NOT go red, because its population is single-pattern artifacts where the new leading SCOPE key is constant — so it does not cover the new ordering, and tests/definitions does. docs/dev/lanes/w13_report.md §§14-18. Formerly BUILT, NOT SUITE-VALIDATED — lane w13, branch `lane/w13`, 2026-09-03 17:00-17:5x, written entirely under the evening box hold, which forbade every test script.** 9 commits. WHAT EXISTS: the composer (`src/parse/rxt_compose.c`, new) with D89's THREE TIERS — delivered/hidden/ERASED, the last of which section 2 of w1_impl has no mechanism for, so the re-basing offset became a MAP that degenerates to `+base` when nothing is erased; the `lib` closure actually READ (W1.2 resolved existence only), a fixpoint keyed on the resolved path with a duplicate-definition refusal; the name grammar admitting `-`/`.` with `pcrec_rxt_prefix_from_name` as the ONE home of the mapping and `target = <def>` deriving the prefix, all THREE legs moved together (rxt_source.c, run.sh:1492, verify_rxt.py); `groups[]` sorted `(ref-is-NULL, name, number)` so the primary's rows are a genuine PREFIX and `nnames` keeps its meaning. MEASURED TONIGHT: w1_impl section 2.5's cell reproduced (dd = `(\d)\1` into `^(\d)-(?&dd)$` matches 5-77, rejects 5-75); **nentries > nnames for the first time** ({ "word", 2, 2, "local" } with ngroups 0, nnames 0, nentries 1); the ERASED tier worth one slot against the naive textual append (NCAPS 4 vs the PCRE2 control's 5, whose groups[] additionally exposes the wrapper name to the caller with ref NULL — what D89(2) forbids); **IDENTITY (A) 81 artifacts / 0 differing** against a compiler built from main 9d8401a, across the default path, --no-captures, --engine=vm, --engine=dfa and 59 corpus patterns; the altwide dogfood LOSSLESS on all 33 with a byte-for-byte round trip, 90 bench ids re-measured (O-13 said 77) and the only prefix collision `floor` being CROSS-SET; the altwide compile census 19 built / 12 size-cap / 2 module, 14 refused under --features all. `make strict` clean. OWED (the hold): make test, test-codegen, the identity gate, `make test-definitions` (the new section), the battery, and the ABI BUMP — the branch does NOT bump the number; w1_impl section 8.7 carries the D94 grep, SIX readers of 17, of which a hand-enumerated four would have missed two. QUESTIONS: Q-W3 (is every named group in a definition delivered? implemented YES), Q-W4 (a definition does not inherit the target's config), Q-W5 (`target = <name>` rather than an implicit target per named block); and the run.sh composed-block path, deliberately NOT written blind — docs/dev/lanes/w13_report.md. CHARTERED 2026-09-03 ~17:3x by Frank: "I'd like to see progress on it. My hope is we can dogfood it with the bench subbench definitions as well as our own usage. Let's add a lane for it" — lane D of the evening wave, opus, feature column). COMPOSITION PROPER + THE DOGFOOD: (1) a definition's own named groups injected into the composed artifact (format_design.md §2.6/§2.7, the S9b revision W1.3's charter folds): `groups[]` grows rows the primary did not declare, sorted BELOW the primary's by the (ref-is-NULL, name, number) key so the primary's rows stay a PREFIX and `nnames` keeps its meaning; `nentries` (appended at abi 15 for exactly this) becomes > `nnames` for the first time; delivery BY NAME only; a lib's groups non-capturing to the caller unless referenced (the ratified Q-W1); the K42 residual (absolute refs / colliding names have no external oracle) stays acknowledged, with a collision REFUSAL where the format can see one. (2) DOGFOOD, the bench's sub-bench definitions as .rxt SOURCES: the bench's O-13 §4 found the `.rx` → `.rxt` pattern-line encoding lossless on all 77 patterns but 63 of 77 `pattern_id`s are not C identifiers — MANAGER'S SYNTAX RULING (dd13b syntax is the manager's, 2026-08-29): block names ADMIT `-` (and `.`), mapped to `_` in the C prefix with a collision refusal when two names map to one prefix; so a bench set becomes an .rxt source verbatim, declaring NO config/flags/engine/budget/encoding (D93 file-wins would otherwise pin their testee matrix from inside the set). Lane D lands one such fixture (altwide@0.2's pattern list as a source with `name` blocks) under tests/rxtsource/ and compiles it via `--source`/`--target`, and asks the bench (through the manager) for the `.rxt` exporter on their side once the names are accepted. (3) OUR OWN USAGE: one of our own corpus directories gains `lib`/`target` composition over a shared definitions file (tests/definitions is the natural one), proving the composed artifact answer-identical to the flat one (C1's identity proof extended to composed sources). Validation BY DAY (suites, the identity proof, the battery at merge; an abi event if `groups[]`'s emission changes shape — it does: the injected rows). W1.4 (PCRE2 grouplist semantics) stays after. **DELIVERY MODEL REVISED 2026-09-03 18:2x EDT (Frank, D89 addendum): (1) numbered references inside a definition always work (D87 rule 1, reaffirmed; w13 M1); (2) a library EXPORTS names explicitly — `export <ident>{, <ident>}` block-line (manager's spelling), default none, unknown name a refusal; unexported named groups fall to the hidden/erased tiers w13 built; (3) delivered groups are SITE-QUALIFIED via the delivering call `(?&site=name)` / `(?&=name)` (format_design §1.5 B3): rows named `site.group`, one set per delivering call, so one definition can be delivered twice — W1.3's flat injection of lib names is WITHDRAWN; (4) the plain `(?&name)` is the no-delivery form (PCRE2's own; no `*` spelling). OPEN (Frank): does the delivering call RETAIN the callee's exported groups in W1.3, or is retention W1.4's (manager recommends: rides the delivering call). Lane w13's tomorrow changes accordingly (rulings file).** **MERGED 2026-09-04 08:1x as 8d68ddc2 (abi 20; chain running). EXCLUDED: the branch's regenerated artifact_size_log.tsv (regenerated once on the merged tree after the battery). W1.3.1 OWED: the run.sh composed-block path (docs/dev/lanes/w13_runsh_composed_path.patch; contract question for Frank: the target's prefix through flush_block vs the CLI allowance W1.2 refused) and `(?&site.group)`; W1.4 next.** **W1.3.1 RULED 2026-09-04 08:4x (Frank): OPTION 1 — the harness carries the target's prefix through flush_block's tail (the CLI allowance W1.2 refused stays refused); build it AFTER [PFX-1] STEP 0 (D96) so the four prefix sites are the exported set only.**
  - [DD-13b] STATE:completed (MERGED daa6b6c, 2026-08-29 ~17:4x, forty-fifth session: docs/design/dd13_format/format_design.md, 2,443 lines, revision 2 on the r44 panel + D87 — HEAD/BODY shape, 13 line-kind additions + `description` (a FIELD, `|` block scalar), the three PATTERN-level extensions settled by measurement against libpcre2 (`(?<3>…)`/`(?<name=3>…)`, `(?&^.name)`, `(?&from=email)`/`(?&=email)`), composition AST-level inside pcrec with assigned numbers (D87), the struct view → [V-I], the five-member piece rule, H11 the target build path, `--emit-composed`; the census 179/3,265/26,691 reproduced to the digit by the panel; Q7 (the oracle control's coverage) accepted for W1 with its trigger. Q2-Q6 RATIFIED by Frank 2026-08-30 ~00:0x as recommended (review file's closing section) — the note has no open questions. NEXT in the feature column: the W1 PARSER + composer ([DD-13b.W1], to be chartered — starts on Frank's word). Lane dd13b, opus, 2026-08-29 14:4x-17:3x) — DESIGN note: grammar + semantics (include model, config
    scoping/precedence, reference/namespace rules, exemplar-file
    addressing, the machine-generation contract), the migration story
    for the existing ~10k-case corpus, and single-vs-multiple emitted
    outputs.
  - [DD-13b.panel] STATE:completed (CLOSED 2026-08-29 ~17:4x: docs/dev/reviews/2026-08-29-r44-dd13b-format.md — r44-grammar confirmed the corpus invariant by run; r44-sem found TWO BLOCKERS on both oracles (absolute refs inverted by relocation; `(?J)` shadowing) + the D61 contradiction; r44-consumers the bench seam gaps; Frank ruled the blockers the OTHER way (D87: renumber, own groups win, the caller-scope prefix, the struct view); revision 2 measured both fixes; syntax settled by the manager) — formerly STATE:started (2026-08-29 ~15:1x, forty-fifth session; FRANK 14:5x: "spelling and syntax details are your decision" — keyword names, line spellings, selector forms (e.g. §0.3 D-e `analysis freq <name>` over §6.5's `freq <name>`, ACCEPTED by the manager on the grammar-ambiguity argument) are ruled by the manager; Frank rules SEMANTICS (what composes, what is refused, what a caller observes) and the §7 questions; review r44 — three read-only critics: r44-grammar (sonnet: §1 grammar re-parsed over the 179-file corpus, ambiguity, keywords, waves, §3 migration), r44-sem (opus: §2.3 EXPAND vs libpcre2 10.46 + pcrec — DEFINE-at-end numbering, capture-transparency's cost to [LIB], closure order, lexical shadowing, §6.0 anchors, config precedence, targets), r44-consumers (sonnet: §4 seams incl. consistency with r43's TAG ruling, §4.5 bench field-by-field, §2.10 freq/provenance, §2.11 accounting, §5 attack answers, §7 the other side). The design note LANDED on lane/dd13b 937c1b9 (1,657 lines): HEAD/BODY shape, 13 additions, `(?&name)` ALWAYS a PCRE2 subroutine call with the definition closure appended as `(?(DEFINE)…)` at the END (D39.2's numbering obtained from PCRE2, MEASURED cells E-J), capture-transparent, four namespaces / one refusal rule, per-option-kind config precedence, waves W1/W2/W3; census re-run 179/3,265/26,691 (3.3× [DD-13a]); THREE corrections to our inputs — the position paper's §3a does not match (anchors in a called body bind the SUBJECT), OD-5's atomicity premise is FALSE on 10.46, `--replace` does not exist; five admitted departures (§0.3); six §7 questions for Frank with recommendations) — D6 ADVERSARIAL PANEL on the design, then Frank's ruling. (Renamed from [DD-13c] 2026-08-25: that tag now names the STAMP SCOPE follow-up below.)
    NO parser is written before (c) closes.
  Scheduling: after the scale work ([M4.6]/[M4.7]) per Frank's
  2026-08-17 sequencing; (a) is read-only fact-gathering and safely
  early-schedulable in a session with spare capacity, but does not
  start unprompted. FRANK'S DESIGN INPUTS accumulate ahead of (a) in
  docs/design/dd13_format/frank_inputs.md (append-only, with the OD-n
  open-decision ledger): per-engine option placement, the
  last-reference-wins options CASCADE over ordered includes, declared
  per-library pattern tweaks, configuration sections unifying bench
  testees with build variants (avx2-vs-baseline), and the
  interface-vs-reference-only pattern distinction. **BENCH-DISCOVERED CANDIDATES (2026-08-25 ~01:0x, pcrecdev2, measured on 8da6120, recorded here as the bench's inputs row — not started):** (a) the DFA artifact has NO prefilter stamp — `RX_VM_PREFILTER` is VM-only and a DFA artifact's only `#define`s are `RX_ALTCLS_*`, so the byte-class skip loop (the email specimen's headline ~23× mechanism) is invisible to the bench except by reading the emitted loop; candidate: an `RX_DFA_PREFILTER`-style stamp in the D46 family (D46: every selection point observable). SECOND INSTANCE (manager, 2026-08-25 03:58): DFA artifacts carry no `#define RX_ENGINE` either — `rx_info.engine` is the only readable selection field on a DFA artifact; the D46 macro family is VM-side on this tree. Same fix, same customer. (b) the `(?:P)\z` form's DFA skip loop is WEAKER than the plain form's — same `rx_can_begin_match` table, but the loop runs `pos + 1 < len` and cannot early-exit at end of subject because the end-of-subject view state (`rx_forward_end_view`) must be evaluated, so the final byte is never skipped; a slightly higher per-scan cost in the match regime, NOT an engine-selection effect; candidate optimization: an end-view the skip loop can fold. Control it verified: `a|ab` on `ab` — anchored `==n` NOMATCH, `(?:a|ab)\z` [0,2), libpcre2 ANCHORED|ENDANCHORED (0,2): the [OS-4] asymmetry is real. **FIRST PRODUCTION SAMPLE (2026-08-25 ~03:3x):** /home/duxevents/pcrec-bench/reports/2026-08-25-email-specimen-0.1-budu-ryzen1600.md (records bf4a415, pin 8da6120 = pre-wave-G); manager feedback and the ranked sub-bench list are in journal part 28 — Frank rules on priorities. **RULED (Frank, 2026-08-25, fortieth session):** sub-bench order 1 log-line search / 2 wide alternations / 3 lookaround+backref / 4 bounded-repeat band / 5 UTF-8 (pcrec-bench inbox I-2); the DFA `RX_ENGINE`/prefilter stamp gap is the first pcrec-side lane after [CHK-1] (a scaffolding change: abi 3→4 + gate re-pin, D76); the edit-test loop's three bench features (scratch tier, `quick`, `pcrec-local`) are bench work, inbox I-4; the durable channel is D78.
- [DD-4] STATE:completed-in-place (2026-08-19: [M6.2] wave D answered it — start_max = startpos as a third compile-time string, no wrap toggle needed, §4.1/§4.3; the find-all sentence is in match_api.md §3.1; row retained here rather than archived because its text is cited from DD-7) — formerly STATE:not-started — \G / global-iteration semantics vs startpos (with M6) (R1 A-11)
- [DD-6] STATE:not-started — multiline ^/$ as DFA state context — interacts with state budget (with assertions module) (R1 A-6)
- [DD-11] STATE:started (BATTERY-PROVEN 2026-08-30 05:17 — battery 3 on the pin 96e44c2 (san 34 scripts / 0 reports incl. the definitions driver once its SANFLAGS were fixed 467c449; mech 189/0/6/0/0; solo codegen 198/0, cli 287/0; the definitions oracle 354 cells / 0 disagreements in every run since); `--list-definitions` archived by the bench at [B19]; the row stays STARTED only for the M6.6-gated follow-on [DD-11.5]/[DD-11.6] — everything chartered for this row is DONE. IMPLEMENTATION [DD-11.1]-[DD-11.4]+[DD-11.4b] MERGED 0f5a98f 2026-08-29 ~23:0x, lane dd11b (acbdeb7): RegRow.definitions + the TAG enum with ONE exhaustive-switch evaluator (containment by grep check), DefKinds DEF_IDENTITY / DEFK_STR / DEFK_BUILDER (with a template in the row) / DEFK_TEXTFN (operand-parameterized, calling the existing decoder) / DEFK_ROW (alias chaining: `$`→`\Z`→`(?=\n?\z)`), a per-entry `operand` for the 14 POSIX names, RK_BARE (a new no-doorway RegKind for `^`, `$`, plain `(`), six RS_BASE literal-escape rows, `--list-definitions` (the fifth registry surface, `--flavour`, registry.md §9 / cli.md / table_contract.md), the structural + containment checks, the OPTION-MATRIX SELF-ORACLE (tests/registry/definitions_oracle_*: 354 cells / 202,488 comparisons / 0 disagreements — A==B via pcrec, A==C via libpcre2; it caught `\ca` decoded without uppercasing and a scanf trap), the recursion-guard fixture; `\Z` and `$` found NOT core under full reduction (the structural check); `\N{U+}` a noted tautology until UTF mode; `\Q…\E` lexical, excluded; guards re-measured from runs throughout. [DD-11.5]/[DD-11.6] stay gated on M6.6 (follow-on row at charter). Union battery 3 pending. DESIGN NOTE MERGED 3cb6721, 2026-08-29 ~15:3x: docs/design/definitions_table.md after the r43 panel (docs/dev/reviews/2026-08-29-r43-dd11-definitions.md — 5 blockers fixed; the predicate is a TAG from a closed enum with ONE exhaustive-switch evaluator in src/parse; Frank's class-escape + literal-escape families are rows; census 28 replacement rows with a RegRow home + 9 base-tier literal escapes with none — RULED by the manager: they get minimal `RS_BASE` rows, one mechanism (D24), [DD-11.4b]; Option A `RegRow.definitions`; `--flavour` yes; close after [DD-11.1]-.4, [DD-11.5]/.6 a follow-on row when M6.6 lands carrying r43-S3's `pcrec_ast_stamp` precondition). IMPLEMENTATION opens in the same lane: [DD-11.1] the table, [DD-11.2] `--list-definitions`, [DD-11.3] the option-matrix self-oracle with libpcre2 as co-equal leg, [DD-11.4] the synthetic-`\w` resolver test, [DD-11.4b] the base rows — sequential, each gated by its checks; the merge battery is the manager's. 2026-08-29 ~14:4x, forty-fifth session, on Frank's "go on all three"; lane dd11, sonnet, worktree worktrees/dd11 — the D86 ADMIN/STRUCTURAL lane; deliverable docs/design/definitions_table.md: the replacement INVENTORY with file:line, the core set, placement A (RegRow list) vs B (own table) measured against D82 rule 4 / D62, the PCRE2 hazards oracle-checked, `--list-definitions`'s shape, the [DD-11.n] sequence; D6 panel gates the code) — formerly STATE:not-started (D85 RULES THE SHAPE, 2026-08-29: the replacement model is a PREDICATE-SCANNED TABLE on the [ENG-FORM] forms model — an ordered list of (option-scope predicate → definition in core syntax) rows per construct, first applicable wins, last always applies; `--list-definitions` walks the same table; each row is a unit for checks and sabotage; the core rx set becomes the minimal set the optimizer must know. Table placement — the registry rows expanded, or its own — is the design note's question. FRANK ASKED 2026-08-29: "we created the rx replacement model (e.g. `$` is replaced depending on options) — can we `--list` those replacements?" — NOT TODAY, and the reason is the deliverable: the replacements are code arms (`$` → N_EOL/N_EOL_M in src/ir/nfa.c:539 off the parse-resolved multiline field; the possessive-suffix desugaring in parse.c; the NEWLINE constant; D66's assertion expansions exist only as lookaround_design.md §6's corpus GENERATOR), so a dump written now would be a hand-copied restatement — the drift hazard `--list-syntax`/`--list-axes` were built to avoid (learnings §3: one derivation, two readers). WHEN THIS ROW LANDS THE DEFINITIONS AS DATA (construct × option-scope → definition), `--list-definitions` walks that table as the FIFTH registry surface, and the same table is what [DD-13b]'s `name`/`lib` resolution and the [LIB] store consume. CUSTOMER ADDED 2026-08-21, D66: the
  tranche C engine items — D63's second prefilter instance and DD-7's
  reverse BOT variant — are RE-BASED onto this row's core-reduction
  work; Frank's direction is to optimize the CORE lookbehind-anchor
  form the reduction produces, so (?m)^'s desugar
  `(?:\A|(?<=\n)(?!\z))` and every other lookbehind-shaped anchor share
  one optimizer; the start=0 `^` is ALREADY `\A` internally, and a
  one-byte fixed lookbehind lowers to the wave B/C context machinery,
  which becomes the core form's compilation target — see D66) — the
  NEWLINE CONVENTION axis (Frank,
  2026-08-12 tenth-session close). Q1 RULED 2026-08-18 (D64): NO axis
  declared; LF stays hardwired through the assertions module, whose
  newline-reference sites are written DEFINITION-SHAPED (a handed-in
  class, LF the sole definition — never scattered '\n' literals); the
  future shape parked here is newline as a TYPED compile-time
  definition consumed by insertion machinery (class-valued: negatable;
  sequence-valued: class-context use is a compile error) with boundary
  consumers as lookaround uses — revisit after M6.6 + the first
  insertion producer. **GENERALIZED (Frank, 2026-08-19, thirty-fourth
  session, mid-[M6.2]): the definition idea extends beyond `\n` to the
  ASSERTION FAMILY.** `$`, `^`, `\b` (and their multiline/caseless/
  encoding variants) become scope-resolved DEFINITIONS: a construct
  searches up the tree for a rebinding (a flag like `(?m)` = a local
  rebinding of `$`/`^`'s definition in its subtree), falling back to the
  default [SUPERSEDED 2026-08-23, RULED by Frank (journal, thirty-seventh
  session part 4; found stale by R34 C3): resolution is PROPAGATE /
  CAPTURE-AT-BUILD, NEVER WALK-UP — the binding point is a sibling event,
  not an ancestor, and injected subtrees must be position-independent; the
  "searches up the tree" phrasing here describes the SCOPE a binding
  covers, not the lookup mechanism] — the replacement value is an inserted rx (`\Z`≡`(?=\n?\z)`,
  `(?m)$`≡`(?=\n)|\z`, `(?m)^`≡`\A|(?<=\n)(?!\z)` — note the `(?!\z)`
  term IS the U11b carve-out, `\b`≡`(?<=\w)(?!\w)|(?<!\w)(?=\w)` with
  `\w` itself a definition). Value per Frank: shrinks the core rx set
  and simplifies the additions path — "we optimize insertions and we
  optimize them all." Manager assessment recorded with it: D62's
  parse-time resolution is already a degenerate form of this (cx->mods =
  the tree search, the node field = the resolved binding); the win is
  the ADDITIONS path (correct-by-composition on day one, folded fast
  path earned later) plus a SELF-ORACLE property (expansion vs folded
  implementation as an in-tree differential once lookaround exists); the
  cost truth is that folding work relocates to a composition RECOGNIZER
  rather than disappearing — today's zero-cost DFA mechanisms (context
  bits, class-indexed views) must still be reached; and the U11b lesson
  binds: a definition is a CLAIM ABOUT PCRE2 like any other — the tidy
  composition for `(?m)^` was exactly wrong until measured. Same
  parking condition as before (M6.6 lookaround + DD-14's call/insertion
  primitive); cross-noted at [DD-14]. **REFINED (Frank, same session):
  bindings are DIRECT REFERENCES TO RX in the appropriate format — NOT
  flags.** The environment maps construct -> rx VALUE directly; a flag
  letter like `(?m)` is a BINDING-MUTATION OPERATOR at its point of
  introduction (it swaps which replacement rx `$`/`^` are bound to, for
  the remainder of its scope); every later reference just DOES the
  replacement — no modifier state exists to thread, consult, or forget.
  Frank's stated value: simpler, and new replacements (encodings,
  newline conventions, user definitions) are added by adding bindings —
  one mechanism. Manager notes recorded with it: (a) this SUBSUMES
  D62's principle at the future architecture — there is no field either;
  a multiline `$` IS the substituted subtree, so the whole D47.5
  scope-blindness class becomes INEXPRESSIBLE rather than guarded
  (stronger than the compile-alarm the node-kind spelling offered);
  (b) "appropriate format" carries D64's typed-value constraint
  (class-valued where class context demands, negatable; sequence-valued
  a compile error in class contexts); (c) scoping matches PCRE2's own
  inline-flag semantics (references AFTER the mutation see the new
  binding, within the enclosing group); (d) HYGIENE — RULED BY SCOPING
  LAW, NOT MECHANISM (Frank, same session): call vs ref is TBD as an
  implementation choice, because inserted groups arise in REFERENCED rx
  either way and the law answers both: **groups inside an insert are
  locally referenceable BY NUMBER only (an insert's \1 is the insert's
  own first group; outer numbering does not see them), and globally
  referenceable BY NAME** (a named group in an insert is addressable
  from anywhere by name). Numbering is scope-local, names are global —
  which composes with D59/D61's caps-slot architecture (insertions
  append; primary prefix permanent) and [M6.5]'s dupnames machinery
  (name-run resolution already answers what a globally-visible inserted
  name means);
  (e) the U11b measurement obligation binds each binding's VALUE;
  (f) SCOPE OF THE REDUCTION (Frank, same session, confirmed by
  measurement): quantifier sugar is ALREADY collapsed (`+`/`{1,}` etc.
  are one A_REP node, engine bodies byte-identical, only the verbatim
  rx_info.pattern stamp differs); the class escapes are already
  hardwired class-valued bindings (D23/OS-1), \N already the newline
  definition (D64); possessive quantifiers reduce to the atomic cut
  per [M6.4]'s own row. The irreducible core after full reduction:
  classes, cat, alt, {m,n}+preference, atomic cut, capture, \A, \z,
  lookaround, and the path-fact family (\K, backrefs, DD-14 call) —
  \G stays primitive (position vs a RUNTIME value). THE DESIGN LANE'S
  FIRST WORK PRODUCT is the construct-by-construct table: primitive vs
  binding, each binding's value MEASURED against libpcre2;
  (g) OPTIMIZATION CONCENTRATION (Frank, same session): a tight core
  focuses every fold/prefilter/rung on few primitives and bindings
  inherit them free — [M6.2] lived the counter-case, five constructs
  each needing a hand-built folding mechanism;
  (h) THE CORE AS A TARGET IR FOR OTHER FLAVOURS (Frank, same session):
  grep/BRE-ERE, POSIX, oniguruma map as front end + BINDING LIBRARY over
  the same core — [V-D]'s translators and [V-A]'s regex.h shim become
  cheaper than their rows assumed (cross-noted there). HONEST LIMIT: a
  flavour's MATCHING DISCIPLINE does not reduce to bindings — POSIX
  leftmost-LONGEST vs PCRE2/oniguruma leftmost-FIRST is preference
  semantics of the core primitives themselves; a POSIX front end needs
  either a core longest-match mode or documented leftmost-first
  semantics. Measured, not assumed, per the U11b lesson.**
  pcrec is NEWLINE_LF today and that is
  ANCHORED, not assumed: every oracle measurement runs libpcre2 at
  options=0 (build default LF on this box), so \N's generated bitmap is
  the measured complement of {0x0A}, `.` is every-byte-but-0x0A, and `$`
  is before-final-\n; a convention change on either side fails PC-4 and
  the census probe loudly. PCRE2 makes newline a per-pattern CONVENTION
  (CR/LF/CRLF/ANYCRLF/ANY/NUL via the start-only (*CR)-family verbs or the
  API option; \R separately via BSR) — pcrec refuses the verbs cleanly
  today (Q1 tables, start-only), so the axis is closed off LOUDLY, no
  miscompile. COST PREDICTION when a consumer arrives (D18 earn-its-axis):
  `.`/\N fold into the front end per-convention like OS-1's caseless
  (byte-set swap, oracle-generated tables, zero engine cost) for CR/LF/
  NUL/ANYCRLF/ANY; the ENGINE work is `$` (and DD-6's multiline ^/$) —
  an EOL assertion that becomes set-valued under ANY/ANYCRLF and a
  TWO-BYTE SEQUENCE under CRLF, in both the forward and reverse DFAs
  (the M2.7/M2.12 EOL-variant machinery is single-byte shaped), and
  CRLF also complicates `.`'s complement. Decide with the assertions
  module or a real consumer, whichever asks first; measure the
  convention's effect on the censuses through the existing probe
  pipeline before writing any table
- [DD-14] ARCHIVED → docs/dev/plan_completed.md ("2026-08-25 (thirty-ninth session — [DD-14]: module `recursion` ships at D69 tier)"): the parent row and every sub-row A, A2, BC, D, LB, E, EMPTY, F, D27, K34, FB, G, CLOSE — all completed. Close summary: journal thirty-ninth session part 45. Open rows that came out of it stay in plan.md: [MECH-REACH], [TT-10] (widened), [OS-4]'s customer note, [DD-13]'s bench candidates, K36/K37 (known_issues). Archived 2026-08-25 by the manager.
- [DD-3] STATE:not-started — generated-API versioning/compat policy for vendored consumers (before M3) (R1 A-10)
- [FREESTANDING] STATE:not-started (BOONIES TIER — Frank, 2026-08-28: "I think memchr requires glibc. If so, there should be a switch for a no glibc build which in our world is a simple scalar replacement on one of the forms for prefilter. File for boonies") — THE LIBC-FREE EMISSION PROFILE. FACT CHECK first: `memchr` is ISO C (`<string.h>`), so every HOSTED libc has it (glibc, musl, newlib…); the target this row serves is a FREESTANDING build (`-ffreestanding`, no libc at all — kernels, bootloaders, bare-metal firmware, the "freestanding/embedded build profile" [M7.0] already names). SHAPE: one emitter switch (`--freestanding`, a generation axis with its stamp, [CHK-2]'s five things) under which the artifact calls NOTHING from libc: the candidate-scan forms that call `memchr` (the k=0 memchr form and [OPT-K]'s offset-set scan at k*) get a scalar byte loop in the same [ENG-FORM] slot — a SELECTION, not a special case — and the D82 bar applies (the scalar loop is a different representation object, measured, not a `#ifdef` inside the memchr one); every other libc reach in the emitted text is censused and replaced or refused: the emitter string literals today carry `memchr` (20 sites), `memset` (18), `memcpy` (16), `strlen` (18), `memcmp` (2), `malloc`/`free` (10/30 — the default frame/trail storage of the un-suffixed VM entries; under freestanding only the `_in` entries with a caller-provided buffer exist, and the un-suffixed ones are refused or emitted as thin `_in` wrappers over a static region, ruled at design time), and `--emit-main`'s driver (stdio) is simply unavailable. MEASURED on filing (the fuzz witness, a VM artifact with prefilter "none", split form): its .c + .h reach NO libc symbol at all and include only `<stddef.h>`/`<stdint.h>` — so a prefilter-less VM artifact is ALREADY freestanding-clean, and the switch's real scope is the DFA/hybrid artifacts' `memchr` scans plus whatever the un-suffixed entries' default storage and the driver pull in. The census of which sites reach a NON-main artifact (vs only the driver) is the row's first step; gcc's own `-ffreestanding` build of the corpus artifacts under the switch is the gate (a link with `-nostdlib` and a stub `_start`, or at least `-ffreestanding -fno-builtin` compile + `nm` showing no undefined libc symbols). Related: [DD-5] (`--std-c` portable emitter fallback — the same family of "emit for a smaller world" switches; the two should share their axis plumbing), [M7.0], [DD-14.FB] (the `_in` entries are the freestanding calling convention already). Not before the spine (queue discipline); no measured need yet (D77) — recorded so it is not re-derived.
- [DD-5] STATE:not-started — --std-c portable emitter fallback (switch-based) (R1 R-5)
- [CC-CLANG] STATE:started (STARTED 2026-08-31 forty-eighth session, lane cc / `lane/cc`, sonnet, steps 1+2 — Frank's close ruling "proceed with queued items" is the charter; abi bump written 13→14 in-branch, final number assigned at merge serialization) — **DELIVERED GREEN 2026-09-01 01:32, awaiting merge review**: branch `lane/cc` 19 commits, FOUR real bugs found+fixed by the pinned validation order (see cc_report.md + journal), codegen 106/106, gate 16/16 five times, CLANGGEN=1 shipped + 5,425 assertions clean, CC=clang survey zero warnings; frameless population bigger than this row's witness (bare `(a)b`, fully-spliced chains) — fold into the row at merge (Frank, 2026-08-30 17:4x: "i installed clang on this machine. we should do some basic compatibility testing and consider a partial bench axis to see if there is a difference"; PROBED the same hour, manager, scratch only) — CLANG AS A SECOND COMPILER FOR THE EMITTED CODE. MEASURED (clang 21.1.8, -O2): a DFA/memchr artifact compiles (one cosmetic warning: `__attribute__((noclone))` unknown to clang) and AGREES with gcc on every probed cell; a BACKTRACKING VM artifact (`(a|ab)+c`, 3 `&&label` sites) compiles with ZERO warnings and agrees cell-for-cell; a recursion artifact agrees. THE ONE REAL INCOMPATIBILITY: a FRAMELESS VM artifact (counter rung, no push sites — probed `[a-z]{0,4096}` --engine=vm) still emits the generic resume dispatch `goto *run->resume_stack[i].resume_label` with no address-of-label expression in the function — gcc accepts, clang errors ("indirect goto in function with no address-of-label expressions"). STEP 1 (small): emit the dispatch only when a push site exists (or a dummy label reference), plus guard `noclone` for clang — BOTH are emitted-scaffolding changes = ONE abi bump, D76's four sites, ritual not avoidance (memory pcrec-abi-changes-pre-release). STEP 2: an opt-in corpus sweep with clang as the COMPILEE axis (the GENCFLAGS/`gen_run` hook precedent from SAN-1 — e.g. `make test CLANGGEN=1`-shaped; writes nothing to build/), plus `make CC=clang` on the compiler itself surveyed once (gcc stays the target, D2). STEP 3 (behind Frank's perf hold): the bench's PARTIAL cc axis — the same artifacts compiled clang vs gcc on a few cells (their build, our ask; goes in I-22). Column: ADMIN, after [M4-QUOTING]. **UNPREDICTED EFFECT, reported by the bench 2026-09-02 (reports-lane flag; ledger in O-14): the forced-VM route is ~×9 FASTER on simple frameless bodies at 1989c62** (bounded floor `:` on t-letters-064k: 174,405 → 19,383 ns/call; email floor ×8.5-8.9, loglines ×4-4.5, search ×2.3-2.8, match ×1.5, hex32/uuid ×1.1; controls flat to four figures) with a −402 B artifact split on 36 simple bodies. MECHANISM (I-31, from emit_vm.c:9482-9560): step 1's has_push gate omits the whole pop-and-resume block INCLUDING the computed goto on any program with no RX_PUSH site and no linked call; a function without a computed goto lets gcc optimize the scan loop whole-function (regalloc/loop opts), which dead-code removal alone would not explain. Not a charter goal, not measured on our side; cited from the bench's durable O-14 per D78. auto's selection unmoved (DFA ×17 ahead on the floor cell); the named follow-up measurement is any frameless VM artifact on the auto arm (declined prefilter / view).
- [DD-10] STATE:not-started — remaining unbounded C-stack recursion in the compiler (R3 critic, critic-perf): trie_build now has an explicit 256-frame/68 KB budget, but compile_ast and clo_visit's t1 edge are still bounded only by pattern structure. A 400-nested-branch-point alternation needs ~192 KB — fine on an 8 MB main thread, not on a musl 128 KB one, and pcrec is a library. Convert clo_visit to an explicit worklist and give compile_ast a stated budget, then the NFA cap can be derived from memory alone

## Option-specialization dimensions (D18) — each must EARN its engine

The caller names a SET of values per dimension and the product is over those
sets (D18). A singleton set is fully specialized and compiled away — asking for
case-insensitive ONLY is hyperspecialization, not an axis. A dimension is an
axis only when its set has 2+ elements. Each dimension below is a candidate for
that case; before it becomes an axis, measure whether specializing buys
anything, since a dimension that folds into the front end, is free at run time,
or is a pure wrapper is NOT an axis even when the caller names it plural. Predictions are in D18 — record the measurement against
the prediction, including when the prediction was wrong.

- [OS-0] STATE:not-started — the optional ENGINE FINDER module (D20). Given the SETS, drive the engine generator once per point of the product and emit the selector over the results. Its set-valued request surface lives HERE, not in `pcrec_options` — D20 deletes the API-change half of this step, because a generator that only ever compiles one point is correctly served by scalars. Two properties from D18 to preserve and to test structurally: dispatch resolves ONCE per search call and never reaches the hot loop, and a request with no plural dimension emits byte-for-byte what pcrec emits today (no dispatcher, no extra parameter, no indirection). Emit named per-combination entry points (`rx_search_ci_utf8`) as well as the selector, so a statically-known caller pays no dispatch at all. DEFERRED BY DESIGN: build this only once a dimension has actually survived D18's earn-its-axis test with a measurement behind it — if the OS-1/OS-2 predictions hold, it has no customer yet, and that is a good outcome rather than a stalled one
- [OS-2] STATE:not-started — encoding ascii/utf8: PREDICTED to fold, since APPROACH §4/§10 already commit to byte-wise UTF-8 automata with no hot-path decode, explicitly so ASCII and UTF-8 share one DFA emitter. Measure when M5 lands: is the emitted hot loop byte-identical in SHAPE between the two encodings for an equivalent pattern? If yes the axis collapses; if the UTF-8 path needs its own loop, that is a real axis and a surprise worth recording
- [OS-3] STATE:not-started — streaming: PREDICTED NOT to be a wrapper, and this is the one prediction with evidence already against the optimistic answer — the reverse pass rescans backward over bytes a stream may no longer hold. Feeds M3.0's design gate; do not write streaming code before it is settled
- [OS-4] STATE:not-started — anchoring: ENG_UNANCH vs ENG_ATTEMPT is ALREADY a cartesian split, and it has never passed this test. It exists because the reverse machine cannot check `^` at pp == 0, not because a per-start attempt loop was measured to be faster. Measure the cost of the split on the known-slow shape (`^` on only some branches, D8) and decide whether to close it by building the reverse BOT variant (DD-7) or to keep it with a number attached. An unjustified axis in the shipped compiler is the strongest possible test case for D18's own rule. **FIRST CUSTOMER (2026-08-25, pcrecdev2 / pcrec-bench):** the bench's whole-subject compliance regime is PCRE2 `ANCHORED|ENDANCHORED`; pcrec has no end-anchored entry, so the adapter's `_match_caps(...) == n` is sufficient-not-necessary (a leftmost-first prefix match that could reach the end by backtracking answers NO). Ruled idiom for now: compile `(?:orig)\z` (NOT `$`, which admits a final newline at options=0) and use the anchored entry — a separate artifact, so bench rows must name which one they timed. `ENDANCHORED` is the GENERATION-AXIS disposition ratified in docs/pcre2_options.md:103; this row is where it would be built. **RULED (Frank, 2026-08-25, fortieth session): NOT NOW — leave the idiom.** The axis is built later UNDER MEASUREMENT if the bench shows the two-artifact cost or the final-byte skip gap at rank; until then `(?:P)\z` + the anchored entry is the answer, and the final-byte fold stays a general-optimization candidate on [DD-13] (a fold on the idiom benefits every `\z` user, not just the bench). Reason in Frank's words: no artificial timeline; if we would be better served building it later under measurement, wait and see, and focus on builds we don't have to rebuild or roll back. This row's ORIGINAL charter (measure the ENG_UNANCH/ENG_ATTEMPT split's cost) stands and comes first — a sibling axis before that measurement is the un-measured cartesian growth D18 forbids

## Parser structure — the syntax construct registry (D24)

**THE AGREED ORDER (R6, 2026-08-10) is COMPLETE — the FIX-1 / PC-3+Q1 / FIX-2 /
Q2+SR-9 / MOD-0 / DOC-1 / PC-4 arc it sequenced is done and archived in
docs/dev/plan_completed.md.** Work these in sequence. Each is a
checkpoint: critic panel (D6), journal entry, plan STATE update, touched
CLAUDE.md files, commit, push.

Sequenced so each step pays for itself before the next is justified. SR-1/SR-2
collapse a duplication that has already produced one shipped bug; everything
after waits for a forcing function. Frank's priority stands throughout: the
95% path stays fast and simple, and exotic constructs earn only the right to be
named, cleanly rejected and queried.

- [SR-5] STATE:not-started — guard the fast path CLAIM, do not just assert it.
  REWRITE THE ASSERTION FIRST (R6): the claim as written here — "base-tier
  patterns must perform ZERO registry lookups (`(?:` excepted)" — is FALSE in
  both directions, measured with an instrumented build on 2026-08-10. `(?:`
  performs zero (parse.c answers it before the registry), while `[abc]` performs
  one and `[a-z]+@[a-z]+\.[a-z]{2,4}` performs three, because the class-bracket
  doorway is on the base-tier path. Written as specified, SR-5 would fail the
  moment it was built, or would have to assert something untrue. The property
  actually worth guarding is a BOUND: lookups <= one per non-negated `[` plus
  one per `[` inside a class, and zero for a pattern with no character class. Use
  an instrumented build with a lookup counter, the way run_trie_identity.sh
  uses `-DPCREC_NO_TRIE`, so no counter exists in the shipped build (TS-1 would
  reject one anyway). Pair with the M2.9 compile-time budgets
- [SR-6] STATE:not-started — MODULE HANDLERS move to their own module TUs as
  each module lands (as-built naming: src/parse/mod_*.c flat in src/parse/ —
  mod_modifiers.c, mod_verbs.c — not the src/parse/ext/*.c subdirectory this
  row originally predicted; R18 docs critic. The verbs entry below remains
  accurately PENDING: MOD-0.4 moved the doorway/tables to mod_verbs.c but no
  verb produces yet, so SR-6's real per-verb handler has not landed)
  (esc_class, esc_assert, esc_backref, esc_uniprop, esc_misc,
  grp_lookaround, grp_named, grp_atomic, grp_cond, grp_recurse, grp_modifier,
  grp_callout, verbs, cls_posix). Not a step to schedule — a rule to follow
  when a module is implemented. NOTE (R4 critic finding): the row does NOT yet
  name a handler — that field arrives with SR-2 — and the status vocabulary has
  no value meaning "implemented by module X"; RS_MODULE unconditionally implies
  rejection today. SR-6 therefore carries an unwritten schema change, not just a
  file move
- [SR-7] STATE:deferred — FLAVOURS (families as named masks: `pcre2-10.46`,
  `pcre2-dfa`, `python-re`, `ere`). Deferred by D18's earn-its-axis rule
  applied to the front end: exactly ONE flavour-varying row is known (`\v`), so
  the selection machinery has a set of size one and no customer. The column
  exists from SR-1; it turns on when a second flavour earns it. Note
  `pcre2-dfa` is the ENGINE-capability axis expressed as a family, so this step
  and DD-7/M4 are related. BLOCKER TO PLAN FOR (R4 critic finding):
  `pcrec_registry_find(kind, sel)` takes no flavour argument and returns the
  first row matching a byte, so SR-1's "short chain for the rare flavour-varying
  byte" is not expressible in the shipped shape — SR-7 must change that
  signature. Today a duplicate selector is caught loudly by tests/registry/
  rather than silently shadowing, so this is a design debt, not a live bug
- [SR-8] STATE:deferred — ENGINE-capability check moves OUT of the parser.
  Today `\1` is rejected by the PARSER as "requires module 'backrefs'", but
  backrefs parse fine and simply cannot LOWER to a DFA. When M4's VM exists the
  honest diagnostic becomes "requires the VM engine", which is a lowering-time
  check against the registry's `engines` column. Blocked on M4 having a second
  engine to choose between. **DISPOSITION (2026-08-17, [M4.7a] + manager
  redirect):** the VM exists now, and lane/m47a found this row's premise
  does not hold in code — `\1` is refused for lack of an implementation
  (no producer), not for engine incapability, so there was never a
  parser-side engine check to relocate; the parser's refusal wording is
  already correct and unchanged. The lowering-time CONSULTATION this row's
  charter describes is DISCHARGED-BY-ARCHITECTURE today rather than built:
  zero producers means zero customers (D18/OS-0/D53), so
  select_engine.c's socket stays unpopulated machinery on purpose, and it
  LANDS WITH THE FIRST VM_ONLY PRODUCER (whichever M6 module gets there
  first) rather than ahead of one guessing at a contract from sample size
  zero. Until then, tests/registry/registry_check.c's
  check_engine_capability_tripwire guards the gap: it asserts every
  VM_ONLY-masked RS_MODULE row has no wired producer, and its failure
  message on the day that stops being true names building this
  consultation as the required next step. This row's own STATE stays
  `deferred` on that basis — not blocked on anything further, but not
  something to build ahead of its first real customer either
- [SR-10] STATE:not-started (CROSS-NOTE 2026-08-23, from the [DD-14] ASK 3 discussion: Frank's 'eat our dogfood' idea — the registry's `syntax` column as a PATTERN pcrec itself compiles, so the row table becomes machine-checkable (witnesses derived from the syntax, a differential over the column) — belongs here/[DD-13] as a CHECK on the table, explicitly NOT as its dispatch, which R6 ruled byte-keyed with measurements.) — SINGLE NAMESPACE DEFINITIONS (Frank,
  2026-08-12: "do we have a single set of 'modules' or 'encodings'? we
  should, and then those should be directly referenced — this enforces
  existence over everyone using string names"). One authoritative table per
  namespace — MODULES, ENCODINGS, (post-D37) NAMED FEATURE SETS, and
  flavours when SR-7 lands — with every renderer and parser of a namespace
  member referencing the table entry (enum/identifier + its one string),
  never a loose literal. Existence becomes a compile-time property: a
  diagnostic cannot name a nonexistent member because the name is not
  reachable except through the table. The both-directions checks then guard
  table⇔docs instead of table⇔scattered-strings. MOTIVATING INSTANCE
  (R20/0.8c): src/core/compile.c:97 hand-wrote "requires module 'utf8'" —
  a member of no namespace — while cli/main.c separately hand-mapped
  "utf8"→PCREC_ENC_UTF8; slice 3's reword fixes the instance, THIS row
  fixes the class. Audit inventory at start: every `module '` /
  `--features` / `-e` string site, the enabled.c parser, the registry
  module column, compile.c's encoding gate
- [DOC-BM] STATE:deferred — **the bound-mode document** (design §18.5):
  full 32-bit option sweep with seeded generators, the `RS_NOT_OFFERED`
  split, the `EXTRA_BAD_ESCAPE_IS_LITERAL` 18-cell migration. Constraint:
  must exist BEFORE §7.1's five escape rows land (their `status` values are
  its output; A1's pins hold the surface meanwhile).

  ~~STATE:blocked (2026-08-11 — the R11 design panel refuted parts of
  D30, exactly as R10 refuted D29, and the resolution is Frank's call.** See
  `docs/dev/reviews/2026-08-11-r11-parse1-mod01.md` and D30's inline R11 marks.
  NOTHING WAS BUILT; the panel ran against a written design and every finding
  cost a paragraph rather than a commit. **Seven dispositions** are listed at the
  end of R11 and they are the specification for the re-resolution, which wants a
  D32 the way D30 answered R10. The three that change the most work: (1) D30
  §2's non-optional check is FALSE as written — "promise a module wherever
  libpcre2 DISPATCHES" has 93 counterexamples in 1,672 probes and ALL 93 are
  pcrec being CORRECT, because "dispatched" does not imply a module is owed;
  (2) rank is almost entirely UNCHECKED — 20 of 22 rows accept any value to 250,
  the single prefix pair is a THRESHOLD not an ordering, and two of D30's three
  required checks fire on identical boundaries in all 5,632 probes, so one of
  them adds nothing; (3) the returning-doorway defect PARSE-1 handed over is
  FOUR call sites across three doorways, and `pcrec_ext_escape`'s pair is
  UNDEFINED BEHAVIOUR — making it return makes `build/pcrec` itself SIGSEGV on
  `[a\qb]` while `a\qb` silently launders the pointer out of `%rax`. Of the
  group-discard class, 7 of 18 generated patterns are byte-identical to a
  SMALLER pattern and 0 of 18 behave as the contract promises.
  Measured facts that survive and should be reused: 100 rows / 18 tails /
  exactly 4 multi-row buckets holding all 18 tailed rows = 22 rows (D30's own
  figure, independently derived); D30's undocumented 0/25/40/70 rank mapping
  recovered and verified 22/22 two ways; `ext.c` never reads `.tail` so its six
  call sites need no change; **[RETRACTED — see R11's addendum: `find()`'s
  same-length tie-break is UNREACHABLE, because it needs an identical
  `(sel,tail)` pair which `registry_check.c:184-193` already forbids, and rank
  would NOT make it loud — two duplicate rows at ranks 25 vs 26 resolve
  silently]**; and existing
  external coverage of tailed rows is 2 prefixes, not the 10,200 probes it
  looks like) — the interface, as D30
  resolves it. **DECLARED RANK**: a row carries an integer rank; every
  recogniser in the bucket runs; the highest-ranked ANSWERING row wins; two
  answering rows at EQUAL rank is the defect. Rank is DATA, so no recogniser
  needs to know its siblings and the bare fallback answering "always" at rank 0
  is correct rather than naive. The doorway's answer is **not an enum** but
  three facts — (dispatched?, compiles?, whose message?) — so that `(*MARK)`'s
  "CLAIM the construct, name NO module" has a cell. Plus `tail_default` and its
  row parameter, and bucket dispatch. THREE checks, and all three are required:
  (a) the **per-row `syntax` check** (C4-6) — a row's own `syntax`, fed to its
  bucket, must be won by THAT row and no other; it is TOTAL over 22 rows, needs
  no generated space and no oracle, and it is the primary instrument; (b) the
  **rank sweep** with `check_tail_precedence`'s **LIVENESS CLAUSE CARRIED OVER**
  — measured, inverting the one prefix-related pair is observable on exactly ONE
  input in 176,544, so a sweep that asserts nothing must SAY so rather than
  print a PASS; (c) the **reachability differential** against libpcre2 — *pcrec
  must promise a module wherever libpcre2 DISPATCHES* — which is the only thing
  that covers the malformed-body class and is NOT optional. Prototyped and
  sabotaged before adoption; see D30 §1-2 for the numbers

## Optimization waves (D21) — algorithmic, then profiled code, then compile time

Not a milestone: a shape applied at appropriate points, in this ORDER. Profiling
a bad algorithm optimizes the wrong loop, and optimizing compile time before
execution speed trades the primary goal (D18) for the secondary one.

- (Frank, 2026-08-12, on this whole block: "this project's greatest benefit
  will be its testing suite. builds confidence and lets us go crazy when we
  get to optimizations" — suite strength is the PREREQUISITE INVESTMENT for
  everything below; an optimization the suite cannot referee does not land)
- [BENCH-CEIL] STATE:not-started — THE EXPERT-C CEILING ARM (Frank,
  2026-08-18, thirtieth session: "for a set of (dfa?) patterns, i'd
  like to directly write performant C code to implement them and
  consider them one engine to compare against... ideally we should be
  able to match the performance but realistically i'm hoping we can
  get close or at least understand how far off we are"). For a chosen
  set of bench patterns, HAND-WRITTEN performant C matchers ride the
  bench as their own engine arm. WHY THIS ARM IS SPECIAL: unlike
  pcre2-interp/jit (different execution architectures), hand C shares
  pcrec's exact execution model — same compiler, same flags, same
  box — so the pcrec-vs-hand gap DECOMPOSES into attributable
  generator overheads (table dispatch vs specialized control flow,
  per-byte machinery vs wide compares/memcmp, prefilter choices), and
  the per-case gap table is the OPT waves' most direct worklist ("hand
  code does X, the emitter does Y" — the [OPT-A] literal-run and
  spelling-menu leads of 2026-08-18 are exactly the first two entries
  this arm would have generated). Rules recorded at filing: (1) v1
  scope is DFA-TIER patterns (hand-writing a correct backtracking/
  capture matcher is a different order of difficulty; VM-tier cases
  optional later, and a hand VM case would first need its semantics
  pinned against the spec); (2) hand code must be OBSERVABLY
  span-identical on the case's subjects — compare.sh's cross-engine
  agreement check already enforces this per case (INVALID, not timed,
  on mismatch), which is the correctness bar; (3) any technique a C
  programmer would write is allowed — that is the point of a ceiling —
  but each case documents its technique so gaps stay attributable;
  (4) same harness, same pinned-run discipline, floors optional (the
  arm is an INSTRUMENT like the prioritizer, not a gate — hand code
  regressing means nothing about pcrec). SCHEDULING (Frank's stated
  timing): after the main spine, with the benchmarking/perf-iteration
  phase — alongside [BENCH-1]'s build-out and feeding the OPT waves;
  a THIN early slice over today's a-m cases is cheap if wanted sooner.
  Authorship is expert-lane work (opus tier) or Frank's own hand;
  cross-links: [BENCH-1] (home instrument), [OPT-A]/[OPT-SIMD] (gap
  consumers), [ENG-PGO] (hand code embodies workload knowledge = what
  PGO should recover mechanically), D52's pcrec-bench (a hand-C testee
  triple is also natural THERE later; the in-repo arm is the
  fast-iteration instrument). (gate re-pointed 2026-09-22: [BENCH-1]
  folded into [OPTLOOP]; the home-instrument role is now the bench
  repo directly plus an [OPTLOOP] cycle's analysis, e.g.
  [OPTLOOP.1.analysis], docs/dev/optloop/cycle1_analysis.md)
- [OPT-3] STATE:started — **STEP 1 MEASURED 2026-08-26** (lane srOpt3, docs/dev/opt3_dfa_scan_measurement.md; measurement only, nothing under src/): the SKIP loop is NOT the loss and SIMD is NOT the fix — it skips ZERO bytes on `t-b`/`t-c` (entered 190,651 times on t-b, skipping nothing: the byte that returns the machine to state 0 is consumed by the TRANSITION loop first, so it can only skip the 2nd..nth byte of a run, and real text's runs are length 1), and a 7x-faster shufti skip makes all three bench subjects SLOWER (+3.9/+0.4/+1.5%); the SIMD crossover is measured at ~32-byte non-candidate runs, which these inputs never reach. ALL the cost is the TRANSITION loop at ~3.2 ns / 10.7 cycles per table step (t-a is 2.000 steps/byte — forward + reverse — hence 6.19; per-match fixed overhead ~0, the extra pass IS the reverse scan), LATENCY-bound on a 7-cycle lea/lea/movslq/load chain with ~2x spare issue width (2 independent streams: 1.97x). LEVER, measured on the real artifact and answer-identical over 40,469 answer lines / 91 subjects: pre-multiply the transition table by its stride — **1.28x on the bench's own three subjects, 1.466x -> 1.149x vs PCRE2-JIT, beating JIT on t-c** (gate on L1 residency, which binds before the short-overflow at states*stride > 32767; surveyed — only the [01]*1[01]{n} explosion family reaches either, and its table is 2.3x L1 by then). Remaining t-a gap is entirely the second pass. STEP 2 is that emitter change. **STEP 2 BUILT 2026-08-26** (lane srPremul, docs/design/premultiplied_dfa_table.md; abi 6 -> 7, all four sites; `<PREFIX>_DFA_TABLE` = premultiplied/indexed/mixed/none + `-fno-premul-table` bit 15, masked out of rx_info.flags; no rx_info mirror, with the trigger named — measured: pcrec-bench's shim.c has no accessor for abi 6's own scan/prefilter either): **MEASURED 1.794x on the bench's set** (t-a 6.2211 -> 3.5158, t-b 3.2683 -> 1.7994, t-c 3.2825 -> 1.8032 ns/byte; taskset -c 3, median of 5, >= 1 s per trial, load1 0.22-1.01, box idle after the battery's trailer), which puts pcrec **AHEAD of PCRE2-JIT on all three** (set 0.819x, i.e. 1.22x faster, against STEP 1's predicted 1.149x behind; JIT figures carried from STEP 1 and justified by the INDEXED column reproducing its baseline within 0.6%). **STEP 1's 1.276x was a FLOOR set by its hand-patched artifact, not a ceiling set by the mechanism** — its own v1 7.751 vs v1b 6.863 puts 0.89 cycles/byte on accept bookkeeping where the shipped indexed loop pays 0.05, the signature of an accept table still indexed by the un-multiplied state; the shipped form indexes accept by the premultiplied value and runs t-c at 5.91 cycles/byte. **THE SIZE BUDGET WAS SPECIFIED AND THEN DELETED ON A MEASUREMENT**: the bound is the RANGE condition alone (n*ncls <= 65,535), because the premultiplied form still wins across the whole L2 band — 1.107x at 18,432 entries, 1.097x at 36,864, **1.287x on the corpus's own largest machine at 40,010** (a REVERSE table, biggest of the four and the biggest gain, which is the opposite of what a cache-eviction story predicts). **TWO IDEAS MEASURED AND REFUSED**: the `__builtin_expect` LAYOUT HINT on the loop exits is a **REGRESSION** (1.263x slower on the set; hinting the dead exit alone is byte-identical because gcc already ranks it unlikely, and hinting the `state == start` guard stops gcc using it as the back-edge, costing a second taken branch per iteration — 1.38x on t-b's 190,651 skip entries, 1.04x on t-c's one), and a `size_t` state variable is a WASH (1.004x) so `unsigned` stays. Frank's DEAD=0 sentinel was BUILT as an answer-gated patched artifact and is also a wash (0.996x); (a) 65535 is kept on the non-speed balance — 3 bytes and a carve-out-free cell invariant against twelve sites that assume state 0 is the start, a `- 1` in the view read, a row on three tables, and the back-edge losing its free `test`. Per-K-byte exit-check unrolling NOT built (D77): after premultiplying, t-b and t-c are within 1.3% of each other, and that is the gap it would have to beat. IDENTITY: 11 patterns x 91 bench subjects x 3 arms (premultiplied / denied / the pre-change compiler), 122,135 answer lines per arm, 0 differences; run_recursion_identity.sh 15/0 with (A) byte-identical against the unchanged pre-module pin and (B) re-pinned. NEW CHECK tests/codegen/run_premul_table.sh (own `make test` section, not smoke), validated red three ways: table-not-premultiplied 13/19, sentinel-collides 12/5 (and it refuted a sentence of the note's — the range conjunct is not redundant with the tighter size one), state-left-`int` 14/1 on the shape arm ALONE, which is the silent regression no answer check can see. COST: the accept table grows n -> n*ncls, visible in emitted LINES only on the K39 family (869 -> 962 at {0,400}, 1,994 -> 2,762 at {0,4000}) — which is the clearest statement of what [OPT-4] would buy. (STEP 1 measurement lane launched 2026-08-26 ~12:0x; the report's per-subject rows now exist — reporter v4 — and half-answer it: on 1 MB of `a` the skip loop never runs, so the 3.26 ns/byte ≈ 11 cycles/byte IS the transition loop; inbox I-7 item 2 — CONFIRMED in part and REFUTED in part by the above) — THE LOOP'S THIRD OUTLIER, with Frank's NAMED WITH ITS NUMBER, NOT CHARTERED (Frank's branch-prediction question, 2026-08-26 ~13:1x): the start-state exit is the loop's one data-dependent branch (1 per ~5.5 bytes on prose, 190,651/MB on t-b); it costs nothing measurable today because mispredict recovery overlaps the dependency-chain stall (t-b vs t-c 3.272 vs 3.265; still ≤ 1.3 % after premultiply, 2.551 vs 2.519) — the general fix if a subject ever makes it move is checking the exit once per K≈32 bytes (OR-accumulated, unrolled), which loses nothing on the skip side because the skip loop only pays on runs ≥ ~32 bytes (STEP 1 §4); `__builtin_expect` layout hints on the exits ride STEP 2 (layout, not prediction). STEP 3 CANDIDATE, NAMED WITH ITS NUMBERS (Frank, 2026-08-26 ~13:2x: "if the next state is often the same, could it speculate?"): MEASURED on the real orig DFA (instrumented loop, scratch): the transition lands on the SAME state 61.5 % of bytes on t-a (runs of 2/3/6), 63.6 % on t-b (runs 2-9), 100 % on t-c. Hardware will not value-predict a load (no x86 does); the software forms are (a) guess-and-verify — branchy; a state change every ~2.6 bytes on address text makes the verify branch fire at token rate, so its worth is a bet on that branch's predictability; and (b) the EXACT form, the skip loop generalized from state 0 to EVERY self-looping state: scan for the run's end with the state's stay-set (a class-membership test, chain-free, bitmap ~1.15 c/byte or shufti ≥32 B) and transition once at the boundary — bounded gain ≈ same-fraction × (chain − scan) ≈ 0.62 × (7.75 − 1.2) ≈ 4 c/byte on t-a after STEP 2, MINUS one data-dependent exit branch per run (every 2-6 bytes; ~19 c per mispredict), i.e. a net LOSS on non-periodic text unless predicted. BLOCKER: the bench's t-a/t-b are PERIODIC (period 26 / 55 — I-10), so every branch-cost figure so far is flattered; the measurement that decides (b) is the same instrumented artifact on a non-periodic 1 MB subject, after STEP 2 lands. D77: not chartered. SECOND STEP 3 CANDIDATE, NAMED (Frank, 2026-08-26 ~13:3x, "keep the failed case free"): the TWO-BYTE transition table — index by (state, c0, c1), one `load, add` per TWO bytes, the chain halves and there is NO miss path because nothing is guessed; cost = table × ncls, so it is for SMALL machines only (states × ncls² ≤ ~16 K entries stays L1-resident: `\w+@\w+\.\w+` 6×4² = 96, IPv4 20×7² = 980 — most of the corpus by count; orig's 249×18² = 80 K is out of L1 and roughly a wash). Ruled OUT by the same principle: guess-and-verify with a branch (a ~19-cycle mispredict at token rate), packing bits into the loaded entry (any on-chain extract costs a cycle — STEP 1 v2 measured +0.34 c/byte), and speculative-cell prefetch (tables are L1-resident; a self-loop's next cell is in the row just loaded). Gated by STEP 2's size rule; needs a non-periodic subject (I-10). D77: not chartered.
  hypothesis (2026-08-25 ~22:5x): orig / large-subject-throughput at
  1 MB, pcrec-auto 13.39 ms vs PCRE2-JIT 9.12 ms (0.68×, i.e. 1.47×
  slower) while 2.1× FASTER than PCRE2's interpreter — "we should
  consider the possibility that a particular piece is slower because we
  are not currently doing SIMD/hardware-specific optimizations; I suspect
  the large-subject numbers are due to that, but it's hard to pick it
  apart". The ordering interp < pcrec < JIT is CONSISTENT with "pcrec
  wins on the algorithm and loses on the vector unit" (the JIT scans
  first bytes/pairs with SSE2/AVX2; interp and pcrec scan with scalar
  code + libc memchr) — consistent is not attributed. STEP 1, a
  MEASUREMENT lane (the [OPT-1] shape): the DFA's per-byte work is two
  loops — the candidate-start SKIP (scalar 256-entry bitmap walk for
  `byte-class`; glibc's AVX2 memchr for `memchr`) and the TRANSITION loop
  (a table lookup per byte, data-dependent). Attribute the 1 MB cost
  between them: subjects that never leave the skip loop (no candidate
  byte) vs all-candidate subjects vs the bench's mixed ones, on the same
  artifact; the `RX_DFA_PREFILTER` stamp says which skip a subject got;
  CONTROL 1: the same subject through a `memchr`-form artifact vs a
  `byte-class`-form one (SIMD vs scalar scan on identical input);
  CONTROL 2: a SIMD "find first byte in class" (the pshufb shufti of
  studies/simd1) dropped into the skip loop in a scratch copy of the
  artifact, timed. Then: if the loss lives in the skip loop, SIMD IS the
  general fix — [OPT-SIMD] earns its charter through the loop (D79 item
  4); if it lives in the transition loop, the levers are table width /
  two-byte transitions / small-DFA SIMD (studies), and SIMD scanning
  alone will not close it. Needs the bench's per-subject rows (three
  1 MB subjects: which carries the loss) and the artifact size beside
  gcc time (repin-v2 reading items). Queued behind [OPT-1] STEP 3.
- [OPT-A] STATE:not-started — (BENCH O-8 2026-08-29, THE MEASURED NEED: after [OPT-K], stack-frame `\bat ` at 1 MB is still 3.0-6.5× BEHIND pcre2-jit (pcrec 0.38-0.56 ns/byte scalar memchr-at-k*+verify vs the JIT's 0.065-0.087 ns/byte SIMD PAIR scan); the search band is within 2× (1.83×), so this is the 1 MB row's ask — the pair scan is this row's territory; the rarest-byte prior needs [DD-13b]'s `freq` block, the pair scan itself does not. Ranked (i) by the bench for the D86 optimization column, after [OPT-4].) ALGORITHMIC search optimization, and research is part of the work: pcrec is open source and pulling from other open-source engines is the point. Survey before hand-tuning. Leads recorded in D21: rare-byte prefilter selection (ripgrep/Hyperscan choose the RAREST byte by frequency; we choose memchr only at exactly one escape byte and otherwise fall to a bitmap — this attacks our case (d) path directly), memchr2/memchr3 for the 2-3 escape-byte gap, multi-byte literal search (Two-Way/Boyer-Moore/memmem) instead of scan-to-a-byte-then-step, Teddy/SIMD multi-pattern prefilter for the keyword-alternation shape M2.8 targets, reverse-inner and suffix literal selection when the prefix is weak, shift-or/bitap for short patterns, and transition-table compression (we do alphabet compression via byte equivalence classes but no table packing). Record rejections with the reason — "Teddy does not fit because X" is worth as much as adopting it. LEAD ADDED 2026-08-18 (Frank's observation, manager probe same conversation, thirtieth session; RE-OBSERVED by Frank 2026-08-21 on the [M6-READ] VM style exemplar — the readable artifact makes the per-byte spelling visible at a glance, where the original observation took an objdump session; recorded as a measured side benefit of M6-READ, not a new item): LITERAL-RUN COALESCING on the VERIFY path — the memmem lead above is scan-side; its verify-side half is that a maximal literal run (`aabb` in `(aabbc+)`) is matched byte-at-a-time by BOTH engines, measured on the shipped emitter at HEAD 0d97d31-era main: the VM path emits per-byte `pos < n && s[pos]==c` + goto, and gcc -O2 does NOT merge them (objdump: four separate cmpb at consecutive offsets — the per-byte bounds checks and distinct branch targets defeat load-merging; a broken first grep regex initially read this as zero compares, re-measured looser), while the DFA path pays the general table machinery (rx_fcls class lookup + rx_ftr step per byte) on a degenerate linear chain with no choice structure. FIX SHAPE: coalesce maximal literal runs at emission into ONE bounds check (n-pos >= k, the same many-bytes-one-check move MRL makes for length-viability) + constant-size memcmp (gcc inlines to wide compares). SOUND by the run's own structure: no choice points inside a run (a failed run fails its alternative whole), no capture edges inside a run (an edge ends the run), intra-run failure position unobservable (start++ retry advances by one regardless). NOT [ENG-ISL] territory (no choice structure to determinize — pure spelling of an already-linear chain; a literal ISL island would degenerate to exactly this, so ISL subsumes it, but no island machinery is needed). FAVORABLE ASYMMETRY vs the [OPT-ALTCLS] stage-3 measured-no: the FIRST-set guard optimized the REJECT path, which the hybrid prefilter absorbs; literal runs sit on the ACCEPT path every successful match pays, prefilter or not. D18 unchanged: lands only with a bench-measured win under the compare.sh instruments. LEAD WIDENED same conversation (Frank: "consider multiple potential implementations... at a class of N there is some N where if-a-or-if-b wins"): BYTE-TEST SPELLING MENU — the VM emitter's per-byte-predicate menu today has exactly THREE rungs, measured on emitted artifacts: N=1 → `s[pos]==c`; ONE contiguous range (incl. 2-member `[bc]`) → the subtract/unsigned-compare idiom `(unsigned)(c-lo)<=span`; EVERYTHING ELSE → the 256-bit bitmap `rx_k1[c>>3]>>(c&7)&1`, a per-byte MEMORY LOAD — and "everything else" includes `[a-zA-Z]` (measured: two ranges → bitmap), i.e. the everyday multi-range unions (\w, hex, alnum) all pay the table. MISSING RUNGS, cheap→general: k-range unions as k range-checks (k=2,3); the 0x20-fold collapse (`[a-zA-Z]` → ONE folded range-check, also the (?i) pair shape); a 64-bit IMMEDIATE-mask test for scattered sets whose span<64 (`(mask>>(c-lo))&1`, no memory touch); OR-chains for tiny scattered N. The crossover N/shape is a MEASUREMENT question (branchless-vs-branchy matters as much as op count; and the winning spelling depends on subject BYTE DISTRIBUTION — an [ENG-PGO] hook: exemplar-informed spelling selection). INTERACTION WITH THE EMIT MODEL (Frank's recall, confirmed against the record): [DD-5] is the switch-based no-computed-goto emitter row, and the recorded evidence is engine_m4.md §8.4 (computed goto is the WRONG lever on case (f)) plus the address-taken-label compile-time superlinearity numbers in decisions.md (2004 labels/0 address-taken 2.70s vs 400 address-taken 11.21s; 8000 grinds cc1 100+ min). Address-taken labels also pin RUN-time codegen (frozen block layout/merging — plausibly the same mechanism behind this lead's measured cmpb non-merge), and gcc's own switch lowering already implements exactly this spelling menu (jump table / bit-test / compare chain by its internal cost model), which a switch-shaped emit model would get for free. TWO COUNTERWEIGHTS, both on the record: only backtrack RESUME points take `&&label` (plain goto targets don't pin — the burden is proportional to resume points, which [OPT-ALTCLS] stage 1 already reduces, 2→0 on its exemplar: rung downgrades un-pin gcc as a second-order benefit); and the computed-goto body's UNOUTLINEABILITY is what structurally protected the VM from K24's .part.0 split — any emit-model change re-opens that exposure and must carry K24's regression evidence with it CROSS-NOTE 2026-08-30 (measured with [OPT-VMLIT]'s probe): gcc never inlines memchr — every emitted memchr is a PLT+ifunc call — so at HIGH ANCHOR DENSITY the per-candidate call overhead is a real term in this row's 0.38-0.56 ns/B scalar figure (the simd1 study's §15 measured 4-7 ns per anchor as free only at REAL densities); STEP 0's measurement should separate call overhead from scan cost before any SIMD design.
- [OPT-5] STATE:started (STEP 0 MEASURED 2026-08-31, lane opt5m, docs/dev/opt5_step0_profile.md, merged a2a9e0d, I-26 sent: the mechanism is a dependency-chain SHAPE — DFA walk data-dependent (pointer-chasing), VM span-loop address-only — ratios flat across a 64x n range on BOTH subjects, so NO count crossover and NO limits.def row; digits' DFA win is fixed per-call overhead under find-all; I-21 supported by mechanism. STEP 1 BATTERY-PROVEN 2026-08-31 20:05 (battery 7 green-by-diagnosis; S213/S214/S215 DETECTED in the matrix — S215 after its first witness was measured INERT and rebuilt cap-binding, the row's own doc carries that K35 story; STEPs 2/3 stay unchartered candidates. Was MERGED ~15:1x): lane opt5s1 (opus), 14 commits + manager landing. The scan edge: src/opt/scanedge.c (chain criterion, 5 preconditions, every one declines), emit_dfa's edge blocks, abi 13 (four sites + FILEPIN re-pin), axes H/I as D82 live-candidate forms (guard 83->91->88, the first decrease, reason written into the guard), RX_DFA_SCAN_EDGE stamp, the cap via limits.def (45 — [LIM-1]'s check FORCED it out of a hand enum), S213/S214 disjoint-detector rows, R4's bound-1 discharge (hand-twin INSTRUCTION-IDENTICAL). MEASURED: letters 2.71x/3.03x (n=256/16384), VM gap 6.00x->2.03x; digits control 1.08x slower (fixed entry cost, peel already in, accepted); embedded chains refuse honestly (unanchored threading — future work). En route it caught S21/S23's stale anchors (quoting's landing never ran test-codegen — manager debt, fixed at landing, both rows re-proven DETECTED). PROVENANCE CORRECTION, recorded at Frank's own reminder: the emit-loops-for-DFA-regions idea was FRANK'S, suggested sessions ago, and a previous session dismissed it as 'not what DFA engines do' — the STEP 0 measurement now shows that dismissal was wrong (category knowledge where a measurement was owed); the mechanism is chartered as he originally suggested. What STEP 0 settled: charter the general fix — emit the VM's address-only bounded-scan shape for any DFA region isomorphic to 'count one class up to a bound' (the row's SIMD-first framing is corrected by the data: address-independence first, SIMD stacks on top). STEP 2 CANDIDATE, named 2026-08-31 (Frank: 'isn't a loop over a static string just a memcmp: (?:ab){1,100}'): the PERIOD-k scan edge — a chain whose advance classes cycle (singletons = a string) collapses to a counted loop of constant-length compares (inlined free, measured 2026-08-30); the DFA twin of [OPT-VMLIT]'s strcmp-straight-runs (also Frank's). REFINEMENT 2026-08-31 (Frank: '(?:ab){10,100} = "abababababababababab" + loop'): the {m,n} split — the mandatory m copies are ONE constant-length compare against the compile-time-expanded m*k-byte string (a few word compares, zero calls, at gcc's by-pieces sizes; one compare rejects where the walk would spend up to m*k-1 state steps), then a counted loop for the optional {0,n-m}. CAP REFINEMENT 2026-08-31 (Frank: '(?:abc){1000000,2000000} would explode the binary. cleanest would be a second loop that degraded to a single iteration -> loop "abc*100" over 10000'): the expansion is CAPPED — the floor compares a B-copy block (B = min(m, CAP/k) copies) in an outer loop of q iterations plus one r-copy remainder compare (q*B+r = m, all compare lengths constant), so the emitted bytes are O(CAP) regardless of m, and m <= B degrades to the single-compare case; the cap is a NUMBER, so per D90 it is born as a limits.def row, and it composes with the [ART-SIZE] size-cap machinery rather than adding a parallel guard. BATCH REFINEMENT 2026-08-31 (Frank: 'the upper loop can also be batched IF you allow a mini-rollback. search in batches of X (e.g. avx2 sized) until fail then repeat last row with normal iteration'): the OPTIONAL {0,n-m} loop compares X bytes per step (X a multiple of k, against the periodic expansion block) until a batch fails or the subject/count bound nears, then re-scans ONLY the failed batch at per-copy granularity for the exact boundary — overshoot-and-refine; the structure is ISA-NEUTRAL (D91-compatible: X = a word-sized constant compare in the scalar form today, a vector width in a scan-simd body later — the rollback logic is identical, only X changes), with the tail below one batch handled per-copy; string-specific — the class case has no expansion to compare against, so scan-string is a genuinely distinct body form, and the same floor-expansion applies to [OPT-VMLIT]'s VM side. Same safety precondition (common exit target per state — refuses the partial-copy-overlaps-suffix case). D77 trigger: a measured counted-string-repeat cell (the bench's nest family is the candidate instrument); STEP 1's rulings R3 keeps the IR representation period-ready at zero cost). STEP 3 CANDIDATE, named 2026-08-31 (Frank: 'if the count optimization works, we wouldn't have all the states to manage, no?... then the cap is arbitrary although specific to when this opt works' — correct): CONSTRUCTION-TIME SCAN-EDGE SYNTHESIS — today the n states are built by subset construction and only THEN collapsed (the caps bind on an intermediate the emitter deletes; the bench's measured 1.8-1.9 s wasted K7 walks are the cost side of the same fact). When the BUILDER sees a counted single-class repeat in a provably-safe context (whole-pattern/anchored is the easy tier — the table-level criterion stays as the general detector for emergent chains like (a|b)-folds and the unsafe embedded contexts), emit a compact counted node (count as a FIELD; the emitted loop's counter is already u64 data) and never materialize the chain. Consequences: [a-z]{0,65535} compiles trivially (bench O-11 ask (ii) answered), the wasted-build cost vanishes (ask (iv) — one mechanism, not two), and the construction caps stop binding on these shapes. PCREC_MAX_REPEAT=65535 stays REGARDLESS — that is the D26 PCRE2-compat bound (PCRE2 refuses {65536}), separate from resource caps. RESIDUAL-GAP CANDIDATE, named 2026-08-31 (Frank: 'the vm gap is better but still present'): the remaining ~2.0x on letters is the TWO-PASS search structure (forward for match end + reverse for match start; both passes are scan loops now, hence the ratio sitting at almost exactly 2) vs the VM's one pass — and REVERSE-PASS ELISION is available where the match region is scan-edge-TOTAL: the forward edge counts from its own entry cursor, so start = end - count is already in a register at loop exit. Falsifiable: letters ~2.0x -> ~parity, digits unchanged. GENERALIZES TO EDGE CHAINS (Frank, same day: 'can that be chained like [a-z]{0,n}[a-z]{0,n} to add them both?'): a match region crossed ONLY via scan edges elides the reverse pass with start = end - SUM(count_i) — same-class adjacents fold to one edge anyway ({0,n}{0,n} = {0,2n} in the DFA), the real chain case is different classes ([a-z]{0,n}[0-9]{0,m}); PRACTICAL CONSTRAINT: the elision reads the FORWARD machine's counts, and today that machine only grows edges on whole-pattern shapes (start-anywhere threading starves it on embedded — measured, r48sem: mc2 forward 0 edges vs reverse/anchored 4 each), so multi-edge elision matures WITH STEP 3's construction-time work. STEP 2 DESIGN NOTE DELIVERED 2026-09-01 (lane opt5d, lane/opt5d 8d36141, docs/design/opt5_step2_twopass.md; Frank ruled all seven §8 questions same day — journal): STEP 2 = the START-PINNED SEARCH ELISION ONLY (Q1); rx_info.search_form mirror APPROVED, rides the implementation's own abi event (Q2, Frank: 'a great idea', with a direction note questioning which stamps should remain #defines at all); view-tolerant edge chartered as [OPT-VEDGE] (Q4); reverse-machine BUILD skip deferred with its own trigger (Q5); asks (ii)/(v) excluded for attribution (Q7); the bench's 2026-09-01 match instrument CONFIRMED the ×2.0 residual is the reverse pass (cross-rung ×1.97-2.04, vm control flat) and found the search-filter fallback's O(subject) failing-call candidate scan (×37 at 1024 B — panel exhibit; frame restated: customers are the search-filter+VM rungs, unwrapped rungs predicted FLAT as a control); two §7 measurements owed BEFORE the D6 panel. Previously: unchartered (D77 — the bench's AFTER at pin a7e0bdf first confirms the 2x from its side) — formerly STATE:not-started (RE-SCOPED 2026-08-30 14:3x on bench O-10 item 7 (2), Frank agreeing: the counted DFA loses to pcrec's OWN VM on letter runs at EVERY rung from 256 — 5.14× at 256, 5.86× at 4096, 5.56× at 16384 — and WINS 1.75× on digits at every rung, so the "knee" is a property of the SUBJECT (does the run stay inside the class?), not of the count, and a count knob would be the wrong mechanism. NOW MECHANISM-FIRST: STEP 0 explains why the counted DFA is 5× slower than its own VM at n=256 on an in-class run (a 3 KB table is not a cache effect; suspect the search regime's extra pass or the per-byte table walk vs the VM's counter loop) with a profile on the bench's own subjects; the selection rule follows from the mechanism and lands as a limits.def row only if it IS a threshold. PREDICTION SENT TO THE BENCH (I-21) for bounded@0.2, falsifiable: letters → the VM wins at every rung including 64/128; digits → the DFA wins at every rung; no count crossover exists on either axis. Order: after [OPT-4.1] and [LIM-1]. Original framing kept below — RULED 2026-08-30 by Frank — the OPTIMIZATION column's row, D86, chartered AFTER [LIM-1] lands so any threshold is born in the limits table, D90) — THE `{0,n}` CLASS-COUNT SELECTION KNEE (bench O-9 item 7 / candidate 2, MEASURED at 36d5963): `auto` selects the counted DFA on `[a-z]{0,n}` at 4096 and 16384 where pcrec's OWN VM is ~6× faster on letters (`t-letters-004k`: DFA 3.61 ns/B vs VM 0.61; set ratios auto÷vm 1.98/2.05/2.05 at 256/4096/16384 and 1.00 at 32768 where auto IS the VM — the 32768 rung is 5.5× faster than the 16384 rung's DFA on the same subject; same inversion in search 2.81×), while the DFA still wins on digits (`t-digits-016k` 82.8 vs 140.5 µs); mechanism: a premultiplied table at ~12 B/count on a run that stays inside the class loses to the counter rung. Build: choose the VM one rung earlier on a MEASURED knee — no new code path; the knee is a `selection knee` row in limits.def (override none until a measured need, D77). Measurement first: the bench adds bounded@0.2's intermediate rungs (O-9 asks iii/vi) to locate the knee — chartered with this row in the inbox. Acceptance: answer-identical over the corpus (test-axes), the knee's rung and its predicted winner stated before the bench's AFTER, [SEL-1]'s stamps name the decision. FRANK, 2026-08-30 14:5x (input to STEP 0 and to the prefilter ladder generally): "The simd research found that you could do a prefilter on a small 2-3 member class without branches (higher and there is some other mechanism) so it may not always be 1 vs 2+." MEASURED in studies/simd1/precompiled-simd-matchers.md's class-shape table (1 MiB haystacks): 1 byte `cmpeq` 17-33 GB/s; 2-3 bytes (incl. a case twin) as OR-of-`cmpeq`s 15-18 GB/s; a contiguous range `[0-9]` by saturating-subtract 13.8 GB/s; ANY ASCII set by the `pshufb` nibble lookup (shufti) 15.3 GB/s — all branchless, all within ~2× of the single byte; only a 4-byte OR-chain falls to 6.0. So a class prefilter of any size is near-free in SIMD, and the ladder's implicit "one rare byte or nothing" model is the wrong frame. And the study's run-extension result (JOURNAL.md §15: branchless classify+clz beats a scalar table loop 2-3× on honest mixed lengths) is the likely MECHANISM behind this row's letter-run numbers — the counted DFA's per-byte table walk IS the scalar table loop; the VM's counter loop is the same loop in another engine; the answer to `[a-z]{0,n}` on an in-class run is a SIMD run extension, which neither engine has today ([OPT-SIMD]'s territory). STEP 0 tests that hypothesis first. **STEP 2 REV 2 LANE LAUNCHED 2026-09-02 ~11:1x (fiftieth session): lane opt5d2 (opus) on worktree opt5d / lane/opt5d, working docs/dev/reviews/2026-09-01-r49-opt5-step2.md §2 (18 items + minors) into docs/design/opt5_step2_twopass.md rev 2 with a per-item disposition table; the opt5m2 premeasure memo (lane/opt5m2 24ba0c4, docs only — its emit_dfa.c hunk is a measurement probe and never lands) rides the same branch; O-14's confirmations are slotted at merge if they land after the lane. Implementation charter waits for rev 2 + Frank's go.** **REV 2 MERGED 66da68c 2026-09-02 ~11:5x**: all 18 r49 items + minors dispositioned in the note's §10 (item 8 as the allowed no-witness branch: S219 UNREACHED with the derivation; the liveness conjunct's real guard is a compiler assertion; §7 item 10 counts P3 EVALUATIONS to settle it); verifier r50verify (sonnet, read-only) confirmed 17/18 + minors, 9/10 sampled code citations, and both of the lane's implementation-lane catches (the rx_info mirror appends after `nentries`; its guard is `pcrec_artifact_has_dfa_scan`), and caught the lane's own inverted `dfa_table_name` line (fixed at landing). r49cons's frame table in §0 is a RECONSTRUCTION (the critic's delivery message was not preserved) — O-14 anchors its numbers; the §0 `[O-14 PENDING]` slots are filled when O-14 lands. NEXT: Frank's implementation go (STEP 2 charter: abi 16, axis J, S218-S222 arbitrated at merge). **STEP 2 BEFORE ON RECORD (O-14, bench dc33947, ledger docs/dev/ledgers/2026-09-02-full-suite-1989c62.md §3): search-filter customers ×1.985 the unwrapped rate on matching calls (3.695 vs 1.861 ns/B; 2048÷1024 = 1.986-2.036 on five letters runs, VM control 0.999) and ×37.1 on the failing anchored call d-01024 (10.6 → 393.7 ns) — O-13 confirmed everywhere, withdrawn nowhere; the note's §0 slots filled 80390a2; the AFTER is read against ledger §10's twelve-point checklist.** STEP 1 FOLLOW-UP CANDIDATE (I-32 (iv), for Frank's ranking): the edge boundary is scanedge.c precondition (5) `m >= 2` on CHAIN LENGTH; three loglines patterns with chains of 2-4 pay ×1.03-1.09 with no win possible (a chain of k scans at most k bytes) — a higher minimum-chain floor, measured under `-fno-scan-edge` on loglines. **STEP 2 IMPLEMENTATION GO (Frank, 2026-09-02 ~14:2x: "Agree to step 2 go"); lane opt5i (opus) launched on worktree opt5i / lane/opt5i against the rev 2 note (docs/design/opt5_step2_twopass.md, merged 66da68c) as its charter: abi 16 (D94 grep), axis J, rx_info.search_form appended after nentries with the has_dfa_scan guard, S218-S222 arbitrated at merge, §5.4's nine structural checks, §5.6's five rows (S219 UNREACHED with its derivation; the P3 liveness conjunct as a compiler assertion), §6's eleven D80 spec deltas, §7's pre-ship measurements; the union battery at merge; the bench's ledger §10 checklist is the AFTER's instrument.** **STEP 2 MERGED 2026-09-02 ~19:3x: da4fe60 (code; 20 lane commits) + de32a4b (the report's F12) — abi 16, identity gate (B) re-pinned to da4fe60 (readers of the old pin by grep, D94), S218-S222 arbitrated as filed (218 definitions → 223; mech's canonical DETECTED figures come from the battery), RX_VM_FRAMELESS riding the same bump. Lane opt5i (opus, ~8 h): the elision at site (b), axis J + `-fno-start-pinned` (bit 22), P0-P5 with P3's liveness conjunct as a compiler ASSERTION, RX_DFA_START, rx_info.search_form after nentries guarded on pcrec_artifact_has_dfa_scan, both stamp folds + the orientation block dropping the reverse machine, P2 LIFTED into pcrec_state_view_invariant shared with scanedge.c, eleven D80 spec deltas, nine structural checks (run_search_pinned.sh, force-axis arm added after 70 pinned HYBRIDS appeared under -fprefilter — M1's 0 was default-axis), run_vm_frameless.sh. MEASURED vs predictions written first: 175 pinned (= M1), rewind_position gone from exactly those, 8 RX_DFA_SCAN_EDGE movements all toward the forward machine, −3,232 B per pinned artifact / +71 declined / +39 per .h, net −311,811 B (−0.69%); test-axes bit 22: 22,309/22,309 agree, 0 mismatches (the five AXIS FAILs are K45, pre-existing). ELEVEN findings against the note (report §7): C3 holds only for startpos ≤ n; the widened-bit plant is inert ALONE (P2 refuses exactly the states the two P1 spellings disagree on — S218 is a two-hunk row, S220 declared UNDETECTED); P3 is evaluated ZERO times on all axes (S219 UNREACHED, measured); P2's real decliners are \B, \B\B, \Bx*. Four in-tree checks moved and re-derived (expansion_diff population, the three size-cap witnesses, premul_table's fold reading machines present, [a-z]{0,4096} removed rather than re-pin [ENG-ABS]'s census). artifact_size_log.tsv kept at main's pinned copy (the deliberate regeneration follows the battery, movement explained). UNION BATTERY: launched after the post-merge build; D6 panel r51 (r51sound, r51check; read-only) runs beside it. Open questions §8: -fno-scan-edge joining strategy_denials (STEP 1's row); [GUIDE-1] unbuilt; C3's corpus population is zero (its witnesses are synthetic).** **UNION BATTERY on de32a4b DONE 23:56 2026-09-02: strict GREEN, san GREEN (0 reports, 110 min), lint GREEN (50 s); test rc=2 with THREE diagnosed reds (K44's cell — solo compile OK, quiet-box cost pair owed; the corpus census pins not moved for the two new .rxt files — lane landing; a latent K24 grep bug — same lane); mech 218 rows: 0 anomalies, 7 undetected (6 prior + S220 declared), 1 unreached, 1 UNEXPECTED = S219 'NOW REACHED' — its SAB_REACH probe watches the conjunct's line, which every pinned candidate executes, not the decline arm nobody reaches; ruled to r51fix as item 6 (re-aim the probe or flip to declared UNDETECTED). PIN STATUS: battery-proven pending the morning's landing merge + test-stage re-run + S219 row fix; the abi-16 pin goes to the bench with WINDOW OPEN after that.** **STEP 2 BATTERY-PROVEN — PIN 288d505 (2026-09-03 07:5x; code = de32a4b, abi 16, gate (B) FILEPIN da4fe60)**: the union battery's reds all closed — landing's census pins + K24 fix merged (70a2a10/886a0ef), the test-stage RE-RUN on 70a2a10 rc=2 with ONLY K44's two cells (both green solo, numbers in journal part 14), r51fix's six check items merged (288d505: S223-S226 DETECTED solo on distinct detector lines, S219/S220 declared UNDETECTED with reach:ok, the §7 wiring grep, S220's 3-pattern manifest, the force-axis floor 20, §8 reworded); merged main: strict clean, test-search-pinned 17/0, test-vm-frameless 6/0. WINDOW OPEN + I-38 hand the pin to the bench for the STEP 2 AFTER (ledger §10's checklist). Mech definitions 223 (S218-S226 used; next free S227); the full mech matrix with the six new rows rides the next battery. OWED: the deliberate artifact_size_log.tsv regeneration (one commit, movement explained); the tests/mech/run_sabotage_matrix.sh prose header's suite vocabulary (pre-existing staleness, flagged by r51fix); K45's tower documentation ([ART-SIZE.2]); the rxtsource header's stale '178/3,262/26,680' prose line (flagged by landing).**
- [OPT-VEDGE] STATE:not-started (CHARTERED 2026-09-01, Frank ruling opt5d's Q4 "agree", forty-ninth session; opened on a measurement that ALREADY EXISTS — bench O-12 §4's two whole-form artifacts at 471,204 and 937,248 B, 93.7% of the [ART-SIZE] cap, owning both surviving size warns; ask (iii) ruled STEP 3-or-own-row and this is the own row) — THE VIEW-TOLERANT SCAN EDGE: relax src/opt/scanedge.c precondition (3) so a counted single-class chain whose every state carries ONLY the END (`\z`) view takes the edge, with the scan's own `pos == n` exit evaluating the view — the emitted block already carries the bound-reached/run-stopped split and the direction-aware step-back (design analysis: docs/design/opt5_step2_twopass.md §2, lane opt5d). CONSEQUENCES when it lands: the whole-subject `[a-z]{0,n}\z`-shaped ladder collapses like the plain ladder did at [OPT-5] STEP 1 (tables → compile-time, the two size warns retire — [ART-SIZE]'s first real customer, answering O-12 ask (iii)'s second half); AND the anchored machine's state count on those skeletons drops below PCREC_ANCHORED_MAX_STATES, so the search-filter fallback band the bench measured at ×37 O(subject) failing-call cost (their 2026-09-01 match instrument; boundary = the 4096 cap × the measured \z reachable-count halving, docs/dev/engabs_reach_probe.md) shrinks by the same move — one mechanism, two customers. First step: opt5d §7 item 3's one-command measurement. Not started unprompted; sequencing vs [OPT-5] STEP 2 is the manager's call at STEP 2's panel.
- [OPT-VMFL] STATE:started (CHARTERED 2026-09-02 by Frank's ruling "agree to three candidate rows", fiftieth session; OPTIMIZATION column). OWN THE FRAMELESS-VM SHAPE: the bench's O-14 measured the forced-VM route ×9 faster on `resume_frames == 1` artifacts at 1989c62 (floor 174,405 → 19,383 ns/call; a −402 B split; the empty-match-and-advance loop ×1.07-1.09 slower on the same population), traced to [CC-CLANG] step 1's `has_push` gate omitting the pop-and-resume block INCLUDING the computed goto (emit_vm.c:9482-9560; I-31/I-32) — a gcc whole-function effect nobody predicted or owns. STEP 0 (lane vmfl0, sonnet, MEASUREMENT ONLY, launched 2026-09-02 ~14:3x): (a) census over the corpus + the bench's four sets of `has_push` (emitted text) vs the `resume_frames` stamp (pre-pass estimate, :8128) — how often they diverge and in which direction; (b) the D77 trigger for a DIRECT-BRANCH dispatcher: hand-twin three frames≥2 artifacts (ctx-*, nest2-64, csv5) replacing the computed-goto dispatch with a switch over the resume-label set, timed on this box against the untouched artifact; (c) a stamp proposal for the frameless property (`RX_VM_FRAMELESS` and/or an rx_info field — a D80 spec hunk drafted, not landed). STEP 1 (unchartered until STEP 0 reads): the dispatcher change if (b) pays; the stamp; a regression net for the ×9 on our side (today only the bench's ledger §10 tripwire pins the SPEED; S217 + gates pin the BYTES). **CROSS-NOTE (Frank, 2026-09-02 ~14:5x): "frameless vm is basically the direct dfa idea we discussed a while ago" — this row is [ENG-DIRECT]'s FIRST MEASURED NUMBER, not a sibling: a VM program with no resume frame is a DIRECT-CODED automaton by construction (the program counter IS the state; labelled straight-line code, no table, no dispatch), and [CC-CLANG]'s omission of the computed goto is what let gcc treat it as one — the bench's forced-VM floor cell is the [BENCH-CEIL]-shaped evidence [ENG-DIRECT] said it needed, obtained from an artifact nobody built for it. STEP 0's memo is written as [ENG-DIRECT]'s STEP 0 too (the D77 trigger it waited for), and STEP 1's selection question (which shapes route to the frameless/direct form, and is it an engine-selection value) is [ENG-DIRECT]'s 'third engine type' ruling, not a new axis.** **STEP 0 MEASURED 2026-09-02 ~15:1x (lane vmfl0, docs/dev/optvmfl_step0.md, merged 6fa1c66)**: (a) CENSUS over 2,603 VM-compiled corpus+bench artifacts — `frames==1 ∧ frameless` 1,090 (41.9%); `frames==1 ∧ dispatch present` (the under-count I-32 called theoretically live) **0**; `frames>1 ∧ frameless` 198 (7.6%, EVERY one a lookaround with a choice-point-free body — vm_cost's uniform lookaround frame charge over-counts; none in the bench's sets); `frames>1 ∧ dispatch` 1,315. Under `auto`, 385 of the 1,090 frameless patterns (35%: 290 as VM hybrids, 95 plain) reach the frameless shape by natural selection — the ×9 is not forced-VM-only. (b) DIRECT-BRANCH DISPATCHER hand-twin (computed goto → switch over the artifact's own label set; answer-identical 15/15 cells; 5-trial process-launch medians, spreads within 2%): nest2-64 (24 labels, scan-loop-dominated body) 3-4% FASTER on both regimes; csv5 (4 labels) and ctx-lazy-256 (6 labels) flat to 2.9% SLOWER. **D77 TRIGGER NOT MET — the dispatcher is not built.** Hypothesis recorded, not tested: the payoff scales with how much of the function IS a tight address-only scan loop (the ×9 witness is 100% that), which is [ENG-DIRECT]/[OPT-5]'s territory, not the dispatcher's. (c) STAMP drafted, not landed: `RX_VM_FRAMELESS` (0/1), a §6.3 (b)-family macro on the VM route, unconditional, read from has_push at its own definition site, no rx_info mirror (RX_DFA_TABLE precedent); §4.3 recommends shipping it now as the VM-route macro that later becomes [ENG-DIRECT]'s sizing input, OR letting RX_ENGINE's third value subsume it — FRANK'S CALL. STEP 1 (awaiting ruling): land the stamp (small D80 change, abi bump ritual; gives the bench the covariate for ledger §10's tripwire); the dispatcher stays unbuilt; the over-count is a stamp-accuracy observation on lookaround (benign — capacity over-provisioned), noted here, not a K entry. **STEP 1 RULED 2026-09-02 ~15:2x (Frank: "Ship now"): `RX_VM_FRAMELESS` ships, riding lane opt5i's abi 16 event (rulings R1 to that lane) — no second bump, no second battery. THE LOOKAROUND OVER-CHARGE IS LEFT AS IS, by design: emit_vm.c:2196-2231 documents it as a deliberate SAFE-DIRECTION UNION (frames +1 is EXACT for the negative form's `L_neg_ok` push and an over-charge for the positive/non-atomic forms; lookbehind `+m` where `m-1` is exact) chosen so the cost pass never reads the lookaround's three flags — `vm_look` stays their single reader (design §3.1(a), the D62 control-3 hazard). Cost of the over-charge: a lower stamped subject_ceiling and 1-m over-provisioned frames; nothing else. An exact charge would need a second reader of the flags (the hazard) or a special case (memory: general mechanisms) — and once RX_VM_FRAMELESS exists, `resume_frames` is no longer anyone's proxy for framelessness, which is the general fix to the measurement problem the over-count caused.** (Bench re-pin at 288d505 confirms the I-34 divergence class on its own witness: `(?=abc)x+` stamps RX_VM_FRAMELESS 1 with RX_RESUME_FRAMES 2 — the lookaround over-charge, as recorded.)
- [OPT-NEG] STATE:not-started (FILED 2026-09-02 by Frank, fiftieth session, "apropos of nothing": "we do scans for chars, and classes separately. But do we check if the class is a single reverse like \N which we could scan as 'does not equal'?" — ANSWER, read off emit_dfa.c:4424: NO. Axis I's scan bodies are exactly TWO — `range` (a contiguous byte range; a positive singleton is a width-1 range) and `bitmap` (a 256-entry table walk) — so every NEGATED class (`[^,]`, `[^"]`, `\N` = `[^\n]`, `[^"\\]`) scans through the bitmap today, one table load per byte. OPTIMIZATION column, an axis-I addition under D82 (a new live-candidate body object, its own stamp value, deny/force flags, answer-identical): (1) `not-equal` — the complement is ONE byte: the edge's loop test is `b != c`, and for the UNBOUNDED run (`[^x]*`) the whole scan IS `memchr(x)` (the prefilter's DFA_PF_MEMCHR shape, :2451, applied to the edge body — find the first excluded byte); (2) `not-in-small-set` — the complement is 2-4 bytes: `b != c1 && b != c2` (or a switch), no table. D77 MEASUREMENT FIRST: a census over the corpus + the bench's four sets of scan chains by COMPLEMENT SIZE of their class (csv5's `[^,]*`, kv-quoted's `[^"\\]`, log fields), and one hand-twin timing of `[^,]*` as memchr vs bitmap on a long line. Sits on axis I only — no change to the chain criterion or the edge emission; queued with [OPT-EDGE] (same file region) and shares its census. CROSS-NOTE 2026-09-02: a candidate ISLAND BODY under [ENG-ISL]'s VM-in-DFA framing once [OPT-EDGE] triggers it — build it as an island shape shared by both engines, not as a DFA-only edge kind.
- [FEAT-VAR] STATE:not-started (FILED 2026-09-02 by Frank, fiftieth session, to be brought up "at some point" — expand on arrival, not before): PATTERN VARIABLES — inject a caller-supplied STRING into the regex at MATCH time (a placeholder in the pattern, e.g. `${name}`, bound to bytes the caller passes with the subject), so one compiled artifact matches a family of patterns. Frank's note: "I'd been assuming it was VM, but given spans, it would work with DFA as well" — the VM has a natural 'consume this runtime string' instruction; the DFA gets it as a STRING EDGE (the [OPT-VMLIT]/[ENG-DIRECT] memcmp-edge shape, or [OPT-5]'s span mechanism) comparing against a runtime pointer+length instead of a compile-time literal. FEATURE column. DESIGN QUESTIONS TO RECORD NOW, not answer: (a) determinization cannot see the variable's bytes, so a variable edge is an OPAQUE symbol — sound only where the automaton's decision at that point does not depend on those bytes (a leaf position with a distinct entry; `(${v}|ab)c` cannot be determinized without the bytes) — the placement rule is the whole design; (b) the class partition (rx_fcls) is built without the variable, so the edge must be a memcmp against raw bytes, never a class walk; (c) captures/backrefs interplay with the VM side ([M4]'s backref module is the nearest existing 'runtime string compare'); (d) the API surface is a D80 spec change (a new entry or a vars array on rx_ctx) and a caller-observable stamp. Nearest neighbours: [OPT-VMLIT] (VM literal strcmp chains), [ENG-DIRECT] (string edges), the backreference module (runtime memcmp with a captured span). No measurement owed until Frank opens it.
- [OPT-ALTHASH] STATE:not-started (FILED 2026-09-02 ~16:4x by Frank, fiftieth session — explicitly NOT folded into current work: "one strategy for large alternations might be a partial hash table. Grab say four chars and look up state for dfa or something for vm.") A k-BYTE BLOCK HASH FOR WIDE ALTERNATIONS: instead of walking a trie one byte per step at the fan-out, hash the next k bytes (k≈4) and look up (DFA) the state reached after consuming the block, or (VM) the surviving branch — the Wu-Manber block-hash idea applied at the alternation's head, a k-stride transition where the automaton's fan-out is widest. Design notes recorded at filing, not answered: (a) soundness on the DFA — a k-block lookup is exact only when the k bytes are fully consumed from that state with no accept or view inside the block; states with an accept within k steps, and positions with fewer than k bytes left, take the per-byte path (the same head/fall-through splice the scan edge uses); (b) priority is the DFA's already (determinized from nfa.c:192's priority-preserving trie), so the DFA side inherits it; the VM side must map a block to the FIRST branch in alternation order among those whose prefix matches, which the trie's rule-1/rule-2 partitions give; (c) the table is a perfect hash over the trie's depth-k prefixes (size = distinct k-prefixes, not 256^k), a miss = dead (DFA) or no branch (VM); (d) relation to the island framing: this is a THIRD dispatch shape beside the first-byte switch on [ENG-ISL] — the switch wins at small fan-out, the block hash where the fan-out at depth 1 is small but the prefix set at depth k is large (dictionary-shaped alternations: srt/pfx3/w-N above a few hundred). TRIGGER (D77): altwide@0.2's measured curve for the DFA route against width (today FLAT to 256 — the determinized trie is already O(1) per byte; the block hash buys a constant factor in bytes-per-step, not a complexity class) and for the VM route under the bigcap axis where the trie dispatch would leave a fan-out too wide for a switch. No measurement owed until Frank opens it.
- [OPT-CLSPACK] STATE:not-started (FILED 2026-09-03 ~17:1x by Frank: "character class compaction: if you have N disjoint char classes, store them in one array with an integer or bit indicating class membership; if it impacts performance, limit to scenarios where we are prioritizing space"). FACTS AT FILING: the VM emits ONE 32-byte bitmap per bitmap-class SITE (`rx_class_bitmapN[32]`, test = load + shift + and: `(bm[b>>3] >> (b&7)) & 1`; contiguous ranges get compares instead), so a pattern with N bitmap classes carries 32N bytes of tables; the DFA route already has the compaction Frank describes — its byte→class PARTITION table (`rx_fcls`, one 256-entry table; the transition table is indexed by class id). THE GENERAL FORM for the VM (handles OVERLAPPING classes too): one shared 256-entry byte→ATOM table (the partition into intersections, the DFA's own construction) plus a 64-bit atom MASK per class; test = `(mask_k >> atoms[b]) & 1` — two dependent loads + shift + and vs today's one load + shift + and; space 256 + 8N vs 32N (wins above ~10 bitmap classes; ≤ 64 atoms, else decline). Frank's rule: if it costs time, it is a SPACE-PRIORITIZED choice — an [ART-SIZE] size-term rung (the K ladder's shape: default form first, the compact form when the artifact is over a size threshold), not the default. MEASUREMENT FIRST (D77): (a) census over the corpus + sets of bitmap-class counts per artifact (how many artifacts exceed 10; the [ART-SIZE] census has the table bytes); (b) a hand-twin of a many-class VM artifact (csv/loglines shapes) with the atom-table test, timed. Sequencing: with [FORM-CHAR] (both touch how a class test is emitted; one consumer object). **FRANK'S QUESTION (2026-09-03 ~17:2x): 'did we measure a single byte load vs all that bit shifting?' — NO, and the two emitters chose differently for the same test: the DFA scan edge's `bitmap` body is a 256-BYTE table (`uint8_t set[256]`, test = ONE load `tbl[byte]`; the name is a misnomer), the VM's class test is a 32-byte BIT array (load + two shifts + and). Cost model, unmeasured: in a latency-bound loop ([OPT-3]'s 7-cycle chain) the bit test's extra ALU ops hide under the load; the byte table costs 4 cache lines vs half a line, which matters when many classes are live. STEP 0 of this row therefore measures THREE shapes per class site — byte table, bit array, range compares — on the VM loop and on the DFA edge, with cache footprint counted, before the compaction question (the atom table is a byte table by construction).** **STEP 0 STARTED 2026-09-04 10:5x EDT in lane form0 (with [FORM-CHAR] STEP 0): byte table vs bit array vs range compares, both engines, cache footprint counted; the shared-atom-table twin if cheap.** **STEP 0 MERGED 2026-09-04 16:58 (c7288a59; lane form0, sonnet; docs/dev/form_char_step0.md + studies/form_char_twins/; measurement only, nothing under src/): SIZE — the folded compare wins the VM caseless chain (family A closed by compiler evidence: gcc folds all three fold-pair spellings to and/cmp/sete, no load; asm committed), the 256-B table loses on size everywhere, the atom table wins at N=16; TIMING (quiet box, 11 rounds, load1 < 0.5 gated, 132 cells, 0 checksum mismatches) — B/general a wash (base 9.74 / table 9.38 / rangecmp 9.16 ns/call, within 6%); B/sparse rangecmp LOSES (36.2 vs table 31.5, +15%) despite the smallest .text — a size-only prediction overturned; C/small a wash (0.8%); D/n16 table 875 and atom 885 tie and both beat the bit array 1,147 by ~24% — atom graduates to supported on size AND time. OWED (§8.4): ci-256/nonpair timing for C, CPU pinning (taskset), a disassembly read of D's atom mechanism, the subject generator as a script. The recommendation STEP 0 can support: build `fold` (family A) on its own merits; `atom` at N≥16 (family D) second; `table` nowhere.**
- [PFX-1] STATE:not-started (FILED 2026-09-04 08:4x EDT by Frank, fifty-first session — D96: "the only items getting the prefix should be the exported items"; ADMIN/ABI column). THE PER-ARTIFACT PREFIX ONLY ON EXPORTED ITEMS: the `-p rx` prefix was designed for several pattern items per .c file; there is one artifact per translation unit, so internal statics, labels and internal macros need no per-artifact spelling — only the entries, `rx_info`, the documented stamps/macros and the header (docs/spec/match_api.md's contract) do. STEP 0 (D77): the CENSUS — every prefixed identifier in a representative artifact set (VM, DFA, hybrid, composed) classified EXPORTED vs INTERNAL against the spec; every harness/test site that reads an INTERNAL prefixed name (W1.3.1's four flush_block sites; the codegen checks' greps; the identity gates' region markers `<p>_L0`/`<p>_accept`); the emitted-bytes saving per artifact. STEP 1: the internal names take a fixed spelling (one abi event, D76/D94; every reader of the internal names updated; the spec states the rule); the harness's prefix sites shrink to the exported set. Sequenced BEFORE W1.3.1's build (its four sites become fewer). **CLARIFIED 08:5x (Frank, D96 addendum): exported = link-visible or in the .h; consider a shared constant header for the cross-artifact items; TEST two headers in one TU — measured now: compiles clean under -Werror (prefixed types/entries; the shared PCREC_* constants are identical redefinitions). STEP 0 adds the two-header codegen check with a byte-identity pin on the shared block, and the shared-header option weighed against the self-containment promise.** **DESIGN DIRECTION 09:0x (Frank, D96 addendum 2): the cross-artifact items — error codes, PCREC_UNSET, engine ids AND the common types (rx_ctx/zz_ctx are distinct types of one shape today, so consumer code cannot be generic over artifacts) — live ONCE, unprefixed, in a shared runtime header versioned with the abi; per-artifact headers carry only the exported entries + rx_info. STEP 0 chooses the delivery: a separate file vs the same block embedded verbatim under an abi-versioned include guard in every header (self-contained; same-abi artifacts share one definition, different-abi artifacts fail loudly on redefinition; a byte-identity pin across artifacts).** **VALIDATED 09:1x (D96 addendum 3): the shared block ALREADY EXISTS in every emitted header under `#ifndef PCREC_RX_ABI_H` — rx_ctx, PCREC_ERR_*, rx_callout_ref, rx_group_entry, struct rx_info, fixed `rx_` spelling, byte-identical across artifacts (measured) — so consumer code is already generic over artifacts; the "distinct types" claim is retracted. Per-artifact: the entries, the `<p>_info` instance, `<p>_buffers`, the `<P>_NCAPS`/frame macros, the outer guard. THE WORK: an abi-VERSIONED guard (different-abi artifacts in one TU fail loudly instead of silently sharing the first block), a byte-identity PIN on the block across artifacts, an optional standalone export of the block, and the fixed spelling for the .c's internal statics/labels/macros.**
- [OPT-VMLIT] STATE:not-started (TRIGGER PARTIALLY MEASURED 2026-08-31 by [OPT-5] STEP 0: literal words confirmed one-byte-per-label consume chains in emitted C, never memcmp; worst-case ctx subject l-03 (no context word, full 256-byte gap walk) costs ~2x a fast-resolving one — a BOUND, not a percentage (perf unusable on this box, paranoid=4); l-03 is the named instrument for the load-bearing number if this row charters) (Frank, 2026-08-30 17:3x, from reading VM output: "there was testing what was essentially a string via full steps. wouldn't it be quicker to do a string compare at that point? for sure possessive" — QUEUED behind [OPT-5], D86/D77) — LITERAL-RUN SUPER-INSTRUCTION IN THE VM: collapse consecutive literal nodes into one memcmp-class comparison (fold-compare under `i`). SOUND FOR ANY literal run, not only possessive: no choice points, captures or frames inside a literal sequence, so backtracking can never re-enter its middle — failing at byte k ≡ failing the run. Contract touchpoint: the step budget (D51 counts resumptions; a run charged as 1 step instead of n is a documented-value change — spec sentence at build time). MEASURED-NEED TRIGGER (D77): the literal-stepping share of a VM verify on the bench's ctx/level-context cells — the alternation branches there are literal WORDS stepped per byte on every verify, and the post-[OPT-4.1] residual vs the JIT (1.5-2.9×) is plausibly mostly this; [OPT-5] STEP 0's profile carries the measurement for free when the perf hold lifts. Adjacent mechanism: studies/simd1's needle table (2-32-byte compares near memchr speed) — the SIMD form is [OPT-SIMD]'s if the scalar memcmp is not already enough. FRANK'S SECOND HALF (17:4x): "can we do strcmp within the world of the string tries? strcmp straight runs" — YES, and the M2.8 trie (src/ir/nfa.c:192, priority-preserving prefix factoring of flat alternations) already IDENTIFIES them: a NON-BRANCHING chain of the trie is a straight run, and path-compressing it into a string edge compared by one memcmp cannot disturb priority (rules 1/2 only constrain where branches MERGE or OVERLAP; a chain has a single successor). Eligibility: the chain's classes must be SINGLETON bytes (or fold-pairs under `i` — compare against a folded copy). WHERE THE COMPARE LANDS IS PER ENGINE: in the VM this row's super-instruction is the landing (after factoring, `fail|abort|panic`'s suffixes are exactly straight runs — one memcmp each); on the DFA side the trie dissolves into subset states and a per-byte table walk, so the string-edge form belongs to [ENG-DIRECT]'s direct-coded emission (cross-note added there), not to this row. INLINING FACTS, MEASURED 2026-08-30 (gcc 15 -O2, this box; scratch probe): constant-length `memcmp` is ALWAYS inlined by gcc's by-pieces expansion — `memcmp(p,"panic",5)` compiles to one dword compare + one byte compare, a 19-byte literal likewise, ZERO calls; variable-length memcmp is a real call; **`memchr` is NEVER inlined — even `memchr(p,'q',8)` with a constant 8 is `jmp memchr@PLT`** (a PLT+ifunc indirection per call). Consequences for this row: emit the compares with CONSTANT lengths (free inlining; no call overhead on short words) and never spell a short-window scan as memchr. Today's emitted memchr (emit_dfa.c DFA_PF_MEMCHR, one candidate byte) scans `subject_length - scan_position` — rest-of-subject, long-run, the call cost amortizes: correct as is. CROSS-NOTE 2026-09-02: a candidate ISLAND BODY under [ENG-ISL]'s VM-in-DFA framing once [OPT-EDGE] triggers it — build it as an island shape shared by both engines, not as a DFA-only edge kind.
- [ENG-ABS] STATE:started (SECOND MECHANISM BATTERY-PROVEN 2026-08-29 ~05:4x on 808740c (code 517be95 = merge dfd112b): strict clean, anchors 186/197 resolve, `make -k -j12 test` 1,711 checks / 0 failed, 27/27 sections (the only reds: counterk's load cell 29 cases + the resource K7 cells INCONCLUSIVE under the load guard — all cleared solo: resource 20/0, counterk 24/0, corpus cell 1,634/0), `make san` rc 0 / 0 report lines both axes, mech 186 rows unexpected 0 / undetected 6 (S150-S153 S160 S178, expected) / unreached 0 / anomalies 0; the battery script's verdict line died on its own `set -u` bug (GUARD unbound — fixed in battery_v3.sh), verdict computed by hand: GREEN. [OPT-2] CLOSES into this row (its measured lever is now built and measured at 1.031×/0.482×). The FIRST mechanism (`^` absorption into ENG_UNANCH) stays gated on [BENCH-1]; this row stays STARTED for it. Bench inbox I-16 sent with the abi-10 pin. SECOND MECHANISM MERGED dfd112b 2026-08-29 ~02:2x — lane/engabs 2e2ce3f, 33 commits; r41 paneled (docs/dev/reviews/2026-08-28-r41-engabs-close.md): NO MISCOMPILE over 148,917 cells, every number reproduced (matching 1.031×/1.036× vs the VM, short emails 0.482×, failing 1 MB probe 363,305×), abi 10, `-fno-anchored-dfa` bit 17, registry 64, `RX_DFA_MATCH` unwrapped/search-filter + `rx_info.match_form`; pre-merge round: the optional machine's OWN ceiling PCREC_ANCHORED_MAX_STATES = 4,096 (S1: +46 % compiler CPU on the resource shapes without it; seven named fallback members — four resource shapes, three counterk 4000-count patterns under --no-captures), the captures-on differential arm (S4, 976 cells, S190 detected by the run), §8 corrected (the anchored machine is LARGER on 26/269, up to 2×). UNION BATTERY PENDING on dfd112b; [OPT-2] closes into this row at the battery; the FIRST mechanism (`^` absorption) stays gated on [BENCH-1]. OPENED 2026-08-28 ~22:3x, forty-fourth session, on Frank's "Go 1&2" — the SECOND MECHANISM only: anchored match-here via the UNWRAPPED forward DFA, lane engabs (opus, worktree lane/engabs); [OPT-2] closes into it. The FIRST mechanism, `^`-absorption into ENG_UNANCH, stays gated on [BENCH-1]'s `^`-on-some-branches case and is NOT opened) — formerly STATE:not-started — ENG_UNANCH absorbs `^` (the DD-7
  absorption half, re-homed here 2026-08-14, D42.7): D8 left `^` on
  ENG_ATTEMPT because the reverse machine has no position-dependent BOT
  variant; the recorded slow shape is `^`-on-only-SOME-branches
  (`(^a|b)c`). GATE (D12/D15 discipline): [BENCH-1] must first add a
  `^`-on-some-branches case and measure an actual loss — no bench case
  exercises `^` today, and unmeasured engine work is not scheduled.
  (gate re-pointed 2026-09-22: [BENCH-1] folded into [OPTLOOP]; the
  `^`-on-some-branches witness now comes from a cycle's analysis —
  cycle 1 named it under [OPT-ATTEMPT-SPLIT],
  docs/dev/optloop/cycle1_analysis.md M5)
  After M4, the change lands in the selection pass
  (src/opt/select_engine.c), not the pipeline driver. GATE MET 2026-08-28 for the SECOND mechanism: [OPT-2] STEP 2 (docs/dev/opt2_anchored_match_measurement.md) measured the reverse pass at ~50 % of the DFA's cost on every matching subject of the bench's compliance set — 2.077× behind the VM with it, 1.046× (parity) without, 0.571× on short valid emails; the unwrapped-forward match-here entry below is the named lever, its number to beat stated there. SECOND MECHANISM
  RECORDED (Frank + manager design thread, 2026-08-18, thirty-second
  session): ANCHORED MATCH-HERE VIA THE UNWRAPPED FORWARD DFA. Today a
  DFA artifact's `<prefix>_match` runs the ordinary unanchored search
  and filters on `caps[0][0] != ctx->pos` (spec §3.2 documents the
  mechanism as non-contractual) — correct, but a FAILING match-here can
  skim the remainder of the subject hunting a later match it will then
  discard, where the VM's match_impl fails at the first divergent byte.
  A runtime "anchored" flag on the existing tables CANNOT fix this —
  the start-anywhere self-loop is baked into the subset construction
  and the merged states erase which start a thread came from
  (DD-7/engine_m4.md §7.3: the wrap is structural). The generation-time
  form works and is CHEAPER than assumed: emit the pattern's UNWRAPPED
  forward DFA and run it from ctx->pos — anchored match needs NO
  REVERSE PASS (the start is known), so the cost is ONE extra table,
  typically smaller than either search table, derived from the same IR
  (same trust model as the existing forward/reverse pair). Failing
  match-here becomes first-divergent-byte on both engines. Partial
  zero-table fallback, recorded with its hazard: for MRL-bounded
  patterns match could clamp the search to pos+MRL, but clamping n
  changes end-of-subject semantics ($-bearing patterns), so it is a
  fallback only. GATE UNCHANGED: measure the failing-match skim on
  realistic subjects first; unmeasured engine work is not scheduled,
  and this does not queue ahead of M6
- [OPT-SIMD] STATE:not-started — SIMD EMISSION for candidate scanning. TARGET NOTE 2026-08-31 (Frank): "i have a macOS laptop using that fancy apple chip so we shouldn't assume i86" — DO NOT ASSUME x86: the design must be ISA-neutral from day one (an aarch64/NEON form beside the SSE one, or GCC/Clang vector extensions where they compile to the same shapes), and studies/simd1's numbers are x86-MEASUREMENTS only — the NEON equivalents (cmeq for cmpeq; tbl for the pshufb/shufti nibble lookup; the range trick via uqsub) exist but carry NO measured numbers until run on that machine; any emitted SIMD is gated per-ISA (#ifdef __SSE2__ / __ARM_NEON tier), with the scalar scan-* forms as the always-available fallback (the D82 candidate list makes this free — scan-simd simply doesn't select on an ISA without a measured form). RULING 2026-08-31 (Frank): BOTH ARCHS ARE SUPPORTED TARGETS — x86-64 and aarch64/NEON, neither primary. ARTIFACT-VARIANT DESIGN NOTE 2026-08-31 (Frank, for the non-memchr SIMD prefilter): "as its not a hot loop, i want to check if its reasonable to implement the simd stuff as a fallback so we don't have 4 versions of the pattern artifact e.g. 'if avx2 then ... else if sse ... else non-simd'" — the CHECK this row owes before emitting any SIMD: (a) MEASURE whether an outlined (noinline, or dispatch-once function-pointer) prefilter helper costs anything vs inline on prefilter-bound cells — if ~0 (plausible: the prefilter is not the innermost loop, and libc's own memchr is already an ifunc-dispatched indirect call that we happily pay today), then ONE emitted helper per class-shape carries the ISA ladder INSIDE it and the artifact has exactly one version of every loop site, with the ladder living in one place (or resolved at artifact-compile time by -march); (b) EVALUATE GCC/Clang portable vector extensions as the write-once alternative — they lower cmpeq/range forms well on both ISAs, but the shufti nibble-lookup (pshufb/tbl) and movemask (absent on NEON; needs the shrn trick) likely still need per-ISA intrinsics, so portable-vector may cover only part of the form table; (c) whichever wins, the D82 scan-simd form's BODY is that one helper — loop sites never grow ISA branches. memchr-shaped prefilters stay memchr (free ifunc dispatch in libc, already measured correct-as-is) EXCEPT where the window is provably short — D91's corollary: proven-short windows (minw/maxw or a counted bound) earn inline scalar/constant-length forms, since memchr never inlines. GOVERNED BY D91 (2026-08-31, Frank): scalar-first before SIMD, and the TWO dispatch budgets — the prefilter site (~once per match point: one artifact, runtime selection is noise — settled) vs the inside-the-pattern site (many hits per match: re-measure, never inherit the prefilter argument). Cross-links: [CC-CLANG] (macOS = clang toolchain, so the cc axis is a precondition for that target's numbers) and [OPT-5] STEP 1's rulings R1/R2 (the scan-simd slot is named ISA-neutrally). CROSS-NOTE 2026-08-30 (Frank): the study's class-shape table — 2-3-member classes, ranges and arbitrary sets all branchless within ~2× of a single `cmpeq` — means class PREFILTERS of any size are candidates here, not only single rare bytes; and its run-extension result (classify+clz 2-3× over a scalar table loop) is [OPT-5] STEP 0's first hypothesis for the counted-DFA letter-run loss. **SEQUENCING RULED (Frank, 2026-09-03): SIMD comes LAST — "get what we can algorithmically and generally first, then pull out SIMD at the end in an 'I'm not left-handed' manner". pcrec exceeds the JIT on most benched cells without it; every row ahead of this one is a general mechanism ([OPT-EDGE], [ENG-ISL], [LIM-2], [OPT-NEG], [OPT-ALTHASH], the census's questions); an outlier whose only fix is SIMD ranks behind one whose fix is general. studies/simd1 stays the reference; this row opens when the general queue is measured out.**
  (Frank, 2026-08-14 nineteenth session, D41.6): AOT-composed SIMD
  prefilters emitted directly into generated matchers — multi-literal
  substring scanning, pshufb nibble-table class tests, 0x20-fold
  case-insensitive comparison — operating 32–64 bytes per iteration with
  little or no conditional branching. Frank's motivating example:
  `((?i)abc|dev|bet|[a-f]{5})` built as a composed SIMD scanner, which
  only an ahead-of-time compiler can specialize this way. Framing
  recorded at D41: this EXTENDS the existing memchr/skip-loop lever
  (libc memchr is already SIMD; this emits the multi-byte/class/
  multi-literal generalizations directly), it is prefilter/DFA-engine
  territory orthogonal to M4's captures work, and it overlaps OPT-A's
  Teddy/shift-or leads — the survey there feeds this row; adoption
  measured under BENCH-1's instruments. (gate re-pointed 2026-09-22:
  [BENCH-1] folded into [OPTLOOP]; adoption is now measured under an
  [OPTLOOP] cycle's own bench carve-out, e.g.
  docs/dev/optloop/cycle1_analysis.md, rather than a standing BENCH-1
  instrument) Portability is part of the
  design: gcc vector extensions vs target-gated intrinsics vs scalar
  fallback is a generation axis that must earn itself (D18), and the
  no-libc/freestanding line must keep a scalar path. Interface
  consequence already ruled (D41.5): one-shot search stays the
  primitive; block-context preservation across matches belongs to
  emitted loops now and the designated `<prefix>_iter` cursor entry
  later — this row does not reopen that
- [OPT-B] STATE:not-started — PROFILED code-level optimization, only after OPT-A. D13's correction says throughput here is dominated by transition PREDICTABILITY, so target branch behaviour and memory layout rather than instruction count. Every number under D12's rules and the R3.10 load guard
- [OPT-C] STATE:not-started — COMPILE-TIME optimization, last. Must include what gcc does with our output, not only what pcrec does: after M2.8, gcc is the LARGER half (0.79 s vs 1.36 s at 3600 words) and M2.9's budgets measure only pcrec's
- [OPT-D] STATE:not-started — NO-IMPACT SPACE SAVINGS in the emitted artifact (Frank, 2026-08-28: "no impact space savings. Eg if the class bitmaps are repeated (ex forward and back dfa) then keep one copy"). Deduplicate emitted read-only data that is byte-identical across the artifact's machines — the byte-equivalence-class map / class bitmaps shared by a forward DFA and its backward (reverse-scan) twin, and any other table the emitter writes once per machine when the contents coincide — so one copy is emitted and every machine's accessor block points at it. SCOPE: artifact SIZE only; answers, stamps and the match API unchanged (answer-identity gate + `make test-axes` are the controls); runtime cost must be ZERO, proven the [ENG-FORM] way (objdump of the hot loops + timing within spread — D82), since a shared table is the same load either way. The [ENG-FORM] representation objects are the natural site: dedup is a SELECTION over the candidate lists (a table candidate that resolves to an already-emitted twin), not a special case in a machine's emit path (memory `pcrec-general-mechanisms-not-special-cases`). Measure first (D77): a census of emitted tables per artifact over the corpus — how many bytes are duplicates, on which patterns — names the win before the code; a change to the emitted scaffolding is an abi bump with its four sites (D76). Low priority; queued behind the bench-gated rows

## Thread-safety (D19) — usable FROM threads; guards, not prose

Audited 2026-08-09: generated code and the library are BOTH thread-safe today
(every emitted static is const, no file-scope mutable state anywhere in src/,
Ctx and its jmp_buf are locals of pcrec_compile). These steps exist to keep
that true, because it is invisible to every current test and a one-line change
can destroy it.

- [TS-4] STATE:not-started — DD-10 is a thread-safety item, not just robustness (D19): musl's default THREAD stack is 128 KB against the main thread's 8 MB, and `compile_ast` plus `clo_visit`'s t1 edge are still bounded only by pattern structure (~192 KB for 400 nested branch points). Give `compile_ast` a stated budget the way trie_build has one, and add a `tests/cli` stack case that binds it — case 8 covers branch COUNT, nothing covers nesting DEPTH

## Process mechanization (session 2026-08-09) — turn recurring lessons into tools

Four consecutive checkpoints have found the same failure class: not compiler
defects, but measurement claims about safeguards that were stale, contaminated,
or never made. Writing the lesson down demonstrably does not install it (this
session restated a load-contamination rule and violated it in the same
document). Mechanize instead.

- [SR-11] archived to plan_completed.md (completed 2026-08-22 — the table contract implemented: tests/lib/table.sh + converted consumers + both checks; docs/spec/table_contract.md is the spec)
- [TT-3] archived to plan_completed.md (completed 2026-08-22 — measured NO for make test / qualified YES for mech; CCACHE=1 wiring merged opt-in; docs/testing.md "Compile caching" is the record)
- [MECH-3] STATE:not-started — a measurement wrapper that refuses to emit a number without provenance: interleaved A/B, N trials, load before AND after (R3.10), min/median/max spread, and a stamped record. Every performance overclaim this project has made — the 27%-recorded-as-+40%, this session's 1.5-4.1% deltas taken at load 4.5-9.7 — would have been blocked at the point of measurement rather than caught in review. Frank's precedent: a claude-safe grep that refuses `| tail` and reports what it actually looked at

## PCRE2 compliance tracking

- [DOC-DRV] archived to plan_completed.md (completed 2026-08-22 — the compliance page is annotated derivation: docs/pcre2_compliance_annotations.txt + the extended compliance_section.py checks; the compliance-refresh skill carries the process)
- [PC-2] STATE:not-started — periodic re-survey: re-read pcre2syntax.html,
- [PCRE2-UP] STATE:not-started (Frank, 2026-08-30 forty-sixth session: the box is at the newest packaged PCRE2 for its Ubuntu (10.46, 2025-08-27); a newer PCRE2 comes only from GitHub — 'we could do a sub-repo in the pcrec repo'; NOT started, no measured need yet) — a git sub-repo of PCRE2 inside pcrec for the differentials (PC-3, the module oracles, W-8) when a construct newer than 10.46 matters: the first known one is the capture-returning subroutine call `(?&name(grouplist))` (D89 item 4; 10.46 REFUSES the spelling, measured). Landing it moves every differential baseline (a PC-2 re-survey fires; refusal cells for the new spellings flip from both-refuse to PCRE2-accepts/pcrec-refuses-by-name — fine under D26) and adds a build dependency the freestanding/stranger's `make` must not need (make test may; make must not).
  re-run tests/reject, move landed modules from REJECTED to OK, re-stamp the
  date. Do this whenever a module lands and at each checkpoint review

## Far-future concepts (the boonies — unscheduled, no milestone)

- [ENC-MODEL] STATE:not-started (CHARTERED unscheduled by Frank 2026-09-06, fifty-sixth session, boonies — "formalize encoding ascii as an encoding ... byte encodings would be translation tables") — THE FULL ENCODING MODEL: pattern encoding vs subject encoding separated, on the architecture that already exists. (1) THE PATTERN SEMANTIC SPACE IS UNICODE, canonically utf8 source — this is DD-12(1) as ruled (one parser, no encoding in the grammar, CharSet code-point intervals, encoding is a LOWERING instance), restated as the model's spine; [UTF-PAT] (raw utf8 pattern text contract) is its existing near-term half. (2) NON-UTF PATTERN SOURCES are a PRE-COMPILE CONVERSION WRAPPER (source-encoding -> utf8 before the parser), never a parser axis — Frank: "Best handled perhaps pre-compile ... it takes out this whole complication to a wrapper". Named risk to design against: misconverting rx OPERATORS — acute for multi-byte source sets whose trail bytes collide with ASCII metacharacters (Shift-JIS class) and for EBCDIC; single-byte sets are near-trivial. (3) SUBJECT ENCODINGS are TRANSLATION-TABLE BACKENDS on the D58 seam: a single-byte encoding = a 256-entry byte->code-point table (standard mappings exist for ISO-8859-x/Windows-125x/etc.); the lowering intersects the pattern's code-point intervals through the table's image to byte sets; src/gen/enc/'s third-encoding recipe is the landing shape. ASCII FORMALIZED as the first such table encoding (identity on 0x00-0x7F, 0x80+ ill-formed per the encoding's own rules — which also gives \d/\w/POSIX classes their per-encoding statement Frank asked for). (4) THE OPEN RULING, recorded not decided: what \x{hex}/octal DENOTE under a table encoding — a Unicode code point (mapped through the table, refusing points outside its image) or a subject-encoding unit; the split is visible exactly where byte != code point (Windows-1252 0x80 = U+20AC). Frank leans subject-encoding for the byte-valued spellings; the stage-2 per-encoding range-check precedent (byte caps at 0xFF) is the nearest ruled neighbour. D77: no build before a consumer; the row opens on Frank's word, likely after M5.0 completes and [UTF-PAT] lands.
- [FOLD-BASE] STATE:not-started (CHARTERED unscheduled by Frank 2026-09-06, fifty-sixth session, boonies, from reading encoding tables: "a flag analogous to i that folds those characters together for matching") — DIACRITIC/BASE-CHARACTER FOLDING, the caseless architecture's second customer. Two strengths, named apart: CANONICAL EQUIVALENCE (precomposed e-acute == e + U+0301 — one abstract character, two encodings; ICU/Java CANON_EQ is the precedent, Java's notoriously slow at match time) and FOLD-TO-BASE (e-acute == e-grave == e — accent-insensitive, collation primary strength; what Frank asked for; subsumes the first). PCRE2 has NEITHER — a beyond-PCRE2 capability in [M4-SUBST]'s mold, pcrec's own flagged namespace per SR-10. THE MECHANISM IS D23's, WIDENED: caseless folds at class CONSTRUCTION and emits a fold-blind automaton; base-fold is the same move over UCD canonical-decomposition data (vendored at M5.0 stage 3 regardless) — a literal widens to its base-variant set. The wrinkle that keeps it honest: COMBINING SEQUENCES (NFD subjects spell e-acute as TWO code points), so under the flag each literal lowers to [base-variants] followed by mark absorption (\p{M}*-shaped) — still regular, still compiled, which is the AOT wedge: the whole equivalence closure lands in the tables at compile time where runtime engines pay per-match normalization or CANON_EQ blowup. \p relation stated so nobody re-derives it: \p{M} gives the VOCABULARY (talk about marks), never the EQUIVALENCE — this flag is new capability. Caveats for the design round: locale sensitivity at the edges (Swedish aa-ring/o-umlaut are distinct LETTERS; Turkish dotless-i) — first form scopes to locale-agnostic UCD decomposition with the caveat stated, never collation tables; and the flag interacts with `i` (case fold and base fold compose — the combined closure is one constructor pass, not two mechanisms, per the general-mechanisms rule). MECHANISM OPTION, NOT A RULING (Frank proposed it same day and downgraded it same day — "Downgrade my alternations as an option not a ruling"; the design round weighs it against the direct constructor-widening shape): the fold DESUGARS each literal to the ALTERNATION of its variants — the fully general form, expressing multi-code-point variants no class can hold — and [OPT-ALTCLS] (already in the tree, running right after parse, whose stage 1 is exactly single-char-run -> class merging) recovers the efficient class form where recoverable. Zero new engine machinery; the feature is a constructor desugar plus the existing optimizer. The unbounded-mark-stacking nuance rides as one desugared branch of shape base-then-mark-absorption beside the enumerated variants — still alternation, still altcls-visible for its class parts. DEPENDS on stage 3's UCD vendoring + stage 4's fold-closure machinery (this is DD-1's constructor one axis over); D18 earn-its-axis; opens on Frank's word after M5.0 completes.
- [MULTI-PAT] STATE:not-started (CHARTERED unscheduled by Frank 2026-09-06, fifty-sixth session, from the Hyperscan discussion: "note it as an unscheduled row and put it in the boonies") — COMPILE A PATTERN SET INTO ONE SCANNER ARTIFACT, the Hyperscan concept on pcrec's own DFA. A plain alternation of N search patterns already yields the union machine (the same construction Hyperscan starts from) but NOT its results — three semantic gaps, recorded so the row starts honest: (1) PATTERN IDENTITY — an alternation reports "a match", not which branch; the subset construction already knows (a state's accept derives from which branches' accept states are in the set) and pcrec's priority-DFA machinery exists precisely to ERASE that (D3 accept-pruning picks the preferred branch), so the extension is multi-accept TAGGING, a reporting discipline on the existing machine; (2) ALL MATCHES vs leftmost-first non-overlapping — Hyperscan reports every pattern's every match end, overlapping included; PCRE find-all resolves by priority and resumes past the match (foo|foobar on "foobar" reports foo and never foobar) — the all-ends walk is CHEAPER than today's (no reverse pass, no priority resolution; the SOM-is-expensive mirror of [OPT-2]'s reverse-pass finding), so the shape is a new all-ends reporting ENTRY beside search, never a change to search's semantics; (3) STREAMING — save/restore automaton state across buffer boundaries, a property a pure DFA has and today's one-shot artifacts do not expose. Scope: DFA-compilable subset only (faithful to Hyperscan's own restriction); pattern-set INPUT rides the .rxt/definitions machinery that exists; scale caveat stated up front — union determinization is where blowup lives and where Intel's real effort went (literal prefilter decomposition, Teddy/FDR); pcrec's subset-elems/state caps refuse large unions HONESTLY, so the first instantiation is tens of patterns, not Snort-scale. D77: no build before a measured need — a named consumer wanting "scan for any of these N patterns with IDs" is the trigger; the design pass then decides tagging layout, the entry's contract (end offsets only vs paid SOM), and whether streaming is in or out of the first slice. SIBLING ROW: [V-E] (multi-pattern COMPILATION UNITS) is the complementary half — V-E packages N independent matchers into one unit with rx_info as identity; this row fuses N patterns into ONE machine with set semantics. A real consumer likely wants both (V-E's unit surface carrying this row's set scanner as one member).

- [JSONL] STATE:not-started (CHARTERED unscheduled by Frank 2026-09-08 late evening, boonies, from watching pcrecdev2's slow jsonl reader: "jsonl is something that this rx might parse very well... not only can it do it, but it would be about as fast as it could be done") — JSONL AS AN APPLICATION DOMAIN, five tiers stated by Frank: (1) legal-JSONL validator, (2) schema validator for a given schema, (3) general parser via CALLOUTS, (4) known-schema jsonl parser, (5) jsonl-to-csv/etc converter via replace. THE ANALYSIS THAT ORDERS THEM (manager, same night): jsonl's line-per-record property makes the LINE the matching unit, so the usual JSON-isn't-regular objection mostly dissolves; tier 4 LEADS — a fixed schema with bounded nesting is a nearly-regular line language compilable onto the DFA/hybrid rungs with capture slots as the field extractor, which is where the as-fast-as-possible claim is strongest AND where the NAMED CONSUMER lives (the bench's store/*.jsonl record-schema reader, measured slow by its own operator — the D77 trigger is already half-met; ask the bench for the reader's baseline numbers + the schema when the row opens). Tier 1 general legality needs module `recursion`'s (?1) machinery (exists; VM rungs — honest boundary: a hand parser competes there; DEPTH-BOUNDED validation unrolls back toward the fast rungs and real schemas are depth-bounded). Tier 5 rides [M4-SUBST]'s direction. Tier 3 needs a NEW drop-in module `callouts` (PCRE2-compatible surface; the AOT form compiles a callout to a DIRECT CALL in the emitted C — no interpreter indirection, a beyond-PCRE2-quality story and a D18 earn-its-axis candidate). Sibling rows: [MULTI-PAT] (a schema set as one scanner), [V-E] (shipping a set of schema matchers behind the organizer). Opens on Frank's word after a design pass; first instantiation = the bench reader's schema, benchmarked against their current python reader.
- [WORD-FOLD] STATE:not-started (CHARTERED unscheduled by Frank 2026-09-11, sixtieth session, boonies, INVESTIGATION, from the cls-fold/cube discussion: "8 byte masks then compare?") — WORD-WIDE CUBE COMPARE FOR LITERAL RUNS: the study's per-position cube analysis (cube_of over the bare set) vectorized ACROSS positions — for an 8-byte literal window where every position's accepted set is a one-cube, THE CANONICAL FORM IS THE AND-MASK (Frank, 2026-09-11, superseding the first OR spelling): `memcpy(&w, subject+pos, 8); (w & K) == T` with per-byte K = ~freebits and T = base & K — fold DOWN, not up — which accepts exactly the cross-product of the per-position cubes AND makes every byte kind one vocabulary: exact byte K=0xFF, caseless letter K=0xDF, any one-cube class K=~free, and a byte PAST THE LITERAL'S END is the degenerate all-bits-free cube K=0x00/T=0x00 — so a short window's tail IS the same test as the rest (load+and+cmp, 3 ops uniformly), not a special case sharing code (sound and exact; case-exact positions mask 0x00, caseless ASCII 0x20, every other one-cube class participates identically — general mechanism, not a caseless special case). MEASURED BASELINE (2026-09-11, emitted artifacts read directly): (a) the VM emits a literal chain as ONE INSTRUCTION PER BYTE — bounds check + single-byte compare + goto per position, both casings (caseless = the same chain with per-byte |0x20) — so an 8-wide window collapses 8 bounds checks + 8 compares + 8 dispatches to 1+1+1; FUSIBILITY PRECONDITION: no label inside the window may be a live resume target (statically knowable; [OPT-VMFL]'s census puts 35% of VM artifacts entirely frameless where it holds trivially). (b) The DFA's offset-k candidate-start skip (rx_ofsskip, docs/design/offset_k_skip.md) DEGRADES TO NOTHING under (?i) — memchr needs one exact byte, a caseless offset has two, so the caseless twin loses the whole mechanism; the cheapest scan-side win is a SELECTION PREFERENCE (scan a case-invariant offset — digits/punct/_ never fold — and cube-compare only the candidate checks), needing no new scan machinery; two-memchr and SWAR-scan are the fallbacks where no invariant offset exists. SCOPE: byte-domain cubes only (ASCII case IS the 0x20 bit; utf8 multi-byte folds were never byte cubes — same boundary D23's construction-time fold draws); NO pattern-length tail ladder (Frank, 2026-09-11 same day; subsumed by the AND-mask canonical form above): every literal length 1..8 is the one `(w & K) == T` compare and longer literals are ceil(len/8) of the identical shape, uniformly; the SUBJECT-end bound is the separate, surviving guard (the 8-byte load reads subject[pos..pos+7] and pcrec does not own the buffer — the page-boundary trick real engines use fails the both-axes ASan/UBSan discipline), reduced to: wide compares wherever pos+8<=n, per-byte fallback only inside the subject's final <8 bytes (fixed-cost epilogue, independent of pattern length; K27-style, UBSan-verified both directions); memcpy loads + M/T/K built from static byte arrays (alignment-UB-free, endian-neutral); non-cube positions split the window or fall back. D77 GATE — THE CENSUS FIRST: count corpus+bench literal runs >=4-8 bytes with >=1 cube-but-not-singleton position (Frank's own caution recorded: the shipped corpus may contain NO example — "there may be scenarios" — so the census enumerates scenario classes too, e.g. caseless keywords/header names, and overlaps [FORM-CHAR2] (ii)'s hot-fold-constant question); a zero-population census parks the row without prejudice. SIBLING: [CLS-TREE] (this is a SEQUENCE-level kit member built on the same cube_of; the design note may cite it but is NOT gated on it). SWAR in portable uint64 C — the scan-edge/mask-test register-width family, not SIMD; does not jump the SIMD-last queue.

- [CLS-TREE] STATE:started **PARKED 2026-09-21 into [BACKLOG-TRIAGE] (Frank, seventy-fourth session: "agree on sequencing" — the bench matrix ranks it against everything else; not before [REL-1]).** (STUDY DELIVERED+MERGED 2026-09-11, lane clstudy, 039cfffb: docs/dev/cls_tree_study.md + studies/cls_tree_study/ — kit-not-per-class-form; sectioning DP with lambda as the dial; D77 verdict = Constraint 2 cache NOT triggered at 25.86ms worst-set discovery; provenance-blindness proptest 438/438; 1.885B compared answers 0 mismatches. NEXT: design note + panel, gated on the memo + the ns/char timing arm. NS/CHAR ROUTING RULED (Frank, 2026-09-11, sixtieth session): the darwin load1<0.5 gate stays UNTOUCHED (calibrated on headless ubuntubudu; unreachable on a desktop Mac — 19 samples min 0.71; darwin timing is never citable, scratch-directional at most); the ns/char run executes on ubuntubudu, riding the I-65 executor session (`make -C studies/cls_tree_study bench CC=gcc`, bench.py's own gate passes there). Both 2026-09-11 charter amendments were in the brief verbatim; Constraint 1 discharged as measurement, Constraint 2 closed by its own gate.) (CHARTERED by Frank 2026-09-10 ~03:0x, from \P{Unknown}'s week: "We need a class implementation that is a binary tree of ranges") — THE THIRD CLASS-EMISSION FORM: a huge class emitted as DATA (the A_CLASS sorted interval set as a compact rodata table, 8 bytes/interval) with membership by BINARY SEARCH over ranges (~log2(n) compares per character), instead of automaton STRUCTURE whose size grows with interval count. The D77 trigger is already MEASURED: the K53 family (six spellings refuse at default axes incl. \p{L} utf8, 1,076,640 bytes) and K55 (\P{Unknown}'s 729 intervals = 689,367 VM bytes vs the 500,000 cap) exist ONLY because today's forms encode intervals as byte-machine structure — the tree form prices \P{Unknown} at ~5.8KB rodata + ~10 compares. DESIGN PASS DECIDES: (1) the VM instruction shape (decode-next-char, search, advance — the natural host; stage 4's caseless-backref residual is the decode-in-artifact precedent and its ill-formed rule is the correctness seam, inherited verbatim: the class test's decoder rejects exactly the automaton's ill-formed set); (2) the DFA/hybrid seam (a byte-wise DFA cannot search mid-state — tree-classes as a rung/edge in the hybrid, not forced into states); (3) DIAL PLACEMENT ([OPT-DIAL]'s threshold shape: bitmap/compares small, byte-automaton medium — fastest per char — tree huge; the crossover interval-count is a MEASUREMENT); (4) caseless composition (the fold applies at construction per stage 4's per-contribution rule — the tree stores the folded interval set, no runtime fold); (5) what it RETIRES (most of tests/known_fail/k53_uprops_oversize.rxt + K55's witness stop refusing; [K53-SELRETRY] stays as selection-layer correctness — sibling, not superseded). ADDENDUM (Frank, minutes later: "Or something other than a bit array. Because that size is crazy"): THE REPRESENTATION IS OPEN — tree-of-ranges is the SEED, not the decision. (Precision for the record: the crazy sizes are not a bit array today — byte classes use 32-byte bitmaps; the megabytes are AUTOMATON STRUCTURE, intervals decomposed into byte-sequence states/instructions — which is why any class-as-DATA layout collapses them.) Candidate layouts the design pass measures against the real 312 script sets + the K53 six, on size × per-char cost: (a) sorted interval table + binary search (the seed; Eytzinger/cache-optimal ordering as a variant); (b) STAGED PAGE TABLES (PCRE2's own property-lookup shape: plane→page index→per-page 32-byte bitmap leaves — O(1) per char, dense-page-friendly); (c) interval/bitmap HYBRID (intervals for sparse spans, bitmap leaves for dense pages — likely the size winner on real script data, which is bursty); (d) branchless/SIMD inversion-list probe if timing demands it. The chosen form is a MEASURED pick, one mechanism (no per-class special cases; the dial threshold selects form by class shape). Engine tier (opus), design note + light panel before implementation; opens after [K53-SELRETRY] lands or on Frank's word. **REFRAMED 2026-09-10 (Frank) from "a third form for huge classes" to THE GENERAL SHAPE: a class matcher is COMPOSED PER-SECTION from a small kit of representations, STATICALLY SELECTED.** A class is a statically-known interval set; partition it into runs at compile time and emit the CHEAPEST test per run — a dense ≤64-code-point span → a single uint64 mask test (NO memory load; [FORM-CHAR] STEP 0 measured branchless mask+compare beating a table load when the run fits a register), a sparse scatter → 2-3 range compares, a huge contiguous block → a bound check, a wide table-worthy region → the tree/page-table/bitmap-leaf (the original [CLS-TREE] payload, now just ONE kit member). Applies to BYTE classes too, not only huge/\p ones: a degenerate `[...]` that today gets a flat 32-byte bitmap may be two 64-bit-fittable runs → two mask tests, no load. The dial ([OPT-DIAL]) keys the PER-SECTION choice, not one form per whole class; because we see the class statically and emit C, the matcher is bespoke straight-line code (the cheapest test per section), not a general-purpose membership loop. SIBLINGS FOLDED IN: [FORM-CHAR]/[FORM-CHAR2] (the caseless class-vs-fold measurements) and [OPT-CLSPACK] are the same kit's members measured piecemeal — the design pass unifies rather than adding a parallel mechanism (general-mechanisms rule). THE STUDY (Frank: "can we test algorithms?" — YES, the prototype-before-commit shape): a read-only study lane builds each candidate representation as a standalone matcher and benchmarks membership (size × ns/char, both engines, cache footprint) over the REAL populations — the 312 script sets + the K53 six + a representative byte-class sample — and the SECTIONING algorithm itself (how to partition an interval set into runs, and the per-run selection rule) is a measured output, not an assumption. Study first (no src/), then the design note picks the kit + the sectioning rule from its numbers, then implementation. **+2 PLACEMENT DIRECTION (Frank, 2026-09-16 ~23:4x EDT, at the dial table's ratification): the kit's EXTREME-SPEED forms — full-table/huge-bitmap class membership, the ~227-294KB-object-bytes family the study measured, its "few huge bitmaps at 5.6x size for 6 probe ops" notch — are +2's INTENDED CONTENT, not a place the dial stops before. The study's own §4 caution ("the dial should stop before the extreme notch") is superseded IN DIRECTION by the D103-addendum art-over-science ruling: +2 exists exactly for ridiculous-but-measured trades chosen deliberately. The design note prices that notch as a real +2 lambda position (with its measured rate cited), rather than excluding it.**

- [TT-4M-TIME] STATE:not-started (owed 2026-09-10) — the CLEAN post-fix HARNESS_BATCH timing: a QUIET-BOX, normal-conditions full `make test` at HARNESS_BATCH unset vs =64/PROCS=8 on the current tree AFTER the 2-char-prefix fix (a2c89c06). The 2d evidence (docs/dev/tt4m_2d_evidence/) is upper-bound-understated (ran under concurrent load) AND predates the prefix fix, so no clean suite-level number exists. Small sonnet lane. CORRECTION 2026-09-10 (Frank): the manager mis-stated the customer set. HARNESS_BATCH batches per-pattern COMPILE+LINK, a DIFFERENT axis from the census's per-case-EXEC finding (corpus's 19,185 subject runs). EVERY section that drives run.sh to compile patterns is a customer — so at least THREE: corpus (done), axes (run_axes.sh re-invokes run.sh — in flight), and **`make san` (run_san_group.sh runs tests/harness/run.sh under GENCFLAGS instrumentation) — likely the HIGHEST-VALUE customer, since sanitized compiles are the suite's slowest and san is the battery's longest compile stage**. San is Linux-only today (K54), so batched san helps the LINUX battery/executor (~68-min stage), not darwin. The lane must GREP the section drivers to enumerate every run.sh-compiling section (do NOT assert from memory — this correction exists because the manager did); the differential-driver sections that compile via their OWN C drivers rather than run.sh are the non-customers, and which is which is a grep result, not a recollection. Deliverables: the clean corpus timing (above) + the enumerated customer list + a san-arm batching feasibility note (does GENCFLAGS's -fsanitize ride the batch link as the design note claims — verify on the Linux arm via executor if needed). Gated on the box being free (axbatch's sweep owns it now); the default-on flip decision reads THIS number, not 2d's.

## Small-debt shelf (light-session filler; pointers, not new rows)

- [FEAT-GLOBAL-RETIRE] (2026-09-21, from [REL-1.11]): `pcrec_enabled_set_spec`'s process-global enabled set survives only for the CLI query modes that never call `pcrec_compile()` (`--probe-ask`, `--count-groups`, `--list-source`, `--list-syntax`'s built-status probe). A parallel path (memory `pcrec-general-mechanisms-not-special-cases`): route those through the pure resolver + an explicit set and delete the global. TRIGGER (D77): the next change that touches those modes' feature path, or [LANG-1]'s C study if it wants the library thread-safe end to end.

Pointers, not queue positions — states live on the real rows cited.

- K9 — the public API takes no pattern length, so a pattern containing NUL
  compiles as its prefix; fix before any V-tier consumer exists
  (docs/dev/known_issues.md).
- TS-4 / DD-10 — stack budgets for `compile_ast` and `clo_visit`'s t1 edge.
- DD-8 — `--emit-ir` / `--emit-dot`, useful during M4 bring-up.
- SR-5 — guard the fast-path lookup-count claim with an instrumented build.
- MECH-3 — schedule before [OPT-A] opens.
- module-swap / row-deletion guard — owed, unruled.
- PC-4 missing shapes — caseless-negated, `\N{n,m}`, MODIFIER, zero-tail
  `(?P`.
- PC-2 — re-survey.

