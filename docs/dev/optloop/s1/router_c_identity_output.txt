S1 review C1 -- recorded output of router_c_identity.sh, run 2026-09-25
against pcrec worktree lane/s1rev at f37e5c23 (HEAD, abi 32) and pin
25b1984f (abi 27), gcc-16 -O2. Reproduce with:
    SCR=<scratch> REPO=/Users/fdicostanzo/pcrec/worktrees/s1rev HEAD=HEAD \
        docs/dev/optloop/s1/router_c_identity.sh

=== diff: 25b1984f default vs HEAD -fno-req-run ===
--- router_pin25b1984f.c
+++ router_head_fno_req_run.c
@@ -1,11 +1,15 @@
-/* Generated by pcrec (abi 27). Pattern: /user|/users */
+/* Generated by pcrec 0.1.0-beta (abi 32). Pattern: /user|/users */
 #define PCREC_FEATURE_SET "std1"
 #define PCREC_FEATURE_MODULES "classes,modifiers"
 #define RX_ALTCLS_MERGES 0
 #define RX_ALTCLS_FACTORED 1
 #define RX_STARTPOS_GUARD "permissive"
+#define RX_END_WINDOW "none"
+#define RX_REQ_BYTE "47"
+#define RX_REQ_RUN "none"
+#define RX_REQ_WHY "dominated"
 #define RX_TUNE "balanced"
-#include "router_pin25b1984f.h"
+#include "router_head_fno_req_run.h"
 #include <string.h>


@@ -229,7 +233,7 @@
 }

 const struct rx_info rx_info = {
-    .abi = 27,
+    .abi = 32,
     .flags = 0ULL,
     .encoding = 0,
     .ncaps = RX_NCAPS,
@@ -254,4 +258,6 @@
     .name = "rx",
     .nentries = 0,
     .search_form = "reverse-pass",
+    .vars = NULL,
+    .nvars = 0,
 };
=== end diff ===

Verdict: EVERY differing line is the header comment (version string +
abi number + include filename), a stamp #define a LATER feature
introduced (RX_END_WINDOW/RX_REQ_BYTE/RX_REQ_RUN/RX_REQ_WHY -- all
reflection stamps, none of them a branch or a table any function reads
to decide behaviour), or rx_info's .abi/.vars/.nvars fields. Every
function body, every table and the whole program's control flow are
byte-identical. This is the "only header/abi/.vars/stamps differ"
claim C1 asked for an artifact behind, and it is what
`docs/design/litscan_s1.md` cites as router's arm (c) at both pins.
