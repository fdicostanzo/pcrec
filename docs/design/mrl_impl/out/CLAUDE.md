# docs/design/mrl_impl/out/ — archived probe output

Verbatim output of `../probes/mrl.py`, stable-named so a re-run diffs against
it (D35's shape). EVIDENCE, NEVER AN ORACLE: no check in the tree reads
anything here, and a stale file is a stale file rather than a silent test
failure.

Maintenance: regenerate by re-running the probe; update this file when the
set of archived outputs changes.
