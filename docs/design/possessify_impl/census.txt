== [ENG-BREP] possessification census ==
date:   2026-08-16T03:40:48Z
commit: baeadb8c3c58d2934119daecbf8f7bfe9269263f
pcrec:  /home/duxevents/pcrec/worktrees/possessify/build/pcrec
locale: LC_ALL=C (R24 M-F1: collation is why this is set)

-- corpus
  distinct patterns in tests/**/*.rxt : 793
  compile under the default options   : 725  (refused: 68)

-- DEFAULT routing (what ships)
  routed to the DFA, never reach the pass : 577
  routed to the VM                        : 148
  ... of those, with >=1 possessified     : 59

-- FORCED --engine=vm (the analysis over the whole corpus)
  patterns measured                       : 726
  SOURCE quantifiers (A_REP nodes)        : 1784
  ... POSSESSIFIED                        : 226
  ... backtracking                        : 1558
  patterns with >=1 possessified          : 210

  emitted quantifiers (rung marks)        : 2929
  ... POSSESSIFIED                        : 223
  (the gap against the SOURCE counts above IS the replication a bounded
   repeat performs: the emitter marks a rung once per emitted copy.)
  possessification rate (quantifiers)     : 12%

-- against eng_brep_design.md §7's predictions
  §7 predicted 613 of 756 corpus patterns capture-free/DFA-routed and
  untouched; measured here: 577 of 725 DFA-routed.
  §7 predicted 174 of 1,725 quantifiers touched (both bounds) and the R24
  fix-lane census 183 possessifiable; measured here: 226 of 1784
  SOURCE quantifiers.

  The denominators are close but NOT the same population, and the
  difference is structural rather than a discrepancy: §2.6 counted
  quantifiers in the PATTERN TEXT with python's parser, and reported 112
  corpus patterns python cannot parse rather than dropping them. This
  counts A_REP nodes in pcrec's OWN tree, over the patterns pcrec
  compiles, so it includes those 112 and excludes the ones pcrec refuses.
  The corpus has also grown since §2.6 was measured. Read the RATE.
