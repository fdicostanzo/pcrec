# R18 — MOD-0.4 close panel (module `verbs`, the migration test)

2026-08-12, twelfth session. Three read-only critics (checks / engine /
docs), narrow briefs per D6, spawned in parallel while the MOD-0.4c battery
ran; subject tree 8e7597d. Panel score: every verified cell held — no wrong
cells found in any report (the second clean panel in a row, R17's
both-sides-measurement standard restated in every brief). The engine critic
independently re-confirmed the migration's byte-identity by diffing
mod_verbs.c's pcrec_ext_verb against the pre-move ext.c, so every
behavioural finding below is PRE-EXISTING, not introduced by the move.

**No tier-1 divergence found anywhere in the panel's sweeps** — no cell
where pcrec accepts what PCRE2 rejects, rejects what PCRE2 accepts, or
attributes a real construct to the wrong module.

## CHECKS critic — one HIGH ratified and fixed, two NOTED

1. **HIGH, FIXED (mod04d-offsets lane): S27's offset-blindness generalizes
   to the other five REFUSE families.** pcrec_ext_verb has five substantive
   `REFUSE(at, ...)` sites beyond the pinned star-blame one (too-long :324,
   unknown-name :329, form-mismatch :381, at-start :404, ROADMAP_NEVER
   :414, module-refusal :417) and every reject pin exercising them was
   message-only (the `reject` helper is a substring match); PC-3 never
   compares offsets (its own comment at pcre2_check.c:1189). A plausible
   "blame the name start for precision" refactor at any of them was caught
   by nothing. Disposition: one representative pin per family gains the
   measured "(pattern offset N)" suffix (offsets measured from the honest
   binary, per-family, both tables where the family is per-table) + mech
   row S30 encoding the nstart-blame refactor, failing direction measured
   both ways. The pins pin PCREC'S OWN answer — they claim no oracle
   agreement (the engine critic measured those offsets DIVERGING from
   libpcre2, tier 2; see below).
2. **NOTED: check07 cannot exercise pcrec_ext_gate at doorway 3.** The
   transition rule fires only for rows accepted under --features all;
   RK_VERB has no producer, so the gate at the verb doorway is covered "by
   construction" only. Inherent to a pure migration; becomes testable with
   the first verb producer (SR-6's verb half).
3. **NOTED: check01_isolation's aperture excludes mod_verbs.o entirely.**
   Discovery is by defined-symbol regex (recognis|extent_scan|...);
   mod_verbs.c's symbols match nothing, so the file is structurally outside
   the check in BOTH directions — a direct pcrec_feature_enabled link
   appearing there would not be caught. Architecturally defensible (a gated
   doorway is ALLOWED to consult enabled-state via pcrec_ext_gate), but the
   0.4b commit message's "check01 re-run manually and still passes" must be
   read as a by-hand nm spot-check, not automated coverage. Also:
   check01 floors none of its populations (no isolation.* rows in
   floors.txt). Pre-existing framework work, recorded for the MOD-0.8
   check-arming pass.

Non-findings ratified: S27-29 anchors unique (count-guarded by replace.py);
the matrix demand-side count is a live glob, no companion edit needed;
REFUSE/BAD_ROW/pcrec_ext_gate single definitions are linker-guarded (a
second definition is a loud multiple-definition error); S28/S29 are
legitimately message-only-detectable (both sabotages change the message
text itself).

NOTED, honestly: the 602-comparison byte-identity sweep was session-scratch
and is not a standing guard — S27-30 are its durable residue, PC-3 the wide
net; no committed harness watches --list-verbs/--probe-ask field regressions
at this doorway. Same disposition as MOD-0.5b's 641-pattern sweep (the
one-time landing bar is the bar; the pins are what persist).

## ENGINE critic — two tier-2 divergences NOTED (one → K15), five clean sweeps

1. **NOTED, tier 2 (D26), pre-existing: systematic offset divergence on
   every unknown/malformed-name answer.** ~35 measured cells: pcrec blames
   `at` (the `(`); libpcre2 blames a shape-dependent position inside the
   construct. Message TEXT agreed in every cell; only the offset differs.
   D26 places diagnostic offsets for unimplemented constructs in the
   wording tier — no action, recorded with cells in the report. The
   counter-cells worth keeping: the star=at+1 empty-name case and
   `(*:)`'s MARK-specific message MATCH libpcre2 offset-for-offset
   (measured this panel, 3 shapes + 1 — the S27 pin's offset happens to
   also be the oracle's, now a measured claim with this citation, not
   prose).
2. **K15 (new entry), tier 2, pre-existing, structurally unguarded: the
   name-too-long diagnostic fires for >128-byte NON-identifier runs where
   libpcre2 answers "not recognized"** — the extent scan swallows every
   byte except `)`, `:`, `=`, EOF; libpcre2 stops at the first
   non-identifier byte. Verified NOT tier-1 (no accept/reject path).
   Unguarded because PC-3's length generator is identifier-only
   (pool_from_lengths) and the mutation pool never nears the boundary —
   the R16/R17 sweep-template lesson, third recurrence. Deferred as a
   LINKED PAIR (fix or rule the divergence, then extend the generator —
   extending first would make PC-3 fail on what it newly sees); see
   docs/known_issues.md K15.

Clean sweeps recorded (all cells agreed both sides): QV_YES/QV_NO
quantifiability column (8 cells); VF_GROUPARG's no-own-paren rule under
truncation (4 shapes); digit/underscore/high-byte first-byte dispatch at
the case boundary; MARK-synonym degradation at EOF (`(*:` generic vs
`(*:)` MARK-specific, both right); ROADMAP_NEVER firing only on
PCRE2-accepted forms; lower-table case sensitivity ((*PLA:x) both sides).

## DOCS critic — two live-doc fixes applied, one pointer

1. **FIXED: src/parse/CLAUDE.md's Conventions closing line** still said
   non-base syntax is "reached through ext.c's four doorways" — the exact
   R17 §9.3 disease (summary line reads as current, detail bullets above it
   were correctly updated). Now names the doorway split.
2. **FIXED: extension_design.md §5.3's "(all three already in ext.c)"** —
   compounding staleness: the delimiter scan left for scans.c at MOD-0.1
   slice 9, verb names + the LIMIT_* rule for mod_verbs.c at MOD-0.4.
   Inline R18-attributed correction, per the document's convention.
3. **FIXED (pointer ratified): plan.md's SR-6 row** predicted
   src/parse/ext/*.c as the handlers' destination; as-built naming is
   src/parse/mod_*.c (pre-dates MOD-0.4). Row now records the as-built
   pattern and states explicitly that the verbs entry remains accurately
   PENDING (no verb produces; MOD-0.4 moved the doorway, not a handler).

Non-findings: pcre2_compliance.md never names source files (consistent with
the SR-4 generated-index rule); decisions.md D25/Q1 and the dated as-built
entries read correctly as records; known_issues.md K-entries describe
behaviour, not locations; the eleven already-fixed spots from
043d78a/94b0693/841d73f verified correct and internally consistent.

## NOTED — the honest unguarded inventory after R18

- check07's gate coverage at doorway 3 waits for the first verb producer.
- check01's aperture excludes mod_verbs.o in both directions; no
  isolation.* floors (MOD-0.8 candidate work).
- mech has no `registry` suite: PC-3/sweep_verb coverage cannot be
  mech-claimed (MOD-0.4c's framework note, restated).
- K15's linked pair: the too-long category divergence and PC-3's
  identifier-only length generator move together or not at all.
- The offset divergence cells (engine finding 1) are recorded, unpinned on
  the oracle side by design — the new offset pins assert pcrec's own
  stability only.
- R17's remaining NOTED items stand where still applicable (PC-4 has no
  modifier shapes; the K12-era dash-offset outlier; check11's classifier
  blindness to wrong-module refusals — reject_gated is that guard).
