# [DD-13b.W23.5] THE ACCEPT-SIDE RECEIPT for `name`/`flags`/`encoding`/
# `engine` (w23_impl.md §3.3's `all-readers` rows), `single_description
# .rxtin`'s own shape one file over: `dup_description.rxtin`'s refusal
# and this one production's own REFUSAL fixtures already prove the
# three legs agree on what to REJECT; this is what proves they agree on
# what to ACCEPT, headless (the population every corpus file is in).
pattern a+
name ok_name
flags i
encoding byte
engine vm
m "aaa" 0 3
