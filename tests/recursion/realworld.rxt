# tests/recursion/realworld.rxt -- module `recursion` ([DD-14]): [LIB] entry #1: the RFC 5322 email specimen in all three spellings -- hand-inlined, `{0}`-factored and `(?(DEFINE)`-factored -- over fourteen of its own manifest's subjects
#
# GENERATED BY tests/recursion/gen_corpus.py, and that is a property rather
# than a convenience: every expectation below -- including every `g` line --
# was produced by driving the cell through libpcre2 10.46 (via the committed
# ctypes binding at docs/design/subroutines_measurements/probes/sr_oracle.py,
# which borrows la_oracle.py -> br_oracle.py -> pcre2_ctypes.py) BEFORE it was
# written. The generator never asks pcrec anything.
#
# THE SOLE ORACLE IS LIBPCRE2 -- THERE IS NO PYTHON ARM, AND THAT IS AN
# ABSENCE RATHER THAN A DIVERGENCE (design SS10.1). python3 `re` has no
# subroutine-call construct at all: every one of the nine call spellings plus
# both zero spellings raises `re.error` at compile time. `\\1` and `(?P=n)`
# DO compile in python, but they are `backrefs`' reference construct, not this
# module's call construct (design SS2.1's one-cell discriminator is why
# `spellings.rxt` opens with it) -- so marking this corpus python-verifiable
# on the strength of those two spellings would be checking the wrong module.
#
# `(?(DEFINE)...)` IS THIS MODULE'S SINCE [DD-14] WAVE F (D71 item 4,
# overruling design SS2.5's "RULED: no DEFINE"). It is a TAILED row on the
# `(?(` doorway -- the rest of `(?(` is still `conditionals`' -- and it lowers
# to the SAME NODE the `{0}`-callee idiom produces, an A_REP with
# rmin == rmax == 0 over the body. That is why define.rxt and zerodef.rxt
# carry the two spellings SIDE BY SIDE on the same cells: they are two
# spellings of one construct, and the corpus asserts they agree rather than
# assuming it. Where a body must be callee-only, `{0}` remains the spelling
# used in the OTHER files, because those cells were oracle-verified in that
# form and re-spelling them would move a measurement without re-measuring it.
#
# NOTHING IN src/ IMPLEMENTS A SUBROUTINE CALL YET. Every ordinary `m`/`n`
# block in this corpus is therefore EXPECTED to report a pattern-compile
# failure under today's harness (`requires module 'recursion'`) -- that is
# the correct, documented state before [DD-14] wave B+C lands (docs/
# testing.md's "expected-unsupported" policy), not a corpus defect.
#
# Design: docs/design/subroutines_design.md. Decisions: docs/dev/decisions.md
# D71 (the give-up-code axis, D71.1) and D72 (PCREC_ERR_INTERNAL).

# ====================================================================
# THE ORIGINAL, hand-inlined -- the CONTROL. It has no call in it at all, needs no module, and is here so the two factored spellings have something to be compared against inside this file rather than only in the specimen's own log.
# ====================================================================

# orig.rx, read from the specimen directory.
pattern (?:[a-z0-9!#$%&'*+/=?^_`{|}~-]+(?:\.[a-z0-9!#$%&'*+/=?^_`{|}~-]+)*|"(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21\x23-\x5b\x5d-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])*")@(?:(?:[a-z0-9](?:[a-z0-9-]*[a-z0-9])?\.)+[a-z0-9](?:[a-z0-9-]*[a-z0-9])?|\[(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?|[a-z0-9-]*[a-z0-9]:(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21-\x5a\x53-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])+)\])
m "user@example.com" 0 16
m "a.b.c@sub.example.org" 0 21
m "u!#$%&'*+/=?^_`{|}~-x@example.com" 0 33
n "\"john doe\"@example.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "\"a\\\"b\"@example.com" 0 18
m "user@[0.0.0.0]" 0 14
m "user@[192.168.1.1]" 0 18
n "user@[256.1.1.1]"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "user@[1.2.3]"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "user@-example.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "userexample.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "user..name@example.com" 6 22
n "user@example"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "user name@example.com" 5 21

# ====================================================================
# THE `{0}`-FACTORED SPELLING (factored.rx): the four textually repeated sub-languages -- atom, qchar, label, octet -- named once and called. Design SS8.4 found the 'calls as FACTORING rather than recursion' population EMPTY; this specimen is its first real member.
# ====================================================================

# factored.rx, read from the specimen directory.
pattern (?:(?<atom>[a-z0-9!#$%&'*+/=?^_`{|}~-]+)){0}(?:(?<qchar>[\x01-\x08\x0b\x0c\x0e-\x1f\x21\x23-\x5b\x5d-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])){0}(?:(?<label>[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)){0}(?:(?<octet>25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)){0}(?:(?&atom)(?:\.(?&atom))*|"(?:(?&qchar))*")@(?:(?:(?&label)\.)+(?&label)|\[(?:(?:(?&octet))\.){3}(?:(?&octet)|[a-z0-9-]*[a-z0-9]:(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21-\x5a\x53-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])+)\])
features recursion,named-groups
m "user@example.com" 0 16
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "a.b.c@sub.example.org" 0 21
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "u!#$%&'*+/=?^_`{|}~-x@example.com" 0 33
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
n "\"john doe\"@example.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "\"a\\\"b\"@example.com" 0 18
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "user@[0.0.0.0]" 0 14
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "user@[192.168.1.1]" 0 18
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
n "user@[256.1.1.1]"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "user@[1.2.3]"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "user@-example.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "userexample.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "user..name@example.com" 6 22
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
n "user@example"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "user name@example.com" 5 21
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1

# ====================================================================
# THE `(?(DEFINE)` SPELLING, DERIVED from factored.rx by this generator (never retyped). It is the spelling a library author would actually write, and wave F is what makes it compile. Its answers must equal the other two spellings' on every subject -- which is the assertion this whole file exists for, since all three cells here get their expectations from the SAME oracle run.
# ====================================================================

# derived from factored.rx: the four {0} wrappers peeled off and their bodies put inside one DEFINE.
pattern (?(DEFINE)(?<atom>[a-z0-9!#$%&'*+/=?^_`{|}~-]+)(?<qchar>[\x01-\x08\x0b\x0c\x0e-\x1f\x21\x23-\x5b\x5d-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])(?<label>[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)(?<octet>25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?))(?:(?&atom)(?:\.(?&atom))*|"(?:(?&qchar))*")@(?:(?:(?&label)\.)+(?&label)|\[(?:(?:(?&octet))\.){3}(?:(?&octet)|[a-z0-9-]*[a-z0-9]:(?:[\x01-\x08\x0b\x0c\x0e-\x1f\x21-\x5a\x53-\x7f]|\\[\x01-\x09\x0b\x0c\x0e-\x7f])+)\])
features recursion,named-groups
m "user@example.com" 0 16
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "a.b.c@sub.example.org" 0 21
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "u!#$%&'*+/=?^_`{|}~-x@example.com" 0 33
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
n "\"john doe\"@example.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "\"a\\\"b\"@example.com" 0 18
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "user@[0.0.0.0]" 0 14
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
m "user@[192.168.1.1]" 0 18
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
n "user@[256.1.1.1]"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "user@[1.2.3]"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "user@-example.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
n "userexample.com"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "user..name@example.com" 6 22
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
n "user@example"
# NOTE: author expected a match here; libpcre2 says nomatch -- the ORACLE rules, not the hint
m "user name@example.com" 5 21
g 1 -1 -1
g 2 -1 -1
g 3 -1 -1
g 4 -1 -1
