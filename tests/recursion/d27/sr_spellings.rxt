# sr_spellings.rxt
# the construct table: every call spelling through the call/reference
# discriminator, plus the relative and forward resolutions (extract
# 2.1-2.3)
#
# [DD-14.D27] blinded corpus for module `recursion`. GENERATED by
# d27/sr_gen.py -- do not hand-edit; edit the spec and regenerate.
# Every m/n span and every g line is libpcre2 10.46's answer through
# docs/design/subroutines_measurements/probes/sr_oracle.py (D26: PCRE2
# is the source of truth). Blocks marked `# perl-diverges` are ones
# where perl 5.40.1 answers differently; perl's answer is RECORDED in
# d27/PERL_DIVERGENCES.md and is never the expectation.

# extract 2.1: THE discriminator, unanchored exactly as measured. A
# CALL re-RUNS the alternation, so 'ab' matches; the reference below
# cannot.
pattern (a|b)(?1)
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "xy"

# extract 2.1 CONTROL, module backrefs: the same shape as a REFERENCE.
# 'ab' must NOT match -- this is the half of the pair that makes the
# call cell mean something. A corpus with only the call row proves
# nothing about which construct was compiled.
pattern (a|b)\1
features recursion,backrefs
n "ab"
m "aa" 0 2
g 1 0 1
n "a"

# spelling (?1) put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)(?1)$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling (?01) put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)(?01)$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling (?001) put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)(?001)$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling (?0001) put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)(?0001)$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling \g<1> put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)\g<1>$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling \g'1' put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)\g'1'$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling \g<01> put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)\g<01>$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling \g'01' put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)\g'01'$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling (?-1) put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)(?-1)$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling (?-01) put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)(?-01)$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling \g<-1> put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)\g<-1>$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# spelling \g'-1' put through the call/reference discriminator: 'ab'
# matches only if this spelling RE-RUNS group 1.
pattern ^(a|b)\g'-1'$
features recursion
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"
n "aba"

# by-name call spelling (?&w) through the discriminator.
pattern ^(?<w>a|b)(?&w)$
features recursion,named-groups
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"

# by-name call spelling (?P>w) through the discriminator.
pattern ^(?<w>a|b)(?P>w)$
features recursion,named-groups
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"

# by-name call spelling \g<w> through the discriminator.
pattern ^(?<w>a|b)\g<w>$
features recursion,named-groups
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"

# by-name call spelling \g'w' through the discriminator.
pattern ^(?<w>a|b)\g'w'$
features recursion,named-groups
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"

# the python-style declaration and the python-style call together.
pattern ^(?P<w>a|b)(?P>w)$
features recursion,named-groups
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"

# the Perl-quoted declaration with a (?&name) call.
pattern ^(?'w'a|b)(?&w)$
features recursion,named-groups
m "ab" 0 2
g 1 0 1
m "aa" 0 2
g 1 0 1
n "a"

# REFERENCE control for the anchored discriminator: \1 wants the same
# TEXT, so 'ab' is a nomatch where every call spelling above matches.
pattern ^(a|b)\1$
features recursion,backrefs
n "ab"
m "aa" 0 2
g 1 0 1

# REFERENCE control for the anchored discriminator: \g{1} wants the
# same TEXT, so 'ab' is a nomatch where every call spelling above
# matches.
pattern ^(a|b)\g{1}$
features recursion,backrefs
n "ab"
m "aa" 0 2
g 1 0 1

# REFERENCE control for the anchored discriminator: \g1 wants the same
# TEXT, so 'ab' is a nomatch where every call spelling above matches.
pattern ^(a|b)\g1$
features recursion,backrefs
n "ab"
m "aa" 0 2
g 1 0 1

# by-NAME reference control: \k<w> is backrefs', not this module's, and
# answers the opposite of \g<w> on 'ab'.
pattern ^(?<w>a|b)\k<w>$
features recursion,named-groups,backrefs
n "ab"
m "aa" 0 2
g 1 0 1

# (?P=w) is the REFERENCE python spelling; (?P>w) two blocks up is the
# CALL. One letter apart, opposite answers on 'ab'.
pattern ^(?P<w>a|b)(?P=w)$
features recursion,named-groups,backrefs
n "ab"
m "aa" 0 2
g 1 0 1

# extract 2.3: THE shape that makes a call unlike a reference -- a
# FORWARD call runs group 1's pattern BEFORE group 1 has run.
pattern ^(?+1)(a|b)$
features recursion
m "ab" 0 2
g 1 1 2
m "aa" 0 2
g 1 1 2
n "a"
n "abc"

# extract 2.3 CONTROL: a forward REFERENCE can only ever read an unset
# group, so it fails where the forward call above succeeds.
pattern ^\2(a|b)(c)$
features recursion,backrefs
n "abc"
n "ac"

# extract 2.3 row 3, with its measured capture: group 1 is set by its
# own LEXICAL run at offset 1, not by the call that ran first.
pattern ^(?+1)(a)$
features recursion
m "aa" 0 2
g 1 1 2
n "a"

# extract 2.3 row 4: (?+2) counts forward PAST one group.
pattern ^(?+2)(a)(b)$
features recursion
m "bab" 0 3
g 1 1 2
g 2 2 3
n "aab"
n "ab"

# extract 2.3 row 6: \g<+N> obeys the same relative rule as (?+N).
pattern ^\g<+1>(a)$
features recursion
m "aa" 0 2
g 1 1 2
n "a"

# extract 2.3 row 1: (?-1) is the NEAREST group to the LEFT -- group 2.
# 'aba' must fail, or the cell would not have distinguished group 2
# from group 1.
pattern ^(a)(b)(?-1)$
features recursion
m "abb" 0 3
g 1 0 1
g 2 1 2
n "aba"
n "ab"

# extract 2.3 row 2: (?-2) counts back two -- group 1.
pattern ^(a)(b)(?-2)$
features recursion
m "aba" 0 3
g 1 0 1
g 2 1 2
n "abb"

# extract 2.3 row 5: a LEADING ZERO on a relative call is accepted and
# the whole digit run is read as decimal.
pattern ^(a)(?-01)$
features recursion
m "aa" 0 2
g 1 0 1
n "a"

# (?-01) == (?-1) == group 2 -- the leading zero does not shift the
# count.
pattern ^(a)(b)(?-01)$
features recursion
m "abb" 0 3
g 1 0 1
g 2 1 2
n "aba"

# the \g<> doorway takes a leading zero on a relative value too.
pattern ^(a)(b)\g<-01>$
features recursion
m "abb" 0 3
g 1 0 1
g 2 1 2
n "aba"

# \g<-02> == \g<-2> == group 1.
pattern ^(a)(b)\g<-02>$
features recursion
m "aba" 0 3
g 1 0 1
g 2 1 2
n "abb"

# the quoted doorway, relative.
pattern ^(a)(b)\g'-1'$
features recursion
m "abb" 0 3
g 1 0 1
g 2 1 2
n "aba"

# relative resolution is at the CALL SITE'S OWN GROUP COUNT -- three
# '(' have been seen, so (?-1) is group 3, not group 1 and not the
# enclosing group.
pattern ^((a)(b))(?-1)$
features recursion
m "abb" 0 3
g 1 0 2
g 2 0 1
g 3 1 2
n "aba"
n "abab"

# …and (?-2) is group 2.
pattern ^((a)(b))(?-2)$
features recursion
m "aba" 0 3
g 1 0 2
g 2 0 1
g 3 1 2
n "abb"

# …and (?-3) is group 1, the group the call site is written INSIDE of
# having already closed -- two characters wide, not one.
pattern ^((a)(b))(?-3)$
features recursion
m "abab" 0 4
g 1 0 2
g 2 0 1
g 3 1 2
n "aba"
n "abb"

# extract 2.2: a TWO-DIGIT absolute call. The whole digit run is
# decimal, so this is group 10 -- 'abcdefghija' (which is what a
# one-digit-prefix misparse into (?1) would accept) must FAIL.
pattern ^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j|k)(?10)$
features recursion
m "abcdefghijk" 0 11
g 1 0 1
g 2 1 2
g 3 2 3
g 4 3 4
g 5 4 5
g 6 5 6
g 7 6 7
g 8 7 8
g 9 8 9
g 10 9 10
m "abcdefghijj" 0 11
g 1 0 1
g 2 1 2
g 3 2 3
g 4 3 4
g 5 4 5
g 6 5 6
g 7 6 7
g 8 7 8
g 9 8 9
g 10 9 10
n "abcdefghija"
n "abcdefghijx"

# the two-digit call through the \g<> doorway.
pattern ^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j|k)\g<10>$
features recursion
m "abcdefghijk" 0 11
g 1 0 1
g 2 1 2
g 3 2 3
g 4 3 4
g 5 4 5
g 6 5 6
g 7 6 7
g 8 7 8
g 9 8 9
g 10 9 10
n "abcdefghija"

# the two-digit call through the quoted doorway.
pattern ^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j|k)\g'10'$
features recursion
m "abcdefghijk" 0 11
g 1 0 1
g 2 1 2
g 3 2 3
g 4 3 4
g 5 4 5
g 6 5 6
g 7 6 7
g 8 7 8
g 9 8 9
g 10 9 10
n "abcdefghija"

# a two-digit RELATIVE call: ten groups back from a call site that has
# seen ten is group 1. 'abcdefghijk' -- which (?-1) would accept --
# must FAIL.
pattern ^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j|k)(?-10)$
features recursion
m "abcdefghija" 0 11
g 1 0 1
g 2 1 2
g 3 2 3
g 4 3 4
g 5 4 5
g 6 5 6
g 7 6 7
g 8 7 8
g 9 8 9
g 10 9 10
n "abcdefghijk"

# control for the row above: (?-1) at the same site IS group 10.
pattern ^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j|k)(?-1)$
features recursion
m "abcdefghijk" 0 11
g 1 0 1
g 2 1 2
g 3 2 3
g 4 3 4
g 5 4 5
g 6 5 6
g 7 6 7
g 8 7 8
g 9 8 9
g 10 9 10
n "abcdefghija"

# a two-digit FORWARD call: ten groups forward from a site that has
# seen none is group 10.
pattern ^(?+10)(a)(b)(c)(d)(e)(f)(g)(h)(i)(j|k)$
features recursion
m "kabcdefghij" 0 11
g 1 1 2
g 2 2 3
g 3 3 4
g 4 4 5
g 5 5 6
g 6 6 7
g 7 7 8
g 8 8 9
g 9 9 10
g 10 10 11
m "jabcdefghij" 0 11
g 1 1 2
g 2 2 3
g 3 3 4
g 4 4 5
g 5 5 6
g 6 6 7
g 7 7 8
g 8 8 9
g 9 9 10
g 10 10 11
n "xabcdefghij"
n "aabcdefghij"

