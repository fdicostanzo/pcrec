# PERL_DIVERGENCES.md — the [DD-14.D27] corpus's second oracle

Perl 5.40.1 against libpcre2 10.46, over every cell of the d27/ corpus.

**D26 rules: libpcre2 is the source of truth for what pcrec matches.** Nothing in this file is an expectation. Perl is here because subroutine calls are Perl's own construct and PCRE2's feature was modeled on them, so a disagreement is worth knowing about — it is RECORDED, never RESOLVED, and no `.rxt` line was written from perl's answer. Blocks whose pattern appears below carry a `# perl-diverges` marker in the corpus.

Generated by `d27/sr_perl.py`; regenerate rather than edit.

## Coverage

| | count |
|---|---|
| cells offered to perl | 587 |
| cells perl RAN | 404 |
| …of which perl and libpcre2 AGREED exactly (span and every group) | 393 |
| give-up (`gu`) cells — a pcrec capacity fact with no perl counterpart | 4 |
| distinct spellings perl REFUSED at COMPILE time | 71 |
| distinct patterns perl COMPILED and then DIED on at MATCH time | 8 |
| divergence rows | 6 |

## The spellings perl 5.40.1 REFUSES

| pattern | perl's message |
|---|---|
| `(a\g'00'?b)` | Unterminated \g... pattern in regex; marked by <-- HERE in m/(a\g <-- HERE '00'?b)/ |
| `(a\g<0>?b)` | Unterminated \g... pattern in regex; marked by <-- HERE in m/(a\g <-- HERE <0>?b)/ |
| `^(?(DEFINE)(?<a>x)\|(?<b>y))(?&a)$` | (?(DEFINE)....) does not allow branches in regex; marked by <-- HERE in m/^(?(DEFINE)(?<a>x)\| <-- HERE (?<b>y) |
| `^(?(DEFINE)(?<g>a(?&g)?b))aabb(?<=(?&g))$` | Lookbehind longer than 255 not implemented in regex m/^(?(DEFINE)(?<g>a(?&g)?b))aabb(?<=(?&g))$/ |
| `^(?(DEFINE)(?<g>a+))aa(?<=(?&g))$` | Lookbehind longer than 255 not implemented in regex m/^(?(DEFINE)(?<g>a+))aa(?<=(?&g))$/ |
| `^(?(DEFINE)(?<g>a{1,300}))aaaa(?<=(?&g))$` | Lookbehind longer than 255 not implemented in regex m/^(?(DEFINE)(?<g>a{1,300}))aaaa(?<=(?&g))$/ |
| `^(?(DEFINE)x\|y)a$` | (?(DEFINE)....) does not allow branches in regex; marked by <-- HERE in m/^(?(DEFINE)x\| <-- HERE y)a$/ |
| `^(?<w>a)(?&nope)$` | Reference to nonexistent named group in regex; marked by <-- HERE in m/^(?<w>a)(?&nope <-- HERE )$/ |
| `^(?<w>a)(?P>nope)$` | Reference to nonexistent named group in regex; marked by <-- HERE in m/^(?<w>a)(?P>nope <-- HERE )$/ |
| `^(?<w>a)\g'nope'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(?<w>a)\g <-- HERE 'nope'$/ |
| `^(?<w>a)\g<nope>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(?<w>a)\g <-- HERE <nope>$/ |
| `^(?<w>a\|b)\g'w'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(?<w>a\|b)\g <-- HERE 'w'$/ |
| `^(?<w>a\|b)\g<w>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(?<w>a\|b)\g <-- HERE <w>$/ |
| `^(?=a\Kb)ab$` | \K not permitted in lookahead/lookbehind in regex; marked by <-- HERE in m/^(?=a\K <-- HERE b)ab$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)(?&a)$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)(?&a)$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)(?P=a)$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)(?P=a)$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)(?P>a)$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)(?P>a)$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)\g'a'$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)\g'a'$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)\g<a>$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)\g<a>$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)\g{a}$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)\g{a}$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)\k'a'$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)\k'a'$/ |
| `^(?J)(?:(?<a>x)\|q)(?<a>y)\k<a>$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?:(?<a>x)\|q)(?<a>y)\k<a>$/ |
| `^(?J)(?<a>x)(q)(?<a>y)(?&a)z$` | Sequence (?J...) not recognized in regex; marked by <-- HERE in m/^(?J <-- HERE )(?<a>x)(q)(?<a>y)(?&a)z$/ |
| `^(a(?00)?b)$` | Sequence (?R) not terminated in regex m/^(a(?00)?b)$/ |
| `^(a(?0001)?b)$` | Sequence (?R) not terminated in regex m/^(a(?0001)?b)$/ |
| `^(a(?001)?b)$` | Sequence (?R) not terminated in regex m/^(a(?001)?b)$/ |
| `^(a(?01)?b)$` | Sequence (?R) not terminated in regex m/^(a(?01)?b)$/ |
| `^(a)(?+0)$` | Illegal pattern in regex; marked by <-- HERE in m/^(a)(?+0 <-- HERE )$/ |
| `^(a)(?+00)$` | Illegal pattern in regex; marked by <-- HERE in m/^(a)(?+0 <-- HERE 0)$/ |
| `^(a)(?+2)$` | Reference to nonexistent group in regex; marked by <-- HERE in m/^(a)(?+2) <-- HERE $/ |
| `^(a)(?-0)$` | Sequence (?-0...) not recognized in regex; marked by <-- HERE in m/^(a)(?-0 <-- HERE )$/ |
| `^(a)(?-00)$` | Sequence (?-0...) not recognized in regex; marked by <-- HERE in m/^(a)(?-0 <-- HERE 0)$/ |
| `^(a)(?-01)$` | Sequence (?-0...) not recognized in regex; marked by <-- HERE in m/^(a)(?-0 <-- HERE 1)$/ |
| `^(a)(?-2)$` | Reference to nonexistent group in regex; marked by <-- HERE in m/^(a)(?-2) <-- HERE $/ |
| `^(a)(?12)$` | Reference to nonexistent group in regex; marked by <-- HERE in m/^(a)(?12) <-- HERE $/ |
| `^(a)(?2)$` | Reference to nonexistent group in regex; marked by <-- HERE in m/^(a)(?2) <-- HERE $/ |
| `^(a)(?9)$` | Reference to nonexistent group in regex; marked by <-- HERE in m/^(a)(?9) <-- HERE $/ |
| `^(a)(b)(?-01)$` | Sequence (?-0...) not recognized in regex; marked by <-- HERE in m/^(a)(b)(?-0 <-- HERE 1)$/ |
| `^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j\|k)\g'10'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j\|k)\g <-- HERE '10' |
| `^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j\|k)\g<10>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)(b)(c)(d)(e)(f)(g)(h)(i)(j\|k)\g <-- HERE <10> |
| `^(a)(b)\g'-1'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)(b)\g <-- HERE '-1'$/ |
| `^(a)(b)\g<-01>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)(b)\g <-- HERE <-01>$/ |
| `^(a)(b)\g<-02>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)(b)\g <-- HERE <-02>$/ |
| `^(a)\g'+00'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE '+00'$/ |
| `^(a)\g'-0'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE '-0'$/ |
| `^(a)\g'7'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE '7'$/ |
| `^(a)\g<+0>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE <+0>$/ |
| `^(a)\g<-00>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE <-00>$/ |
| `^(a)\g<-0>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE <-0>$/ |
| `^(a)\g<-2>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE <-2>$/ |
| `^(a)\g<7>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a)\g <-- HERE <7>$/ |
| `^(a\g'0'?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE '0'?b)$/ |
| `^(a\g'00'?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE '00'?b)$/ |
| `^(a\g'01'?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE '01'?b)$/ |
| `^(a\g'1'?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE '1'?b)$/ |
| `^(a\g<00>?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE <00>?b)$/ |
| `^(a\g<01>?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE <01>?b)$/ |
| `^(a\g<0>?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE <0>?b)$/ |
| `^(a\g<1>?b)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\g <-- HERE <1>?b)$/ |
| `^(a\|b)(?-01)$` | Sequence (?-0...) not recognized in regex; marked by <-- HERE in m/^(a\|b)(?-0 <-- HERE 1)$/ |
| `^(a\|b)(?0001)$` | Sequence (?R) not terminated in regex m/^(a\|b)(?0001)$/ |
| `^(a\|b)(?001)$` | Sequence (?R) not terminated in regex m/^(a\|b)(?001)$/ |
| `^(a\|b)(?01)$` | Sequence (?R) not terminated in regex m/^(a\|b)(?01)$/ |
| `^(a\|b)\g'-1'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\|b)\g <-- HERE '-1'$/ |
| `^(a\|b)\g'01'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\|b)\g <-- HERE '01'$/ |
| `^(a\|b)\g'1'$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\|b)\g <-- HERE '1'$/ |
| `^(a\|b)\g<-1>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\|b)\g <-- HERE <-1>$/ |
| `^(a\|b)\g<01>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\|b)\g <-- HERE <01>$/ |
| `^(a\|b)\g<1>$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^(a\|b)\g <-- HERE <1>$/ |
| `^\g<+1>(a)$` | Unterminated \g... pattern in regex; marked by <-- HERE in m/^\g <-- HERE <+1>(a)$/ |
| `^ab(?<=(a(?1)?b))$` | Lookbehind longer than 255 not implemented in regex m/^ab(?<=(a(?1)?b))$/ |

## Patterns perl COMPILES and then DIES on at MATCH time

A different fact from a refused spelling, and the one worth reading beside PCRE2's own `rc -52`: perl accepts the pattern and then declines to answer when it runs. pcrec's counterpart is the `PCREC_ERR_FRAMES` give-up the `gu` cells in `sr_depth.rxt` expect.

| pattern | perl's message |
|---|---|
| `((?1)*a)` | Infinite recursion in regex |
| `((?1)?a)` | Infinite recursion in regex |
| `((?1)a)` | Infinite recursion in regex |
| `(?R)a` | Infinite recursion in regex |
| `^(?R)*$` | Infinite recursion in regex |
| `^(?R)?$` | Infinite recursion in regex |
| `^(?R){0,2}$` | Infinite recursion in regex |
| `^(a\|(?1)a)$` | Infinite recursion in regex |

## Blocks pcrec refuses that BOTH libpcre2 and perl accept

Not a perl divergence — recorded so the row below is not mistaken for one. These are the [DD-14.LB] amendment's pcrec-only refusals: pcrec declines a lookbehind branch whose width is variable even though both reference engines accept it.

- `^(?(DEFINE)(?<g>a\|ab))ab(?<=(?&g))$`

## Where perl and libpcre2 DISAGREE

| # | cell | pattern | subject | startpos | libpcre2 10.46 | perl 5.40.1 | reading |
|---|---|---|---|---|---|---|---|
| 1 | sr_depth.rxt:45 | `^(a\|(?1)a)$` | `aaaaaaaaaa` | 0 | (0,10) (0,10) | DIED at match time: Infinite recursion in regex | libpcre2 answers; perl gives up at match time |
| 2 | sr_depth.rxt:47 | `^(a\|(?1)a)$` | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | 0 | (0,100) (0,100) | DIED at match time: Infinite recursion in regex | libpcre2 answers; perl gives up at match time |
| 3 | sr_depth.rxt:49 | `^(a\|(?1)a)$` | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | 0 | (0,199) (0,199) | DIED at match time: Infinite recursion in regex | libpcre2 answers; perl gives up at match time |
| 4 | sr_depth.rxt:51 | `^(a\|(?1)a)$` | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | 0 | (0,200) (0,200) | DIED at match time: Infinite recursion in regex | libpcre2 answers; perl gives up at match time |
| 5 | sr_depth.rxt:53 | `^(a\|(?1)a)$` | `` | 0 | (0,342) (0,342) | DIED at match time: Infinite recursion in regex | libpcre2 answers; perl gives up at match time |
| 6 | sr_interactions.rxt:72 | `^(?:(a\Kb)){0}(?=(?1))ab$` | `ab` | 0 | (1,2) (-1,-1) | nomatch | libpcre2 matches, perl does not |

