# sr_root.rxt
# (?R) / (?0) / \g<0> re-run the WHOLE PATTERN, anchors included, and
# the whole-digit-run rule (extract 2.4, 2.4a)
#
# [DD-14.D27] blinded corpus for module `recursion`. GENERATED by
# d27/sr_gen.py -- do not hand-edit; edit the spec and regenerate.
# Every m/n span and every g line is libpcre2 10.46's answer through
# docs/design/subroutines_measurements/probes/sr_oracle.py (D26: PCRE2
# is the source of truth). Blocks marked `# perl-diverges` are ones
# where perl 5.40.1 answers differently; perl's answer is RECORDED in
# d27/PERL_DIVERGENCES.md and is never the expectation. Blocks carrying
# a `PARKED` stanza had one or more cells MOVED to tests/known_fail/ --
# a confirmed pcrec defect, not a corpus gap; see the stanza's
# citation.

# extract 2.4/2.4a: (?1) targets group 1. The ROOT rows re-run '^' and
# '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a nomatch;
# the group-1 rows have the anchors OUTSIDE what they call and match.
# This is the module's most counter-intuitive fact and the one an
# implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a(?1)?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: (?01) targets group 1. The ROOT rows re-run '^' and
# '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a nomatch;
# the group-1 rows have the anchors OUTSIDE what they call and match.
# This is the module's most counter-intuitive fact and the one an
# implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a(?01)?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: (?001) targets group 1. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a(?001)?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: (?0001) targets group 1. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a(?0001)?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: \g<1> targets group 1. The ROOT rows re-run '^' and
# '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a nomatch;
# the group-1 rows have the anchors OUTSIDE what they call and match.
# This is the module's most counter-intuitive fact and the one an
# implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g<1>?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: \g<01> targets group 1. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g<01>?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: \g'1' targets group 1. The ROOT rows re-run '^' and
# '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a nomatch;
# the group-1 rows have the anchors OUTSIDE what they call and match.
# This is the module's most counter-intuitive fact and the one an
# implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g'1'?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: \g'01' targets group 1. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g'01'?b)$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "aab"
n "abb"

# extract 2.4/2.4a: (?R) targets the ROOT. The ROOT rows re-run '^' and
# '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a nomatch;
# the group-1 rows have the anchors OUTSIDE what they call and match.
# This is the module's most counter-intuitive fact and the one an
# implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a(?R)?b)$
features recursion
n "aabb"
m "ab" 0 2
g 1 0 2
n "aaabbb"
n "aab"

# extract 2.4/2.4a: (?0) targets the ROOT. The ROOT rows re-run '^' and
# '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a nomatch;
# the group-1 rows have the anchors OUTSIDE what they call and match.
# This is the module's most counter-intuitive fact and the one an
# implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a(?0)?b)$
features recursion
n "aabb"
m "ab" 0 2
g 1 0 2
n "aaabbb"
n "aab"

# extract 2.4/2.4a: (?00) targets the ROOT. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a(?00)?b)$
features recursion
n "aabb"
m "ab" 0 2
g 1 0 2
n "aaabbb"
n "aab"

# extract 2.4/2.4a: \g<0> targets the ROOT. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g<0>?b)$
features recursion
n "aabb"
m "ab" 0 2
g 1 0 2
n "aaabbb"
n "aab"

# extract 2.4/2.4a: \g<00> targets the ROOT. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g<00>?b)$
features recursion
n "aabb"
m "ab" 0 2
g 1 0 2
n "aaabbb"
n "aab"

# extract 2.4/2.4a: \g'0' targets the ROOT. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g'0'?b)$
features recursion
n "aabb"
m "ab" 0 2
g 1 0 2
n "aaabbb"
n "aab"

# extract 2.4/2.4a: \g'00' targets the ROOT. The ROOT rows re-run '^'
# and '$' TOO, so the inner '^' fails at offset 1 and 'aabb' is a
# nomatch; the group-1 rows have the anchors OUTSIDE what they call and
# match. This is the module's most counter-intuitive fact and the one
# an implementer is most likely to get wrong in the same direction a
# promise-first author might.
pattern ^(a\g'00'?b)$
features recursion
n "aabb"
m "ab" 0 2
g 1 0 2
n "aaabbb"
n "aab"

# extract 2.4 last row: with the anchors GONE, the very same (?R)
# reaches depth 2 and matches. The contrast with the anchored (?R)
# block above is the whole fact -- neither cell alone shows it.
pattern (a(?R)?b)
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
m "aaabbb" 0 6
g 1 0 6
n "ba"

# \g<0> is a synonym for (?R), not 'call group 0': unanchored it
# recurses exactly as (?R) does.
pattern (a\g<0>?b)
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2

# …and so does the quoted double-zero spelling.
pattern (a\g'00'?b)
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2

# a ROOT call and a GROUP call in ONE pattern: a lowering that
# collapsed the two targets would have to answer this wrong. The (?R)
# here re-runs the anchored whole pattern from the current position, so
# it can never contribute.
pattern ^(a(?1)?b)(?R)?$
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2
n "abab"

# the root call under '?', with the anchors: PCRE2 re-runs '^x(?0)?y$'
# from the inner position, where '^' cannot hold.
pattern ^x(?0)?y$
features recursion
m "xy" 0 2
n "xxyy"

# extract 3.4(f): (?R) under a quantifier is an ordinary repeatable
# item; its interaction with the anchors is unchanged by the
# quantifier.
pattern ^a(?R)*b$
features recursion
m "ab" 0 2
n "aabb"

# extract 3.4(f), unanchored: (?R) under '*'.
pattern (a(?R)*b)
features recursion
m "aabb" 0 4
g 1 0 4
m "ab" 0 2
g 1 0 2

