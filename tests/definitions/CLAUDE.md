# tests/definitions — [DD-13b.W1.3] the COMPOSITION IDENTITY PROOF

pcrec's own corpus composing over a shared definitions file, and the check
that a composed artifact answers exactly what a hand-written flat one does.
This is part 3 of [DD-13b.W1.3] ("our own usage"); part 2, the bench's
pattern set as a source, lives in `tests/rxtsource/fixtures/`.

## Files

- `defs_common.rxtin` — THE SHARED DEFINITIONS FILE. No `target` line and
  several blocks, so it is a LIBRARY: `pcrec --source` on it builds nothing
  at exit 0 (format_design §6.1). Each definition is written to exercise one
  of D89's three tiers — `w` and `hostname` NAME a group (delivered),
  `dd`'s group is referenced by its own `\1` (hidden), `dotted`'s two groups
  are unnamed and unread (erased, spending no number at all).
- `composed.rxtin` — the COMPOSED SOURCE: four blocks, each calling
  definitions by name, each with a `target = <name>` row. None of them can
  be compiled from its own text; the definitions are a FILE-level fact.
- `flat.rxtin` — THE HAND-WRITTEN FLAT CONTROL: one block per composed
  block, same name, same cases, the definitions inlined by hand. Nothing
  here is generated by pcrec, and that is the property the whole check rests
  on — a flat pattern produced by `--emit-composed` would be the composer
  describing itself.
- `run_definitions_tests.sh` — the proof. `make test-definitions`.

## What the check is, and what it is not

THREE SOURCES, NO TWO OF WHICH SHARE ONE: python `re` on the flat pattern
(an outside oracle), the flat pattern compiled by pcrec (pcrec with no
composer), and the composed source compiled by pcrec (the thing under test).
The two pcrec legs agreeing AGAINST the oracle is reported as a defect below
the composer rather than as a pass, which is why the oracle leg is there at
all.

It compares the WHOLE-MATCH SPAN and deliberately not the later capture
fields: the flat control's groups are its own and the composed artifact's
are the definitions', and that difference is the thing being built.

It also asserts three structural facts off the EMITTED ARTIFACT, read as
text — never off the composer's own report: that a definition was bound at
all (`RX_NCAPS - 1 > ngroups`, the [MECH-REACH] guard against a witness that
stopped reaching its site), that `nnames <= nentries`, and that every
ref-NULL row in `groups[]` precedes every ref-bearing one, which is the ABI
contract `match_api.md` §6's bsearch depends on.

Its population is PINNED (four targets, fifteen cases) and compared against
an awk pass over the raw bytes, because two artifacts agree perfectly about
zero cases.

## Why the fixtures are `.rxtin`

`tests/harness/run.sh` finds `*.rxt` under `tests/`, and a composed block
cannot be compiled from its own text — so a `.rxt` here would be a corpus
file the corpus runner cannot build. It would additionally move four pinned
census numbers (`tests/rxtsource/run_rxtsource_tests.sh`'s CENSUS_* and the
RUNSH_* they reconcile against). The `.rxtin` extension is
`tests/rxtsource/fixtures/`'s own convention; this script copies them into a
scratch directory as `.rxt`, which is also what makes `composed.rxtin`'s
`lib "defs_common.rxt"` reference resolve.

**The open item this leaves**, and it is named rather than buried: these
files are NOT in the harness-scored corpus. Putting a composed block there
needs `tests/harness/run.sh` to compile such a block through
`--source --target` instead of `-p rx` with the bare pattern, plus a loud
floor for a composed block with no target (its cases would otherwise go
unscored). That is a real harness change and wants a real run behind it;
`verify_rxt.py` already carries the matching skip predicate, with a
population of zero.

Maintenance: update this file when fixtures are added/removed or the check's
shape changes.
