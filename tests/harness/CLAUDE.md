# tests/harness — test execution engine

Test runner and driver template. The runner (run.sh) orchestrates compilation and execution; the driver (driver.c) template is linked against generated code and exercises the matcher.

## Files

- **run.sh** — bash harness: parses .rxt, compiles patterns with pcrec, builds test executable, runs driver against each case, diffs output. Honours the per-block `flags` directive (only `i` -> `pcrec -i`) and the per-block `features` directive (MOD-0.3c: `features classes` -> `pcrec --features classes`; the SPEC is validated once per distinct list against a trivially-valid pattern, because pcrec refuses an unknown module name with exit 1 and a perr block would read that typo as its expected rejection); an unknown flag letter is a hard error rather than a silent no-op, since a dropped flag would compile a different automaton and verify the block against it. Hardened per R1 review: perr requires clean exit 1 (crash != rejection), unparseable lines and zero-case files are hard failures, per-stage timeouts, -Wall -Wextra -Werror on generated code. `PROCS=N` (2026-08-12) fans out per-FILE workers (self-reinvocations, own temp dirs, output replayed in file order); the parent judges workers ONLY by their printed summaries and hard-fails when one vanishes — validated in the failing direction by deleting a worker's output (guard fires, exit 1) and by a planted wrong expectation (propagates with its detail line). Serial and PROCS=6 runs measured identical: 1139/0/0 over 25 files, 4m25s → 55s. **[M4.5a] (2026-08-14):** parses `g`/`gp` capture-group expectation lines (attached to the preceding `m`/`ms` case's `case_gspec[i]`, "slot,start,end,pending;..."); reads each block's actual `RX_NCAPS` straight from its generated `gen.h` (never assumed); a `g` (live) line whose slot is `>= RX_NCAPS` is a hard FAILURE (population accounting, never a silent skip), a `gp` (pending-VM) line in the same position is counted in a separate `group cases pending-vm:` bucket instead — printed in both the serial and the `PROCS>1` aggregated summary. A block-level compile/build failure fails every attached `g`/`gp` too (`record_case_group_fail`), `gp` included — see docs/testing.md's "Capture-group expectations" section for the full design and sabotage-validation record. **Fix (2026-08-15, post-merge with [M4.5b]):** the `m`/`ms` case check now compares only the WHOLE-MATCH pair (fields 2/3 of the driver's `match ...` line), not the whole line — the original whole-line compare only ever worked by coincidence while `RX_NCAPS` was 1 (this lane's own test environment before the VM emitter existed) and broke the instant a real artifact delivered group pairs on the same line (219 corpus failures on the composed tree, e.g. `(a)` on `"a"`: expected `match 0 1`, got `match 0 1 0 1`, every number correct). Still a strict parsed-field compare, not a substring match: `match` vs `nomatch`, a short/malformed line, or a wrong whole-match pair (even with correct trailing group pairs) all still fail loudly — sabotage-validated in both directions. **[K21-class fix, 2026-08-15]:** a new per-case branch, `trc -eq 3`, sits beside the existing timeout (`-eq 124`)/crash (`-ge 126`) branches — driver.c's own give-up exit (see its own CLAUDE.md entry) — and records a HARD harness-level failure naming which bound fired, the same shape as those two, rather than letting the give-up's `"steps"`/`"frames"` line fall through to the ordinary `match`/`nomatch` comparison (which would already fail it, just without saying why). **[DD-14 wave A commit 3, 2026-08-24, §10.3]:** three new directives. `gu <code> "<subject>"` (`<code>` in `steps`/`frames`/`work`/`recurse`; `internal` is refused BY NAME at parse time — nothing may EXPECT `PCREC_ERR_INTERNAL`, that is what tests/mech's S136 sabotage row is for) is a new case kind carrying its own eval branch, placed BEFORE the existing unconditional `trc -eq 3` hard-failure branch so it can intercept: exit 3 + a matching printed word is a PASS, anything else (including a clean match/nomatch, which is the FAILING-direction property the design's landing bar requires) is a FAIL naming what was expected vs. what happened. `engine vm` and `budget steps=<n>`/`budget frames=<n>` are two new block-scoped directives, same shape as `flags`/`features` (parsed into `cur_engine`/`cur_stepbudget`/`cur_framebudget`, reset at both the per-file and per-`pattern` block boundaries, folded into `flush_block`'s `pflags` array) — together the minimal route that lets a `.rxt` block actually reach `--engine=vm` with a tiny `--step-budget`/`--backtrack-frames`, which nothing in the directive vocabulary could do before this commit. `case_gucode[]` is a new parallel array beside `case_kind`/`case_line`/etc., kept dense (an empty string pushed at every non-`gu` case site) rather than sparse, matching `case_gspec`'s own convention. **[DD-14 wave G, 2026-08-24]:** `RXTFLAGS` is a new ENV var (not a directive) carrying EXTRA pcrec flags appended to every compile in a run, so one corpus can be swept over a COMPILER AXIS the `.rxt` format has no directive for. It is threaded through the `PROCS>1` worker reinvocation with `PCREC`/`CC`/`GENCFLAGS`, appended LAST to `pflags` so a directive on the same axis wins, and empty by default — a plain run is byte-for-byte unchanged. Added for `-fno-splice-calls` (design §9.2's `A == B` control: the SPLICE-linked and LINKAGE-linked artifacts must agree on every cell; MEASURED `tests/recursion` 593/0 on both arms), and deliberately a GENERAL knob rather than that one flag, because the `-fno-` deny family already has six members and each of them is a corpus-wide axis somebody will want to sweep. **[DD-14.FB], 2026-08-25:** the `frames-buffer=<route>` directive — POSITIONAL WITHIN THE BLOCK, not block-scoped, which is the whole reason it exists in this shape: it names the ENTRY the cases BELOW it run through (`default` / `null` / `<n>` / `<frames>,<trail>`), so one block can hold a `gu frames` through `<prefix>_search` and an `m` through `<prefix>_search_in` for the SAME subject off ONE compile of ONE artifact — the two cells then differ in the entry called and in nothing else, which a block-scoped directive could not express at all and a per-case-kind spelling (`mb`, `nb`, `gub`, …) would have bought by multiplying the case kinds by the number of routes. Parsed into `cur_route` (reset at both the per-file and per-`pattern` boundaries, to `$RXTROUTE` rather than to `default`, so the env axis is a floor a block may raise), recorded per case in a dense `case_route[]` beside `case_gucode[]`, and passed to the driver as its THIRD argument. `valid_route` is the ONE definition of the route grammar, shared by the directive and by `RXTROUTE`, so the two cannot drift into accepting different spellings. Every failure message in the per-case loop gains a ` via <prefix>_search_in route …` tail, because "which entry answered" is the first thing a reader of one of these failures needs. It does NOT overlap `budget frames=<n>`: that one is `--backtrack-frames`, sizing the ARTIFACT at compile time, where this sizes the CALL, and a block may carry both. `RXTROUTE` (env, same grammar) sets the run-wide initial route; `RXTROUTE=null` re-runs any corpus through `<prefix>_search_in(..., NULL)`, which spec §10.3 defines to BE the un-suffixed call, so it must reproduce that corpus cell for cell — the NULL-equivalence control as a corpus-wide axis. `tests/harness/giveup.rxt` is the permanent positive corpus the `gu` route exists to serve — two cells (`steps`, `frames`) using the identical pattern/budget pairs `tests/vm/run_vm_tests.sh`'s own `build()` calls use, now reachable through `.rxt` vocabulary instead of only that file's separate C driver. **[K35], 2026-08-25 ([DD-14] close):** this file now `export LC_ALL=C` immediately after `set -u`, so NOTHING it runs inherits the ambient locale. Under `en_US.UTF-8` `sort` treats punctuation as ignorable, which for a corpus of REGEXES means `a{0,0}b` and `(a){0,0}b` compare EQUAL and `sort -u` drops one — measured on this tree, the corpus pattern extraction yields 1,784 patterns ambient against 2,758 under `LC_ALL=C`. The export is the GENERAL fix Frank ruled at the close; every individual site is guarded too, and `run_codegen_tests.sh` carries the structural check that fails naming any `sort` in a `tests/**/run_*.sh` that is guarded by neither. **[ART-SIZE.1b], 2026-08-28:** `SIZELOG` is a new env var, same shape as `RXTDUMP` (per-worker path under `PROCS>1`, parent merges in `files[]` order) — when set, one TSV row is appended per SUCCESSFUL compile at the existing `gen_cc` call site: pattern id, D46 engine/rungs/prefilter stamps, comment-excluded size (`tests/lib/size_count.sh`), gcc CPU/wall seconds (measured by wrapping the SAME `gen_cc` call in bash's own `time` reserved word — no second compile), and `load1`. Empty (the default) means zero rows are computed and a plain run is byte-for-byte unchanged in cost as well as output. `total_sizelog`/`size-log rows: N` is printed in both the serial and `PROCS>1` aggregated summary, the same shape `pattern-compile failures (distinct)` already has. See `tests/size/CLAUDE.md` for the wrapper (`run_size_log.sh`) that turns a run's rows into the stable log, the tripwire that reads it, and the measured per-compile overhead (20.4% naive, 1.79% after consolidating to one subprocess spawn per compile).
  **[DD-13b.W1.1], 2026-08-30:** the `.rxt` format grew a HEAD, and this script gains **NO HEAD ARMS AT ALL**. For a file whose first non-comment line is not `pattern` it makes ONE `pcrec --list-source` call and starts its existing per-line loop at the `line` column of the FIRST `pattern` row — the head is an untouched byte range whose boundary comes from the one head parser (`src/parse/rxt_source.c`). What this script decides for itself is only "is there a head at all", which is a single token compare against `pattern` and is not head syntax, so it cannot go stale as the head grammar grows. MEASURED: 0 of the corpus's 179 files are head-bearing, so all 179 take a byte-identical code path; that zero is asserted TWICE and from two sources in `tests/rxtsource/`. A head-bearing file with NO `pattern` rows runs zero blocks and the existing P-C2 floor fires on its own — a DISTINCT observable from a `--list-source` call that failed, which is reported as a harness failure carrying pcrec's own diagnostic. Four new arms, APPENDED after the `--- END PINNED 13-ARM REGION ---` marker rather than inserted (`[[ =~ ]]` clobbers `BASH_REMATCH`, so the chain is order-sensitive and appending is the only edit that cannot change which arm an EXISTING line reaches): `name <ident>`, `description <text>` (one-line form only — `|` is a head form, refused by name in all three parsers), `encoding <ident>` (routed to `--encoding=` in `flush_block`, per-block like every other directive), and `features only <list>`. The arm chain between the BEGIN/END markers is **HASH-PINNED** by `tests/rxtsource/run_rxtsource_tests.sh`; the update rule lives in that check's own failure message, where a person looking at the failure will read it. **The `have_block` guard was GENERALISED to the case arms**: six directive arms and `g`/`gp` already carried it, while `perr`/`m`/`n`/`ms`/`ns`/`gu` pushed unconditionally and the EOF flush is gated — so a case line with no open block was silently DROPPED rather than refused, and nothing counted it. MEASURED FREE: 0 of 26,691 case lines precede a `pattern` line. It is what makes the third case of a mis-reported body-start line (several blocks, cases discarded by the next `pattern` reset) LOUD instead of silent. **`--dump`** is leg B of the C1 parse differential: it parses the named files, prints one TSV row per block and per case, and compiles nothing — fed from `flush_block` rather than from the arm chain, so installing it did not move the pin it is protected by.

- **driver.c** — test driver template: calls generated matcher (rx_search), decodes escape sequences in subjects, prints match/nomatch result. **[M4.4] (D44.2, 2026-08-14):** consumes the caps-array search signature (`ptrdiff_t (*caps)[2]`, not the retired `rx_span *m` out-struct) and prints `caps[0][0]`/`caps[0][1]` via `%td` (`ptrdiff_t`'s conversion specifier), not `m.start`/`m.end` via `%zu` — textually identical decimal output for the corpus's byte offsets, so no .rxt expectation changes. **[M4.5a] (2026-08-14):** prints EVERY `caps[k][0]`/`caps[k][1]` pair for `k` in `[0, RX_NCAPS)`, not just slot 0 — since `RX_NCAPS` is always 1 before [M4.5]'s VM lands, this is a superset that is byte-identical to before on the whole pre-existing corpus, not a reshape. **[K21-class fix, 2026-08-15]:** `rx_search`'s return is THREE-valued, not boolean — the driver used to test it with `if (found)`, which is C-truthy on the negative VM give-up sentinels (`PCREC_ERR_STEPS`/`PCREC_ERR_FRAMES`, unprefixed since [ABI-NS]/D60) too, so a give-up took the match branch and printed `caps`, which the give-up path never writes (uninitialized stack reported as a confident match — verified live: the pre-fix driver against a tiny `--step-budget` artifact printed a fabricated `match` line with garbage spans). Now discriminates `found == 1`/`found == 0`/otherwise explicitly, printing `"steps\n"`/`"frames\n"` and exiting 3 (distinct from the normal exit 0 and the usage/malformed-input exit 2) on a give-up, so run.sh can — and now does — score it as its own HARD harness-level failure rather than comparing it against a `match`/`nomatch` expectation. Same bug shape as `tests/fuzz/fuzz_driver.c` (fixed, fuzzfix arc) and `src/gen/emit_dfa.c`'s `pcrec_emit_main` (K21, docs/dev/known_issues.md); `tests/registry/pc4_driver.c` still has the untouched `if (rx_search(...))` form as of this fix — a fourth, still-open site, flagged rather than silently patched here since PC-4 is a different subsystem outside this fix's scope. **[DD-14 wave A, 2026-08-24]:** the give-up print itself had the SAME bug shape one level down — it named exactly `PCREC_ERR_STEPS` and folded every OTHER code into `"frames"`, so a `PCREC_ERR_WORK` give-up (reachable via `tests/vm/`'s own driver, just never through this one before this fix) printed as a frame-capacity failure. Now an explicit chain names all four give-up codes (`steps`/`frames`/`work`/`recurse`; `recurse` is `PCREC_ERR_RECURSE`, reserved with no producer yet, D71 item 1) and an unrecognized negative return prints `"giveup %d\n"` rather than guessing. **[DD-14 wave A commit 2, 2026-08-24, D71 item 1]:** a FIFTH word, `"internal"` (`PCREC_ERR_INTERNAL`), joins the chain — it is BELOW the give-up floor and is NOT a give-up (the artifact caught its own analysis/emission inconsistency; module `lookaround`'s negative-polarity lookbehind end-check is the one producer today), but it still exits 3 like the four give-up codes, since this driver has no third outcome shape, and run.sh's `gu` directive refuses to let any corpus block EXPECT it. **[DD-14.FB] critic pass, 2026-08-25:** on any non-default route the driver ALSO cross-checks `<prefix>_match_in` and `<prefix>_match_caps_in` against their un-suffixed siblings on the same `rx_ctx`, exiting **4** (its own outcome, named by run.sh ahead of the `gu` branch so it applies to every case kind) on a disagreement. This exists because those two entries had **no behavioural coverage anywhere in the tree** — every cell, driver and measurement drove `<prefix>_search_in` alone, so three entries shipped and one was exercised. It is a CROSS-CHECK rather than a second answer: the `.rxt` `m`/`n` vocabulary means a SEARCH and the anchored entries answer a different question, so each is compared to its OWN sibling (§10.2's "its un-suffixed sibling in every respect") and needs no oracle. The permitted divergence is SYMMETRIC — when a buffer is supplied the two may differ if EITHER is a give-up, since the caller's capacity is not the stamped one — and getting that wrong is how the check introduced itself: the first version allowed only the downward direction and went RED on a correct build at the `1024,8192` cell, where the `_in` entry MATCHES what its sibling refuses, which is the whole feature. MEASURED non-vacuous: an emitter whose `<prefix>_match_in` ignores its descriptor takes framebuffer.rxt from 16/0 to 12/4. **[DD-14.FB], 2026-08-25:** takes an optional THIRD argument, the ENTRY ROUTE (`default`/absent, `null`, `<n>`, `<frames>,<trail>`) that run.sh's `frames-buffer=` directive supplies. It changes WHICH ENTRY answers and nothing else — same subject, same startpos, same printed protocol, same exit codes — which is what makes a route a control rather than a variant. Buffers are `malloc`'d (so `RX_BUFFER_ALIGN` holds by definition) and sized in BYTES from the artifact's own `RX_RESUME_FRAME_SIZE`/`RX_TRAIL_FRAME_SIZE`, i.e. through exactly the arithmetic spec §10.4 publishes those macros for; on an artifact whose sizes read 0 (every DFA artifact) the route degenerates to a NULL descriptor, which that engine ignores anyway. The two capacities are separately settable on purpose: sabotage row S180 (each capacity bound to the other's array) is a NO-OP under any equal pair, so a route that could only set them equal could not detect it. Dormant here today: nothing in the .rxt `flags`/`features` directive vocabulary can select `--engine=vm` or a tiny budget, so no corpus case reaches the give-up path — deliberately not exercised by a new .rxt case in this commit (see this file's `run.sh` entry for the follow-on `gu`/`engine`/`budget` directives)
- **verify_rxt.py** — python-re oracle: cross-checks every corpus expectation (D4); `# pcre2-only` blocks are skipped (see docs/testing.md). Understands the `features` directive as a no-op (python re has no module gate; gate-only constructs are `# pcre2-only` blocks like any python-inexpressible pattern). Honours `flags i` as `re.IGNORECASE | re.ASCII` — the `re.ASCII` is load-bearing, since python's IGNORECASE otherwise folds Unicode (Kelvin sign, long s) and would silently disagree with pcrec's ASCII-only fold. **[M4.5a] (2026-08-14):** checks `g`/`gp` lines against python `re`'s `match.span(<slot>)` on the preceding `m`/`ms` case's subject/startpos, identically for `g` and `gp` (pending-ness is a fact about the pcrec ARTIFACT this oracle has no notion of); a slot beyond the pattern's own `compiled.groups` is always a hard failure. **[DD-14 wave A commit 3]:** had NO notion of `gu`/`engine`/`budget` — its parser raised `ValueError` on any of the three (an unrecognized line, the same as a typo). That entry called it "harmless by construction" because the default `BASE_DIR` glob was `tests/base/*.rxt` only, so no automated invocation reached it, and recorded it as "a live gap, recorded rather than silently worked around". **[DD-13b.W1.1] CLOSED IT, and the gap turned out to be the CONDITION on that step rather than a footnote.** The moment this oracle is pointed at the whole corpus — which W1.1 does, because C3 has no meaning otherwise — the three unknown kinds stop being unreachable and become five files that DIE BEFORE SCORING A SINGLE EXPECTATION (`harness/giveup.rxt`, `recursion/d27/sr_depth.rxt`, `recursion/framebuffer.rxt`, `recursion/quantified.rxt`, `size/size_term.rxt`). Corpus census: `gu` 23 lines, `frames-buffer=` 9, `engine` 5, `budget` 3. All four now PARSE; `engine`/`budget`/`frames-buffer=` are recognised and ignored (they size and select pcrec's own machinery and mean nothing to python `re`), and `gu` is a COUNTED SKIP for the reason the old entry gave — a give-up is a pcrec-VM property no oracle can arbitrate — rather than an ignored line, because a population nobody counts is not a population. **The old entry was RIGHT that the gap was harmless and RIGHT to record it; what it could not see is that "no automated invocation reaches it" was a fact about the WIRING, and the wiring was the thing about to change.** Also new at W1.1: `name`/`description`/`encoding`/`features only` (the W1 block directives), a head-bearing file REFUSED BY NAME (the head has one parser, pcrec's, and a fourth would be one more thing to keep in step), directory discovery by `os.walk` instead of the one-level glob that made `verify_rxt.py tests` verify ZERO files and exit 0, a `--min-files N` short-list HARD FAIL whose floor is supplied by the caller and never derived from the discovery it checks, an aggregate `SKIP=` total broken out by reason (there was no total at all before, so "the same skip count" had nothing to read), and `--dump` — leg C of `tests/rxtsource/`'s three-way parse differential
- **giveup.rxt** — **[DD-14 wave A commit 3, 2026-08-24]** the `gu` directive's own positive corpus: two cells (`steps`, `frames`) where a give-up code IS reachable today via `engine vm` + `budget`, using the SAME pattern/budget pairs `tests/vm/run_vm_tests.sh`'s own `build()` calls drive the same two bounds with. Lives here (not under a module directory) because it tests HARNESS VOCABULARY, not a regex construct. Picked up by `run.sh`'s tree-wide `.rxt` discovery and by every `tests/codegen/run_*_identity.sh` script's `grep -h '^pattern '` extraction (harmless there — both patterns are ordinary VM-engine loop shapes with no special identity-gate interaction); NOT picked up by `tests/mrl/maxw_check.c`'s sweep in any way that matters (its line scanner ignores unrecognized directives, so these two blocks register with zero spans and no violation)

## Conventions

run.sh accepts file/directory arguments or scans tests/ recursively. Each .rxt pattern block is compiled to C, linked with driver.c (which includes the generated code), and executed for each m/n line. The driver decodes \" \\ \n \t \r \f \v \xHH escapes. Output is one line per case: "match START0 END0 [START1 END1 ...]" (one pair per RX_NCAPS slot — just "START END" while RX_NCAPS is 1, i.e. today) or "nomatch", compared against expected results; g/gp lines pick their slot's pair out of that line by position rather than triggering a separate driver invocation.

Maintenance: update this file when files are added/removed or their roles change.
